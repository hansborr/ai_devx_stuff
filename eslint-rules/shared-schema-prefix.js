// @ts-check

// Shared by runtime-loaded ESLint rules and TypeScript codemods. Keep this
// module plain JavaScript because ESLint loads its rule graph before any build.
export const SHARED_SCHEMA_PREFIX = "@musi/shared/schemas/";
