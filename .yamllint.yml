---
extends: default

ignore: |
  .playwright-cli/**
  docs/refs/**
  node_modules/**
  worktrees/**

rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  indentation:
    spaces: 2
  line-length:
    max: 120
  truthy:
    check-keys: false
