# Status

**Last updated**: 2026-05-10 (hot-path trim)
**Branch**: `feat/harness-improvements-v4` (unpushed)
**Test suite**: Green at last merge. Exact counts belong in `LOG.md` or task
notes.

## Current Queue

- `NEXT.md` promotes one ready leaf: add `docs/guides/add-module-doc.md`.

## Active Notes

No active `in_progress/` note is required for the current leaf.

## Cold Reads

- Read `NEXT.md` after this file.
- Read `DECISIONS.md` only when changing a cross-cutting pattern.
- Read `LOG.md`, `finished_work/README.md`, or `backlog/README.md` only for
  retained history, archived operational detail, or explicit re-triage.
- After the current `NEXT.md` leaf lands, future harness leaves come from
  `backlog/ai-harness-followups.md`.

## Handoff

If `NEXT.md` is empty, stop for human re-triage or promote exactly one ready
leaf from the source named above. If a session ends mid-flight, update the
matching `in_progress` note and this snapshot.
