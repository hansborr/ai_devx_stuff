# Author packet — R4C-001 → `264-make-ad-hoc-fastify-test-teardown-failure-safe.md`

- Ruling: **new-leaf**
- Leaf number: **264**
- Title (settled): Make ad-hoc Fastify test teardown failure-safe
- Theme: Ad-hoc Fastify tests skip teardown when assertions fail
- Area: tests · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Register every custom Fastify instance for per-test cleanup immediately after construction, before readiness, injection, or assertions. Keep each custom configuration local.

## Problem statement from the promotion candidate

Six app tests create custom Fastify instances but close them only after all requests and assertions. A failure skips teardown; the socket-enabled case also skips its registered Socket.IO cleanup, leaving resources alive during the failure run.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/app.test.ts:42-57` — The rate-limit app is created and readied, then closed only after request execution and the assertion; either failure bypasses close.
- `packages/server/src/app.test.ts:219-230` — The Socket.IO-enabled variant reaches close only after its decorator assertions.
- `packages/server/src/app.ts:264-268` — Fastify close is the lifecycle boundary that invokes Socket.IO cleanup.
- `packages/server/src/app.test.ts` — measurement: 6 ad-hoc Fastify instance/close pairs (`rg -n 'const (rateLimitApp|testApp|customApp|socketApp|socketlessApp) =|await (rateLimitApp|testApp|customApp|socketApp|socketlessApp)\.close\(\)' packages/server/src/app.test.ts`) — All six constructions have a tail-position close and no failure-safe per-test cleanup registration.

## Author constraints (all must be honored)

- Do not widen `useTestApp` into a configuration or per-test framework.
- Preserve the existing custom Fastify and Socket.IO test variants.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-121` — Custom per-test Fastify instances remain outside the landed suite-scoped `useTestApp` beforeAll/afterAll lifecycle.

## Provenance (origins — carry into the leaf Source line context)

- R4C · lane-06-tests · `wave-1/lane-06.json::coverage.cut[6]` — original title: "Ad-hoc Fastify app failure-safe teardown"; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Leaf `164-drift-ai-suites-leave-dozens-temporary.md:10-12,23-33` is limited to drift-AI temporary roots. Prior leaf `39-server-test-lifecycle.md:180-198`, reached through CQ25-121, owns only the universal suite-scoped app lifecycle.
- promoter hint (non-authoritative): looks close to leaf 164 — hint only; both concern test-resource cleanup, but leaf 164 covers drift-AI temporary roots

