# Author packet — R4B-031 → `004-character-creation-large-pseudo-module-loose.md`

- Ruling: **augment-existing-leaf** (edit `004-character-creation-large-pseudo-module-loose.md` in place)
- Leaf number: **004**
- Title (settled): Enforce starting equipment's transaction-only contract
- Theme: Starting-equipment persistence erases its transaction-only capability
- Area: server · Severity: **high** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make the internal starting-equipment stage accept only `TxClient` as part of leaf 004's folder move. Preserve the current character-creation transaction and persistence behavior.

## Augment instruction (what to add or change in the target leaf)

Amend the problem/evidence to record that `createStartingInventory` accepts `DbClient | TxClient` despite having one transaction-scoped production caller. In the move to `character-create/starting-equipment.ts`, narrow the parameter to `TxClient` and adapt focused tests to enter a transaction or test a pure data-building seam; update scope to state that this is compile-time capability narrowing with no runtime transaction-shape change.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `004-character-creation-large-pseudo-module-loose.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Starting equipment is an internal stage of atomic character creation, but its type advertises an unused top-level autocommit mode. A future caller can persist it outside the character transaction and permit partial character assembly.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/services/starting-equipment-service.ts:63-80` — The persistence stage accepts either DbClient or TxClient and performs its reads and bulk insert through that widened capability.
- `packages/server/src/services/character-create.ts:230-248` — The only production caller invokes the stage inside the transaction that creates the character and level-one features.
- `packages/server/src/utils/prisma-types.ts:142-171` — TxClient and DbClient intentionally expose different transaction capabilities.

## Author constraints (all must be honored)

- Apply the change at leaf 004's post-move path and update the tests leaf 004 already relocates.
- Do not introduce a second transaction or change transaction ordering, error behavior, or persistence semantics.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-03-server · `wave-1/lane-03.json::coverage.cut[5]` — original title: "Starting-equipment persistence advertises an unused autocommit mode."; rejected because: per-lane-budget:weak-or-absent-rationale (rank 4); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `004-character-creation-large-pseudo-module-loose.md:100-112` already moves the implementation and tests, while `:119-124` and `:163-169` define starting equipment as an internal transaction stage. Its no-runtime-behavior constraint is at `:147-151`.
- promoter hint (non-authoritative): looks close to leaf 004 — hint only

