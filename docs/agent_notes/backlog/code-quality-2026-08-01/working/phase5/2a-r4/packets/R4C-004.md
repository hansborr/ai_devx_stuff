# Author packet — R4C-004 → `189-downstream-packages-keep-semantic-copies.md`

- Ruling: **augment-existing-leaf** (edit `189-downstream-packages-keep-semantic-copies.md` in place)
- Leaf number: **189**
- Title (settled): Use the shared description limit in campaign and collection forms
- Theme: Three description controls cap valid input at half the shared contract limit
- Area: cross-cutting · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Extend leaf 189's bounded consolidation list with description length. Import `MAX_DESCRIPTION_LENGTH` into all three controls and pin that their exposed `maxLength` matches the shared create/update schemas.

## Augment instruction (what to add or change in the target leaf)

Expand the problem, evidence, Part A, Part B, and all seven-authority counts to eight. Add `MAX_DESCRIPTION_LENGTH` as the authority for the two campaign and one collection Textareas, replace their `5000` literals, add focused UI assertions for the shared limit, and include description-limit stragglers in the required semantic sweep without creating a generic literal detector.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `189-downstream-packages-keep-semantic-copies.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Campaign and homebrew collection authoring surfaces prevent users from entering descriptions longer than 5,000 characters even though their canonical create and update schemas deliberately accept 10,000. The duplicated HTML literals silently make valid input unavailable and can drift again when the shared contract changes.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/constants.ts:55-60` — The canonical constant explicitly unifies description fields at 10,000 characters.
- `packages/shared/src/schemas/campaign-inputs.ts:9-52` — Campaign creation and updates validate descriptions against MAX_DESCRIPTION_LENGTH.
- `packages/shared/src/schemas/homebrew-inputs.ts:15-30` — Homebrew collection creation and updates use the same 10,000-character contract.
- `packages/client/src/components/campaign/settings/create-campaign-dialog.tsx:60-71` — The campaign creation form independently hard-codes maxLength 5000.
- `packages/client/src/components/campaign/settings/campaign-settings-panel.tsx:91-101` — The campaign settings form repeats the 5,000-character cap.
- `packages/client/src/components/homebrew/collections/collection-form-fields.tsx:48-60` — The shared create/edit collection control also repeats maxLength 5000.

## Author constraints (all must be honored)

- Apply the prior-pack CQ25-110 whole-tree semantic-straggler search to this eighth authority.
- Do not introduce a general constants framework or generic duplicate-literal detector.
- Update leaf 189's seven-authority counts and narrow regression-test inventory consistently.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4C · lane-08-cross-cutting · `wave-1/lane-08.json::coverage.cut[4]` — original title: "HTML maxLength duplication was routed to bugsSideList because the observed 5,000 versus 10,000 mismatch is behavior-shaped."; rejected because: coverage.cut; routed to bugsSideList because the mismatch was behavior-shaped

## Existing-leaf relations

- Dedup basis from 1c: Leaf `189-downstream-packages-keep-semantic-copies.md:10-12,55-81` defines the same consolidation fingerprint but currently lists only seven authorities; `:100-101` bounds the mechanism and preserves the whole-tree acceptance criterion.
- promoter hint (non-authoritative): looks close to leaf 189 — hint only; that leaf consolidates seven other downstream copies of shared authorities but does not include description limits

