# Author packet — R4B-032 → `033-attunement-game-rule-lives-request-schema.md`

- Ruling: **augment-existing-leaf** (edit `033-attunement-game-rule-lives-request-schema.md` in place)
- Leaf number: **033**
- Title (settled): Make the attunement concurrency header truthful
- Theme: Inventory header falsely claims its transaction prevents concurrent over-attunement
- Area: server · Severity: **high** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

During the attunement constant relocation, correct the service header so it distinguishes transactional grouping from concurrent exclusion. State that policy accepts the theoretical race because this is a recoverable single-writer path.

## Augment instruction (what to add or change in the target leaf)

Replace the direction's `inventory-service.ts` documentation no-op with an explicit header correction. Add evidence that the default count-then-write transaction provides atomic grouping but not serialization, rewrite the header to describe the deliberately accepted single-writer race, and amend the scope so no CAS, lock, or Serializable gate is added.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `033-attunement-game-rule-lives-request-schema.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

A default read-then-write transaction groups statements atomically but does not prevent two transactions from observing free capacity. The false guarantee can mislead future invariant changes and serve as an incorrect concurrency precedent.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/services/inventory-service.ts:25-45` — The header claims the transaction prevents concurrent over-attunement and then says the path is deliberately single-writer and outside every race pattern.
- `packages/server/src/services/inventory-service.ts:221-244` — Create performs count and insert in a default transaction without serializable isolation, a lock, or a conditional write.
- `packages/server/src/services/inventory-service.ts:270-279` — Update uses the same default count-then-write transaction shape.
- `docs/CONCURRENCY.md:19-29` — Policy treats single-writer, recoverable races as reasons not to gate, which is accepted risk rather than serialization.

## Author constraints (all must be honored)

- Comment correction only; do not alter `assertAttunementLimit`, isolation level, or write protocol.
- Preserve the admission bar in `docs/CONCURRENCY.md`; do not infer broader Serializable eligibility from leaf 092.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-03-server · `wave-1/lane-03.json::coverage.cut[8]` — original title: "Inventory's concurrency header makes incompatible atomicity and race-prevention claims."; rejected because: per-lane-budget:weak-or-absent-rationale (rank 1); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `033-attunement-game-rule-lives-request-schema.md:30-34,71-84` already relies on and evaluates the same service header, while leaf `092-concurrency-rulebook-claims-three-exhaustive.md:59-84` is docs-only and does not cover the local false guarantee.
- promoter hint (non-authoritative): looks close to leaf 033 — hint only
- promoter hint (non-authoritative): looks close to leaf 092 — hint only

