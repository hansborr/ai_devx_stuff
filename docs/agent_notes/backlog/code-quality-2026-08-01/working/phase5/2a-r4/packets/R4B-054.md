# Author packet — R4B-054 → `261-debounce-notes-search-before-changing-its.md`

- Ruling: **new-leaf**
- Leaf number: **261**
- Title (settled): Debounce notes search before changing its infinite-query key
- Theme: Debounce notes search before changing the infinite-query key
- Area: client · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Keep immediate controlled input state but feed a trimmed debounced value into `note.list` and active-filter derivation. Add focused fake-timer coverage for settling, clearing, and whitespace normalization.

## Problem statement from the promotion candidate

Typing immediately starts a separate infinite query for each intermediate prefix and retains each parameterized key until normal garbage collection, adding avoidable API, database, and cache churn.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/campaign/notes/notes-panel.tsx:79-84` — Every input event immediately updates search state.
- `packages/client/src/components/campaign/notes/notes-panel.tsx:232-251` — Immediate search state is embedded directly in infiniteQueryOptions.
- `packages/client/src/hooks/use-debounced-value.ts:15-25` — The existing hook explicitly documents settling text before it feeds a query key.
- `packages/client/src/hooks/MODULE.md:45-55` — useDebouncedValue is an advertised client-hook entry point.

## Author constraints (all must be honored)

- Do not add `maxPages`, `gcTime: Infinity`, custom stale time, or other retention/freshness policy.
- Reuse the existing `useDebouncedValue` hook.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-207` — Request and query-key frequency while typing remains; the prior ruling only settles page retention, garbage collection, and normal staleness.

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-05-client · `wave-1/lane-05.json::coverage.cut[32]` — original title: "Notes search creates an infinite-query key on every keystroke."; rejected because: per-lane-budget:weak-or-absent-rationale (rank 2); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: CQ25-207 is recorded at `working/dedup-corpus.md:281`; prior leaf `56-infinite-list-refetch-cost.md:61-71` expressly separates finite-GC and max-pages policy from this input-frequency concern.

