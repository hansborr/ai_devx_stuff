# Author packet — R4B-011 → `253-converge-typecheck-fan-out-on-the-shared-parallel.md`

- Ruling: **new-leaf**
- Leaf number: **253**
- Title (settled): Converge typecheck fan-out on the shared parallel runner
- Theme: Converge typecheck fan-out on the shared live-output parallel runner
- Area: harness · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add the smallest per-lane logging or failure-report seam needed to `parallel-runner.sh`, then express typecheck's four lanes through it. Keep only TypeScript diagnostic excerpting and presentation policy in `typecheck.sh`.

## Problem statement from the promotion candidate

Two gate facades maintain the same subtle shell process lifecycle independently: FIFOs and reader processes, signal cleanup, wait ordering, and multi-exit aggregation. A lifecycle fix in the shared runner does not reach typecheck even though typecheck differs only by retaining logs for diagnostic excerpts.

## Verified evidence (existence-checked at step 1b)

- `scripts/typecheck.sh:20-114` — Typecheck privately implements temp/FIFO state, child cleanup, signal traps, prefixed streams, and child launch; its only extra is teeing each stream to a lane log.
- `scripts/lib/parallel-runner.sh:20-119` — The existing shared runner owns the same initialization, cleanup, signal, FIFO-stream, PID, and launch lifecycle.
- `scripts/typecheck.sh:120-163` — Diagnostic excerpting is the genuine typecheck-specific behavior.
- `scripts/typecheck.sh:191-230` — Typecheck waits all lanes and merges matching nonzero statuses, falling back to 1 when they disagree.
- `scripts/lib/parallel-runner.sh:130-164` — The shared runner independently implements the same wait and exit-aggregation rule.

## Author constraints (all must be honored)

- Preserve existing signal handling, lane-prefix output, exit aggregation, and diagnostic excerpts.
- Do not fold this into leaf 204's `verify-engine.sh` decomposition; coordinate only if shared harness fixtures overlap.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-01-harness-core · `wave-1/lane-01.json::coverage.cut[21]` — original title: "typecheck.sh substantially duplicates scripts/lib/parallel-runner.sh."; rejected because: per-lane-budget:weak-or-absent-rationale (rank 3); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `204-decompose-the-stateful-bash-verification-engine.md:10-18,41-72` exclusively owns `verify-engine.sh`; `scripts/lib/parallel-runner.sh:4-9` distinguishes its lifecycle from verify/pre-commit orchestration.
- promoter hint (non-authoritative): looks close to leaf 204 — hint only; that leaf targets scripts/verify.sh rather than typecheck.sh's separate fan-out

