# Author packet — R4C-002 → `265-document-cross-worktree-status-modes.md`

- Ruling: **new-leaf**
- Leaf number: **265**
- Title (settled): Document cross-worktree status modes
- Theme: Document the two cross-worktree status modes in the per-worktree guide
- Area: docs · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add `worktree:status --all` and `worktree:status --lanes [--base <ref>]` to the command table and multi-lane workflow, distinguishing provisioning diagnostics from Git-work coordination.

## Problem statement from the promotion candidate

Operators following the dedicated guide cannot discover the read-only commands designed to inspect every worktree's provisioning or coordinate parallel lanes. The omission is especially costly in the guide's multi-lane workflow, where ahead/behind and staged/unstaged state are the useful overview.

## Verified evidence (existence-checked at step 1b)

- `docs/guides/per-worktree-dev.md:14-29` — The command table describes only the current-worktree worktree:status behavior.
- `docs/guides/per-worktree-dev.md:64-109` — The multi-lane orchestration and recovery sections never mention either cross-worktree mode.
- `scripts/worktree-db.sh:14-20` — The authoritative header defines --all provisioning blocks and --lanes [--base <ref>] Git-work summaries.
- `scripts/worktree-db.sh:1536-1565` — The dispatcher implements both modes, validates their exclusivity, and restricts --base to --lanes.

## Author constraints (all must be honored)

- Keep `scripts/worktree-db.sh` as the behavior authority.
- State that `--base` belongs only to `--lanes` and that the two modes are mutually exclusive.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4C · lane-07-docs-dx · `wave-2/lane-07-topup.json::coverage.cut[7]` — original title: "The per-worktree guide omits worktree:status --all and --lanes."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Leaf `096-per-worktree-guide-omits-mandatory.md:10-23,48-79` exclusively covers the primary checkout credential prerequisite and related leaf-198 coordination.
- promoter hint (non-authoritative): looks close to leaf 096 — hint only; that leaf covers a different prerequisite omission in the same guide

