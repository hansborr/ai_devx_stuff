# Author packet — R4B-052 → `259-extract-a-one-pass-homebrew-item-read-adapter.md`

- Ruling: **new-leaf**
- Leaf number: **259**
- Title (settled): Extract a one-pass homebrew-item read adapter
- Theme: Give homebrew-item conversion a pure adapter boundary
- Area: client · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Extract a client-local adapter that normalizes and parses each entry once and returns both display data and the inventory create input. Leave query state and rendering in the component.

## Problem statement from the promotion candidate

A query/view module owns a substantial persisted-data compatibility and domain-conversion adapter, and each row parses the same entry twice. Compatibility, mapping, rendering, and pure-helper tests consequently share one change surface.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/sheet/homebrew-item-tab.tsx:46-61` — The view module owns persisted-data compatibility normalization and schema parsing.
- `packages/client/src/components/sheet/homebrew-item-tab.tsx:64-143` — Category mapping plus weapon, armor, and inventory-input conversion policy occupy the query component file.
- `packages/client/src/components/sheet/homebrew-item-tab.tsx:157-176` — Each row parses the entry for display and then calls a converter that parses it again.
- `packages/client/src/components/sheet/homebrew-item-tab.test.tsx:223-242` — The component suite establishes a separate pure-converter test seam.

## Author constraints (all must be honored)

- Do not change persisted schemas, writers, storage spellings, or the existing `twoHandedDice`/`versatileDice` translation boundary.
- Retain unknown-value failure behavior and the current legacy damage-type normalization.
- Keep leaf 195's parsed-value submit validation independent.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-173` — Client-local separation of linked-item read normalization and conversion from React, including one-pass parsing, remains outside the refused shared persisted-schema normalization.

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-05-client · `wave-1/lane-05.json::coverage.cut[2]` — original title: "Homebrew item tab embeds compatibility parsing and conversion inside a query view."; rejected because: per-lane-budget:weak-or-absent-rationale (rank 5); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `195-validate-concrete-homebrew-entry-data-before.md:25-41` is limited to the editor submit boundary. CQ25-173 is recorded at `working/dedup-corpus.md:239-242`, with its storage-spelling refusal reinforced by prior `CONSTRAINTS.md:28`.
- promoter hint (non-authoritative): looks close to leaf 195 — hint only; that leaf validates authoring submissions rather than linked-item read conversion

