# Author packet — R4B-022 → `255-make-exact-token-collection-explicit-at-the.md`

- Ruling: **new-leaf**
- Leaf number: **255**
- Title (settled): Make exact-token collection explicit at the near-duplicate runner boundary
- Theme: Make near-duplicate exact-token collection an explicit runner choice
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Require `includeExactTokens` at the runner boundary and pass each production caller's intended boolean explicitly. Either require the extractor choice too or document its standalone default without allowing the runner to inherit it accidentally.

## Problem statement from the promotion candidate

The same optional setting has opposite meanings when omitted at adjacent API layers. Future callers can silently lose exact-clone input or incur unexpected token collection depending on which layer they enter.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/near-duplicates-fingerprint.ts:38-52` — The extractor treats an omitted includeExactTokens option as true.
- `scripts/drift-ai/near-duplicates-runner.ts:45-56` — The adjacent runner exposes the identically named option as optional.
- `scripts/drift-ai/near-duplicates-runner.ts:114-123` — The runner converts omission to false before calling the extractor.
- `scripts/drift-ai/clone-candidates-command.ts:81-98` — Clone candidates omits the option and silently relies on the runner's false default.
- `scripts/drift-ai/near-duplicates-check.ts:45-56` — The registered near-duplicates check explicitly passes true, demonstrating that production callers need different choices.

## Author constraints (all must be honored)

- Preserve eligibility filtering for exact-clone files.
- Do not combine this semantic correction with leaf 173's test-ownership move.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[8]` — original title: "Near-duplicate extractor and runner assign opposite defaults to includeExactTokens"; rejected because: per-lane-budget:weak-or-absent-rationale (rank 4); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `173-near-duplicate-sensor-gate-tests-buried.md:44-71` is a move-only test-ownership proposal and explicitly excludes production sensor semantics.

