# Author packet — R4B-033 → `256-constrain-class-caster-metadata-with-database-enums.md`

- Ruling: **new-leaf**
- Leaf number: **256**
- Title (settled): Constrain class caster metadata with database enums
- Theme: Class caster metadata bypasses existing database enum boundaries
- Area: server · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add a Prisma `CasterType` enum, reuse `AbilityAbbreviation` for both spellcasting-ability columns, validate existing values, and land the required migration. Remove only boundary parsing or assertions made genuinely redundant by the generated enum types.

## Problem statement from the promotion candidate

Closed caster vocabularies are enforced only after rows leave the database. Invalid write data can persist and then crash parsers or pass through unsound assertions, while two boundary strategies are maintained for the same columns.

## Verified evidence (existence-checked at step 1b)

- `packages/server/prisma/schema.prisma:468-499` — Class and Subclass store casterType and spellcastingAbility as String/String? fields constrained only by comments.
- `packages/server/prisma/schema.prisma:244-253` — The schema already defines the AbilityAbbreviation enum needed by both spellcastingAbility columns.
- `packages/shared/src/schemas/srd.ts:126-165` — Shared closes casterType to four values and spellcastingAbility to the ability-abbreviation vocabulary.
- `packages/server/src/utils/caster-resolver.ts:5-20` — Rules-facing reads runtime-parse the wide Prisma strings before use.
- `packages/server/src/utils/srd-narrowing.ts:28-58` — Router mapping instead uses marked Prisma assertions based on seed assumptions.

## Author constraints (all must be honored)

- Follow the Prisma migration guide and validate existing persisted values before altering columns.
- Honor leaf 006's binding guard not to restructure `routers/srd.ts` row types or its `narrow*EnumColumns` helpers without a separate overturn.
- Keep any runtime parsing still needed at untrusted JSON or transport boundaries.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R4B · lane-03-server · `wave-1/lane-03.json::coverage.cut[21]` — original title: "Caster metadata remains comment-constrained TEXT rather than Prisma enums."; rejected because: redistributed-unused-capacity:weak-or-absent-rationale (rank 8); recorded rationale: none

## Existing-leaf relations

- Dedup basis from 1c: Leaf `006-server-mappers-maintain-parallel-handwritten.md:147-159` excludes `routers/srd.ts` restructuring and retains runtime parses; its 14-shape mapper scope does not include Class/Subclass caster columns.
- promoter hint (non-authoritative): looks close to leaf 006 — hint only

