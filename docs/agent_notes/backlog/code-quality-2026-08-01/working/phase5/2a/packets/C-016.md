# Author packet — C-016 → `224-remove-lint-ratchet-compatibility-apis-used-only-by.md`

- Ruling: **new-leaf**
- Leaf number: **224**
- Title (settled): Remove lint-ratchet compatibility APIs used only by tests
- Theme: Superseded lint-ratchet convenience APIs survive only in tests
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Delete the singular debt-log append wrapper, the string-only zero-baseline wrapper, and the legacy registry-validation set overload. Retarget tests to the canonical batch, result-returning, and options-object APIs.

## Problem statement from the promotion candidate

Two superseded exports enlarge the governance API and retain dedicated tests although no production or example adapter calls them.

## Verified evidence (existence-checked at step 1b)

- `tools/lint-ratchet/src/governance/debt-log-write.ts:113-142` — The batch append operation is followed by a singular exported wrapper that only boxes one entry into an array.
- `tools/lint-ratchet/src/governance/zero-baseline.ts:304-310` — The string-only audit export delegates to the richer result-returning operation used by the adapter.
- `tools/lint-ratchet/src/governance` — measurement: Exact-word search over non-documentation source excluding tests found zero call sites for appendValidatedDebtLogEntry and runLintRatchetZeroBaselineAudit outside their declarations — Only tests adopt them; production uses the batch/result replacements.

## Author constraints (all must be honored)

- Coordinate debt-log-write.ts with leaf 160 and registry-validation.ts with leaf 175.
- Confirm the production and example consumer census before deleting each export.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-09-lint-machinery · `wave-1/lane-09.json::coverage.cut[7]` — original title: "superseded governance convenience exports remain — low-confidence dead-surface cleanup"; rejected because: per-lane-budget:weak-or-absent-rationale (rank 2)

## Existing-leaf relations

- Dedup basis from 1c: Leaf 160's debt-log changes are at 160-debt-log-union-uses-absence-discriminator.md:23-68 and leaf 175's registry work at 175-metric-strategy-registry-does-not-make.md:14-40; neither covers these compatibility APIs.

## Folded-in promotions (merge-with-promotion rulings)

### C-017 — Remove the test-only registry-validation set overload

Contribution: Delete the ReadonlySet overload and duck-typed normalizer, and update the sole test to pass ValidateLintRatchetRegistryOptions with localRuleIds.

A public kernel validator advertises two argument shapes and performs runtime duck typing solely for one internal test, obscuring the canonical extensible contract.

- `tools/lint-ratchet/src/kernel/registry-validation.ts:70-85` — A duck-typed has/forEach predicate and normalizer preserve the legacy ReadonlySet form.
- `tools/lint-ratchet/src/kernel/registry-validation.ts:311-319` — The public validator signature exposes the set-or-options union.
- `tools/lint-ratchet/src/kernel/baseline.test.ts:2511-2515` — The only surviving direct-set caller is an internal validation test.
- `tools/lint-ratchet/src/kernel/registry-validation.ts` — measurement: Three non-test validateLintRatchetRegistry callers all pass the canonical options object; the only direct ReadonlySet second argument is baseline.test.ts:2514 — No production or example consumer needs the compatibility form.

