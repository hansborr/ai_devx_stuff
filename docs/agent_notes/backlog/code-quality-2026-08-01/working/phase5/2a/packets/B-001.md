# Author packet — B-001 → `204-decompose-the-stateful-bash-verification-engine.md`

- Ruling: **new-leaf**
- Leaf number: **204**
- Title (settled): Decompose the stateful Bash verification engine behind its existing facade
- Theme: The verification engine remains a framework-sized stateful Bash module
- Area: harness · Severity: **medium** · Size: **L** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Keep process orchestration and the public sourcing facade in Bash. Extract cohesive Bash libraries for lifecycle/cleanup, policy validation, and evidence transactions in behavior-preserving slices.

## Problem statement from the promotion candidate

Process cleanup, policy validation, locking, slot execution, evidence transactions, and reporting remain coupled through ambient mutable state and string-named contracts in one module. The substrate ruling rejects a TypeScript rewrite but does not remove this high-context maintenance cost.

## Verified evidence (existence-checked at step 1b)

- `scripts/lib/verify-engine.sh:162-183` — Adjacent mutable globals carry cleanup, process, evidence-transaction, registration, and reporting state across the module.
- `scripts/lib/verify-engine.sh:329-390` — A name-passed associative array plus string-named callback fields acts as the gate-policy schema and dependency-injection protocol.
- `scripts/lib/verify-engine.sh:555-653` — Backup, staging, displacement, rollback, and finalization of live evidence form a stateful transaction embedded in the lifecycle module.
- `scripts/lib/verify-engine.sh` — measurement: 991 lines and 30 top-level shell functions at the audit pin — The shared gate engine remains framework-sized.

## Author constraints (all must be honored)

- Do not introduce a TypeScript core or move locks, mktemp, Git, process control, or evidence publication out of Bash.
- Preserve all sourced names and public entry paths during the move.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-124` — Move-only internal Bash decomposition remains outside the substrate refusal.

## Provenance (origins — carry into the leaf Source line context)

- B · lane-01-harness-core · `triage/batch1-rejected.json::rejected[0]` — original title: "Verification lifecycle implemented as an untyped Bash framework"; rejected because: The verify-stage rejection held that a TypeScript core re-litigated the Bash-vs-TS substrate ruling and CQ25-124/CQ25-125 while accepting that the factual observations held.

## Existing-leaf relations

- Dedup basis from 1c: Current leaf 117 expressly excludes scripts/lib/verify-engine.sh as a separate finding at 117-verify-metadatash-second-kitchen-sink-shell.md:167-169; the substrate boundary is recorded at code-quality-2026-07-25/HARNESS-CLUSTER-PLAN.md:62-75.
- promoter hint (non-authoritative): looks close to leaf 117 — hint only; that leaf calls verify-engine.sh the same smell but excludes it as separate work

