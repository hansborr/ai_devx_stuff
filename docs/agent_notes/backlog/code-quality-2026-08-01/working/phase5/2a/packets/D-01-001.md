# Author packet — D-01-001 → `228-align-shared-export-guidance-with-adr-0005-s.md`

- Ruling: **new-leaf**
- Leaf number: **228**
- Title (settled): Align shared export guidance with ADR-0005's scoped-only decision
- Theme: Resolve the promised shared aggregate-export review against ADR-0005's barrel prohibition
- Area: docs · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Reaffirm scoped-only shared imports and remove the MODULE.md promise of a future aggregate/root barrel review. Point contributors to ADR-0005 and its specific-subpath extension process.

## Problem statement from the promotion candidate

The live shared guide promises a separate backlog decision about root or schema aggregate exports, while ADR-0005 treats those exports as a prohibited, load-bearing architecture choice. CQ25-115 landed schema-layout work but never performed that promised review. Contributors and audits therefore receive incompatible answers about whether an aggregate entrypoint is intended future work or a forbidden regression.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/schemas/MODULE.md:30-34` — The live module guide says there is no schema or root barrel and explicitly defers a barrel/aggregate export to a separate backlog leaf.
- `docs/adr/0005-shared-subpath-exports.md:29-43` — ADR-0005 makes the opposite durable decision: only scoped subpaths are allowed, the schemas barrel stays removed, and the absent root export is actively gated.
- `packages/shared/package.json:8-32` — The implemented package surface contains wildcard scoped exports and constants, with neither a root dot export nor a schemas aggregate.
- `packages/client/src/components/character-create/steps/spell-selection-step.tsx:1-3` — A representative consumer needs three defining-file imports across constants, rules, and schemas, illustrating the navigation/API question the deferred review would adjudicate.

## Author constraints (all must be honored)

- Do not add a root or schemas aggregate export.
- Do not weaken leaf 153's restricted-import gate or ADR message.
- A future reconsideration requires bundle and dependency-graph evidence plus an ADR and gate update.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-115` — The live MODULE deferred an aggregate-export review that the closed cluster never performed.

## Provenance (origins — carry into the leaf Source line context)

- D-01 · lane-04-shared · `wave-1/lane-04.json::droppedAsPriorPackDuplicate[0]` — original title: "Add a shared root or schemas barrel"; rejected because: The completed shared cluster deliberately retained concrete deep subpaths.

## Existing-leaf relations

- Dedup basis from 1c: The conflicting promise is packages/shared/src/schemas/MODULE.md:30-34 and :192-195; ADR-0005's binding decision is docs/adr/0005-shared-subpath-exports.md:29-53. Leaf 021 excludes export redesign at :128-132, and leaf 153 preserves the ADR fence at :58-104.
- promoter hint (non-authoritative): Looks adjacent to leaf 021, which audits the shared package's wildcard export/build surface — hint only.
- promoter hint (non-authoritative): Any aggregate-export decision would interact with leaf 153's enforcement of the removed schemas-barrel restriction — hint only.

