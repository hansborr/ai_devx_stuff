# Author packet — C-009 → `025-spellcastingts-contains-five-independently.md`

- Ruling: **augment-existing-leaf** (edit `025-spellcastingts-contains-five-independently.md` in place)
- Leaf number: **025**
- Title (settled): Name cantrip fallback tests as Musi policy
- Theme: Spellcasting fallback tests hide that their tables are Musi policy
- Area: tests · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Rename the affected test cases during leaf 025's cantrip test split so their full names identify Musi non-SRD fallback policy.

## Augment instruction (what to add or change in the target leaf)

In the cantrips test-split step and acceptance criteria, require every fallback-policy describe/it name—including the unknown-class case—to say it pins Musi non-SRD policy. Preserve the tables, assertions, and existing provenance comments.

## Problem statement from the promotion candidate

Test output makes fallback values look like generic 5E progression rules although production classifies them as Musi policy and SRD class-specific counts differ.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/rules/spellcasting.ts:251-252` — The full-caster cantrip table is explicitly classified as Musi non-SRD fallback policy.
- `packages/shared/src/rules/spellcasting.ts:276-300` — Half- and third-caster variants carry the same explicit Musi non-SRD classification.
- `packages/shared/src/rules/spellcasting.test.ts:569-610` — Nine policy-protecting cases use generic progression names with no Musi-policy classification.
- `packages/shared/src/rules/spellcasting.test.ts:666-670` — The unknown-class fallback case describes behavior without identifying its authority.
- `docs/guides/change-rules-logic.md:38-40` — The guide requires behavior absent from the SRD to remain explicit app policy and test names to identify that policy.

## Author constraints (all must be honored)

- Do not invent SRD provenance or replace the accepted fallback policy.
- Apply the standing provenance requirement to every policy-protecting case moved by the slice.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-95` — Existing generic fallback test names do not state the policy they pin.
- `CQ25-182` — The accepted Musi-policy classification must be propagated into test names.

## Provenance (origins — carry into the leaf Source line context)

- C · lane-06-tests · `wave-1/lane-06.json::coverage.cut[1]` — original title: "Shared policy-provenance naming sweep"; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 025 moves the cantrip tables and splits their tests at 025-spellcastingts-contains-five-independently.md:99-140; the binding naming requirement is code-quality-2026-07-25/CONSTRAINTS.md:30.
- promoter hint (non-authoritative): Leaf 25 moves the same cantrip family and preserves comments but does not classify existing test names.

