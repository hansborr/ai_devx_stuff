# Author packet — C-014 → `082-devcontainer-quick-start-depends.md`

- Ruling: **augment-existing-leaf** (edit `082-devcontainer-quick-start-depends.md` in place)
- Leaf number: **082**
- Title (settled): Remove the unused duplicate devcontainer server-start helper
- Theme: Devcontainer installs an unused second server-start helper
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Prefer deleting the unused helper and its image-install wiring, leaving container-entrypoint.sh as the sole supported server-start path.

## Augment instruction (what to add or change in the target leaf)

Before the replacement-point checklist, add a source/consumer census for start-servers.sh. If the existing zero-consumer result holds, delete the helper and remove its Dockerfile COPY/chmod and README checklist mentions; keep container-entrypoint.sh as the sole startup path. If a supported consumer is discovered, document that workflow and share the setup-sentinel contract instead.

## Problem statement from the promotion candidate

The image carries two divergent server-start paths, one setup-gated and one not. The uninvoked executable can drift while appearing supported.

## Verified evidence (existence-checked at step 1b)

- `.devcontainer/Dockerfile:3-5` — Copies and chmods both container-entrypoint.sh and start-servers.sh.
- `.devcontainer/container-entrypoint.sh:14-17` — The active entrypoint independently starts bun run dev after the setup sentinel.
- `.devcontainer/start-servers.sh:1-4` — The second helper starts the same command but omits the setup-sentinel contract.
- `.devcontainer/start-servers.sh` — measurement: 0 tracked execution or documentation references; search finds only Dockerfile installation plus lint/test inventory mentions — No supported workflow invokes or explains the installed helper.

## Author constraints (all must be honored)

- Do not change post-create provisioning or entrypoint sentinel behavior.
- Re-verify all factual claims in the edited devcontainer documentation.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-07-docs-dx · `wave-1/lane-07.json::coverage.cut[9]` — original title: "The devcontainer installs an unused second server-start helper."; rejected because: per-lane-budget:weak-or-absent-rationale (sample rank 3)

## Existing-leaf relations

- Dedup basis from 1c: Leaf 082 identifies start-servers.sh at 082-devcontainer-quick-start-depends.md:59-64 and schedules its reuse-checklist treatment at :101-111, providing the exact augmentation seam.
- promoter hint (non-authoritative): Leaf 82 covers devcontainer prerequisites and reuse accuracy, not this duplicate startup path.

