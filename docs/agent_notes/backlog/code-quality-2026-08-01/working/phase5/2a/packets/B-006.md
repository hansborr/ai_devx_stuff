# Author packet — B-006 → `208-converge-drift-ai-command-result-and-sentinel-error.md`

- Ruling: **new-leaf**
- Leaf number: **208**
- Title (settled): Converge drift-ai command result and sentinel-error lifecycles
- Theme: Drift-ai command families redeclare one result and error lifecycle
- Area: harness · Severity: **medium** · Size: **M** · Confidence: medium
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Introduce a neutral run-result contract and one helper for mapping DriftAiHelp and DriftAiError to command results. Converge the five command families while retaining command-specific parsing, execution, and formatting.

## Problem statement from the promotion candidate

One CLI contract is represented by per-command result aliases, prototype forwarding wrappers, and five independent help/error mappings. A lifecycle rule or result-field change therefore requires a broad coordinated edit, and copies can diverge in which phases they catch.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/prototype-command.ts:14-21` — The prototype framework defines the common exitCode/stdout result and descriptor boundary.
- `scripts/drift-ai/prototype-command.ts:43-53` — Prototype commands map DriftAiHelp and DriftAiError to exit codes 0 and 2 locally.
- `scripts/drift-ai/class-construction-command.ts:39-50` — A representative prototype command redeclares the result shape and forwards through a near-identical wrapper.
- `scripts/drift-ai/runner.ts:66-69` — The main runner independently declares the same base result fields.
- `scripts/drift-ai/runner.ts:106-125` — The main path separately implements the same help/error mapping.
- `scripts/drift-ai/config-inspect-command.ts:47-87` — Config inspection carries another complete parse/run/output/error lifecycle.
- `scripts/drift-ai/hotspots.ts:64-88` — Hotspots declares the same result shape and help/error mapping.
- `scripts/drift-ai/coldspots.ts:65-91` — Coldspots independently repeats the same result and error lifecycle.

## Author constraints (all must be honored)

- Do not derive command parsing, help prose, or full result assembly from descriptors; preserve leaf 120's boundary.
- Do not combine this with leaf 161's entrypoint-only cleanup; sequence overlapping runner edits.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[5]` — original title: "Prototype commands redeclare identical result and forwarding shells"; rejected because: per-lane-budget:weak-or-absent-rationale
- B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[22]` — original title: "Prototype and main CLI paths duplicate error/output lifecycle policy"; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 120 preserves hand-written result assembly at 120-cli-option-models-remain-parallel-registries.md:125-145; leaf 161 excludes runner/output behavior at 161-drift-ai-executable-exposes-incoherent-23.md:66-87.
- promoter hint (non-authoritative): looks adjacent to leaf 120, which owns option parsing and registries rather than result/error lifecycle — hint only
- promoter hint (non-authoritative): looks adjacent to leaf 161, which excludes runner/output behavior — hint only

