# Author packet — C-004 → `219-give-repository-workflows-cwd-stable-scope-bearing.md`

- Ruling: **new-leaf**
- Leaf number: **219**
- Title (settled): Give repository workflows cwd-stable, scope-bearing command names
- Theme: Repository workflow commands remain dependent on the caller's current directory
- Area: cross-cutting · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add explicit root-workflow aliases such as repo:dev and repo:build, retain conventional package-local names, and migrate contributor-facing snippets to root-anchored invocations. Do not add a dispatcher.

## Problem statement from the promotion candidate

A copied bun run dev, build, or root-only workflow changes meaning or fails solely because of shell location. The documented workaround helps informed contributors but does not make the vocabulary unambiguous.

## Verified evidence (existence-checked at step 1b)

- `package.json:22-31` — At repository root, dev launches the repository workflow and build composes all three packages.
- `packages/shared/package.json:34-36` — In shared, the same vocabulary means TypeScript watch or package-only compilation.
- `packages/server/package.json:12-22` — In server, dev starts only the server and the manifest exposes package-local database operations.
- `scripts/README.md:118-124` — Documentation confirms that Bun selects the nearest package.json and root-only commands fail from package subdirectories.

## Author constraints (all must be honored)

- Respect the original rejection of a command dispatcher.
- Use the canonical bun --cwd="$(git rev-parse --show-toplevel)" run form in documentation.
- Coordinate catalog metadata with leaf 085.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-08-cross-cutting · `triage/batch2-rejected.json::rejected[8]` — original title: "Repository command meaning changes silently with the current working directory"; rejected because: The cwd tripwire is real and recurring (documented in scripts/README.md:118-124 and worked around by the AGENTS.md --cwd convention), but the proposed dispatcher does not eliminate the underlying nearest-package.json ambiguity — package-local `bun run dev` would still mean something different — it only adds a parallel bespoke entry point. The canonical `bun --cwd="$(git rev-parse --show-toplevel)"` form is already documented, dependency-free, and itself copyable, while a dispatcher adds a new hand-maintained harness surface (script registration, doc/guide migration across AGENTS.md and generated verify wiring) whose churn outweighs the marginal DX gain. Rejection is on fix economics, not on the problem's existence.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 085 excludes renaming/restructuring at 085-specialist-package-script-surface-has-no.md:139-145; leaf 104 is confined to one recovery edit and excludes manifest changes at 104-scripts-readme-gives-non-root-recovery.md:49-70.
- promoter hint (non-authoritative): Leaves 85 and 104 address command discoverability and broken nested-cwd recovery, but not the broader dev/build scope collision.

