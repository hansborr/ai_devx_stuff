# Author packet — B-002 → `205-rename-auth-service-to-reflect-its.md`

- Ruling: **new-leaf**
- Leaf number: **205**
- Title (settled): Rename auth-service to reflect its authentication-primitive boundary
- Theme: auth-service is named and catalogued as orchestration although it contains only authentication primitives
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Keep authentication workflows in the router and rename auth-service.ts to an accurate primitive-oriented name such as auth-primitives.ts. Update imports and the service inventory atomically.

## Problem statement from the promotion candidate

The filename and service inventory promise request-level authentication orchestration, but the file is a cryptography/token primitive module and the router is the actual lifecycle owner. Repository navigation therefore points maintainers to the wrong boundary.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/services/auth-service.ts:23-114` — The module exports password, timing-oracle, JWT, and refresh-token primitives but owns no account or session workflow.
- `packages/server/src/routers/auth.ts:69-98` — The router-local helper creates persisted sessions, tokens, request metadata, and cookies.
- `packages/server/src/routers/auth.ts:100-330` — Registration, login, refresh, logout, profile, password, and deletion workflows remain in the router.
- `packages/server/src/services/README.md:198-202` — The service inventory labels auth-service.ts a flat service, reinforcing the misleading ownership signal.

## Author constraints (all must be honored)

- Do not extract router workflows or alter authentication behavior.
- Coordinate the services/README.md edit with leaf 004 if both are implemented nearby.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-03-server · `triage/batch1-rejected.json::rejected[5]` — original title: "The auth router is the request-facing auth service in everything but name"; rejected because: The judge rejected the broad workflow extraction but explicitly identified the misleading auth-service name as a real, unadjudicated S-sized residual suitable for renomination.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 004 edits the same inventory only for character creation and is confined to that move at 004-character-creation-large-pseudo-module-loose.md:130-151; no prior-pack refusal covers this rename.

