# Author packet — C-008 → `222-make-multiclass-prerequisite-table-states-exclusive.md`

- Ruling: **new-leaf**
- Leaf number: **222**
- Title (settled): Make multiclass prerequisite table states exclusive and non-empty
- Theme: Multiclass prerequisite types admit rule states the table never defines
- Area: shared · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Define an exclusive union between a non-empty all-requirement map and a non-empty anyOf tuple. Preserve current table rows and add focused shape/evaluator coverage.

## Problem statement from the promotion candidate

The exported table value type admits {}, both modes, empty arrays, and empty requirements; those states silently pass, fail malformedly, or acquire semantics absent from the rule table.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/rules/multiclass-rules.ts:18-28` — The two rule modes are optional over empty-permitting records, admitting neither, both, empty requirements, and empty groups.
- `packages/shared/src/rules/multiclass-rules.ts:30-49` — Every actual SRD row uses exactly one non-empty mode.
- `packages/shared/src/rules/multiclass-rules.ts:69-85` — An empty anyOf group passes vacuously, while an empty group list fails and renders an empty description.
- `packages/shared/src/rules/multiclass-rules.ts:92-99` — The evaluator independently runs both branches, assigning mixed rows implicit AND semantics.

## Author constraints (all must be honored)

- Do not change numeric prerequisites or public string-lookup fallbacks.
- Classify and name rule provenance in tests per the standing rules guide.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-115` — Optional-bag prerequisite values remain inference-visible impossible states.

## Provenance (origins — carry into the leaf Source line context)

- C · lane-04-shared · `wave-1/lane-04.json::coverage.cut[6]` — original title: "MulticlassPrerequisite admits empty and mixed all/anyOf states absent from the rule table."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: CQ25-115/I2 changes only keys and preserves behavior at code-quality-2026-07-25/SHARED-CLUSTER-PLAN.md:560; leaf 026 covers monster provenance only at 026-monster-provenance-invariants-disappear.md:77-109.
- promoter hint (non-authoritative): Similar impossible-state mechanism to leaf 26, but on a distinct multiclass rule-table contract.

