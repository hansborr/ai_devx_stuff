# Author packet — C-001 → `217-correct-the-architecture-plan-s-unsupported-level-up.md`

- Ruling: **new-leaf**
- Leaf number: **217**
- Title (settled): Correct the architecture plan's unsupported level-up rollback claim
- Theme: Architecture plan overstates level-choice audit rows as rollback support
- Area: docs · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Describe CharacterLevelChoice as partial read-only audit/history data. State explicitly that rollback or downgrade needs a separate product, data, and inverse-operation design.

## Problem statement from the promotion candidate

The stable architecture guide says persisted choice rows support rollback, but those rows are partial read-only history rather than a reversible mutation manifest. Contributors can design around an inverse workflow the data and router do not provide.

## Verified evidence (existence-checked at step 1b)

- `docs/architecture-plan.md:75-77` — States in the present tense that CharacterLevelChoice records let recent decisions be reviewed and rolled back.
- `packages/server/src/services/level-up/apply-level-up.ts:16-26` — The level-up transaction performs ten classes of writes, including stats, class rows, features, sorcery state, subclass state, and spell slots.
- `packages/shared/src/schemas/character.ts:206-213` — The generic audit row records only HP method/gain, roll, and class ID, not complete pre-state or inverse operations.
- `packages/server/src/routers/character.ts:121-149` — The router exposes levelUp but no undo, downgrade, or rollback mutation.

## Author constraints (all must be honored)

- Do not add a rollback feature to this docs correction.
- Verify the entire materially edited architecture claim, not only the word rollback.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-07-docs-dx · `triage/batch2-directions.json::refuteReviews[1]` — original title: "Architecture documentation promises level-up review and rollback that the product does not expose"; rejected because: All three evidence defects verify: pinned client grep shows 2 levelChoices matches (sheet-helpers.test.ts, test/fixtures-character.ts), both non-production, not the finding's claimed 3; architecture-plan.md is a stable-architecture rationale doc that routes execution status elsewhere, and the sentence is data-model enablement rationale, not a shipped-workflow promise; character-mapping.ts:19,101 includes and maps levelChoices into CharacterDetail, so character.get does expose the review data. The finding materially overreads.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 192 states the read-only limitation at 192-expose-level-up-history-already-returned.md:10-12 and expressly leaves the architecture wording correction separate at :56.
- promoter hint (non-authoritative): Leaf 192 treats levelChoices as read-only history and leaves the architecture-plan wording correction outside that feature leaf.

