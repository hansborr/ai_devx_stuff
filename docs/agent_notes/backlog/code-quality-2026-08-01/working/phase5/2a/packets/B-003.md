# Author packet — B-003 → `206-single-source-memory-wait-timeout-validation-across.md`

- Ruling: **new-leaf**
- Leaf number: **206**
- Title (settled): Single-source memory-wait timeout validation across verification entry paths
- Theme: Memory-wait timeout validation diverges across verify entry paths
- Area: harness · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add one shared signed-range timeout parser beside the memory-budget implementation and use it from both scheduler forms. Let pre-commit apply its separate policy cap after shared validation.

## Problem statement from the promotion candidate

The same environment knob has three validation contracts. Direct verify and direct-tool paths accept digit strings outside the shell's usable integer range and later feed them to numeric comparisons, while pre-commit alone bounds them, so malformed large values can break rather than enforce timeout handling.

## Verified evidence (existence-checked at step 1b)

- `scripts/verify/steps-lib.sh:257-263` — The parallel scheduler accepts every non-empty digit string as a valid wait timeout.
- `scripts/verify/steps-lib.sh:310-311` — The accepted string is later consumed by a Bash numeric comparison without an overflow bound.
- `scripts/verify/memory-budget.sh:436-461` — Direct-tool admission repeats the digit-only validation and later performs the same numeric comparison.
- `.husky/pre-commit:315-345` — Pre-commit separately normalizes the value, rejects values beyond signed-64-bit range, applies its policy cap, and exports the result.

## Author constraints (all must be honored)

- Preserve the pre-commit policy cap as a distinct layer.
- Reject values that Bash numeric comparisons cannot safely consume.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-01-harness-core · `wave-1/lane-01.json::coverage.cut[11]` — original title: "Memory-budget and timeout parsing are repeated across verify helpers."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 110 covers named artifact dependencies at 110-parallel-verify-dependencies-hidden.md:95-159 and separates memory-scheduler hygiene at :179-190; no timeout-validation coverage appears there.
- promoter hint (non-authoritative): looks close to leaf 110 — hint only; that leaf addresses scheduler dependency data and leaves memory-scheduler hygiene outside its core direction

