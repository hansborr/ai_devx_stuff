# Author packet — B-010 → `212-give-initiative-orchestration-layers.md`

- Ruling: **new-leaf**
- Leaf number: **212**
- Title (settled): Give initiative orchestration layers depth-signalling names
- Theme: Initiative rolling obscures three orchestration depths behind two names and an alias
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Rename the caller-owned middle layer to a core-signalling name and give the persistence implementation a role-specific name. Import all layers without same-name aliases.

## Problem statement from the promotion candidate

Three initiative layers are exposed through two names, with an import alias hiding the collision. Navigation lands on indistinguishable symbols and violates the depth convention used by adjacent orchestration.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/services/encounter-combat/initiative-action.ts:7-28` — The request-facing rollAllInitiative imports a same-named deeper function under the alias executeRollAllInitiative.
- `packages/server/src/services/combat-actions/combat-actions.ts:187-203` — The middle caller-owned layer is also named rollAllInitiative and invokes a lower executeRollAllInitiative.
- `packages/server/src/services/combat-actions/initiative.ts:27-55` — The lowest layer performs loading, rolling, sorting, and persistence under executeRollAllInitiative.
- `packages/server/src/services/README.md:151-171` — The repository requires wrapper/core pairs to use depth-signalling names.

## Author constraints (all must be honored)

- Coordinate with CQ25-58/T3 if it edits the low-level module.
- Do not change initiative ordering or persistence behavior.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-58` — Service-layer orchestration names and their import alias remain uncovered.

## Provenance (origins — carry into the leaf Source line context)

- B · lane-03-server · `wave-1/lane-03.json::coverage.cut[11]` — original title: "Initiative orchestration uses three overlapping depth names."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: CQ25-58/T3 covers ordered reads and activation sorting at code-quality-2026-07-25/53-PLAN.md:302, not the three service names; no current exact match was found.

