# Author packet — C-005 → `220-add-a-joined-explain-view-for-harness-control.md`

- Ruling: **new-leaf**
- Leaf number: **220**
- Title (settled): Add a joined explain view for harness control provenance
- Theme: Harness provenance remains split across non-joinable views
- Area: harness · Severity: **medium** · Size: **M** · Confidence: medium
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add a read-only --explain mode to an existing harness registration/check entrypoint, keyed by path, control ID, or package-script name, with stable text and JSON output. Derive results from existing manifest and path-policy authorities.

## Problem statement from the promotion candidate

No supported query joins a changed path or package-script name to the controls, smoke subjects, verify slots, hooks, and generated outputs governing it. Existing surfaces answer isolated portions only.

## Verified evidence (existence-checked at step 1b)

- `scripts/README.md:126-140` — Adding or moving a script requires manually correlating smoke coverage, path policy, package scripts, manifest data, and prose.
- `scripts/harness/registration-check.ts:66-80` — The API returns validation failures, state, and generated surfaces but offers no path/control provenance query.
- `scripts/path-policy/path-policy-query-core.ts:136-154` — The path query computes selected smoke names without explaining the matching subject or connecting controls, slots, hooks, and generated outputs.
- `docs/generated/harness-controls.md:3-8` — The generated authoritative view is a control-kind inventory, not a machine query keyed by path or package script.

## Author constraints (all must be honored)

- Do not create the separately registered command rejected by triage.
- Do not create a second manifest or hand-maintained join table.
- Discovery must be non-vacuous and independent of any allowlist it validates.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-08-cross-cutting · `triage/batch2-rejected.json::rejected[9]` — original title: "Expose a machine-queryable harness provenance view"; rejected because: Recorded rejection: the common provenance questions are already answered by existing surfaces — path-policy query core for changed-path-to-smoke selection, the generated harness-controls Markdown rendered from the manifest, harness:check/registration-check diagnostics, and doctor's follow-up commands. A new joined query tool would itself become a registered harness surface (package script, smoke, path-policy row, manifest entry, docs) that must track every manifest facet change; that ongoing churn exceeds the low-severity discoverability gain, and the harness's copyability rests on the controls and generators, not on an extra viewer.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 085's catalog is command-keyed at 085-specialist-package-script-surface-has-no.md:86-108; leaf 094 only settles documentation authority at 094-two-documents-both-claim-be-authoritative.md:53-72; leaf 152 excludes new query results at 152-path-policy-query-core-closed-over-musis.md:108-112.
- promoter hint (non-authoritative): Leaves 85 and 94 address command documentation and inventory authority but do not supply a joined path-to-control query.

