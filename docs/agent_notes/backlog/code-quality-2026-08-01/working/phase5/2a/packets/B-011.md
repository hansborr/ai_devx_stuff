# Author packet — B-011 → `182-logging-producers-their-auditor-separately.md`

- Ruling: **augment-existing-leaf** (edit `182-logging-producers-their-auditor-separately.md` in place)
- Leaf number: **182**
- Title (settled): Close server-local mutation event and reason vocabularies
- Theme: Mutation logging documents stable vocabulary but accepts arbitrary event and reason strings
- Area: server · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Close mutation event and reason strings at the server logger boundary while keeping them server-local. Sweep all producers before defining the unions so no live vocabulary is lost.

## Augment instruction (what to add or change in the target leaf)

Add a producer-side step defining server-local MutationEvent and MutationReason unions from the complete production literal census, apply them to MutationLogPayload and runTopLevelCommand, and add compile-time/runtime coverage. Clarify that these exact names remain server-local and are not exported through shared logging policy.

## Problem statement from the promotion candidate

Operational grouping depends on exact event and reason spellings, but the logger and shared wrapper accept any string. A typo compiles and silently creates a new event family or reason bucket, unlike the adjacent closed authorization vocabulary.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/utils/request-logger.ts:94-114` — The contract calls mutation events and reasons stable low-cardinality codes, yet declares both as unrestricted string.
- `packages/server/src/services/character-live-state/side-effects.ts:27-38` — The shared command wrapper accepts an arbitrary event string and forwards it to logMutation.
- `packages/server/src/routers/auth.ts:145-179` — Authentication producers rely on exact repeated event and reason literals.
- `packages/server/src/routers/encounter.ts:170-177` — Encounter failure logging adds another stable pair with no compile-time vocabulary check.

## Author constraints (all must be honored)

- Do not share exact event/reason names with logs-audit or @musi/shared.
- Preserve leaf 182's producer/auditor asymmetry and sequence jointly with leaf 171.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-03-server · `wave-1/lane-03.json::coverage.cut[16]` — original title: "Mutation log events and reasons remain open strings."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 182 centralizes outcomes at 182-logging-producers-their-auditor-separately.md:51-95 but keeps exact vocabularies server-local at :78-81 and :118-120; that is the natural augmentation seam.
- promoter hint (non-authoritative): looks close to leaf 182 — that leaf centralizes logging policy but leaves exact mutation vocabularies server-local

