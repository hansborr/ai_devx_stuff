# Author packet — C-007 → `221-rename-metamagic-slot-terminology-to-options-known.md`

- Ruling: **new-leaf**
- Leaf number: **221**
- Title (settled): Rename metamagic slot terminology to options-known terminology
- Theme: Metamagic option counts are named as slots in the shared API and caller
- Area: shared · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Rename getMetamagicSlotsAtLevel to getMetamagicOptionsKnownAtLevel and maxSlots to maxOptions, updating focused tests without changing arithmetic.

## Problem statement from the promotion candidate

Metamagic choices are known options, not spell slots. In a level-up path that also manages actual spell slots, the naming obscures which resource is capped.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/rules/sorcery-points.ts:160-170` — JSDoc says the function returns cumulative metamagic options known, while the export is getMetamagicSlotsAtLevel.
- `packages/server/src/services/level-up/sorcerer.ts:56-62` — The sole production caller propagates the misnomer into maxSlots although the error says metamagic options.
- `packages/shared/src/rules/sorcery-points.test.ts:211-234` — The focused tests repeat the slots name across every level band.

## Author constraints (all must be honored)

- Coordinate same-file edits with leaf 029.
- Do not alter option counts or progression.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-04-shared · `wave-1/lane-04.json::coverage.cut[5]` — original title: "getMetamagicSlotsAtLevel names known options as slots."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 029 identifies the current consumer at 029-one-metamagic-constant-controls-two.md:31-34 but its remedy is limited to the per-cast constant at :56-68.
- promoter hint (non-authoritative): Leaf 29 touches the consumed metamagic-picks constant but does not correct this function or caller naming.

