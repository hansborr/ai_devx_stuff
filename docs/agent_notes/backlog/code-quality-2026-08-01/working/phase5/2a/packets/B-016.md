# Author packet — B-016 → `214-derive-the-character-sheet-srd-lookup-bundle-once-at.md`

- Ruling: **new-leaf**
- Leaf number: **214**
- Title (settled): Derive the character-sheet SRD lookup bundle once at composition
- Theme: Character-sheet composition derives the SRD lookup bundle twice
- Area: client · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Pass the already-resolved lookup bundle from CharacterSheetContent into the sheet body and useSheetState boundary, removing the nested useSrdLookups invocation.

## Problem statement from the promotion candidate

One character-sheet mount creates two observers of the same query and independently rebuilds the complete lookup projection. The parent already owns the loading boundary, so the second derivation is avoidable and obscures ownership.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/pages/character-sheet/sheet-layout.tsx:72-97` — CharacterSheetContent invokes useSrdLookups to gate loading and error rendering.
- `packages/client/src/pages/character-sheet/sheet-layout.tsx:100-114` — The mounted body immediately calls useSheetState without receiving the lookup result already obtained by its parent.
- `packages/client/src/pages/character-sheet/sheet-state.ts:98-117` — useSheetState invokes useSrdLookups a second time and returns that second bundle.
- `packages/client/src/hooks/use-srd-lookups.ts:118-148` — Each hook instance memoizes its own derivation of five collections into maps, records, and lookup closures.

## Author constraints (all must be honored)

- Land after or rebase onto leaf 200's lookup-bundle rename.
- Coordinate with leaf 039 if it changes the sheet composition boundary.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-05-client · `wave-1/lane-05.json::coverage.cut[23]` — original title: "The character sheet derives the full SRD lookup bundle twice."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Leaf 200 preserves behavior and changes vocabulary only at 200-generic-srd-getall-contract-client-specific.md:43-75; leaf 039 excludes SheetSharedProps changes at 039-responsive-character-sheets-keep-two.md:118-125.
- promoter hint (non-authoritative): looks adjacent to leaf 200, which renames the lookup-bundle contract but does not remove the duplicate hook invocation

