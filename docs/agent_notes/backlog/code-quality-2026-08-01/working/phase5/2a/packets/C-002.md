# Author packet — C-002 → `218-replace-the-coverage-cadence-guide-s-deleted.md`

- Ruling: **new-leaf**
- Leaf number: **218**
- Title (settled): Replace the coverage-cadence guide's deleted baseline pointer
- Theme: Coverage cadence points operators at deleted baseline records
- Area: docs · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Replace the nonexistent LOG/AUD-COV pointer with the exact historical path and baseline commit, or restore a current tracked summary. Keep Git-history recovery instructions concrete and reproducible.

## Problem statement from the promotion candidate

The weekly procedure is technically recoverable from full Git history, but its documented baseline pointer is false and omits the deleted artifact's path and commit. Operators must perform archaeology before applying the stated drift thresholds.

## Verified evidence (existence-checked at step 1b)

- `docs/guides/coverage-cadence.md:14-23` — Points to AUD-COV entries allegedly in LOG.md, admits the original notes were deleted, and tells operators to use Git without naming the deleted path or commit.
- `docs/agent_notes/LOG.md` — measurement: 0 matches for `AUD-COV|2026-05-13`, reproduced with `rg -n 'AUD-COV|2026-05-13' docs/agent_notes/LOG.md` — The guide's direct comparison pointer does not resolve in the named document.

## Author constraints (all must be honored)

- Do not claim the historical baseline is irrecoverable.
- Use commit 54f2eb910 and the historical docs/agent_notes/in_progress/codebase-audit/coverage.md path if retaining the Git-history approach.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- C · lane-07-docs-dx · `triage/batch2-directions.json::refuteReviews[2]` — original title: "Coverage cadence has no retained baseline to compare against"; rejected because: Commit 54f2eb910 ('docs(audit): record coverage baseline (AUD-COV-001)') contains the full per-scope metric tables in docs/agent_notes/in_progress/codebase-audit/coverage.md, recoverable by searching git history for the AUD-COV identifiers the guide supplies, and the guide instructs git log -p against the previously tracked baseline. The core claim of literal irreproducibility fails; the surviving stale-pointer hygiene point is conceded within the refutation itself.

## Existing-leaf relations

- Dedup basis from 1c: No current semantic match was found; leaf 083's LOG.md references are unrelated at 083-sole-progress-queue-mostly-stale-view.md:72-81.

