# Author packet — B-005 → `207-encode-ghost-files-mode-dependencies-as.md`

- Ruling: **new-leaf**
- Leaf number: **207**
- Title (settled): Encode ghost-files mode dependencies as discriminated scope contracts
- Theme: Ghost-files mode requirements are not encoded in its scope types
- Area: harness · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make DetectorScope a mode-discriminated union whose file variants match the mode. Give ghost-files mode-specific option variants that require inventory for current scope and directory listing for changed scope.

## Problem statement from the promotion candidate

The type system accepts impossible ghost-file scan inputs: a mode can carry the other mode's file variants, current scope can omit inventory, and changed scope can omit directory listing. Those states throw only at execution or quietly erase files during filtering.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/scope.ts:19-22` — DetectorScope stores scopeMode beside an unconstrained ScopeFile array, so mode and file variants can disagree.
- `scripts/drift-ai/scope.ts:42-52` — changedFilesFromScope silently filters away non-changed variants instead of receiving a mode-consistent collection.
- `scripts/drift-ai/ghost-files.ts:35-49` — RunGhostFilesCheckOptions makes both changed-mode listDirectory and current-mode inventoryByDir optional without a mode-discriminated relationship.
- `scripts/drift-ai/ghost-files.ts:51-70` — The implementation restores the missing contract through branches and runtime checks.

## Author constraints (all must be honored)

- Preserve current findings and scope behavior for valid callers.
- Do not broaden the CQ25-10 triage-contract slices into this work.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-10` — DetectorScope mode/file correspondence and ghost-files dependencies remain uncovered.

## Provenance (origins — carry into the leaf Source line context)

- B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[0]` — original title: "Ghost-files scope requirements and positional pair state remain convention-only"; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: CQ25-10's live scope is bounded to unrelated analyzer contracts at code-quality-2026-07-25/34-PLAN.md:110-169; no matching authored leaf was found.

