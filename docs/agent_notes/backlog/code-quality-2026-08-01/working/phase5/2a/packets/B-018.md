# Author packet — B-018 → `216-route-npc-mutations-through-the-campaign.md`

- Ruling: **new-leaf**
- Leaf number: **216**
- Title (settled): Route NPC mutations through the campaign invalidation facade
- Theme: NPC mutations bypass the campaign invalidation facade
- Area: client · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add an explicit invalidateNpcs(campaignId) closure to useQueryInvalidation and use it from NPC create, update, and delete mutations.

## Problem statement from the promotion candidate

NPC-list invalidation is the sole campaign CRUD policy hidden inside its panel rather than the facade intended to own campaign-wide invalidation. Future consumers cannot discover or reuse it, and it uses a raw query-key shape instead of the facade convention.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/lib/query-invalidation.ts:6-16` — The facade declares ownership of campaign-wide cache invalidations and says additions should be one-place closure edits.
- `packages/client/src/lib/query-invalidation.ts:21-115` — The registry covers campaign, invite, homebrew, encounter, map, chat, note, and character domains but has no NPC-list invalidation.
- `packages/client/src/components/campaign/npcs/npc-panel.tsx:162-172` — The NPC panel builds a private invalidator from queryOptions(...).queryKey.
- `packages/client/src/components/campaign/npcs/npc-panel.tsx:179-205` — Create, update, and delete all depend on the panel-local invalidator.

## Author constraints (all must be honored)

- Do not introduce a descriptor map or generic invalidation binder.
- Preserve existing query-key and invalidation behavior.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-196` — The explicit NPC invalidation closure remains absent; the generic binder remains refused.

## Provenance (origins — carry into the leaf Source line context)

- B · lane-05-client · `wave-1/lane-05.json::coverage.cut[33]` — original title: "NPC CRUD bypasses the centralized invalidation layer."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: CQ25-196's explicit-closure boundary and generic-binder refusal are at code-quality-2026-07-25/CLIENT-CLUSTER-PLAN.md:260 and :592-593; no current exact match exists.

