# Author packet — B-017 → `215-converge-destructive-client-actions-on-one.md`

- Ruling: **new-leaf**
- Leaf number: **215**
- Title (settled): Converge destructive client actions on one accessible confirmation dialog
- Theme: Map and token destructive actions bypass the dialog confirmation system
- Area: client · Severity: **low** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Generalize the existing common destructive-confirmation dialog only as needed, then migrate both token removals, fog reset, drawing clear, and Dashboard character deletion. Preserve action-specific copy and pending behavior.

## Problem statement from the promotion candidate

Four destructive VTT actions use blocking browser prompts while entity deletion uses styled dialogs. This splits confirmation copy, accessibility, focus behavior, and pending-state policy; token removal is duplicated twice.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/campaign/tokens/token-sidebar.tsx:53-64` — Sidebar token removal uses a blocking browser confirm.
- `packages/client/src/components/campaign/tokens/token-context-menu.tsx:213-224` — Context-menu token removal independently repeats browser-confirm policy and copy.
- `packages/client/src/components/campaign/maps/map-toolbar-fog-tools.tsx:61-72` — Resetting all fog uses window.confirm instead of an application dialog.
- `packages/client/src/components/campaign/maps/map-toolbar-drawing-tools.tsx:73-84` — Clearing all drawings is the fourth independently worded browser confirmation.

## Author constraints (all must be honored)

- Do not introduce a global modal framework.
- Preserve each action's authorization and mutation ownership.
- Include B-015's pending-state requirements and remove its bespoke component.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- B · lane-05-client · `wave-1/lane-05.json::coverage.cut[29]` — original title: "Four map/token destructive actions use window.confirm beside DeleteConfirmDialog."; rejected because: per-lane-budget:weak-or-absent-rationale

## Existing-leaf relations

- Dedup basis from 1c: Prior F2 supplies the common component at code-quality-2026-07-25/CLIENT-CLUSTER-PLAN.md:263-264; current-pack search found no leaf covering the four window.confirm sites or Dashboard duplicate.

## Folded-in promotions (merge-with-promotion rulings)

### B-015 — Converge character deletion on the shared destructive-confirmation dialog

Contribution: Add the Dashboard character-deletion dialog as a fifth migration target; preserve its pending-state behavior, then delete the bespoke component.

Two near-identical confirmation components independently own destructive-dialog behavior and copy. Their pending state and labels have already diverged, so interaction and accessibility fixes must be remembered twice.

- `packages/client/src/components/delete-character-dialog.tsx:21-55` — A dedicated character dialog reimplements the full confirmation shell and disables both actions while deletion is pending.
- `packages/client/src/components/common/delete-confirm-dialog.tsx:22-54` — The common dialog implements the same flow but has already drifted on pending cancellation and label punctuation.
- `packages/client/src/pages/dashboard-page.tsx:237-243` — Dashboard wires the bespoke dialog even though its state and callbacks match the common component's contract.

