# Author packet — R3B-002 → `246-replace-the-eslint-cache-shell-to-typescript-text.md`

- Ruling: **new-leaf**
- Leaf number: **246**
- Title (settled): Replace the ESLint cache shell-to-TypeScript text protocol with one typed cache plan
- Theme: Give ESLint cache policy a typed reusable boundary
- Area: harness · Severity: **low** · Size: **M** · Confidence: medium
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Create one importable TypeScript cache-plan module for fingerprinting and the typed cache result, with lint-agent calling it directly and eslint-main.sh using a thin adapter. Preserve the current identity inputs, pruning behavior, keyed locations, and correctness-first uncached fallback.

## Problem statement from the promotion candidate

Cache identity correctness determines whether rule, configuration, and type-graph edits can be masked by stale ESLint diagnostics. The policy is single-sourced but trapped behind sourced shell, ambient globals, an embedded Node heredoc, and a line-oriented subprocess protocol. Tests cover current behavior and overt failures degrade safely to no cache, but the compiler cannot connect either consumer to the returned argument shape.

## Verified evidence (existence-checked at step 1b)

- `scripts/lib/eslint-main-cache.sh:60-117` — The shell helper writes its NUL-delimited path input to a temporary file, then embeds a Node program in a heredoc to hash paths and contents.
- `scripts/lib/eslint-main-cache.sh:148-187` — Cache preparation, stale-directory pruning, and argument construction communicate through ambient MUSI_ESLINT_MAIN_CACHE_DIR and MUSI_ESLINT_MAIN_CACHE_ARGS globals.
- `scripts/lint-agent.ts:38-72` — The TypeScript consumer constructs inline Bash that sources the shell library, splits stdout into argument lines, and checks only the first token before forwarding the remainder.

## Author constraints (all must be honored)

- Honor CQ25-124's measured boundary: concentrate deterministic analysis in TypeScript while leaving genuinely shell-specific process glue in the adapter.
- Do not retain parallel Bash and TypeScript implementations or replace the current line protocol with another weak textual protocol.
- Preserve lint-agent's safe degradation to an uncached run on every preparation or protocol failure.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-124` — The landed record supplies the Bash-facade/TypeScript-core precedent but does not schedule the ESLint cache identity, pruning, or argument boundary.

## Provenance (origins — carry into the leaf Source line context)

- R3B · lane-01-harness-core · `wave-1/lane-01.json::coverage.cut[23]` — original title: "ESLint cache policy mixes shell sourcing, embedded JavaScript, and textual protocols."; rejected because: coverage.cut — omitted for lane budget; no recorded rationale

## Existing-leaf relations

- Dedup basis from 1c: Opened 141-latest-log-discovery-mirrors-verify-state.md:10-25,94-114,140-156 and working/dedup-corpus.md:180, plus the live precedent boundary at ../code-quality-2026-07-25/CONSTRAINTS.md:66-73. None reaches scripts/lib/eslint-main-cache.sh or lint-agent's cache protocol.
- promoter hint (non-authoritative): looks close to leaf 141 — hint only; leaf 141 owns duplicated verify log-state discovery, not ESLint cache identity or argument transport

