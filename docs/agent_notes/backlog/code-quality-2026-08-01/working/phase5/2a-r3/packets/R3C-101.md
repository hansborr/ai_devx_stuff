# Author packet — R3C-101 → `053-filterselect-erases-schema-derived-option.md`

- Ruling: **augment-existing-leaf** (edit `053-filterselect-erases-schema-derived-option.md` in place)
- Leaf number: **053**
- Title (settled): Characterize SpellFilterBar's rendered select interactions
- Theme: Spell filter selects lack rendered interaction coverage
- Area: client · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make the three rendered select interactions the characterization spine for leaf 053's SpellFilterBar call-site migration.

## Augment instruction (what to add or change in the target leaf)

Add the rendered SpellFilterBar gap to Problem and Evidence, and require focused pre-refactor characterization for Level, School, and Status. Render controlled filters, exercise each select, assert representative labels and complete onFilterChange payloads, and retain those tests through the generic FilterSelect migration; clarify in Scope that this is caller-boundary coverage, not a broader sheet-filter refactor.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `053-filterselect-erases-schema-derived-option.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

The level, school, and prepared-status controls are user-facing filter paths, but tests stop at pure guards and text-search composition. Regressions in option population, current-value projection, labels, or callback wiring can make a select ineffective or update the wrong field while every existing test remains green.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/sheet/spell-filter-bar.tsx:74-112` — SpellFilterBar renders search plus Level, School, and Status controls with separate value conversions and filter patches.
- `packages/client/src/components/sheet/spell-filter-bar.test.ts:1-45` — The colocated suite imports and tests only pure conversion helpers; it never imports or renders SpellFilterBar.
- `packages/client/src/components/sheet/spells-panel.test.tsx:190-218` — The composition suite checks filter-bar presence and drives only text search, leaving all three rendered selects unexercised.

## Author constraints (all must be honored)

- Write the rendered characterization before changing FilterSelect's generic contract.
- Preserve leaf 053's numeric-level and domain-sentinel mappings.
- Do not expand into unrelated sheet filtering behavior.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R3C · lane-06-tests · `wave-1/lane-06.json::coverage.cut[12]` — original title: "Spell-filter rendered-control coverage"; rejected because: Coverage cut with no recorded rationale after the lane budget was filled.

## Existing-leaf relations

- Dedup basis from 1c: Opened 053-filterselect-erases-schema-derived-option.md:25-49,51-89. Its production scope directly edits the uncovered controls, making augmentation more coherent than a standalone test leaf.
- promoter hint (non-authoritative): looks close to leaf 053 — hint only

