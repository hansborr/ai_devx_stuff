# Author packet — R3B-003 → `184-human-readable-server-messages-act-client.md`

- Ruling: **augment-existing-leaf** (edit `184-human-readable-server-messages-act-client.md` in place)
- Leaf number: **184**
- Title (settled): Classify socket rate limits with a stable error discriminator
- Theme: Make socket rate-limit classification independent of human-readable text
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Treat socket throttling as one more bounded typed-error-contract flow inside leaf 184, using a local stable discriminator and an emitted-payload test.

## Augment instruction (what to add or change in the target leaf)

Extend Problem, Evidence, Proposed direction, and Scope with the socket rate-limit flow at connection-handler.ts:14-19,36-39. Add a tagged/custom rate-limit error, map its stable discriminator to RATE_LIMIT while keeping message text presentational, and require the focused test to assert the emitted payload code; state explicitly that this local Socket.IO tag does not widen the shared tRPC reason-code vocabulary or generalize every socket error.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `184-human-readable-server-messages-act-client.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

A presentation string is also an undocumented sentinel between two branches. Editing or wrapping only the producer text makes genuine throttling errors reach clients as INTERNAL_ERROR, and the existing categorization test does not inspect the emitted payload. Nothing structurally connects the rejection cause to its protocol code.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/socket/connection-handler.ts:14-19` — The per-packet middleware represents rate-limit rejection only as an ordinary Error with an English message.
- `packages/server/src/socket/connection-handler.ts:36-39` — The error handler derives the client-facing RATE_LIMIT code by comparing err.message to that exact text.
- `packages/server/src/socket/connection-handler.test.ts:176-193` — The test named for RATE_LIMIT categorization describes the string mapping but does not assert the emitted error code.

## Author constraints (all must be honored)

- Preserve leaf 184's rule that user-facing messages remain unchanged and presentation-only.
- Do not turn the addition into a repository-wide Socket.IO error taxonomy.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R3B · lane-03-server · `wave-1/lane-03.json::coverage.cut[14]` — original title: "Socket rate-limiter wiring couples machine behavior to an English error string."; rejected because: coverage.cut — omitted for lane budget; no recorded rationale

## Existing-leaf relations

- Dedup basis from 1c: Opened 184-human-readable-server-messages-act-client.md:8-14,30-53. Its theme directly owns prose-as-protocol, while its current bounded scope leaves this socket instance available as a compact augmentation.

