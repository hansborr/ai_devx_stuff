# Author packet — R2B-009 → `237-associate-homebrew-validation-errors-with-their.md`

- Ruling: **new-leaf**
- Leaf number: **237**
- Title (settled): Associate homebrew validation errors with their controls
- Theme: Associate homebrew validation errors with their controls
- Area: client · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Update only controls that render validation errors: reuse established field primitives where they fit, and otherwise wire alert role, stable error ID, aria-invalid, and aria-describedby semantics appropriate to each control shape.

## Problem statement from the promotion candidate

Many homebrew validation messages are only visually adjacent to controls. Those controls are not marked invalid and do not reference their messages, so assistive-technology users may miss new errors or lose the relationship when returning to a field.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/homebrew/shared/homebrew-textarea-field.tsx:28-47` — The established homebrew field contract wires invalid state and described-by association to an identified alert.
- `packages/client/src/components/homebrew/species/species-form-fields.tsx:58-68` — The species-name error is a visual paragraph only, with no invalid state or control relationship.
- `packages/client/src/components/homebrew` — measurement: 11 error-bearing field components (`rg -l 'fieldErrors' packages/client/src/components/homebrew --glob '*.tsx' | xargs rg -l '<p[^>]*className="text-sm text-destructive"' | wc -l`) — The accessibility-contract bypass is distributed across homebrew entity forms.

## Author constraints (all must be honored)

- Exclude controls without rendered validation errors.
- Do not hoist FormFieldError, make FormField polymorphic, or revive the general Label/Input normalization sweep.
- Preserve select, group, and list-row interaction semantics rather than forcing every shape through an Input abstraction.
- Coordinate with leaf 195 where both edit homebrew validation surfaces.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-187` — The broad style-driven form sweep remains refused; only the eleven verified error-bearing controls are reopened.

## Provenance (origins — carry into the leaf Source line context)

- R2B · lane-05-client · `wave-1/lane-05.json::coverage.cut[35]` — original title: "Most homebrew validation errors bypass the established field contract."; rejected because: coverage cut; recorded rationale: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: The permanent broad-sweep refusal is recorded at `docs/agent_notes/backlog/code-quality-2026-07-25/CLIENT-CLUSTER-PLAN.md:230` and its landed a11y boundaries at `:263` and `:489`. Leaf 195 owns pre-submit validation, not control association, at `docs/agent_notes/backlog/code-quality-2026-08-01/195-validate-concrete-homebrew-entry-data-before.md:8` and `:25`.

