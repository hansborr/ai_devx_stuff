# Author packet — R2C-007 → `245-reject-empty-fixture-and-smoke-discovery-sets.md`

- Ruling: **new-leaf**
- Leaf number: **245**
- Title (settled): Reject empty fixture and smoke discovery sets
- Theme: Require fixture and smoke discovery to reject empty subject sets
- Area: harness · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add owner-local, subject-specific non-empty preconditions before codemod fixtures reach it.each and before smoke definitions generate outputs. Cover empty directories and missing smoke trees.

## Problem statement from the promotion candidate

Two discovery-backed gates treat an empty subject population as success. Empty codemod fixture directories silently erase regression coverage, while an empty smoke tree can regenerate authoritative artifacts with no registered tests. Green output then means the scanner found nothing, not that subjects passed.

## Verified evidence (existence-checked at step 1b)

- `scripts/codemods/lib/fixture-runner.test-helper.ts:68-72` — The fixture enumerator returns every child directory without asserting that at least one case exists.
- `scripts/codemods/concurrency-guard.test.ts:83` — A representative suite passes the discovered array directly to it.each, so an empty result removes every fixture case.
- `scripts/codemods` — measurement: 4 fixture-driven codemod test files call enumerateFixtures (`rg -l 'enumerateFixtures\(' scripts/codemods --glob '*.test.ts' | wc -l`) — The vacuous behavior affects concurrency-guard, expand-barrel, structured-logging-fix, and the two-kind tRPC fixture suite.
- `scripts/path-policy/smoke-subject-headers.ts:28-30` — Smoke discovery returns [] when scripts/tests is missing and likewise for an existing empty directory.
- `scripts/path-policy/smoke-subject-headers.ts:122-131` — Collection validates individual definitions and order uniqueness but has no non-empty precondition.
- `scripts/path-policy/smoke-subject-headers.ts:167-180` — The empty definition set flows into both generated outputs.

## Author constraints (all must be honored)

- Keep each scanner's diagnostic and reason local; do not create a shared assertion helper.
- Confirm no companion guard already makes the same population non-empty before adding an assertion.
- Do not work concurrently with leaf 167 in fixture-runner.test-helper.ts.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-106` — The standing non-vacuity requirement remains unmet for codemod fixtures and smoke-subject discovery.
- `CQ25-215` — Only the shared-helper remedy was refused; local preconditions remain required.

## Provenance (origins — carry into the leaf Source line context)

- R2C · lane-09-lint-machinery · `wave-1/lane-09.json::coverage.cut[11]` — original title: "fixture and smoke discovery accept an empty subject tree — routed as a low-priority scanner-hardening observation"; rejected because: fixture and smoke discovery accept an empty subject tree — routed as a low-priority scanner-hardening observation

## Existing-leaf relations

- Dedup basis from 1c: The binding scanner rule is at `docs/agent_notes/backlog/code-quality-2026-07-25/CONSTRAINTS.md:41`. Leaf 167 uses the same fixture helper only for missing-after semantics at `docs/agent_notes/backlog/code-quality-2026-08-01/167-codemod-fixtures-duplicate-identical-before.md:8` and `:22`, not discovery non-vacuity.

