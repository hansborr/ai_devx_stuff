# Author packet — R2B-001 → `107-ai-hook-policy-2121-line-shell-parsing.md`

- Ruling: **augment-existing-leaf** (edit `107-ai-hook-policy-2121-line-shell-parsing.md` in place)
- Leaf number: **107**
- Title (settled): Project Claude native hard denies from the declarative command-policy authority
- Theme: Project Claude's native destructive-command denies from shared command-policy data
- Area: harness · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Generate or freshness-check Claude's native context-free hard-deny patterns from leaf 107's commandPolicy records. Keep contextual rules in shared policy and make every native exclusion explicit.

## Augment instruction (what to add or change in the target leaf)

Extend Slice A and the linked 107-PLAN S0/S1 sequence with metadata identifying context-free native-projectable hard denies, generation or freshness checking of `.claude/settings.json`'s deny array, and parity coverage for intentional non-projections. Update the companion plan in lockstep.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `107-ai-hook-policy-2121-line-shell-parsing.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

One safety policy is encoded in two languages: Claude matcher strings and procedural shared-shell rules for other harnesses. Family-level parity mitigates current drift but preserves synchronized edits and cannot guarantee that individual native patterns remain projections of policy authority.

## Verified evidence (existence-checked at step 1b)

- `.claude/settings.json:5-88` — Claude's native permissions contain the separately maintained 81-entry deny array.
- `scripts/ai-hooks/policy.sh:1059-1213` — Shared hooks independently implement the hard destructive Git and GitHub command policies represented in Claude settings.
- `scripts/ai-hooks/test.sh:253-301` — A bespoke classifier and two-way family corpus are required to keep the representations aligned but do not derive either one.
- `scripts/ai-hooks/test.sh:515-522` — The split already needs an explicit stash exclusion because native denial would preempt the shared policy's contextual reason.

## Author constraints (all must be honored)

- Sequence this inside leaf 107 S0/S1; do not create an independent policy registry.
- Preserve stash and any other rule whose contextual shared-policy reason would be preempted by native denial.
- Do not reopen CQ25-129's rejected jsonc-parser rewrite.
- Keep the fail-closed path free of new Bun or TypeScript runtime dependencies.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-122` — Generated provider shims do not own Claude's native permission-deny array.
- `CQ25-129` — The refusal covers replacing the localized settings scanner, not projecting deny data from an existing authority.

## Provenance (origins — carry into the leaf Source line context)

- R2B · lane-01-harness-core · `wave-1/lane-01.json::coverage.cut[12]` — original title: "Claude settings maintain an independent 81-entry deny list alongside shared hook policy."; rejected because: coverage cut; recorded rationale: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Leaf 107 defines the commandPolicy authority and generated shell projection at `docs/agent_notes/backlog/code-quality-2026-08-01/107-ai-hook-policy-2121-line-shell-parsing.md:107`; its executable S0/S1 scope omits native settings at `docs/agent_notes/backlog/code-quality-2026-08-01/107-PLAN.md:29`.
- promoter hint (non-authoritative): looks close to leaf 107 — hint only

