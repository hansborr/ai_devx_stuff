# Author packet — R2C-005 → `189-downstream-packages-keep-semantic-copies.md`

- Ruling: **augment-existing-leaf** (edit `189-downstream-packages-keep-semantic-copies.md` in place)
- Leaf number: **189**
- Title (settled): Consolidate ability and spell-school inventories around shared schema authorities
- Theme: Schema-defined ability and spell-school vocabularies retain handwritten production inventories
- Area: cross-cutting · Severity: **medium** · Size: **M** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Treat abilities and spell schools as separate subparts under leaf 189's shared-authority program. Repoint loops and validators to schema options, retain typed presentation maps, and remove the private spell-school fallback set.

## Augment instruction (what to add or change in the target leaf)

Add two independently reviewable authority-consolidation subparts to leaf 189: derive ability iterations from abilityAbbreviationSchema while retaining explicit field maps, and derive homebrew spell-school options/parsing from spellSchoolSchema plus one typed label map. Extend the leaf's whole-tree semantic-straggler acceptance check to both vocabularies.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `189-downstream-packages-keep-semantic-copies.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Closed shared vocabularies are independently maintained by production consumers. Ability copies can omit or reorder an option, positional mappings can mislabel scores, and the homebrew spell copy can silently rewrite a shared-valid but locally absent school to Evocation.

## Verified evidence (existence-checked at step 1b)

- `packages/shared/src/schemas/srd.ts:26` — abilityAbbreviationSchema is the closed canonical six-value ability contract.
- `packages/shared/src/rules/character-rules.ts:67-76` — SAVE_ABILITIES repeats the schema literals beside the canonical name map.
- `packages/client/src/components/homebrew/monster/monster-form-data.ts:99` — The homebrew monster form repeats the full ability inventory.
- `packages/client/src/components/sheet/asi-step.tsx:16` — The ASI picker repeats the ability inventory while importing adjacent shared rules.
- `packages/client/src/components/campaign/npcs/monster-detail-dialog.tsx:23-31` — The NPC dialog synchronizes abbreviation and property-key arrays by positional index.
- `packages/shared/src/schemas/spell.ts:22-31` — spellSchoolSchema is the canonical eight-value spell-school contract.
- `packages/client/src/components/homebrew/spell/spell-form-data.ts:34-45` — The homebrew editor repeats all eight schools and builds its validator Set from that private copy.
- `packages/client/src/components/homebrew/spell/spell-form-data.ts:79-81` — A school absent from the private inventory is silently normalized to evocation.
- `packages/client/src/components/sheet/add-spell-dialog.tsx:26-29` — Neighboring spell UI already derives school options from spellSchoolSchema.options.

## Author constraints (all must be honored)

- Do not touch leaf 045's character-creation wizard surfaces.
- Keep display labels and abbreviation-to-property mappings explicit and typed.
- Preserve intentional ordering and classify every remaining semantic duplicate during the CQ25-110 sweep.
- Do not introduce a general constants framework.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-110` — The whole-tree semantic-straggler criterion applies to these two newly added authority consolidations.

## Provenance (origins — carry into the leaf Source line context)

- R2C · lane-08-cross-cutting · `wave-1/lane-08.json::coverage.cut[12]` — original title: "Ability and spell-school literal inventories were cut as lower-value semantic stragglers near recently landed shared-constant work."; rejected because: Ability and spell-school literal inventories were cut as lower-value semantic stragglers near recently landed shared-constant work.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 045 explicitly excludes sheet and homebrew ability surfaces at `docs/agent_notes/backlog/code-quality-2026-08-01/045-character-creation-ability-rules-have-four.md:117`; leaf 053 owns only FilterSelect at `docs/agent_notes/backlog/code-quality-2026-08-01/053-filterselect-erases-schema-derived-option.md:8`. Leaf 189's same authority-consolidation structure and bounded acceptance sweep are at `docs/agent_notes/backlog/code-quality-2026-08-01/189-downstream-packages-keep-semantic-copies.md:8`, `:24`, and `:36`.
- promoter hint (non-authoritative): looks close to leaf 045 — hint only; that leaf excludes these homebrew and sheet surfaces
- promoter hint (non-authoritative): looks close to leaf 189 — hint only; that leaf consolidates other shared authorities
- promoter hint (non-authoritative): looks close to leaf 053 — hint only; it does not cover the homebrew editor

