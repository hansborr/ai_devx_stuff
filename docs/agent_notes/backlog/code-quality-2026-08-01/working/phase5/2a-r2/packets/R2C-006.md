# Author packet — R2C-006 → `244-let-multiclass-characters-choose-their-unarmored.md`

- Ruling: **new-leaf**
- Leaf number: **244**
- Title (settled): Let multiclass characters choose their Unarmored Defense calculation
- Theme: Let multiclass characters choose among valid Unarmored Defense calculations
- Area: cross-cutting · Severity: **medium** · Size: **L** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Define and persist a validated Unarmored Defense formula choice, pass it explicitly through shared AC calculation, and expose it on the character sheet. Reconcile sheet and encounter AC authority and provide a deterministic legacy fallback.

## Problem statement from the promotion candidate

Musi silently chooses the first eligible Unarmored Defense formula even though its seeded rule grants the player a choice. Multiclass row order can lower displayed AC, and the result can disagree with encounter AC because the two surfaces use different authorities.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/seed/data/5e-srd-rules-glossary.json:54` — The seeded rule explicitly requires choosing which base-AC calculation to use.
- `packages/shared/src/rules/armor-class.ts:139-145` — The implementation silently selects the first matching Unarmored Defense class.
- `packages/shared/src/rules/armor-class.test.ts:266-299` — Tests establish Monk AC 15, Barbarian AC 13, and pin Barbarian-first multiclass AC to 13.
- `packages/client/src/pages/character-sheet/sheet-helpers.ts:36-46` — The sheet forwards class order into the shared computation without a choice input.
- `packages/server/prisma/schema.prisma:918-943` — CharacterStats persists numeric AC but no selected AC calculation.
- `packages/server/src/utils/encounter-query.ts:155-164` — Encounter combat reads persisted stats.ac rather than the sheet calculation.

## Author constraints (all must be honored)

- Add a Prisma migration for any persisted selection.
- Do not fold in CQ25-219's broader class-ID normalization work.
- Do not move or split calculateArmorClass's existing shield ownership rejected under CQ25-174.
- Use a stable formula discriminator rather than multiclass row order.
- Classify and test the rule provenance under `docs/guides/change-rules-logic.md`.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-219` — Player-choice behavior remains separate from the prior compile-time class-identity fix and its excluded persisted normalization.
- `CQ25-174` — The prior shield-ownership ruling remains binding and is not needed for this choice feature.

## Provenance (origins — carry into the leaf Source line context)

- R2C · lane-08-cross-cutting · `wave-2/lane-08-topup.json::coverage.cut[3]` — original title: "Pointer 26's Unarmored Defense half was not promoted: unlike the retained Finesse choice, it requires a persisted preference and interaction with CQ25-219's separated AC-authority work rather than a cheap input extension."; rejected because: Pointer 26's Unarmored Defense half was not promoted: unlike the retained Finesse choice, it requires a persisted preference and interaction with CQ25-219's separated AC-authority work rather than a cheap input extension.

## Existing-leaf relations

- Dedup basis from 1c: Leaf 191's Finesse-only contract is at `docs/agent_notes/backlog/code-quality-2026-08-01/191-let-players-choose-strength-dexterity.md:8` and `:24`, with persistence explicitly excluded at `:46`; no AC implementation appears there.
- promoter hint (non-authoritative): looks close to leaf 191 — hint only; leaf 191 covers the Finesse choice, not Unarmored Defense

