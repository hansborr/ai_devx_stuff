# Author packet — R2C-004 → `243-correct-prototype-headers-that-deny-registered.md`

- Ruling: **new-leaf**
- Leaf number: **243**
- Title (settled): Correct prototype headers that deny registered advisory commands
- Theme: Prototype source headers deny their now-registered advisory commands
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Rewrite only the three milestone headers to describe current layering: inventory and config modules remain finding-free libraries, while the named command modules own registered prototype surfaces.

## Problem statement from the promotion candidate

In a large flat analyzer subsystem without a local MODULE.md at the pin, source headers are important ownership maps. Three headers still describe pre-wiring milestones and tell maintainers that four live advisory commands do not exist, inviting completed work to be re-planned and production behavior to be misclassified as test-only.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/config.ts:116-121` — The coverage config header says no check, subcommand, or advisory reads these artifact declarations.
- `scripts/drift-ai/coverage-unused-correlation-command.ts:46-69` — The production command resolves config.coverage.artifacts, reads them, correlates them, and renders an advisory.
- `scripts/drift-ai/clone-candidates.ts:5-10` — The module says no CLI or advisory is registered and a later task will wire it.
- `scripts/drift-ai/class-construction.ts:1-6` — The module describes itself as library/test-only with no subcommand or advisory row.
- `scripts/drift-ai/prototype-subcommands.ts:49-60` — The live handler registry exposes coverage-evidence, coverage-unused-exports, clone-candidates, and class-construction.

## Author constraints (all must be honored)

- Limit scope to the three claims contradicted by registered handlers.
- Do not revive the 60-plus coordinate-qualification sweep.
- Do not trim routes/upload-routes.ts or harness-diagnostics-output.ts headers under this leaf.
- Verify every factual claim materially rewritten.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-168` — The refusal remains binding for bulk qualification and the specifically named header trims; these three false current-state claims are outside it.
- `CQ25-35` — A future module map does not repair false source-local ownership statements.

## Provenance (origins — carry into the leaf Source line context)

- R2C · lane-07-docs-dx · `wave-2/lane-07-topup.json::coverage.cut[0]` — original title: "Prototype milestone comments deny production command surfaces that now exist."; rejected because: Prototype milestone comments deny production command surfaces that now exist.

## Existing-leaf relations

- Dedup basis from 1c: CQ25-168's exact refusal is recorded at `docs/agent_notes/backlog/code-quality-2026-08-01/working/dedup-corpus.md:233`. Current leaf 147 covers only harness and code-intel MODULE maps at `docs/agent_notes/backlog/code-quality-2026-08-01/147-major-harness-implementation-directories.md:10` and `:68`, not drift-ai prototype headers.

