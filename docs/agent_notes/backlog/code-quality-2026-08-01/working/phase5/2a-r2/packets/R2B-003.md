# Author packet — R2B-003 → `231-fail-closed-when-similarity-ts-output-no-longer.md`

- Ruling: **new-leaf**
- Leaf number: **231**
- Title (settled): Fail closed when similarity-ts output no longer matches the supported protocol
- Theme: Fail closed when similarity-ts stdout no longer matches the supported protocol
- Area: harness · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Declare a supported similarity-ts protocol or version, prefer structured output if the installed tool supports it, and fail a successful run whose non-empty output is unrecognized. Pin valid zero-pair, malformed, and changed-protocol cases.

## Problem statement from the promotion candidate

The optional analyzer integrates an unpinned external binary through a private parser for human-readable stdout. A presentation change with status 0 can yield no rows and be reported as a clean successful result, creating silent false negatives.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/README.md:536-539` — The documented install command fetches the current similarity-ts release without pinning a supported version.
- `scripts/drift-ai/near-duplicates-runner.ts:156-159` — A zero subprocess status is accepted as success even when parsing recognizes no pairs.
- `scripts/drift-ai/near-duplicates-runner.ts:180-198` — The parser silently skips unrecognized stdout and has no completeness or unexpected-output signal.
- `scripts/drift-ai/near-duplicates-runner.ts:215-246` — Pair and score decoding depend on exact human-text markers, spacing, range punctuation, and percent formatting.

## Author constraints (all must be honored)

- Distinguish a valid empty result from non-empty output the parser failed to understand.
- Keep this bounded to the similarity-ts adapter rather than generalizing every analyzer result protocol.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R2B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[9]` — original title: "Similarity-ts stdout is an unversioned text protocol"; rejected because: coverage cut; recorded rationale: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Leaf 140's two owned prose protocols and exact scope are at `docs/agent_notes/backlog/code-quality-2026-08-01/140-analyzer-presentation-logic-uses-english.md:8`, `:60`, and `:98`; similarity-ts versioning and stdout parsing are absent.
- promoter hint (non-authoritative): looks close to leaf 140 — hint only

