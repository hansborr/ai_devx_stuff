# Author packet — R2B-005 → `233-bulk-load-encounter-participants-before-character.md`

- Ruling: **new-leaf**
- Leaf number: **233**
- Title (settled): Bulk-load encounter participants before character deletion reconciliation
- Theme: Character deletion serially reloads every active encounter's participant list
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Load ordered participants for all affected encounter IDs in one query and group them in memory before computing turn-index adjustments. Preserve all existing adjustment, CAS, transaction, and tolerant-clear semantics.

## Problem statement from the promotion candidate

Character deletion performs 1+N pre-transaction reads, with the N participant queries awaited serially. Many active encounters increase user-visible latency and widen the interval between the reconciliation snapshot and the transaction applying it.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/services/character-delete.ts:29-34` — The service first loads every active participant belonging to the character.
- `packages/server/src/services/character-delete.ts:68-73` — An awaited participant-list query then runs inside the loop for each active encounter.
- `packages/server/src/services/character-delete.test.ts:66-94` — The focused harness codifies one initial query followed by encounter-keyed ordered-list queries.

## Author constraints (all must be honored)

- Read `docs/CONCURRENCY.md` before changing this service.
- Do not move these reconciliation reads into or out of a transaction without an explicit concurrency decision.
- Do not work concurrently with leaf 007; both edit character-delete.ts.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R2B · lane-03-server · `wave-1/lane-03.json::coverage.cut[6]` — original title: "Character deletion uses one participant-list query per active encounter."; rejected because: coverage cut; recorded rationale: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Leaf 007 limits itself to the in-transaction linked-token snapshot, delete, and clear protocol at `docs/agent_notes/backlog/code-quality-2026-08-01/007-cascade-deleters-duplicate-load-bearing-turn.md:10` and `:84`, with the turn-index loop excluded at `:144`.
- promoter hint (non-authoritative): looks close to leaf 007 — hint only; leaf 007 covers the in-transaction cascade protocol, not these pre-delete reads

