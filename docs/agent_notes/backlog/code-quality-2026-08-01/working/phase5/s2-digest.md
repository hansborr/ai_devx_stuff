# S2 compact leaf digest

Generated by `working/phase5/build-s2-input.mjs` from S0/S1. Fields: T=title, M=Theme · Area · Severity · Size, F=problem fingerprint subject | mechanism | invariant, O=targetOps, R=relations. `pN` identifies the exact target at zero-based `targetIndex[N]` in `s2-channels.json`. Op codes: c=create, m=modify, v=move, d=delete, x=consume, o=document; roles: w=write, d=dependency, t=truth-source. `base` is unconditional; each `alt(name)` is a separate alternative. Relations are kind>leaf[confidence].

## 001 `001-chat-persistence-delivery-policy.md`
T: Six chat-message writers each re-invent persistence and delivery policy because there is no chat-message application boundary
M: chat-message coordination boundary · server · medium · L
F: campaign chat-message persistence and delivery across message-producing features | writers independently coordinate shared persistence and broadcast primitives and choose inconsistent failure policies, including surfacing post-commit delivery failures | one coordination boundary exposes composable persistence/delivery halves and named policies; in-request actions throw, post-commit chat is best-effort, and rest retains retry-safe transactional persistence with post-commit delivery
O: base:p707[cw]; p706[ct]; p628[mw]; p630[mw]; p622[mw]; p702[mw]; p750[mw]; p794[vw]; p793[xd]; p769[xd]; p685[ot]; p749[ot]; p653[ot]
R: serialize>008[explicit];rebaseOn>008[explicit]

## 002 `002-request-socket-boundaries-capture-production.md`
T: Request and socket boundaries capture the production Prisma singleton at module load, so `buildServer` composes dependencies it never actually provides
M: composition-root dependency injection · server · medium · L
F: server composition-root database dependency | request and socket boundary modules capture the production Prisma singleton at module load, hiding dependencies from their signatures and preventing alternate composition | the production default is resolved exactly once in buildServer and every interior boundary receives a required injected PrismaClient or narrowed DbClient
O: base:p612[mw]; p659[mw]; p782[mw]; p611[mw]; p772[mw]; p771[mw]; p767[mw]; p811[ot]; p658[ot]; p765[ot] || alt(optional-lint-enforcement):p197[mw@optional-lint-enforcement]; p149[mt@optional-lint-enforcement]
R: -

## 003 `003-seed-json-boundaries-alternate-between.md`
T: Seed JSON boundaries alternate between Zod schemas and unchecked assertion chains because no directory-level rule says what a seed-data boundary guarantees
M: seed validation boundary policy · server · medium · L
F: seed JSON load and validation boundaries | seed consumers alternate between schema parsing and unchecked assertions without a directory policy, and monster spellcasting narrows the same external data repeatedly | every corpus has a declared load class and validation level, narrowing occurs exactly once at the load call, and external corpus schemas remain local to the seed module
O: base:p661[ct]; p40[mt]; p674[mw]; p676[mw]; p677[mw]; p679[mw]; p680[mw]; p682[mw]; p681[mw]; p665[mw]; p667[mw]
R: serialize>009[explicit]

## 004 `004-character-creation-large-pseudo-module-loose.md`
T: Character creation is a 640-line four-file pseudo-module loose in the services root, and the services README misclassifies it as a flat service
M: service module boundaries · server · medium · M
F: character-creation service module and transaction stages | a four-file workflow is misclassified as flat, has a helper-to-facade type cycle, and exposes its internal starting-equipment stage to non-transactional callers | character creation has one documented folder boundary with independently owned types, a logic-bearing facade, and a TxClient-only starting-equipment stage inside the service-owned transaction
O: base:p697[vw]; p692[vw]; p694[vw]; p763[vw]; p699[ct]; p698[ct]; p696[vw]; p691[vw]; p693[vw]; p762[vw]; p695[vw]; p626[mw]; p625[mw]; p654[mw]; p623[mw]; p735[mw]; p791[mw]; p685[ot]; p115[ot]; p40[mt]
R: serialize>006[explicit];serialize>042[explicit];serialize>045[explicit]

## 005 `005-spell-participant-loading-discards-prisma.md`
T: Spell participant loading routes the caster query through `unknown` into a hand-mirrored type that weakens always-selected fields to optional
M: Prisma boundary type fidelity · server · medium · M
F: spell-casting caster participant query contract | an unknown-to-handwritten Prisma payload assertion decouples the query from its consumer and weakens always-selected non-null fields to optional | TypeScript checks the inferred query result against a narrow resolver contract whose required fields exactly match the fixed include, with no cast or unreachable absence fallbacks
O: base:p761[mw]; p756[mw]; p759[mw]; p601[xt]; p758[mw]; p760[mw]; p755[mw]
R: serialize>006[explicit];rebaseOn>006[explicit]

## 006 `006-server-mappers-maintain-parallel-handwritten.md`
T: Server mappers hand-transcribe 14 Prisma payload shapes that the package's own `GetPayload` convention already derives for free
M: Prisma payload derivation · server · medium · M
F: server mapper Prisma read payload types | hand-transcribed interfaces drift independently from query projections and widen generated enum and relation guarantees | read-side mapper inputs derive from named Prisma select/include truth sources, except for an explicit multi-projection seam that preserves optional relations
O: base:p802[mw]; p805[mw]; p800[mw]; p801[mw]; p803[mw]; p799[mw]; p654[mw]; p700[mw]
R: serialize>005[explicit];rebaseOn>005[explicit]

## 007 `007-cascade-deleters-duplicate-load-bearing-turn.md`
T: Two cascade deleters hand-copy the load-bearing snapshot-then-cascade-then-clear turn-origin sweep, with comments as the only synchronization mechanism
M: transaction protocol single-sourcing · server · medium · M
F: turn-origin cleanup during map-token cascade deletion | map and character deleters duplicate an order-sensitive transaction protocol whose synchronization depends only on comments | one sanctioned coordinator structurally enforces in-transaction linked-participant snapshot, caller-owned cascade deletion, and tolerant post-cascade origin clearing
O: base:p807[mw]; p743[mw]; p701[mw]; p62[ot]; p741[ot]; p646[xd]; p700[xd]; p742[xd]
R: serialize>017[explicit]

## 008 `008-chat-broadcast-facade-bypasses-registry.md`
T: The chat broadcast facade alone demands a concrete socket server and a caller-resolved room, so all seven call sites repeat policy the registry already owns
M: broadcast facade consistency · server · medium · M
F: chat socket broadcast facade and campaign-room delivery | the facade requires a concrete server and caller-derived room, causing every caller to duplicate guards and bypass the registry's no-socket-server outcome logging | chat follows the other broadcast families: nullable io reaches the registry, the payload determines the room, and every skipped delivery produces the standard outcome log
O: base:p769[mw]; p766[mt]; p630[mw]; p622[mw]; p628[mw]; p750[mw]; p794[mw]; p702[mw]; p768[mw] || alt(null-io-dm-resolution:always-query):p628[mw@null-io-dm-resolution:always-query] || alt(null-io-dm-resolution:query-only-with-io):p628[mw@null-io-dm-resolution:query-only-with-io]
R: serialize>001[explicit];rebaseOn>001[explicit]

## 009 `009-all-four-seed-generators-can-overwrite.md`
T: All four SRD seed generators warn and still write, so a run that dropped records or blanked descriptions exits 0 and overwrites committed artifacts
M: seed generator error policy · server · medium · M
F: SRD artifact generation and Prisma seed process lifecycle | lossy generator warnings still permit committed-output overwrites and exit zero, while seed failures force process exit before asynchronous disconnect reliably finishes | incomplete generation fails before every output write unless partial mode is explicit, clean generation remains byte-identical, and failed seed commands retain nonzero status while awaiting cleanup
O: base:p671[mw]; p670[mw]; p672[mw]; p669[mw]; p673[cw]; p116[ot]; p604[mw]; p603[mw]
R: serialize>003[explicit];rebaseOn>087[ambiguous]

## 010 `010-trpc-error-sanitization-depends-unstable.md`
T: The wire-facing error sanitizer types its security policy against tRPC's unstable import path and casts sanitized issues back into Zod's framework union
M: transport-owned error types · server · medium · M
F: wire-facing tRPC validation-error sanitization | a security-sensitive recursive sanitizer is typed against an unstable tRPC import and Zod's framework union, forcing sanitized children back through repeated framework assertions | the transport owns its recursive safe issue shape, uses only the public tRPC error type, and strips input recursively while preserving all other diagnostic fields
O: base:p784[mw]; p783[mw]; p36[xd]; p42[xt]
R: -

## 011 `011-encounter-map-behavior-duplicated-across-two.md`
T: Two large router suites both assert the encounterMap link/unlink/auto-link contracts, and one of them re-tests its own failure paths a second time
M: single-authority test suites · server · medium · M
F: encounterMap link, unlink, and auto-link procedure tests | procedure contracts are duplicated across two router suites and failure contracts are duplicated again within the authority suite, with unique assertions hidden among the copies | one authoritative test per procedure contract preserves all unique behavior and exact error semantics while the combat-map suite covers only mapId integration
O: base:p634[mw]; p632[mw]; p744[xt]; p88[xt]
R: -

## 012 `012-server-utilities-lack-orientation-contract.md`
T: The 46-file flat server utils directory owns authorization, race-sensitive mutation, and Prisma trust boundaries but has no MODULE.md to orient anyone
M: module orientation contracts · server · medium · M
F: server utility authorization, Prisma, and race-sensitive mutation boundaries | a large flat utilities directory has no module orientation layer distinguishing sanctioned capability entry points from convenience helpers | a concise MODULE.md maps utility families and sanctioned entry points while linking to, rather than duplicating, deeper contracts
O: base:p786[cw]; p63[ow]; p97[xd]; p115[xt]; p811[xt]; p785[xt]; p62[xt]; p88[xt]; p65[xt]
R: -

## 013 `013-spell-eligibility-bypasses-module-facade.md`
T: The character-spell router imports spell eligibility from a spell-casting internal file that the module's own doc says must be reached through the facade
M: module facade discipline · server · medium · S
F: combat spell eligibility response projection | an external router bypasses the documented spell-casting facade to import a pure internal implementation file | pure shared read projections live in the sanctioned utilities layer and external callers do not depend on spell-casting internals
O: base:p753[vw]; p752[vw]; p815[xd]; p624[mw]; p759[mw]; p751[ow]; p790[xd]
R: -

## 014 `014-map-layer-responses-mapped-twice-divergent.md`
T: Map-layer rows are mapped to the wire contract twice, and the two mappers disagree on malformed-JSON handling
M: duplicated Prisma read mappers · server · medium · S
F: MapLayer database-row to wire-contract mapping | two handwritten mappers duplicate the same fields but disagree on malformed JSON and timestamp normalization | one shared mapper validates persisted JSON against the shared field schema, returns schema-input Dates, and lets the output schema normalize wire timestamps
O: base:p804[mw]; p804[xd]; p645[mw]; p803[mw]; p924[xt]; p830[xt]; p810[xd]
R: serialize>006[explicit]

## 015 `015-participant-create-data-helpers-erase-their.md`
T: The participant create-data builder erases the discriminated type it just built, so its only caller mutates an opaque record and re-stamps it as Prisma create data
M: Prisma boundary types · server · medium · S
F: encounter-participant create payload construction | the builder erases its discriminated payload to an opaque record, forcing unchecked mutation and Prisma re-casts while hiding an unreachable merge branch | the builder produces a compiler-checked Prisma create input containing typed conditions and the caller passes it without assertions or post-build mutation
O: base:p798[mw]; p798[xd]; p720[mw]; p902[xd]
R: -

## 016 `016-two-narrow-operations-receive-unrestricted.md`
T: Presence and session cleanup accept the unrestricted generated Prisma client although each needs exactly one delegate, bypassing the compile-time `DbClient` boundary
M: narrow database ports · server · medium · S
F: presence and expired-session database dependencies | narrow operations accept the unrestricted generated PrismaClient type and bypass the auditable compile-time database boundary | each operation receives only its required delegate through a structural port derived from DbClient without new narrowing sites or casts
O: base:p748[mw]; p814[mw]; p747[mw]; p811[xt]
R: -

## 017 `017-participant-mutation-comments-misstate.md`
T: The participant-stats mutation header misroutes visibility and conditions writes to the wrong concurrency helpers
M: Concurrency-boundary comment accuracy · server · medium · S
F: EncounterParticipant mutation-helper selection documentation | the sanctioned write-boundary header misclassifies visibility and DM override writes and names the wrong conditions helper | file-local guidance exactly reflects the racing/non-racing field classification and the helpers invoked by production paths
O: base:p807[ow]; p62[xt]; p720[xt]
R: -

## 018 `018-legacy-invite-suite-duplicates-newer-split.md`
T: The legacy monolithic invite suite survived the split into focused manage/join suites, duplicating 16 of its 17 cases
M: duplicate test suites · server · medium · S
F: invite router management and join test ownership | a legacy monolithic suite duplicates focused split suites while retaining one unique end-to-end URL-extraction case | focused manage and join suites are the sole authorities and the unique pasted-URL procedure coverage survives migration
O: base:p640[mw]; p641[dw]
R: -

## 019 `019-utility-broadcast-tests-seed-through.md`
T: Mapper and broadcast suites seed characters through character-create service internals instead of a DB-level fixture
M: test fixture layering · server · medium · S
F: character-row setup for mapper, broadcast, normalization, and level-up tests | low-level and unrelated suites depend on character-create workflow internals because no DB-level fixture exists | non-creation tests seed included character rows through a Prisma-level fixture while creation internals are consumed only by creation-service tests
O: base:p776[mw]; p776[xd]; p791[mw]; p625[mw]; p654[mw]; p735[mw]; p1169[mw]; p792[xd]
R: prefersBefore>004[explicit];serialize>006[explicit];rebaseOn>109[explicit]

## 020 `020-opposite-spell-slot-operations-duplicate.md`
T: `consumeSpellSlot` and `recoverSpellSlot` hand-maintain mirror copies of the same 30-line Pattern B CAS protocol
M: duplicated concurrency protocol · server · low · S
F: character spell-slot consume and recovery mutations | opposite operations hand-maintain mirrored copies of the same Pattern B compare-and-swap protocol, allowing race semantics to diverge | one private table-local primitive owns the identical load, bounds, CAS, conflict, and refetch protocol while public wrappers preserve direction-specific errors and return shapes
O: base:p816[mw]; p795[mw]; p655[xt] || alt(optional-export-rename):p705[mw@optional-export-rename]; p704[mw@optional-export-rename]; p757[mw@optional-export-rename]; p754[mw@optional-export-rename]; p62[ow@optional-export-rename]; p98[ow@optional-export-rename]; p751[ow@optional-export-rename]
R: -

## 021 `021-shared-production-builds-expose-colocated.md`
T: A normal shared build emits all 73 colocated test suites into dist, and the wildcard exports turn nearly all of them into valid package subpaths
M: build/export surface hygiene · shared · medium · M
F: shared package production build and exported subpath surface | the sole emitting TypeScript project includes colocated tests, and wildcard exports make their emitted artifacts public package subpaths | production builds emit only production modules plus the intentional test helper, while typechecking, ESLint, and code-intel remain test-inclusive
O: base:p942[cw]; p943[mw]; p825[mw]; p821[mw]; p593[mw]; p1310[mw]; p1430[mw]; p1447[mw]; p941[xd]
R: -

## 022 `022-saving-throw-phases-exchange-oversized.md`
T: Saving-throw phases pass the full 13-field input everywhere, so a check and the input it was rolled from travel as an unlinked pair callers must keep synchronized
M: Phase-boundary data contracts · shared · medium · M
F: staged saving-throw resolution contract | phase APIs exchange an oversized original input and an unlinked check, allowing callers to pair save results with a different DC or modifier | each phase accepts only its required data and every check is the sole coherent source of its DC, modifier, roll, total, and verdict
O: base:p862[mw]; p759[mw]; p861[mw]; p933[xt]; p761[xd]
R: rebaseOn>193[explicit]

## 023 `023-five-summary-projections-hand-copy-canonical.md`
T: Five summary projections hand-copy 33 canonical entity fields, and the copies have already drifted looser than their entity schemas
M: derived schema projections · shared · medium · M
F: campaign, character, magic-item, monster, and encounter summary schemas | hand-copied projection fields drift independently from canonical entity validators and silently lose constraints | verbatim summary fields inherit their owning entity schemas, while every computed or intentionally looser field is an explicit extension
O: base:p882[mw]; p921[mw]; p926[mw]; p902[mw]; p886[xt]; p881[mw]; p920[mw]; p925[mw]; p901[mw]
R: -

## 024 `024-encounter-inputs-monolith-spanning-three.md`
T: The encounter input contract is one 329-line, 20-schema module whose three router consumers each use a disjoint slice
M: Contract layout mirrors router boundaries · shared · medium · M
F: shared encounter input contracts | six disjoint concerns serving three routers share one large module and test file, creating unnecessary navigation and edit contention | contract and test modules mirror router ownership, with each schema having one defining deep-specifier path and shared participant fragments remaining together
O: base:p898[mw]; p896[cw]; p900[cw]; p637[mw]; p633[mw]; p635[mw]; p720[mw]; p718[mw]; p721[mw]; p719[mw]; p744[mw]; p797[mw]; p798[mw]; p897[mw]; p895[cw]; p899[cw]; p876[ow]
R: serialize>026[explicit];prefersBefore>026[explicit]

## 025 `025-spellcastingts-contains-five-independently.md`
T: spellcasting.ts packs five independently changing rule areas — caster resolution, slot progression, prepared limits, cantrip fallback, and creation selection — into one 380-line module
M: one rule area per file · shared · medium · M
F: shared spellcasting rule modules and tests | five independently changing rule areas and their tests share one broad module and import path | each spellcasting rule area has a focused public module and colocated tests, with minimal explicit dependencies and preserved behavior, directives, and policy provenance
O: base:p854[cw]; p865[cw]; p860[cw]; p847[cw]; p857[cw]; p868[dw]; p867[dw]; p866[dw]; p869[cw]; p790[mw]; p818[mw]; p750[mw]; p740[mw]; p730[mw]; p692[mw]; p809[mw]; p694[mw]; p547[mw]; p368[mw]; p817[mw]; p693[mw]; p1054[mw]
R: serialize>021[explicit];rebaseOn>021[explicit]

## 026 `026-monster-provenance-invariants-disappear.md`
T: Monster provenance mutual exclusion lives only in a superRefine, so the inferred type admits states the parser rejects and the exported monster schema skips the check entirely
M: inference-visible schema invariants · shared · medium · M
F: monster participant provenance contract | mutual exclusion and homebrew id/version pairing exist only in an outer runtime refinement, so inferred types and direct parses admit impossible provenance states | the exported monster schema and inferred participant union represent exactly SRD, complete homebrew, or custom provenance states
O: base:p898[mw]; p897[mw]; p798[xd] || alt(optional-server-follow-through):p720[mw@optional-server-follow-through]
R: serialize>024[explicit]

## 027 `027-condition-damage-modules-mix-contracts.md`
T: Condition and damage-type vocabulary, Zod contracts, and persisted-JSON repair live in rules modules, completing a bidirectional edge with the encounter schema
M: shared contract layering · shared · medium · M
F: condition and damage-type contract ownership | vocabulary, Zod validators, persisted-data repair, and executable rules are mixed in rules modules, producing a bidirectional rules-schema dependency and contradictory ownership guidance | contract vocabulary and normalization live in dependency-leaf schema modules, while rules modules contain executable calculations and all consumers import the defining contract path
O: base:p890[cw]; p892[cw]; p850[mw]; p853[dw]; p902[mw]; p884[mw]; p886[mw]; p911[mw]; p926[mw]; p878[mw]; p917[mw]; p933[mw]; p936[mw]; p938[mw]; p843[mw]; p870[mw]; p607[mw]; p263[mw]; p889[cw]; p849[mw]; p852[dw]; p891[cw]; p876[ow]; p836[ow]
R: serialize>031[inferred];serialize>035[inferred];serialize>193[inferred];serialize>191[inferred]

## 028 `028-map-inputs-tests-obscure-mutations-inside.md`
T: The map-input schema suite buries the one field under test inside dozens of repeated full-payload literals
M: typed test payload builders · shared · medium · M
F: map and token input-schema boundary tests | repeated full payload literals bury each tested field and amplify required-field changes across dozens of cases | tests express only the boundary delta over minimal typed valid payloads while preserving defaulting, mutation coverage, and case diagnostics
O: base:p922[mw]; p923[xd]; p941[xd]; p910[xd]
R: -

## 029 `029-one-metamagic-constant-controls-two.md`
T: Cast-input validation caps metamagic per cast with the level-up picks constant, coupling two unrelated rules that only coincidentally equal 2
M: single-purpose rule constants · shared · medium · S
F: metamagic selection limits at level-up and cast input | two semantically unrelated limits share the value 2, and the cast schema imports the level-up constant instead of the per-cast rule constant | level-up selection and per-cast validation each depend on their own purpose-named constant even when their numeric values coincide
O: base:p864[mw]; p935[mw]; p934[mw]
R: -

## 030 `030-attack-spell-contracts-duplicate-behavior.md`
T: Attack and spell-action contracts privately re-compose the same combat field validators and -10/99 bonus bounds, which can silently diverge
M: shared combat field schemas · shared · medium · S
F: attack-roll and spell-action combat field validators | sibling contract families privately recompose identical Zod fields and bonus bounds, allowing accepted combat values to diverge | shared combat names, dice strings, and action bonus bounds have one dependency-leaf definition while family-specific constraints remain local
O: base:p888[cw]; p878[mw]; p933[mw]; p830[xt] || alt(optional-homebrew-bounds-repoint):p936[mw@optional-homebrew-bounds-repoint]
R: serialize>031[ambiguous];serialize>024[ambiguous]

## 031 `031-attack-damagets-eight-concern-rules-grab-bag.md`
T: Spell save DC and spell attack bonus derivation lives in the weapon attack-damage module instead of `rules/spellcasting.ts`
M: rules module placement · shared · low · S
F: spell save DC and spell attack bonus derivation | spellcasting helpers and tests are owned by a weapon-attack module, forcing spell consumers to depend on the wrong rules boundary | spellcasting derivations live under rules/spellcasting with unchanged formulas while attack-damage remains focused on weapon attacks
O: base:p843[mw]; p868[mw]; p547[mw]; p759[mw]; p842[mw]; p867[mw]; p841[mw]; p866[mw]; p848[xd]
R: serialize>027[explicit];serialize>035[explicit];serialize>193[explicit];serialize>191[explicit];prefersBefore>025[explicit]

## 032 `032-encounter-calculators-re-declare-participant.md`
T: The encounter difficulty and XP calculators hand-write the participant discriminator instead of typing it from the canonical schema
M: schema-derived vocabulary typing · shared · low · S
F: encounter participant discriminator vocabulary | difficulty and XP calculator interfaces independently hand-write the closed participant union, so schema vocabulary changes cannot produce compiler errors at those boundaries | all shared TypeScript encounter participant discriminators derive from the canonical schema-defined ParticipantType
O: base:p856[mw]; p874[mw]; p902[xt]
R: -

## 033 `033-attunement-game-rule-lives-request-schema.md`
T: The attunement limit — a game rule enforced by the server and displayed by two UIs — is declared in a request-schema module none of whose schemas use it
M: rules constant placement · shared · low · S
F: attunement limit rule and inventory-service concurrency contract | a cross-layer game rule is owned by an unrelated request-schema module, while service documentation falsely describes a default count-then-write transaction as preventing concurrent over-attunement | the attunement limit is sourced from a concept-owning rules module and service documentation accurately records the accepted concurrency race with behavior unchanged
O: base:p846[cw]; p845[cw]; p915[mw]; p914[mw]; p726[mw]; p436[mw]; p473[mw]; p836[xt]; p62[xt]
R: serialize>027[explicit];serialize>044[explicit]

## 034 `034-campaign-homebrew-procedure-contracts-split.md`
T: The campaign-homebrew router's four output schemas live alone in a 19-line module, split from their matching inputs
M: contract co-location · shared · low · S
F: campaign-homebrew tRPC request and response contracts | four matching outputs are isolated in an ownership-free module separate from their inputs, creating an unnecessary navigation and import boundary | each campaign-homebrew procedure's request-side input and output contracts are colocated in homebrew-inputs with shapes preserved
O: base:p907[dw]; p909[mw]; p639[mw]; p876[ow]; p911[xd]
R: serialize>194[explicit];rebaseOn>194[explicit]

## 035 `035-weapon-helpers-widen-closed-vocabularies.md`
T: Weapon attack and proficiency helpers widen the closed WeaponCategory and DamageTypeName vocabularies back to string
M: Closed vocabularies erased at helper boundaries · shared · low · S
F: weapon category and damage-type contracts at attack helper boundaries | helpers widen already-closed WeaponCategory and DamageTypeName values to string, losing exhaustive map checking and permitting invalid categories | closed weapon vocabularies remain closed through attack results, proficiency maps, and helper parameters
O: base:p843[mw]; p870[xt]; p853[xt]
R: serialize>027[explicit];serialize>031[explicit];serialize>193[explicit];serialize>191[explicit]

## 036 `036-combat-result-schemas-make-always-produced.md`
T: Combat result schemas declare `rollMode` optional even though every producer always emits it
M: schema/producer contract alignment · shared · low · S
F: combat attack and saving-throw result roll mode | result schemas and tests permit omission even though every canonical live producer always emits rollMode, creating phantom optionality for consumers | rollMode is required on live combat result contracts while secondRoll and secondSaveRoll remain optional and persisted historical roll records remain opaque
O: base:p878[mw]; p933[mw]; p877[mw]; p932[mw]; p851[xt]; p844[xt]; p862[xt]; p902[xt]
R: -

## 037 `037-static-rules-tables-expose-inconsistent.md`
T: Uppercase SRD reference tables export mutable arrays and records while the neighboring difficulty table shows the readonly idiom
M: reference-data mutability contracts · shared · low · S
F: exported static SRD reference tables | mutable array and Record types allow consumers to corrupt shared singleton reference data despite its immutable intent | canonical reference-data containers are readonly at public boundaries while tolerant homebrew lookup indexes and row mutability remain unchanged
O: base:p848[mw]; p850[mw]; p870[mw]; p840[mw]; p871[mw]; p856[xt]
R: serialize>027[explicit]

## 038 `038-dice-grammar-semantics-depend-positional.md`
T: Dice grammar semantics ride on four positional regex captures plus a mislabeled keep-discriminator assertion
M: typed regex decoding · shared · low · S
F: dice-notation regex grammar decoding | semantic fields depend on synchronized numeric capture positions and an unchecked keep discriminator whose fallback silently maps unexpected values to keep-lowest | grammar fields are decoded by stable names and keep semantics are runtime-validated without changing accepted notation
O: base:p832[mw]; p110[xt]
R: -

## 039 `039-responsive-character-sheets-keep-two.md`
T: Responsive character sheets always mount both layout trees and hide one with CSS, keeping duplicate live panel state at every viewport
M: responsive layout boundary · client · high · L
F: responsive character-sheet layouts and chat panels | CSS hides one of two simultaneously mounted layout trees, duplicating hooks, subscriptions, local panel state, and controls that can diverge across breakpoint changes | the live viewport mounts one sheet layout and one chat panel, with synchronous initial selection and intentional state-resetting remounts documented at the lg breakpoint
O: base:p443[mw]; p431[mw]; p441[mw]; p550[mw]; p548[mw]; p423[ow]; p130[mw]; p265[xt] || alt(generic-hook):p516[cw@generic-hook]; p515[cw@generic-hook] || alt(dedicated-hook):p513[cw@dedicated-hook]; p512[cw@dedicated-hook]
R: serialize>192[explicit]

## 040 `040-vtt-actions-tab-duplicates-two-dedicated.md`
T: The VTT Actions tab reimplements the Spells and Features tabs' rows with divergent eligibility, and its Use button is a console.log placeholder
M: compact/full drawer surface duplication · client · high · L
F: VTT drawer compact and dedicated spell and feature controls | independent row implementations duplicate eligibility logic, causing contradictory spell controls and leaving the compact feature Use action as a tested console placeholder | both drawer surfaces derive spell and feature eligibility from shared models or primitives and invoke the same real feature mutation while preserving intentional presentation differences
O: base:p465[cw]; p464[cw]; p467[mw]; p476[mw]; p466[mw]; p472[mw]; p470[mw]; p469[mw]; p475[mw]; p471[mw]; p451[ow]; p526[xd] || alt(shared-primitives-in-existing-module):p474[mw@shared-primitives-in-existing-module] || alt(shared-primitives-in-new-module):p463[cw@shared-primitives-in-new-module]
R: -

## 041 `041-character-hooks-expose-second-identity.md`
T: Nine character-scoped hooks redundantly expose character scope in at least one action payload, with mixed identity-field conventions
M: single identity channel · client · medium · L
F: character-sheet mutation identity | action payloads repeat hook-captured character scope under mixed field names, allowing the RPC target and character-keyed cache work to address different characters | the hook supplies the sole character-scope identity while entity IDs such as inventory item IDs remain explicit
O: base:p499[mw]; p506[mw]; p504[mw]; p497[mw]; p502[mw]; p493[mw]; p501[mw]; p495[mw]; p507[mw]; p492[xd]; p422[mw]; p544[mw]; p488[ow]; p875[xt]
R: requires>062[explicit]

## 042 `042-flat-character-creation-contract-forces.md`
T: The character-creation reducer stores step data pre-flattened, forcing steps into mirrored local state, paired writes, and lossy two-way converters
M: draft-state ownership · client · medium · L
F: character-creation wizard draft state | pre-flattened reducer slices force mirrored state, whole-slice paired writes, and lossy reconstruction of richer editing models | the reducer exclusively owns lossless editing drafts and flattening occurs only through shared validation/submission projections
O: base:p368[mt]; p367[mw]; p357[mw]; p358[mw]; p359[mw]; p355[mw]; p354[mt]; p370[mw]; p353[mw]
R: serialize>004[explicit];serialize>045[explicit];serialize>056[explicit]

## 043 `043-encounter-detail-behavior-fragmented-across.md`
T: Encounter-detail mutation behavior is fragmented across a 384-line view and its presentation files instead of one controller module
M: detail-surface controller ownership · client · medium · L
F: encounter-detail mutation behavior | race-sensitive payload construction and mutation ownership are split across composition and presentation files and exposed through raw mutation bags | one controller module owns encounter-detail mutations and concurrency tokens while presentation receives named intents
O: base:p293[ct]; p295[mw]; p299[mw]; p292[mw]; p296[mw]; p298[mw]; p294[mw]; p289[ow]; p313[xd]
R: rebaseOn>088[inferred]

## 044 `044-optimistic-writers-spread-transport.md`
T: Three optimistic writers separate transport control fields from cached entity data with three incompatible conventions, and one of them leaks `campaignId` into cached rows today
M: transport vs domain projection · client · medium · M
F: optimistic character-sheet cache patches | untyped spreads and hand-maintained control-field filters allow transport identifiers, authorization scope, and CAS tokens to leak into cached entities | optimistic writers apply only positively typed persisted-entity patches derived from the shared wire contract
O: base:p915[mt]; p884[mt]; p501[mw]; p499[mw]; p495[mw]; p500[mw]; p498[mw]; p494[mw]; p488[ow]
R: -

## 045 `045-character-creation-ability-rules-have-four.md`
T: Character-creation ability rules — point-buy table, standard array, metadata, modifier math, boost aggregation — are re-declared across four wizard surfaces with no type-level link
M: Single source for ability rules · client · medium · M
F: character-creation ability-score rules | point-buy, standard-array, metadata, modifier, and boost rules are independently redeclared across four wizard surfaces | exact 5E rules and wizard-specific models each have one typed source consumed consistently by editing, validation, review, and submission
O: base:p838[ct]; p837[cw]; p352[ct]; p356[mw]; p357[mw]; p370[mw]; p362[mw]; p354[mw]; p848[xt]; p938[xt]; p349[mw]
R: serialize>004[explicit];serialize>042[explicit]

## 046 `046-generic-map-canvas-performs-combat-domain.md`
T: The generic map canvas imports the encounter contract and derives combat turn/HP/condition state that its own module doc assigns to the combat feature
M: feature-boundary layering · client · medium · M
F: generic map-canvas combat projection | the low-level renderer imports the encounter contract and derives turn, HP, condition, and participant state inside the maps feature | combat owns encounter-derived token presentation while MapCanvas only forwards an optional precomputed token-state map
O: base:p273[mt]; p272[mw]; p309[mw]; p311[mw]; p274[mw]; p312[mw]; p346[ow]; p270[ow]; p303[ow]; p346[xd]
R: rebaseOn>088[explicit]

## 047 `047-toolhandler-erases-coordinate-domains.md`
T: ToolHandler's runtime `coordinateSpace` flag erases the pixel/cell type distinction, forcing a no-op `onStart` and an out-of-registry eraser special case
M: descriptor unions over runtime flags · client · medium · M
F: canvas tool input descriptors | runtime coordinate flags erase pixel/cell types, mandatory callbacks invent unsupported capabilities, eraser bypasses the registry, and all drawing tools accumulate freehand history | descriptor variants encode coordinate and callback capabilities, every interactive tool is registered, and only freehand records point history
O: base:p483[mt]; p486[mw]; p579[mw]; p485[mw]; p484[mw]; p578[mw]; p482[ow]; p311[xd]
R: moots>105[ambiguous]

## 048 `048-monsteradddata-erases-mutually-exclusive.md`
T: MonsterAddData models two mutually exclusive monster origins as three unrelated optional fields, and three adapters hand-copy its participant defaults
M: discriminated union carriers · client · medium · M
F: monster-to-encounter add carrier | three optional provenance fields admit impossible origin combinations and three adapters duplicate participant defaults | current UI monster origins are mutually exclusive and complete at the type level, defaults have one owner, and flattening preserves the wire schema's provenance rules
O: base:p330[ct]; p334[mw]; p331[mw]; p329[mw]; p291[mw]; p295[mw]; p333[mw]; p898[xt]; p926[xt]
R: rebaseOn>024[ambiguous];rebaseOn>026[ambiguous]

## 049 `049-player-monster-attack-hooks-duplicate-target.md`
T: The player and monster attack hooks each own a full private copy of the stateful target-pick lifecycle
M: attack target-pick lifecycle duplication · client · medium · M
F: VTT attack target-pick lifecycle | weapon and monster hooks privately duplicate the same stateful query, eligibility, mutation, invalidation, close-pick-restore protocol and have already drifted | one coordinator owns the target-pick lifecycle while attack-specific adapters preserve payloads, gates, error text, and captured drawer restoration
O: base:p525[ct]; p524[cw]; p530[mw]; p528[mw]; p529[mw]; p527[mw]; p523[xd]; p590[xd]; p522[ow]
R: -

## 050 `050-optimistic-character-sheet-mutations-omit.md`
T: Seven of eight optimistic character-sheet rollback branches — and every settled invalidation binding — have no consumer-level test
M: optimistic rollback test coverage · client · medium · M
F: character-sheet optimistic mutation bindings | consumer suites do not exercise most onError snapshot restoration or any onSettled query-key binding, so broken wiring remains green | every optimistic branch proves exact rollback at the rollback moment and every mutation proves its complete settled invalidation family
O: base:p500[mw]; p505[mw]; p503[mw]; p496[mw]; p501[xt]; p506[xt]; p504[xt]; p497[xt]; p491[xt]; p489[xd]
R: serialize>044[explicit];serialize>062[explicit]

## 051 `051-flat-route-tree-makes-authentication-eleven.md`
T: The flat route tree turns authentication into an eleven-site copy-paste convention instead of a route-hierarchy guarantee
M: route hierarchy owns auth · client · medium · S
F: client authenticated route hierarchy | a flat route tree requires every protected route module to repeat an optional wrapper convention, allowing new routes to omit authentication silently | the route hierarchy must encode the exact protected route set through one authenticated parent while preserving URLs and redirect behavior
O: base:p562[cw]; p575[mw]; p563[mw]; p564[mw]; p565[mw]; p566[mw]; p567[mw]; p568[mw]; p569[mw]; p570[mw]; p571[mw]; p573[mw]; p577[mw]; p268[xd]
R: -

## 052 `052-campaign-tabs-manually-synchronized-across.md`
T: Eight campaign tabs are kept aligned across trigger and panel registries by a doc checklist instead of the compiler
M: derived tab registry · client · medium · S
F: campaign detail tab registry | tab identifiers, panels, presentation metadata, and the DM-only gate are duplicated across independently maintained JSX registries | every `CampaignTab` must have exactly one exhaustive descriptor that drives matching trigger and content rendering with one access gate
O: base:p542[mw]; p541[ow]; p531[xt]
R: -

## 053 `053-filterselect-erases-schema-derived-option.md`
T: The shared FilterSelect widens schema-derived option unions to bare string, so every consumer re-narrows through casts or hand-rolled guards
M: shared control type fidelity · client · medium · S
F: shared `FilterSelect` option-value contract | the component widens schema-derived string-literal unions to `string`, forcing consumers to recover types with casts or hand-written guards while rendered filter wiring lacks characterization | option values, selected values, callbacks, and the all sentinel must retain one inferred literal-union type across the shared-control boundary, with caller behavior pinned by rendered tests
O: base:p446[vw]; p447[cw]; p376[mw]; p334[mw]; p378[mw]; p448[mw]; p425[mw]; p375[xt]
R: serialize>048[explicit]

## 054 `054-tokencontextmenu-option-bag-two-different.md`
T: TokenContextMenu's props type admits combat mode without the combat callbacks it needs
M: discriminated component contracts · client · medium · S
F: `TokenContextMenu` map-versus-combat props contract | independent optional combat fields allow combat UI to render enabled actions without the callbacks needed to perform them | selecting the combat surface must statically require the complete combat data and callback set, while the map surface exposes only base props
O: base:p345[mw]; p312[mw]; p274[mw]; p344[mw]
R: -

## 055 `055-token-placement-mirrors-store-state-dialog.md`
T: Token placement mirrors store state into dialog state through an effect hidden behind a callback, leaving two sources of truth every close path must coordinate
M: Derived dialog state · client · medium · S
F: map token-placement dialog visibility | an effect mirrors the canvas store's pending cell into separate component state through an opaque callback, creating two truths that every close path must synchronize | placement-originated visibility must be derived directly from the pending cell while manual opening remains independent and clearing the cell closes the placement dialog
O: base:p314[mw]; p312[mw]; p315[mw]; p316[xd]; p579[xt]
R: -

## 056 `056-wizard-contexttesttsx-stale-duplicate-wizard.md`
T: wizard-context.test.tsx never tests the context it is named for — it re-runs stale reducer cases that wizard-state.test.ts already owns
M: stale duplicate test coverage · client · medium · S
F: character-creation wizard context test boundary | a context-named suite duplicates reducer/validation authority through re-exports, contains stale step semantics, and never exercises the provider or hook it claims to cover | reducer and validation behavior must have one authoritative suite, and any context suite must test only the provider/hook facade contract
O: base:p367[xt] || alt(remove-context-suite):p365[dw@remove-context-suite] || alt(replace-with-facade-contract-suite):p365[mw@replace-with-facade-contract-suite]; p366[xd@replace-with-facade-contract-suite]; p592[xd@replace-with-facade-contract-suite]
R: -

## 057 `057-character-sheettesttsx-no-longer-tests.md`
T: character-sheet.test.tsx tests eleven leaf components, not a character sheet, and half of them are already tested beside their subjects
M: test file identity · client · medium · S
F: character sheet aggregate test identity and legacy death-saves component | a misleading composition-named suite duplicates colocated leaf coverage, hides sole coverage for unrelated panels, and is the only consumer keeping an obsolete production component alive | each live component must retain one discoverable authoritative suite, composition coverage must remain with the actual layout suite, and orphaned production/test code must be removed
O: base:p428[mw]; p427[vw]; p444[cw]; p430[dw]; p236[mw]; p429[xt]; p548[xt]
R: -

## 058 `058-vtt-spell-tests-copy-full.md`
T: VTT drawer spell suites each hand-copy a full `CharacterSpellWithDetails` payload as their private base fixture
M: test fixture duplication · client · medium · S
F: VTT drawer `CharacterSpellWithDetails` test fixtures | eight suites copy complete nested spell payloads, obscuring scenario differences and multiplying schema-change edits | drawer spell scenarios must derive from one typed lean canonical default while each test states only its meaningful overrides and preserves rendered data
O: base:p455[mw]; p453[mw]; p452[mw]; p454[mw]; p456[mw]; p475[mw]; p477[mw]; p469[mw]; p582[xt] || alt(drawer-local-helper):p462[cw@drawer-local-helper] || alt(shared-test-fixture):p584[mw@shared-test-fixture]; p580[ow@shared-test-fixture]
R: -

## 059 `059-fog-overlay-component-tests-assert-only.md`
T: Fog overlay component tests assert only that Konva stand-ins exist, never the geometry or opacity their names promise
M: meaningful test oracles · client · medium · S
F: fog-of-war overlay component test oracles | tests named for geometry and viewer-dependent presentation assert only element existence or count, so coordinate, sizing, fill, and opacity regressions remain invisible | component tests must observe the rendered Konva attributes that enforce cell scaling, positive normalized bounds, fog mode, and DM/player visibility semantics
O: base:p306[mw]; p585[xd]; p307[xt]
R: -

## 060 `060-participant-presentation-duplicated-between.md`
T: State-independent participant presentation is duplicated between encounter setup and combat because the generic primitives are filed under combat
M: shared participant UI primitives · client · low · S
F: encounter participant presentation primitives shared by setup and combat | generic identity, HP/AC, and visibility UI is split between setup and combat directories, producing duplicated markup and bidirectional cross-directory dependencies | state-independent participant presentation primitives must have one neutral home and be reused through composition while setup/combat-specific behavior and intentional visual variants remain local
O: base:p288[cw]; p297[vw]; p285[vw]; p299[mw]; p283[mw]; p276[mw]; p280[mw]; p270[ow]; p289[ow]; p284[vw]; p282[mw]; p275[mw]; p298[mw]
R: serialize>043[explicit];serialize>088[explicit]

## 061 `061-rollmodetoggle-complete-production-orphan.md`
T: RollModeToggle is a 60-line combat component no production code imports
M: dead component removal · client · low · S
F: RollModeToggle combat UI component | an exported, tested component has no production importer but is documented as active | maintained combat components are production-reachable, or their implementation and tests are removed atomically
O: base:p287[dw]; p286[dw]
R: rebaseOn>088[explicit]

## 062 `062-character-key-hook-constructs-three-filter.md`
T: useCharacterKeys builds three query-filter members and their QueryFilter type that no production caller ever reads
M: dead cache-key surface · client · low · S
F: useCharacterKeys cache-key return contract | the hook constructs an unused duplicate broad-invalidation API whose comments falsely imply socket consumption | the hook exposes only consumer-used exact keys, while broad invalidation has one authoritative implementation
O: base:p491[mw]; p490[mw]
R: serialize>105[explicit]

## 063 `063-worktree-shell-suite-four-suites-hidden-one.md`
T: The worktree shell suite is four independent test suites hidden in one 1,986-line shared-state script
M: shell smoke suite decomposition · tests · high · L
F: worktree shell smoke suite | four independent narratives share one fail-fast process, ambient stubs, counters, and accumulated cleanup traps | each worktree subject suite runs independently with isolated state and cleanup while preserving all 71 assertions and complete registration
O: base:p1434[mw]; p1437[cw]; p1435[cw]; p1436[cw]; p1447[xd]; p1449[xd]; p1448[xd]; p1058[xd]; p225[mw]; p77[ow]; p74[ow] || alt(shared-fixture-hoist-if-used-by-two-or-more-suites):p1412[cd@shared-fixture-hoist-if-used-by-two-or-more-suites]
R: serialize>198[explicit]

## 064 `064-pagination-suites-systematically-avoid-tie.md`
T: Pagination suites never seed equal-order-key rows at page boundaries, so three timestamp-cursor endpoints silently skip tied rows today and none of the five can catch a tie regression
M: pagination tie coverage · tests · high · M
F: server cursor-pagination endpoints and their boundary tests | fixtures avoid tied sort keys, hiding timestamp-cursor row skipping and non-total ordering at page boundaries | pagination uses a stable total order and unique cursor so tied rows are returned exactly once in deterministic order
O: base:p627[mw]; p650[mw]; p717[mw]; p643[mw]; p647[mw]; p778[mw]; p628[mw]; p651[mw]; p718[mw]; p644[mw]; p648[mw]; p897[mw] || alt(if-wording-implies-timestamp-cursor-semantics):p715[ow@if-wording-implies-timestamp-cursor-semantics]
R: -

## 065 `065-server-service-suites-redundantly-wipe.md`
T: Fifteen server service suites re-wipe a database the global test lifecycle already cleans before every test
M: test lifecycle ownership · tests · medium · M
F: server service-suite database isolation lifecycle | suite hooks repeat the global beforeEach database wipe, obscuring the few load-bearing mid-test resets | the global lifecycle is the sole start-of-test isolation owner and every exceptional mid-test reset is explicit and documented
O: base:p696[mw]; p736[mw]; p745[mw]; p746[mw]; p727[mw]; p764[mw]; p762[mw]; p693[mw]; p695[mw]; p723[mw]; p731[mw]; p734[mw]; p733[mw]; p732[mw]; p725[mw]; p779[xt]; p777[xt]
R: -

## 066 `066-three-mutation-test-lanes-can-strand-live.md`
T: Three in-place Stryker lanes can strand live source mutations in the contributor's worktree, with no stale-state detection or recovery rail
M: in-place mutation-run safety · tests · medium · M
F: in-place Stryker mutation-test lanes | hard kills can strand mutants in tracked source because wrappers lack target preflight, stale-state detection, and bounded restoration | mutate targets are verified clean before execution and any run-created dirt is detectable and recoverable without overwriting legitimate work
O: base:p258[mw]; p956[cw]; p1459[ow]; p1406[ow]; p1529[ow]; p1459[xt]; p1406[xt]; p1529[xt] || alt(shell-runner):p1377[cw@shell-runner] || alt(bun-ts-runner):p957[cw@bun-ts-runner]
R: prefersBefore>101[explicit]

## 067 `067-lint-ratchet-acceptance-fixtures-emit-321.md`
T: Five lint-ratchet acceptance fixture writers emit 321 lines of private, already-divergent copies of the package's exported config types
M: fixture type single-sourcing · tests · medium · M
F: lint-ratchet acceptance fixture configuration types | five generated private facsimiles of exported package types have diverged from the authoritative unions and fields | fixture registries reference the public authoritative config-type module and contain no shadow type vocabulary
O: base:p1353[mw]; p1420[mw]; p1496[xt]; p1346[xt]
R: serialize>068[inferred]

## 068 `068-one-lint-ratchet-acceptance-suite-serializes.md`
T: One 809-line lint-ratchet acceptance suite serializes 32 blocking CLI runs in a single Vitest worker
M: acceptance-suite decomposition and parallelism · tests · medium · M
F: lint-ratchet CLI output acceptance suite | one large file performs 32 blocking child-process runs serially in one worker and couples unrelated subject feedback | independent acceptance subjects distribute across files, use an awaitable runner, and never allow concurrent cleanup to delete live sibling fixtures
O: base:p1352[cd]; p1353[mw]; p1351[cw]; p236[mw]; p1420[mw]; p225[mw]; p1409[xd]
R: serialize>067[explicit];rebaseOn>067[explicit]

## 069 `069-eslint-configuration-suites-repeat-real.md`
T: Ten ESLint-config suites each rebuild the same real-repository lint harness instead of importing it once
M: lint-config test plumbing · tests · medium · S
F: ESLint repository-policy test harness | ten suites reconstruct the real-config ESLint instance and common query helpers, allowing setup variants to drift | repository policy suites share one harness implementation while retaining suite-specific assertions and file placement
O: base:p166[ct]; p177[mw]; p178[mw]; p179[mw]; p181[mw]; p187[mw]; p188[mw]; p189[mw]; p190[mw]; p191[mw]; p192[mw]; p197[xt]; p180[xd] || alt(optional-adjacent-comment-fix):p180[ow@optional-adjacent-comment-fix]
R: -

## 070 `070-server-mutation-vitest-config-re-declares.md`
T: The Stryker-only server Vitest config re-declares the regular server test lifecycle field by field, with no parity guard
M: single-source test config · tests · medium · S
F: regular and Stryker-only server Vitest project configuration | the mutation config duplicates lifecycle and environment fields without a parity guard, so infrequent mutation runs can use stale semantics | common server test lifecycle values have one plain truth source and every mutation-specific difference remains explicit and local
O: base:p822[mw]; p823[mw] || alt(extend-existing-module):p781[mt@extend-existing-module] || alt(new-src-test-module):p773[ct@new-src-test-module] || alt(new-top-level-config-module):p596[ct@new-top-level-config-module]; p149[mw@new-top-level-config-module]
R: serialize>066[explicit]

## 071 `071-paired-shared-schema-suites-split-request.md`
T: Character and encounter request-schema tests hide inside entity-named suites instead of their dedicated `*-inputs.test.ts` files
M: request-contract test ownership · tests · medium · S
F: character and encounter request-schema test ownership | request-schema describes are split between entity-named and dedicated input suites, including multiple authorities for the same schemas | request payload schemas are tested in their dedicated `*-inputs.test.ts` suites while entity suites cover persisted/output schemas
O: base:p885[mw]; p883[mw]; p901[mw]; p897[mw]; p876[xt]; p75[xt]
R: serialize>024[explicit]

## 072 `072-collector-scheduler-tests-coordinate-through.md`
T: Collector scheduler tests manufacture ordering, overlap, and pending workers out of millisecond wall-clock sleeps instead of driving the state machine directly
M: deterministic concurrency tests · tests · medium · S
F: lint-ratchet collector scheduler tests | millisecond sleeps manufacture worker ordering and pending states while a single type-aware fixture makes the single-flight assertion vacuous | tests deterministically drive promise-settlement transitions and exercise type-aware exclusion with at least two type-aware workers
O: base:p1498[mw]; p1497[xt]
R: -

## 073 `073-portable-lint-ratchet-tests-depend.md`
T: The portable lint-ratchet package's tests create directory symlinks, a privileged operation on Windows, with a one-word fix available
M: Windows-portable test fixtures · tests · low · S
F: portable lint-ratchet test fixture dependency links | directory symlink creation requires Windows Developer Mode or administrator privileges and fails with `EPERM` before tests execute | fixture `node_modules` links work on stock Windows without elevated privileges and retain POSIX behavior
O: base:p1524[mw]; p1527[mw]; p1526[mw]; p1525[mw]; p1485[mw]; p1475[xt]; p1474[xt]
R: rebaseOn>067[explicit];rebaseOn>068[explicit]

## 074 `074-coverage-gates-count-490-lines-test.md`
T: Coverage gates count 490 lines of test scaffolding as production code
M: coverage denominator integrity · tests · low · S
F: Vitest production coverage denominators | test helpers and fixture runners are counted as easily exercised production code, padding coverage totals and thresholds | coverage includes production modules only and its thresholds are derived after all test scaffolding exclusions
O: base:p1534[mw]; p944[mw]; p594[mw]; p1446[mw]; p1458[xt]; p822[xt]
R: -

## 075 `075-live-message-identity-golden-stale-machine.md`
T: The message-identity golden calls itself a live collector run while freezing a deleted developer worktree 50 times over
M: golden fixture labeling · tests · low · S
F: lint-ratchet message-identity golden corpus | a frozen historical fixture is labeled live and embeds a deleted, machine-specific developer worktree path throughout | the corpus is explicitly historical, machine-independent, and normalized through one consistent synthetic repository root
O: base:p1504[mw]; p1506[mw]; p93[mw]; p1507[xt]
R: -

## 076 `076-schema-tests-retain-opaque-audit-numbers.md`
T: Schema tests label durable rationale with opaque audit numbers and line references that have already drifted
M: comment provenance · tests · low · S
F: shared schema test rationale comments | durable test intent is mixed with unresolvable audit numbers and line coordinates that drift as source moves | comments explain invariants through stable symbols and retain only resolvable, useful provenance
O: base:p897[mw]; p910[mw]; p923[mw]; p925[mw]; p911[xt]
R: -

## 077 `077-eleven-non-encounter-e2e-suites-present.md`
T: Eleven whole-file serial E2E suites present shared-state workflow narratives as 77 nominally independent tests
M: e2e test isolation · e2e · high · L
F: eleven non-encounter serial E2E suites | nominally independent tests share narrative state established in `beforeAll` or earlier tests, causing hidden source-order prerequisites and cascade skips | genuine workflows are represented as single stepped scenarios while independent contracts own their prerequisites and parallelize only with per-test isolation
O: base:p30[cw]; p124[mw]; p125[mw]; p126[mw]; p127[mw]; p128[mw]; p129[mw]; p130[mw]; p131[mw]; p136[mw]; p138[mw]; p143[mw]; p133[xd]; p134[xd]; p96[xt]; p947[xt]
R: serialize>079[explicit]

## 078 `078-axe-allowlists-can-outlive-accessibility.md`
T: The axe baseline only subtracts, so repaid accessibility debt never forces its exemption to be retired
M: ratcheting test baselines · e2e · medium · S
F: per-view axe accessibility debt baseline | subtraction-only filtering never detects repaid debt, so stale rule-ID exemptions can mask later regressions | every baseline entry corresponds to a currently observed serious or critical violation and must be retired when that violation disappears
O: base:p123[mw]
R: serialize>079[explicit]

## 079 `079-e2e-specs-bypass-page-objects-through-raw.md`
T: E2E specs assert wizard state through raw selectors and public page-object locator fields instead of one page-object contract
M: page-object encapsulation · e2e · low · S
F: character-wizard E2E page-object contract | specs bypass page-object behavior through raw selectors and public locator fields, duplicating interactions and scattering selector ownership | repeated wizard interactions and state assertions use named atomic page-object methods while genuinely one-off assertions may remain direct
O: base:p139[mw]; p144[mw]; p129[mw]; p122[xt]
R: rebaseOn>091[explicit]

## 080 `080-public-harness-copy-recipes-not-closed-over.md`
T: The public harness adoption recipes hand-list copy sets that are not closed over their imports, and the "portable" controls generator imports a file the same document calls a Musi adapter
M: portable-harness copy boundary · docs · high · L
F: public harness adoption copy recipes and portable-core boundary | hand-listed copy sets omit transitive imports and a file classified as portable statically imports Musi-specific adapter policy | recipe entrypoints mechanically produce complete, freshness-checked, fully classified closures with every adapter dependency carrying an explicit replacement contract
O: base:p948[cw]; p1262[mw]; p1243[cw]; p89[cw]; p228[mw]; p84[mw]; p1250[xd]; p1451[xd]; p1245[xd]; p1346[xt] || alt(S3-inject-registry):p1253[mw@S3-inject-registry]
R: rebaseOn>168[ambiguous]

## 081 `081-lint-ratchet-adoption-docs-mix-packaged.md`
T: The lint-ratchet adoption docs teach the retired copy-set architecture alongside the landed package-plus-adapter seam, down to a file that no longer exists
M: Docs teach the landed architecture · docs · high · M
F: lint-ratchet package-adoption documentation | The guides splice retired copy-set instructions and Musi-only adapter files and envelopes into the package-copy workflow, including dead paths and manifest concepts. | Adoption documentation consistently describes the packaged engine and adopter-owned adapter/envelope, and every cited path or export is live and correctly scoped.
O: base:p107[mw]; p108[mw]; p1475[xt]; p1474[xt]; p1528[xt]; p198[xt] || alt(coherence-sweep-hit):p109[mw@coherence-sweep-hit]
R: rebaseOn>124[explicit]

## 082 `082-devcontainer-quick-start-depends.md`
T: The devcontainer quick start and its copy-and-reuse pitch both depend on infrastructure the repository does not contain
M: devcontainer bootstrap prerequisites · docs · high · M
F: fresh-checkout and copied-project devcontainer bootstrap | Undocumented external image, cgroup, volume, and firewall dependencies prevent startup, while an unused second startup executable bypasses the supported setup gate. | Every non-repository prerequisite is explicit or self-provisioned, reuse replacement points are identified, and every supported server-start path honors the setup-complete contract.
O: base:p15[mw]; p18[xt]; p37[xt] || alt(zero-consumer-confirmed):p21[dw@zero-consumer-confirmed]; p14[mw@zero-consumer-confirmed] || alt(supported-consumer-found):p21[mw@supported-consumer-found]; p16[mw@supported-consumer-found] || alt(optional-volume-self-provisioning):p17[mw@optional-volume-self-provisioning]
R: -

## 083 `083-sole-progress-queue-mostly-stale-view.md`
T: The repository's only in-progress queue lists eleven open items, and ten are already done, resolved in place, or point at deleted leaves
M: active-work signal accuracy · docs · high · S
F: the repository's sole in-progress work queue | Terminal, locally resolved, deleted-target, and circular-pointer entries remain presented as open work. | The active-work queue contains only genuinely live residue, and every retained item has a non-circular recoverable source with authoritative status semantics.
O: base:p83[mw]; p81[mw]
R: -

## 084 `084-backlog-simultaneously-action-queue-evidence.md`
T: The backlog directory is simultaneously an action queue, an evidence archive, and a completed-work store, and only reading all 301 files tells you which is which
M: backlog actionability legibility · docs · medium · L
F: backlog record actionability and lifecycle classification | Actionable work, terminal history, provenance, and ledgers share one namespace without a reproducible typed index, forcing readers to inspect files individually. | Every tracked backlog record is reproducibly classified from structured headers and shared status semantics, with a fresh generated catalog that presents state but does not rank work.
O: base:p985[mw]; p984[mw]; p71[cw]; p228[mt]; p1444[mw]; p72[mw]; p68[mw] || alt(status-vocabulary-extension-needed):p986[mt@status-vocabulary-extension-needed] || alt(optional-json-sibling):p70[cw@optional-json-sibling]
R: rebaseOn>083[explicit]

## 085 `085-specialist-package-script-surface-has-no.md`
T: The 177-key package-script surface has no contributor-facing command catalog — its only documentation is a raw manifest dump, and 22 root scripts are described nowhere
M: command-surface discoverability · docs · medium · M
F: repository package-script command surface | Raw manifest dumps and prefix-limited parity leave specialist, non-prefix, and non-root scripts without purpose, audience, side-effect, cwd, or ownership metadata. | Every package script has exactly one authoritative metadata source and appears in a fresh generated catalog with actionable usage semantics.
O: base:p228[mt]; p1251[cw]; p91[cw]; p1266[mw]; p1249[mw]; p258[mw]; p597[xt]; p261[xt]; p825[xt]; p200[xt]; p1475[xt]; p1444[mw]; p41[mw]; p84[mw]
R: -

## 086 `086-driftai-operator-guide-890-line-multi.md`
T: The drift:ai operator guide is one 890-line README serving five audiences, restating the supported invocation four times
M: audience-split operator docs · docs · medium · M
F: drift:ai operator and maintainer documentation | One monolithic README interleaves five audiences and repeats invocation guidance, while drift guards are pinned to content locations that a split can silently unprotect. | External operators get one complete canonical invocation, audience-specific material lives in focused portable docs, and every authoritative enumeration or backlog link remains parity-pinned after movement.
O: base:p1068[mw]; p1111[cw]; p1113[cw]; p1112[cw]; p1181[mw]; p93[mw]
R: -

## 087 `087-server-contributor-maps-contradict-omit.md`
T: The server's contributor maps leave the homebrew validation boundary undefined, hide a socket handler's direct Prisma reads, and give the 46-file seed subsystem no map at all
M: contributor orientation maps · docs · medium · M
F: server contributor orientation maps for routers, sockets, and seed infrastructure | The maps leave a validation ownership seam implicit, omit a persistence-read collaborator, and provide no role or flow map for the seed subsystem. | Mandatory module maps accurately define ownership boundaries and collaborators, while dynamic order and generated provenance remain sourced from implementation rather than duplicated prose.
O: base:p653[mw]; p722[xt]; p765[mw]; p767[xt]; p661[cw]; p682[xt]; p675[xt]; p115[xd]; p97[xd]
R: rebaseOn>097[explicit];rebaseOn>203[ambiguous]

## 088 `088-client-component-module-documents-misstate.md`
T: Three high-change client component MODULE maps are unreliable: the VTT map credits `VttSurface` with composition it does not do, the combat map advertises orphaned UI and paging ownership that does not exist, and the encounters map is nine lines for a 1,888-line feature
M: component MODULE map accuracy · docs · medium · M
F: client VTT, combat, and encounters MODULE maps | The maps attribute composition and paging to the wrong components, advertise orphaned UI, and omit the ownership and flow structure of a large feature directory. | Component maps reflect the production import and mount graph, name only real ownership, and provide enough standard module sections to orient a first edit.
O: base:p450[mw]; p270[mw]; p289[mw]; p479[xt]; p274[xt]; p311[xt]; p271[xt]; p295[xt]; p97[xd]
R: rebaseOn>061[explicit]

## 089 `089-client-hook-page-entry-point-maps-omit-major.md`
T: The client hooks and pages entry-point maps hide most of their production surface and advertise a file-private component as importable
M: module-doc entry-point completeness · docs · medium · M
F: MODULE External Entry Points inventories for client hooks and pages | A charter without a completeness or derivation rule allows hand-curated lists to omit production-consumed exports and advertise file-private symbols. | Entry-point inventories are reproducibly complete for their declared scope, contain no private symbols, and distinguish external imports from internal composition landmarks.
O: base:p115[mw]; p97[mw]; p481[mw]; p541[mw]; p480[xt]; p540[xt]; p561[xt]; p1227[xd]
R: rebaseOn>061[explicit];rebaseOn>062[explicit]

## 090 `090-lint-ratchet-newcomer-docs-omit-prerequisite.md`
T: The lint-ratchet demo omits the flock prerequisite and misstates its debt invariant, while the live zone-cap comment cites a deleted ruling and calls a landed package move pending
M: Public adoption docs accuracy · docs · medium · M
F: public lint-ratchet demo and live zone-policy guidance | The demo omits a required executable whose advisory failure is misleading, states a debt invariant contradicted by its own accepted-increase workflow, and the config comment describes landed work as pending while citing a deleted ruling. | The optional installer names and safely preflights its prerequisite, debt changes require explicit committed explanation in both directions, and live policy comments describe current layout with durable provenance.
O: base:p199[mw]; p1237[mw]; p203[mw]; p1420[mw]; p148[mw]; p109[xt]; p114[xt]; p78[xt]; p208[xd]
R: serialize>112[explicit]

## 091 `091-e2e-guide-permits-raw-locators-mandatory.md`
T: The e2e guide's raw-locator fallback is impossible as written — the paired lint rule rejects every `locator()` call with no reason inspection
M: guide-lint parity · docs · medium · S
F: e2e raw-locator escape policy | the paired guide permits a reason-comment fallback that the unconditional error-level locator rule cannot recognize | the e2e guide must describe an escape the enforced lint rule and suppression-ledger workflow actually permit, or state that no fallback exists
O: alt(document-suppression):p96[mw@document-suppression]; p175[xt@document-suppression]; p1460[xt@document-suppression] || alt(forbid-raw-locators):p96[mw@forbid-raw-locators]; p175[mw@forbid-raw-locators]; p176[mw@forbid-raw-locators]
R: serialize>079[explicit]

## 092 `092-concurrency-rulebook-claims-three-exhaustive.md`
T: The concurrency rulebook opens by declaring its three patterns exhaustive and forbidding a fourth, while its own Serializable-exception section and a production module use a shape those patterns cannot express
M: doc taxonomy contradicts itself · docs · medium · S
F: concurrency-control pattern taxonomy | the rulebook declares three row-level patterns exhaustive although its own set-level Serializable exception cannot fit them | the rulebook's headline taxonomy must acknowledge the narrowly admitted set-level Serializable mechanism while retaining the closed row-level taxonomy
O: base:p62[mw]; p808[xt]
R: serialize>099[explicit]

## 093 `093-harness-manifest-guide-undercounts-omits.md`
T: The manifest guide's exception inventory claims two sanctioned direct readers remain while the enforced allowlist holds five
M: harness doc inventory drift · docs · medium · S
F: harness manifest direct-reader exception inventory | a hand-maintained guide claims two sanctioned readers while the enforced allowlist contains five distinct sanctioned roles | the contributor guide's complete exception inventory must match the enforced `MANIFEST_DIRECT_READERS` allowlist and its recorded reasons
O: base:p104[mw]; p1274[xt]
R: -

## 094 `094-two-documents-both-claim-be-authoritative.md`
T: docs/README.md brands ai-harness.md the authoritative harness inventory while the generated controls projection claims the same authority — and ai-harness.md itself disclaims exhaustiveness
M: single authority chain for harness docs · docs · medium · S
F: agent-harness documentation authority chain | multiple documents claim exhaustive authority while the conceptual map disclaims exhaustiveness, omits guides, and generated control metadata omits an enforced policy | exhaustive control membership must have one manifest-backed authority, the conceptual guide map must completely index guides, and generated descriptions must expose every enforced control policy
O: base:p64[mw]; p84[mw]; p1141[mw]; p1140[mw]; p193[mw]; p92[mw]; p228[xt]; p1253[xd]
R: serialize>093[ambiguous]

## 095 `095-homebrew-module-index-sends-contributors.md`
T: The homebrew module index's add-entity recipe points contributors at a registry consumer and hides five of six shared helpers
M: Parent index drift after module split · docs · medium · S
F: homebrew parent module index | the parent index duplicates volatile child-module details, misidentifies a registry consumer as the registry owner, and underlists shared helpers | the parent index must own stable layout and routing while focused child docs and registry code remain authoritative for per-folder implementation details
O: base:p382[mw]; p392[xt]; p407[xt]; p397[xt]
R: rebaseOn>180[explicit]

## 096 `096-per-worktree-guide-omits-mandatory.md`
T: The per-worktree guide advertises self-contained provisioning while the implementation dies without the primary checkout's `.devcontainer/.env`
M: undocumented provisioning prerequisite · docs · medium · S
F: secondary-worktree database provisioning | the guide presents provisioning as self-contained while credential resolution exclusively requires an undocumented primary-checkout devcontainer env file | before provisioning commands are shown, the guide must accurately state the credential file, key, database reachability requirement, and distinction from root environment setup
O: base:p112[mw]; p1447[xt]; p15[xt]; p13[xd]
R: rebaseOn>198[explicit]

## 097 `097-two-database-writing-srd-backfill-commands.md`
T: Two database-writing SRD backfill commands sit in the server manifest with no operator documentation anywhere
M: operator runbook coverage · docs · medium · S
F: database-writing SRD backfill commands | destructive-by-default package scripts are exposed beside file generators without an operator runbook, despite fresh seeding already producing the same persisted fields | each retained database-writing command must have accurate operational safety documentation, or obsolete backfill entry points and implementations must be removed
O: alt(document-backfills):p116[mw@document-backfills]; p663[xt@document-backfills]; p667[xt@document-backfills]; p665[xt@document-backfills] || alt(remove-backfills):p597[mw@remove-backfills]; p663[dw@remove-backfills]; p667[dw@remove-backfills]; p665[dw@remove-backfills]; p683[mw@remove-backfills]; p116[mw@remove-backfills]
R: moots>203[explicit];rebaseOn>203[explicit]

## 098 `098-scripts-guide-lists-only-three-twelve.md`
T: The scripts guide's "Current generated surfaces" list presents three of twelve registered generator families as the complete set, and includes a nongenerated demo
M: generated-surface inventory drift · docs · medium · S
F: generated-surface inventory in the scripts guide | a hand-maintained list presents three of twelve registered generator families as complete and mixes in a nongenerated consumer | the guide must distinguish examples from the manifest-backed complete inventory and classify nongenerated consumers separately
O: base:p958[mw]; p228[xt]; p92[xd]; p1262[xt]
R: -

## 099 `099-concurrency-rulebook-mislocates-participant.md`
T: The concurrency rulebook sends readers to a facade router for the participant writer and misnames its lock ordering
M: concurrency guide accuracy · docs · medium · S
F: participant concurrency ownership and lock-order documentation | the rulebook points to a thin router instead of the service owning the writer and field map, and misnames UTF-16 code-unit sorting as code-point sorting | the concurrency rulebook must identify the actual participant-policy owner and state the comparator's precise ordering contract
O: base:p62[mw]; p720[xt]; p820[xt]; p898[xt]
R: serialize>092[explicit]

## 100 `100-shared-local-documentation-carries-stale.md`
T: Shared-package docs carry three stale claims contradicted by a colocated test, the import graph, and the SRD 5.2.1 oracle
M: stale doc claims vs code oracles · docs · medium · S
F: shared-package local documentation inventories and ruleset provenance | hand-maintained claims drifted from a colocated test, the current import graph, and the project's SRD oracle | shared-package contributor docs and comments must match colocated test coverage, actual direct consumers, and the SRD 5.2.1 source of truth
O: base:p876[mw]; p834[mw]; p874[mw]; p918[xt]; p579[xt]; p310[xt]; p872[xt]
R: serialize>024[explicit];serialize>034[explicit]

## 101 `101-mutation-docs-promise-sandbox-isolation.md`
T: Mutation-testing docs still promise sandbox isolation while three of the four lanes mutate tracked source in the live worktree
M: mutation-run operator-doc accuracy · docs · medium · S
F: mutation-testing operator documentation for Stryker lanes | blanket sandbox claims and incomplete lane labels conceal that three lanes mutate tracked source and may strand mutants after a hard interruption | operator docs must reflect each executable lane’s isolation model and provide recovery instructions matching the final runtime mechanism
O: base:p79[ow]; p84[ow]; p1458[xt]; p1406[xt]; p1459[xt]; p1529[xt]
R: requires>066[explicit]

## 102 `102-generated-doc-ownership-table-misclassifies.md`
T: The generated-docs ownership table labels the hybrid lint coverage map wholly hand-maintained and invites edits to generator-owned bytes
M: doc ownership contracts · docs · low · S
F: generated-document ownership contract for lint-coverage-map.md | the directory README classifies a marker-generated document as wholly hand-maintained, inviting edits that generation will overwrite | the ownership table must distinguish generator-owned spans from directly editable prose and name the correct refresh and freshness commands
O: base:p90[ow]; p93[xt]; p1328[xd]
R: -

## 103 `103-primary-onboarding-omits-bashunix-runtime.md`
T: Primary onboarding lists no host-environment prerequisite even though its first development command requires Bash
M: onboarding prerequisites · docs · low · S
F: primary contributor onboarding host prerequisites | platform-neutral prerequisites omit the Bash/Unix runtime required by the first development command and most root workflows | onboarding must state the supported host environment before contributors invoke Bash-backed commands
O: base:p41[ow]; p258[xt]; p15[xt]
R: rebaseOn>082[explicit];rebaseOn>106[explicit]

## 104 `104-scripts-readme-gives-non-root-recovery.md`
T: The scripts README's nested-cwd troubleshooting offers a recovery command that fails for the exact reason it just diagnosed
M: self-defeating troubleshooting advice · docs · low · S
F: nested-working-directory recovery guidance for root-only scripts | the suggested relative module command resolves from the same nested cwd that caused the diagnosed failure | every documented recovery command must resolve the root script independently of the caller’s current directory
O: base:p958[ow]; p37[xt]
R: -

## 105 `105-client-source-comments-preserve-three.md`
T: Three client comments document superseded contracts: a phantom onFinalize snapshot argument, an abandoned socket-filter design, and a nonexistent server file
M: stale boundary comments · docs · low · S
F: client boundary comments for canvas finalization, character query keys, and map-image upload responses | comments preserve superseded callback, consumer, and server-path contracts that disagree with current signatures and references | boundary comments must describe current call arguments, actual consumers, and resolvable truth-source paths without changing runtime behavior
O: base:p483[ow]; p514[ow]; p486[xt]; p508[xt]; p659[xt] || alt(before 062 removes the unused filter members):p491[ow@before 062 removes the unused filter members]
R: serialize>062[explicit];rebaseOn>062[explicit]

## 106 `106-root-devcontainer-compose-bootstrap.md`
T: Root and devcontainer Compose each mount their own copy of the test-DB init SQL, with nothing keeping the copies equal
M: enforced single-source bootstrap assets · docs · low · S
F: PostgreSQL test-database bootstrap SQL shared by root and devcontainer Compose | two independently mounted copies are manually synchronized with no equality or generation guard and one copy carries environment-specific prose | both Compose environments must consume one authoritative bootstrap asset or have enforced byte equality, with comments accurate for every mounting environment
O: alt(single-source root init-test-db.sql):p18[mw@single-source root init-test-db.sql]; p19[dw@single-source root init-test-db.sql]; p232[mt@single-source root init-test-db.sql] || alt(single-source .devcontainer/init-test-db.sql):p60[mw@single-source .devcontainer/init-test-db.sql]; p232[dw@single-source .devcontainer/init-test-db.sql]; p19[xt@single-source .devcontainer/init-test-db.sql] || alt(retain copies with doctor equality check):p1059[mw@retain copies with doctor equality check]; p232[mw@retain copies with doctor equality check]; p19[xd@retain copies with doctor equality check] || alt(retain copies with generated-surface equality check):p210[mw@retain copies with generated-surface equality check]; p232[mw@retain copies with generated-surface equality check]; p19[xd@retain copies with generated-surface equality check]
R: -

## 107 `107-ai-hook-policy-2121-line-shell-parsing.md`
T: The AI-hook command policy is a 2,121-line bespoke shell-language analyzer fused into two shared files, and every rule change taxes several private parsers
M: Declarative policy over bespoke parsers · harness · high · XL
F: shared AI-hook command-policy enforcement | declarative safety rules, shell parsing, Git semantics, and decision emission are fused across large shell bodies while Claude-native denies duplicate part of the policy | policy records must be declarative and projection-checked while fail-closed shell parsing and enforcement remain behaviorally identical behind bounded modules and one decision interface
O: alt(Slice A):p228[mt@Slice A]; p1247[cw@Slice A]; p973[cw@Slice A]; p3[mw@Slice A]; p980[mw@Slice A]; p92[mw@Slice A] || alt(Slices A-C):p974[mw@Slices A-C] || alt(Slice B):p965[cw@Slice B]; p966[cw@Slice B]; p970[cw@Slice B]; p967[mw@Slice B] || alt(Slices B-C):p972[cw@Slices B-C] || alt(Slice C):p963[md@Slice C]; p962[md@Slice C] || alt(each slice):p961[ow@each slice]; p84[ow@each slice]
R: serialize>114[explicit];serialize>116[explicit];serialize>125[explicit];serialize>126[explicit];rebaseOn>114[explicit]

## 108 `108-worktree-seed-validation-implements-private.md`
T: The repo's shared import-closure walker masquerades as five seed-private `worktree-seed-*` scripts, with its contract stated nowhere
M: misnamed shared harness library · harness · high · XL
F: repository-wide source import-closure walker | seed-specific names and placement conceal a shared fail-closed library and leave its deliberate resolution divergence, loader threat model, and alias drift unguarded | the shared closure walker must have neutral discoverable ownership, an explicit source-fingerprinting contract, unchanged loader soundness, and workspace-alias parity
O: base:p77[ow]; p1450[vw]; p1287[cw]; p1288[ct]; p1447[md]; p1380[md]; p1250[md]; p1353[md]; p1420[md]; p1434[md]; p1416[md]; p228[mt]; p1388[mw]; p1261[mw]; p1411[mw]; p93[mw]; p996[xt]
R: -

## 109 `109-musi-repository-policy-embedded-throughout.md`
T: Musi repository policy is hard-coded inside all three ostensibly portable analyzer families, so renames tax private switchboards and silent heuristic misses read as authoritative empty results
M: portable kernels vs repo policy · harness · high · XL
F: repository-policy seams in drift-ai, code-intel, and drift-triage analyzers | Musi-specific topology, path, and naming literals are embedded in portable analysis machinery, while heuristic misses and unsupported discovery scope yield authoritative-looking empty results | each analyzer family must expose repository policy through its native configuration or conventions idiom and clearly diagnose empty or unsupported coverage
O: alt(drift-ai slice):p1167[mt@drift-ai slice]; p1169[mw@drift-ai slice]; p1168[md@drift-ai slice]; p1103[mw@drift-ai slice]; p119[mt@drift-ai slice]; p118[mw@drift-ai slice]; p1068[ow@drift-ai slice]; p1181[mw@drift-ai slice] || alt(code-intel conventions slice):p1013[ct@code-intel conventions slice]; p1012[mw@code-intel conventions slice]; p1015[xd@code-intel conventions slice] || alt(application-only supported-scope decision):p1023[mw@application-only supported-scope decision]; p1020[mw@application-only supported-scope decision] || alt(supported-scope decision):p102[ow@supported-scope decision] || alt(drift-triage slice):p1206[mw@drift-triage slice]; p1202[ow@drift-triage slice]
R: serialize>132[explicit];serialize>134[explicit]

## 110 `110-parallel-verify-dependencies-hidden.md`
T: The generated verify slot data looks like the complete execution contract, but dependency and producer semantics live only in hand-coded Bash
M: single-sourced slot scheduling semantics · harness · high · L
F: parallel verify-slot artifact dependencies | dist producer/consumer semantics are absent from generated slot data and reconstructed through hard-coded Bash slot names, allowing newly registered consumers to race silently | artifact production and conditional consumption must be declared and closure-validated in the manifest, generated for generic parallel scheduling, and inert when no artifacts are declared
O: base:p1433[mw]; p1286[mt]; p228[mt]; p1260[mw]; p1444[mw]; p1443[mw]; p1300[xd]; p113[ow]; p92[mw]
R: serialize>125[explicit];serialize>116[explicit]

## 111 `111-lint-coverage-map-reverse-parses-generated.md`
T: Lint-coverage policy lives as 186 hand-maintained rows in a generated-directory Markdown file that a checker reverse-parses through a private glob dialect
M: typed manifest single-sourcing · harness · high · L
F: lint-coverage policy and generated documentation | hand-maintained Markdown is reverse-parsed through an order-dependent row grammar and a private glob dialect, while only one block is generated | one typed manifest owns policy, all membership uses the canonical ESLint-compatible matcher, and the entire Markdown document is generated without changing policy membership
O: base:p252[ct]; p1326[mw]; p1324[mw]; p1513[xd]; p162[xd]; p1328[mw]; p1325[mw]; p93[mw] || alt(migration-parity):p1462[cw@migration-parity] || alt(post-cutover-cleanup):p1462[dw@post-cutover-cleanup]
R: -

## 112 `112-public-lint-ratchet-demo-asks-adopters.md`
T: The lint-ratchet adoption demo makes outside repos vendor a 1,245-line operational rail around a 17-line app
M: portable-package adoption seam · harness · high · L
F: lint-ratchet operational adoption rail | merge-driver, installer, truth-up, preflight, and recovery machinery is omitted from the package and duplicated as unchecked root/demo forks | one versioned package implementation supplies the operational rail and both Musi and outside-adopter examples consume it through thin adapters
O: base:p1472[cw]; p1475[mt]; p201[dw]; p206[dw]; p207[dw]; p200[mw]; p951[mw]; p1231[xt]; p1236[xd]; p199[ow]; p208[mw] || alt(fixed-git-command-required):p235[cw@fixed-git-command-required]
R: serialize>124[explicit]

## 113 `113-baseline-updates-serialize-stale-local.md`
T: Ordinary baseline updates serialize the caller's stale installed toolchain into repository-wide hash churn
M: baseline update toolchain hygiene · harness · high · M
F: lint-ratchet baseline ruleSourceHash metadata | the update command hashes versions from the caller's potentially stale node_modules without checking them against bun.lock installation freshness | baseline writes occur only from a fresh toolchain identity, preserve the existing update contract, and expose identity-only churn separately from debt changes
O: base:p1334[mw]; p1519[xd]; p199[ow] || alt(adapter-local-shell-helper):p1057[xt@adapter-local-shell-helper] || alt(shared-ts-reader):p1454[ct@shared-ts-reader] || alt(optional-engine-hook):p1471[mw@optional-engine-hook]
R: rebaseOn>122[ambiguous];rebaseOn>124[explicit]

## 114 `114-harness-controls-represented-competing.md`
T: The harness generator and checker keep two same-named raw-control models with duplicated, already-drifting non-lint field walks below the typed manifest parser
M: raw-control model duplication · harness · medium · M
F: raw harness control validation below the typed manifest schema | generator and checker maintain different RawControl types and duplicate non-lint field walks whose checks and message placement have drifted | one raw field inventory and tagged non-lint walker serves both consumers while each retains its sanctioned parse mode, ordering, short-circuiting, and live-tree checks
O: base:p1249[mt]; p1253[mw]; p1252[mw]; p1264[mw]; p104[ow]
R: prefersBefore>127[explicit];serialize>125[explicit];serialize>126[explicit]

## 115 `115-gate-parity-checks-fingerprint-source-text.md`
T: Gate parity checks pin wiring by fingerprinting shell and YAML source text instead of sharing the data that creates the wiring
M: contracts over source fingerprints · harness · medium · L
F: harness gate-wiring parity checks | checks parse and fingerprint shell or YAML source formatting instead of sharing the structured data that defines wiring | data-shaped wiring is generated or parsed as structured data, while only irreducible behavioral tamper invariants retain narrow and explicitly documented source fingerprints
O: alt(pre-push-generation):p249[cw@pre-push-generation]; p29[mw@pre-push-generation]; p228[mt@pre-push-generation]; p149[mt@pre-push-generation]; p1276[dw@pre-push-generation]; p1275[dw@pre-push-generation]; p1245[mw@pre-push-generation] || alt(ci-contract-data):p25[mt@ci-contract-data]; p1266[mw@ci-contract-data] || alt(preflight-shrink):p1254[mw@preflight-shrink]; p1280[mw@preflight-shrink]; p949[ow@preflight-shrink]
R: serialize>114[explicit];serialize>116[explicit]

## 116 `116-generated-surface-dependencies-manually.md`
T: Twelve generated-surface manifest records hand-copy one dependency graph across trigger and fixture facets that the closure validator already computes
M: manifest facet derivation · harness · medium · L
F: generatedSurface trigger and fixture dependency declarations | the manifest manually repeats generator import closures across triggerPaths and fixturePaths even though the validator already computes the graph | the walkable closure is computed once into checked-in projections and only non-derivable semantic extras remain declared with reasons, without weakening walker-less or fail-closed behavior
O: base:p251[ct]; p1250[mw]; p1262[mw]; p1260[mw]; p1445[mw] || alt(required-fixtures-slice):p230[mt@required-fixtures-slice]; p1411[mw@required-fixtures-slice]; p104[ow@required-fixtures-slice] || alt(optional-trigger-slice):p231[mt@optional-trigger-slice]
R: serialize>114[explicit];serialize>125[ambiguous];serialize>126[ambiguous]

## 117 `117-verify-metadatash-second-kitchen-sink-shell.md`
T: `verify-metadata.sh` is a 1,406-line kitchen-sink shell library conflating six gate-metadata concerns behind one ambient namespace
M: gate shell library decomposition · harness · medium · L
F: verify gate metadata shell library | six gate subsystems and 74 functions share one 1,406-line ambient shell namespace with weak concern boundaries | stable public sourcing and function ABI are preserved by a side-effect-free aggregator over acyclic, single-concern internal Bash libraries
O: base:p1321[vw]; p1313[vw]; p1315[vw]; p1319[vw]; p1316[vw]; p1320[vw]; p1318[mt]; p113[ow] || alt(only-if-parsing-is-discovered):p1317[mw@only-if-parsing-is-discovered]
R: serialize>141[explicit]

## 118 `118-doctorsh-organized-around-ambient-mutable.md`
T: Every inline doctor check hand-manages ambient section state, and adding a diagnostic means picking among five undocumented runner protocols with no recipe
M: diagnostic check registration · harness · medium · S
F: doctor diagnostic registration and section attribution | multiple undocumented runner protocols coexist with imperative registration, while inline checks manually mutate and clear ambient control and hint state on every return path | each check is visibly registered through a named protocol adapter and one wrapper owns inline section-state cleanup without changing upstream output contracts
O: base:p1059[mw]; p229[xt]
R: serialize>119[explicit]

## 119 `119-migration-safety-validation-embeds-multiple.md`
T: Migration safety scanning embeds an SQL lexer, acknowledgement policy, and both renderers in one 653-line Bash script
M: Bash-to-TS analytical core · harness · medium · L
F: Prisma migration destructive-operation safety scanner | SQL lexing, allowlist and stale-ack policy, repeated classification, and two renderers are embedded in Bash and awk and coupled through a tab-framed temporary file | one typed, unit-testable TypeScript analysis produces both byte-compatible renderings behind the stable shell facade and shared envelope serializer
O: base:p1303[ct]; p1302[cw]; p1376[mw]; p1246[mw]; p905[xd]; p1422[mw]; p1388[mw]; p84[ow]
R: serialize>118[explicit];rebaseOn>117[explicit]

## 120 `120-cli-option-models-remain-parallel-registries.md`
T: Drift CLI options are spelled in up to six parallel per-tool registries, and drift:ai subcommands still parse through a bespoke callback layer the shared substrate was meant to retire
M: CLI option single-sourcing · harness · medium · L
F: drift-triage and drift:ai CLI option contracts | option names and semantics are repeated across parser specs, Zod schemas, usage, dependency lists, argv rescans, callbacks, and manual result assembly with no agreement check | both stacks use parseCli plus per-tool Zod schemas, parser-observed presence replaces rescans, and parity tests detect registry drift while help and error bytes remain hand-owned and unchanged
O: alt(slice-a):p1296[mt@slice-a]; p1203[mw@slice-a]; p120[mw@slice-a]; p1082[mw@slice-a] || alt(slice-b):p1193[mw@slice-b]; p1072[mt@slice-b]; p1064[mw@slice-b]; p1061[mw@slice-b]
R: serialize>132[explicit];serialize>135[explicit]

## 121 `121-drift-ai-code-intel-independently-encode.md`
T: Drift-ai and code-intel independently encode what counts as a TypeScript import edge
M: Single source for import-edge semantics · harness · medium · M
F: TypeScript module-reference syntax classification in the drift-ai import-cycle graph and code-intel import graph | Duplicated AST walkers use different compiler API dialects and inverse type-only/runtime encodings, allowing syntax evolution to make the analyzers disagree. | One pure compiler-node kernel defines import, export-from, and dynamic-import references and their type-only semantics; stack-specific graph policy remains separate and parity is test-pinned.
O: base:p1312[ct]; p1311[cw]; p1150[mw]; p1010[mw]; p1060[cw]
R: -

## 122 `122-portable-lint-ratchet-package-hard-codes.md`
T: The portable lint-ratchet package hard-codes Musi's `bun run` command vocabulary through its kernel and governance remediation output
M: portable-kernel host-vocabulary injection · harness · medium · L
F: Portable lint-ratchet engine and governance command vocabulary | Musi-specific `bun run lint:ratchet:*` literals are embedded across portable package code and the persisted baseline regenerate annotation, so copied adapters emit wrong recovery instructions and script renames require scattered edits. | Every operational command comes from one required host-adapter vocabulary; baseline writing and staleness validation use byte-identical bindings, and portable kernel/governance code contains no host command literals.
O: base:p1499[mt]; p1493[mw]; p1514[mw]; p1494[mw]; p1519[mw]; p1476[mw]; p1344[mt]; p1342[md]; p1357[md]; p1343[md]; p205[mt]; p1473[cw]
R: serialize>124[explicit]

## 123 `123-concurrency-policy-fragmented-across.md`
T: Concurrency-guard repair guidance is duplicated in two divergently-worded hand maps instead of riding the generated policy graph
M: generated policy descriptor · harness · medium · L
F: Concurrency-guard repair guidance for gated Prisma delegates | Two hand-written delegate-to-suggestion maps carry different prose while the generated policy graph contains only structural policy, so helper guidance and documentation anchors drift independently. | Codemod-owned structured repair metadata is generated into the authoritative relation descriptor, generation fails on incomplete delegate coverage, and every consumer reads the same canonical suggestions.
O: base:p1027[mt]; p1028[mw]; p617[mt]; p1031[md]; p170[md]; p173[mw]; p1026[mw]; p174[mw]; p228[ow]
R: rebaseOn>092[ambiguous];rebaseOn>099[ambiguous]

## 124 `124-ratchet-cli-has-no-authoritative-command.md`
T: The ratchet CLI's fourteen modes are hand-synchronized across five files because no command catalog owns a mode's spelling, options, policy, help, or handler
M: single-source CLI command registration · harness · medium · L
F: Lint-ratchet CLI command registration for fourteen modes | Mode spelling, options, validation, help, preflight policy, and handlers are hand-synchronized across five files, and an omitted dispatch branch silently falls through to the default gate. | One typed catalog owns each command's complete registration, all secondary tables are derived, and missing handler or preflight coverage is a compile-time or test failure.
O: base:p1336[ct]; p1337[md]; p1341[md]; p1339[md]; p1338[md]; p1350[md]; p1340[mw]; p1349[mw]
R: -

## 125 `125-manifest-copies-verify-slot-programs-across.md`
T: The manifest hand-copies nearly identical ordered verify slot programs across four gate controls
M: single-source gate slot programs · harness · medium · M
F: Ordered verify-slot programs for verify, verify-changed, verify-parallel, and pre-commit gates | Four near-identical authored slot arrays duplicate membership, order, commands, and reasoned differences, while existing subset checks cannot detect all omissions or cross-gate drift. | One ordered catalog declares every slot's full and explicit changed-mode disposition; thin profiles carry only reasoned gate overrides, all consumers receive resolved arrays, and generated gate programs remain byte-identical.
O: base:p228[mt]; p1268[mw]; p1286[mw]; p1260[md]; p1285[dw]; p1259[mw]; p1223[xd]; p1444[xd]
R: serialize>116[explicit];serialize>126[explicit]

## 126 `126-hook-wiring-repeats-adapter-templates-leaves.md`
T: All 32 hook bindings hand-author derived command strings, and 29 also hand-author standard matcher syntax, without canonical-projection enforcement
M: hook-wiring provenance enforcement · harness · medium · S
F: AI hook binding matcher and command syntax in `harness.controls.json` | All bindings hand-copy command templates and most copy standard matcher syntax even though generators already parse those strings as derived values; validation checks grammar and presence but not the canonical projection from harness, surface, and body. | The manifest remains policy authority while one fail-closed projection table owns adapter syntax, and eligible authored matcher/command copies are canonical-checked with explicit opt-outs for architectural asymmetries.
O: base:p1271[mt]; p228[mw]; p1242[cw]; p1270[md]; p92[ow]
R: serialize>125[explicit];serialize>116[explicit];serialize>114[explicit]

## 127 `127-public-harness-manifest-has-no-versioned.md`
T: The 2,859-line public harness manifest has no machine-readable published schema — its contract exists only in internal TypeScript
M: published manifest schema · harness · medium · M
F: Public machine-readable contract for `harness.controls.json` | The public-reference manifest has no schema pointer or published schema; its shape and strictness boundaries exist only in internal Zod and hand-validator code, leaving external validation, completion, and compatibility detection unavailable. | The existing Zod root remains the single source of truth and deterministically emits a freshness-gated public JSON Schema whose documented strictness matches what Zod owns, with round-trip fidelity checked.
O: base:p1268[mt]; p1258[cw]; p953[cd]; p228[mw]; p1267[mw]; p1245[mw]; p1266[mw]; p1279[mw]; p258[mw]; p104[ow]
R: serialize>114[explicit]

## 128 `128-baseline-merge-metadata-distributed-across.md`
T: Baseline-family metadata (label, baseline path, CLI path, install command) is repeated per key across five hand-maintained surfaces with nothing checking they agree
M: cross-surface metadata parity · harness · medium · S
F: Metadata tuple for generated-baseline merge-driver families | Labels, baseline paths, CLI paths, install commands, driver names, and attributes are independently repeated across shell and TypeScript surfaces, so adding or renaming a family can leave a silently partial or inconsistent integration. | Intentionally separate runtime surfaces remain hand-authored but a fail-loud parity gate enforces complete key coverage and agreement, with an explicit authority selected for every field.
O: base:p988[cw]; p1231[ot]; p1232[xt]; p1233[xt]; p989[xd]; p1226[md]; p1229[xd]; p1235[xd]; p1234[xd]; p1230[xd]; p258[xd]
R: serialize>129[explicit]

## 129 `129-post-merge-truth-up-parses-human-diagnostics.md`
T: Post-merge baseline truth-up and pre-push classify tool verdicts by grepping human diagnostics instead of reading exit codes
M: exit codes as verdict contract · harness · medium · S
F: Baseline-check verdict contract consumed by post-merge truth-up and pre-push hooks | Hooks classify tool outcomes by grepping human-facing diagnostic prefixes, so cosmetic wording changes can misroute repair advice or stop a stale-baseline push from being blocked, while crashes can masquerade as verdicts. | Dedicated exit codes 3+ are the complete test-pinned verdict API, codes 1 and 2 remain unclassified failure, and hook consumers never parse stdout/stderr prose for control flow.
O: base:p1333[mt]; p1399[mt]; p1398[mt]; p1402[mt]; p1233[md]; p29[md]; p1420[mw]; p1425[mw]; p1421[mw]; p1400[mw]; p106[ow]; p105[ow]
R: -

## 130 `130-workflows-embed-substantial-reporting-api.md`
T: CI workflows embed ~110 lines of reporting and GitHub-API shell inline in YAML instead of delegating to versioned scripts
M: workflow shell extraction · harness · medium · M
F: CI reporting and GitHub API programs embedded in workflow YAML | Substantial shell logic lives only in `run: |` blocks, placing fallback, markdown-safety, pagination, and summary behavior outside local execution, ShellCheck, smoke tests, and normal source tooling. | Workflow YAML owns orchestration and environment binding while versioned shell scripts own reporting behavior, are ShellCheck-visible, and have smoke coverage for critical branches.
O: base:p993[ct]; p995[ct]; p994[ct]; p25[md]; p26[md]; p1419[cw]; p1428[cw]; p1427[cw]; p955[mw]
R: -

## 131 `131-codex-agent-skill-depends-claude-owned.md`
T: The provider-neutral agent dispatch executables live only under `.claude/`, so the Codex skill is a broken unit that points every entrypoint at another provider's tree
M: provider-neutral executable placement · harness · medium · M
F: cross-provider agent-cli dispatch executables | provider-neutral wrappers live only in the Claude-owned skill tree while the Codex projection points into that tree | dispatch executables live in a neutral repository path and every provider skill documents the same explicit portable layout
O: base:p7[vw]; p8[vw]; p959[vw]; p960[vw]; p4[ot]; p5[ow]; p6[ow]; p11[mw]; p1426[mw]; p1391[mw]; p1390[mw]; p1388[mw]; p228[mw]; p84[ow]
R: -

## 132 `132-drift-check-registration-has-three-manually.md`
T: Registering a drift-ai check means updating four manually synchronized inventories the compiler never cross-checks
M: compiler-verified registration · harness · medium · M
F: drift-ai check registration | check IDs, metadata, plugins, and configuration are parallel manually synchronized inventories enforced mainly by tests | one typed membership authority and one typed order authority make every registration surface exhaustive at compile time
O: base:p1200[mt]; p1103[mw]; p1075[mt]; p1077[mw]; p1074[mw]; p1184[mw]; p1186[mw]; p1148[mw]
R: serialize>120[explicit];serialize>135[explicit]

## 133 `133-code-intel-daemon-guards-promise-wire-types.md`
T: The code-intel daemon's wire guards promise types they do not check, and ping/pong crosses the socket outside the declared protocol
M: honest IPC boundary contracts · harness · medium · M
F: code-intel daemon IPC protocol | kind-only guards claim fully validated command/result types while ping/pong bypasses the declared protocol and a private CLI union contains an unreachable arm | every socket arm is declared and decoded by the protocol owner, request validation proves required fields, and shallow response trust is explicitly and narrowly represented
O: base:p1004[mt]; p1023[xt]; p1006[mw]; p1003[mw]; p1002[mw]; p1011[mw]; p1019[mw]; p1005[mw]; p1018[mw]
R: serialize>145[explicit]

## 134 `134-analyzer-families-maintain-divergent-source.md`
T: Seven private source/test path classifiers across drift-ai, drift-triage, code-intel and the codemods disagree about which files are tests
M: shared path taxonomy · harness · medium · M
F: source-code and test-path classification across script analyzers | multiple private extension, basename, directory, and fuzzy-path classifiers encode unnamed policies and disagree on concrete paths | shared primitives and explicitly named policy predicates provide one discoverable taxonomy while preserving intentional per-consumer behavior
O: base:p1306[ct]; p1199[mw]; p1189[mw]; p1147[mw]; p1080[mw]; p1211[mw]; p1021[mw]; p1029[mw]; p1305[cw] || alt(migrate-additional-classifier):p1274[mw@migrate-additional-classifier] || alt(retain-local-classifier):p1274[ow@retain-local-classifier] || alt(module-contract):p1295[ow@module-contract] || alt(header-contract):p1306[ot@header-contract]
R: -

## 135 `135-hotspot-lenses-registered-through-parallel.md`
T: Hotspot lenses are wired through five parallel per-lens surfaces, and the three that are not compile-checked fail silently
M: single-source lens registry · harness · medium · M
F: drift-ai hotspot lens registration | lens identity and per-lens behavior are repeated across typed switches, untyped sets, literal gates, and prose, with compile-silent omissions | one typed lens definition table derives every compile-silent registration surface and any retained dispatch remains exhaustive
O: base:p1146[ct]; p1145[mw]; p1143[mw]; p1142[mw]; p1149[mw] || alt(optional-dispatch-migration):p1144[mw@optional-dispatch-migration]
R: serialize>132[explicit];serialize>120[explicit]

## 136 `136-environment-provider-metadata-has-four.md`
T: Environment-provider metadata is enumerated independently in four places with nothing enforcing exhaustiveness
M: descriptor-table single-sourcing · harness · medium · M
F: drift-ai environment-provider metadata | provider identities and per-provider policies are independently enumerated across types, parsing, advisory collection, and read resolution without exhaustiveness checks | one typed provider descriptor table derives every provider-indexed surface and makes the special shared `env` fallback explicit
O: base:p1125[mt]; p1123[mw]; p1119[mw]; p1124[mw]; p1122[mw]; p1121[mw]; p1118[mw]; p1120[mw]
R: -

## 137 `137-knip-check-reparses-same-cached-report.md`
T: Each Knip category adapter carries its own copy of the report-envelope parser and runs it again over the one report the whole run shares
M: Parse the Knip report once · harness · medium · M
F: Knip JSON report parsing in drift-ai category adapters | three adapters independently parse the same memoized raw report and duplicate the external envelope and item-location contract | the Knip envelope is parsed and documented once, category adapters only project heterogeneous rows, and raw-text compatibility entrypoints compose the shared parser
O: base:p1161[ct]; p1155[mw]; p1158[mw]; p1166[mw]; p1160[mw]; p1153[mw]; p1156[mw]; p1164[mw]; p1154[mw]; p1157[mw]; p1165[mw]; p1159[mw]; p1162[mw] || alt(cache-weakmap):p1163[mw@cache-weakmap] || alt(cache-reportCache):p1076[xd@cache-reportCache]
R: -

## 138 `138-triage-merge-keys-leak-directly-public.md`
T: Drift-triage merge keys double as the published verdict-protocol IDs, with undocumented per-adapter grammars and a JSON.stringify segment
M: protocol identity fencing · harness · medium · M
F: drift-triage item identity and merge keys | adapter-local template strings simultaneously control merging and form the public reviewer verdict token, with undocumented grammars and property-order-dependent Semgrep encoding | one producer-side mint module owns typed, documented, deterministic per-run IDs while downstream verdict IDs remain opaque strings validated against manifest provenance
O: base:p1204[ct]; p1213[mw]; p1211[mw]; p1215[mw]; p1214[mw]; p1202[ow]; p1216[xd]; p1208[xt]
R: serialize>139[explicit]

## 139 `139-triage-locations-have-two-independently.md`
T: Every triage item carries a display-string location list and a structured location list that three producers and both merge paths must keep in step by hand
M: One authoritative location model · harness · medium · M
F: drift-triage item locations | display strings and structured locations are independently produced and merged with different deduplication keys, allowing the two published representations to diverge | one structured location model is authoritative and canonical boundary formatters derive stable display strings with deliberate deduplication semantics
O: base:p1213[mw]; p1215[mw]; p1207[mw]; p1214[mw]; p1212[mw]; p1208[xt] || alt(support-unit):p1211[mt@support-unit] || alt(new-location-unit):p1211[mw@new-location-unit]; p1205[ct@new-location-unit]; p1202[ow@new-location-unit]
R: requires>138[explicit];serialize>138[explicit];serialize>166[explicit]

## 140 `140-analyzer-presentation-logic-uses-english.md`
T: Harness analyzers branch on editable English prose instead of typed outcome discriminants
M: typed analyzer outcomes · harness · medium · M
F: machine-readable analyzer outcomes in drift-ai and logs-audit | formatters branch on editable human-facing reason or message prose because producers omit available typed discriminants | typed additive outcome codes determine control flow while prose is presentation-only and can change without changing remediation
O: base:p1200[mt]; p1194[mw]; p1076[ow]; p1186[mw]; p1185[mw]; p1129[mw]; p1130[mw]; p1373[mt]; p1370[mw]; p1367[mw]; p1374[mw]
R: -

## 141 `141-latest-log-discovery-mirrors-verify-state.md`
T: The log auditor re-implements the shell verify state-path protocol in TypeScript, and only a comment holds the two copies together
M: Cross-language protocol fork · harness · medium · M
F: per-worktree verify-state path protocol used by logs-audit `--latest` discovery | TypeScript independently reconstructs the bash-owned root, identity, hash, normalization, and join protocol with divergent edge cases and no parity enforcement, causing silent searches in directories no writer uses | `verify-metadata.sh` is the single runtime owner of state-path derivation, and the auditor receives its resolved directories through the process environment and fails closed when that contract is absent
O: base:p238[cw]; p258[mw]; p1318[xt]; p1369[mw]; p1359[mw]; p1432[mw]; p1408[mw]; p1388[mw]; p1225[mw]
R: serialize>117[explicit]

## 142 `142-code-intelts-maintains-unused-pseudo-library.md`
T: The `code:intel` entrypoint wraps a three-line executable in 125 lines of lazy pseudo-library facade that nothing imports
M: Entrypoints as executables · harness · medium · M
F: `scripts/code-intel.ts` analyzer executable boundary | an unimported root entrypoint retains a hybrid lazy library facade whose dead exports are hidden from unused-export checks because root scripts are configured as entrypoints | root analyzer entrypoints are thin execution-only programs, while programmatic consumers import the focused internal modules directly
O: base:p997[mw]; p1000[xd]; p999[mw]; p93[ow]; p1414[mw]; p1388[mw]; p1225[mw]
R: prefersBefore>161[explicit];rebaseOn>147[explicit]

## 143 `143-two-command-test-filenames-substantially.md`
T: Two harness suites are named after commands they never load, so the license gate and the near-duplicate benchmark have no command-level contract at all
M: Test names overstate coverage · harness · medium · M
F: license-audit and near-duplicate-benchmark command test contracts | command-named suites cover only exported classification helpers or a core statistics module, leaving traversal, CLI parsing, worker validation, dependency selection, flagging, and exit behavior unreachable while filenames imply command coverage | suite names identify their actual production subjects and each live command has focused characterization of its load-bearing boundary behavior
O: base:p991[vw]; p982[vw]; p93[mw]; p992[mw]; p240[cw]; p983[mw]; p248[cw]; p1307[xd] || alt(extract benchmark parsing and validation boundaries):p990[cw@extract benchmark parsing and validation boundaries]
R: -

## 144 `144-logs-audit-policy-tests-encode-50-scenarios.md`
T: Two logs-audit policy tests encode fifty scenarios as positional JSONL blobs, so a failure names a line number instead of a policy
M: Scenario tables over positional blobs · harness · medium · M
F: logs-audit request-id and business-event policy test matrices | fifty scenarios and forty diagnostics are encoded as positional JSONL blobs inside two tests, coupling expectations to global line numbers and hiding cross-record arming and emission-order dependencies | each policy scenario has a stable descriptive test identity and local record numbering while correlation, exemptions, finding counts, and ordered cross-check behavior remain preserved
O: base:p1374[mw]; p1373[xd]
R: prefersBefore>182[explicit]

## 145 `145-daemon-querytestts-combines-daemon.md`
T: `daemon-query.test.ts` hides daemon-client and graph-cache authority behind an integration-suite name
M: subject-owned test suites · harness · medium · M
F: test authority for code-intel daemon routing, daemon client protocol, and graph-cache invalidation | client and cache contracts are hidden inside a resident-daemon integration suite, so subject-named focused verification omits load-bearing behavior and unrelated cases inherit heavyweight fixtures | each production subject owns a discoverable focused suite with only the fixtures required for its contracts, while all existing cases and behavior remain unchanged
O: base:p1005[mw]; p1001[cw]; p1008[mw]; p1022[xd]
R: -

## 146 `146-duplicates-suite-umbrella-four-production.md`
T: `duplicates.test.ts` hides three production modules’ tests inside one umbrella suite
M: discoverable test ownership · harness · medium · M
F: test ownership for drift-ai duplicates core, runner, plugin, and JSCPD resolver modules | three production modules' complete test authority is hidden under the `duplicates.ts` suite filename, obscuring focused verification and forcing unrelated imports and setup into one umbrella | each independently named production module has a discoverable subject-matched suite, with shared test helpers limited to genuinely multi-suite fixtures and all existing assertions preserved
O: base:p1117[mw]; p1116[cw]; p1115[cw]; p1114[cw]; p1152[cw]
R: -

## 147 `147-major-harness-implementation-directories.md`
T: Two major harness implementation directories lack local ownership maps
M: local harness orientation · harness · medium · M
F: local orientation contracts for the harness and code-intel implementation directories | large contract-heavy directories lack local module documentation, forcing contributors to reconstruct ownership, authority, generated surfaces, state, layering, and entrypoints from filenames and remote summary rows | each major harness implementation family has a durable local ownership and flow map that distinguishes authorities, generated artifacts, external entrypoints, state owners, tests, and gotchas
O: base:p1248[cw]; p998[cw]; p40[mw]; p115[xt]; p97[xd]; p1378[xd]; p228[xt]; p102[xt] || alt(optional README discoverability update):p958[ow@optional README discoverability update]
R: rebaseOn>109[explicit];rebaseOn>142[explicit];rebaseOn>161[inferred]

## 148 `148-rule-source-hashing-depends-home-grown.md`
T: Parse local-rule imports with TypeScript instead of maintaining a partial JavaScript lexer
M: rule-source identity · harness · medium · M
F: local lint-rule source-closure dependency discovery and identity hashing | a partial regex lexer plus independent masking pass recognizes only limited formatting, rejects valid multiple imports per line, and requires duplicated syntax handling to avoid silently mis-hashing rule dependencies | supported JavaScript static dependencies are discovered through the TypeScript parser, runtime-resolved dependencies remain rejected, each source is parsed once, and unchanged closures retain identical raw-byte hashes
O: base:p1522[mw]; p1520[dw]; p1521[mw]; p1475[xd]
R: rebaseOn>150[inferred]

## 149 `149-generic-codemod-infrastructure-hidden-behind.md`
T: Give reusable codemod infrastructure a neutral home outside the tRPC schema family
M: codemod foundations · harness · medium · M
F: shared codemod infrastructure and import-normalization ownership | generic project, error, import, and write helpers are owned and re-exported by the tRPC schema family, while producers and the shared writer redundantly normalize transformed files and real writes alone receive a further ESLint pass | reusable codemod foundations live in neutral function-oriented modules, domain barrels expose only domain behavior, and each write plan receives one shared normalization pass with explicit preview, fallback, and ESLint post-pass semantics
O: base:p1039[cw]; p1041[cw]; p1040[cw]; p1042[cw]; p1048[mw]; p1046[mw]; p1044[mw]; p1047[mw]; p1035[mw]; p1051[mw]; p1049[mw]; p1030[mw]; p1461[mw]; p1053[mw]
R: -

## 150 `150-engine-context-leaves-local-rule-cache.md`
T: Make local-rule and cache directories explicit lint-ratchet engine bindings
M: portable engine layout · harness · medium · M
F: lint-ratchet engine bindings for local-rule and generated cache/config directories | kernel modules bypass the existing repository-binding seam by hard-coding the local-rule directory and independently reconstructing the cache root, forcing adopters and layout changes to patch or synchronize kernel code | all repository-relative local-rule, config, and cache paths derive from one explicit engine binding with stable defaults, while current hashes, keys, and default on-disk locations remain byte-for-byte compatible
O: base:p1499[mw]; p1522[mw]; p1501[mw]; p1503[mw]; p1521[mw]; p1500[mw]; p1502[mw]; p199[ow]; p205[ow]
R: prefersBefore>122[explicit];rebaseOn>148[inferred]

## 151 `151-fixture-copies-parsed-grouped-two.md`
T: Derive shell-closure copy groups from the canonical fixture sandbox model
M: fixture copy modeling · harness · medium · M
F: fixture copy facts used by shell-source and static-import closure | the same scoped `cp` commands are orchestrated twice into models whose supported syntax and grouping behavior have diverged | one canonical sandbox parse and grouping stream supplies distinct closure-policy consumers without changing shell-closure eligibility or ordered diagnostics
O: base:p1382[mt]; p1384[mw]; p1380[xd]; p1383[mw]
R: -

## 152 `152-path-policy-query-core-closed-over-musis.md`
T: Separate the reusable path-policy query engine from Musi's generated smoke registry
M: portable policy query engine · harness · medium · M
F: path-policy query evaluation and smoke-subject metadata | apparently reusable handlers close over Musi policy, generated registry data, eager filesystem discovery, and independently implemented metadata parsers and matchers | the query engine has zero Musi imports and injected smoke data, while all producers and consumers share one smoke-subject parser and selector contract
O: base:p1386[cw]; p1385[mw]; p1397[mt]; p1391[mw]; p1389[xd]; p1388[mw]; p1395[mw]; p1384[mw]; p1387[mw]; p1390[mw]; p1396[mw]; p1378[ow]; p228[mw]
R: -

## 153 `153-global-restricted-import-policy-survives.md`
T: Compose restricted-import rules so scoped flat configs cannot erase the global schemas-barrel fence
M: additive restricted-import policy · harness · medium · M
F: the repository-wide ban on the removed `@musi/shared/schemas` barrel | later scoped flat-config entries replace the complete restricted-import rule value and can omit the manually repeated global pattern | every configured restricted-import rule site is composed with the canonical schemas-barrel pattern, including future scopes
O: base:p148[mw]; p147[mw]; p160[mw]; p157[mw]; p163[mw]; p188[mw] || alt(157-not-landed):p162[mt@157-not-landed] || alt(157-landed):p233[mt@157-landed] || alt(only-if-a-TypeScript-consumer-imports-the-composer):p1219[mw@only-if-a-TypeScript-consumer-imports-the-composer]
R: rebaseOn>157[explicit]

## 154 `154-concurrency-guard-compresses-two-analyzers.md`
T: Split the concurrency guard's direct and nested write analyzers behind one registered rule
M: separate lint analyzers · harness · medium · M
F: the direct and nested analyzers inside `local/concurrency-guard` | two analyzers with different authority, state, traversal, and exemptions are interleaved in one rule lifecycle | distinct analyzer factories sit behind one registered rule and preserve existing alias collection, exemption scope, diagnostics, and report ordering
O: base:p173[mw]; p169[cw]; p172[cw]; p168[xt]; p171[xt]
R: -

## 155 `155-transaction-callback-tracking-duplicated.md`
T: Share transaction-callback discovery and lifecycle tracking between the two transaction lint rules
M: shared transaction callback state · harness · medium · M
F: interactive-transaction callback context used by two local lint rules | the rules duplicate transaction recognition and function-stack lifecycle logic with different visitor coverage and implicit versus explicit inheritance | one tracker supplies identical inherited transaction state across arrow functions, function expressions, and declarations while rule-specific resolution remains local
O: base:p194[ct]; p195[cw]; p182[mw]; p185[mw]; p183[mw]; p186[mw]; p167[xd]
R: -

## 156 `156-hook-edit-check-protocol-maintained-manually.md`
T: Generate the hook edit-check field contract from one schema instead of hand-synchronizing TypeScript and shell
M: Generated wire contracts · harness · medium · M
F: the tab-separated lint-ratchet edit-check wire protocol | field order and arity are repeated manually across TypeScript, shell readers, and test emitters, while malformed rows can degrade quietly | one declarative schema generates TypeScript codecs and shell field contracts, with freshness and real-emitter parity enforced for all row kinds
O: base:p1484[mt]; p1487[mw]; p243[cw]; p968[cw]; p975[mw]; p971[mw]; p979[mw]; p978[mw]; p1413[mw]; p258[mw]; p228[mw]
R: rebaseOn>124[explicit]

## 157 `157-shared-policyjs-grab-bag-unrelated-lint.md`
T: Split `shared-policy.js` so unrelated lint domains no longer share an import-time I/O boundary
M: Cohesive lint policy modules · harness · medium · M
F: the shared ESLint policy import surface | one high-fan-in module mixes unrelated glob, inventory, package-boundary, and max-lines domains, so narrow imports trigger baseline I/O and inherit unrelated failures | focused modules expose cohesive policy domains with narrow import-time dependencies while preserving every policy value, diagnostic, and ordering contract
O: base:p162[dw]; p246[ct]; p247[ct]; p245[ct]; p244[ct]; p151[xd]; p59[mw]; p1219[mw]; p1220[mw]; p228[mw]
R: rebaseOn>153[explicit];prefersBefore>158[explicit];serialize>158[explicit];rebaseOn>152[explicit]

## 158 `158-typescript-consumers-depend-hand-written.md`
T: Generate TypeScript declarations from checked JavaScript policy instead of maintaining an ambient shadow
M: Generated policy declarations · harness · medium · M
F: TypeScript declarations for checked JavaScript lint-policy modules | a distant ambient wildcard file manually shadows runtime semantic types and is guarded only by shallow parity tests | checked JavaScript is the semantic type source and emits fresh colocated declarations without widening, while only unchecked rule modules retain hand-authored ambient bindings
O: base:p151[mt]; p155[mt]; p242[cw]; p241[cw]; p150[cw]; p154[cw]; p1219[mw]; p1220[mw]; p958[ow]; p258[mw]; p228[mw] || alt(157-not-landed):p162[mt@157-not-landed]; p161[cw@157-not-landed] || alt(157-landed-preferred):p234[mt@157-landed-preferred]; p53[cw@157-landed-preferred]
R: serialize>157[explicit]

## 159 `159-path-policy-gives-single-segment-glob-two.md`
T: Name and validate path-policy’s two segment-pattern dialects instead of treating both as one glob language
M: Explicit glob dialects · harness · medium · M
F: single-segment patterns used by path-policy selectors and fixture shell expansion | two private dialects interpret `?` and bracket punctuation differently while sharing an undifferentiated glob label and duplicated matchers | each dialect is named, documented, validated, and implemented once, with star-only policy patterns rejecting unsupported syntax and fixture behavior preserved
O: base:p1393[ct]; p1392[cw]; p1385[mw]; p1391[mw]; p1379[mw]; p1381[mw]; p1390[mw]; p228[mw]; p1378[ow]
R: serialize>152[explicit]

## 160 `160-debt-log-union-uses-absence-discriminator.md`
T: Accepted debt is the only debt-log variant whose kind is inferred from a missing discriminator
M: discriminated persistence contract · harness · medium · M
F: the persisted lint-ratchet debt-log record union | accepted debt alone lacks a discriminator, so compatibility leaks into writers and consumers, classification uses overlapping predicates, and unknown kinds fall into the accepted schema | new records use a uniform discriminator, legacy absence is normalized only during parsing, unknown kinds receive kind-level errors, and downstream dispatch is exhaustive
O: base:p1479[mt]; p1481[mw]; p1483[mw]; p1477[mw]; p1478[mw]; p1523[xd]; p237[xt]; p58[mw]
R: -

## 161 `161-drift-ai-executable-exposes-incoherent-23.md`
T: The drift-ai executable doubles as a selective 23-module pseudo-API used only by tests
M: executable entrypoint boundary · harness · medium · M
F: drift-ai executable entrypoint and its test-only TypeScript facade | tests import a selectively curated barrel, forcing an import-safe entrypoint guard and implying an unsupported pseudo-API | analyzer root files are executable-only and tests import canonical side-effect-free owner modules directly
O: base:p1063[mw]; p1062[mw]; p1094[mw]; p1095[mw]; p1117[mw]; p1135[mw]; p1195[mw]; p1075[xd]; p1151[xd]; p1184[xd]; p1189[xd]; p1200[xt]; p1077[xd]; p1137[xd]; p1126[xd]; p1186[xd]; p1081[xd]; p1187[xd]
R: requires>142[explicit]

## 162 `162-test-orphaning-indexes-history-then-rescans.md`
T: Test-orphaning builds a touch index and then rescans full history for every source
M: indexed history reuse · harness · medium · S
F: test-orphaning advisory recent commit subjects | each source rescans full bounded Git history even though a path-to-commit-index map was already built | recent subjects reuse ordered touch indexes while preserving rows, order, limits, overlays, and output bytes
O: base:p1142[mw]; p1197[mw]; p1196[mw]; p1071[xd]
R: -

## 163 `163-ratchet-driven-file-boundaries-strand.md`
T: A line-cap extraction strands generic repository behavior behind a sensor-specific module
M: cohesive repository adapters · harness · medium · S
F: shared repository-ignore and Knip subprocess adapters | generic behavior is owned by sensor-specific modules and hard-coded command labels force callers to emit or rewrite the wrong diagnostic identity | generic adapters have neutral ownership and caller-supplied labels while preserving subprocess, normalization, memoization, and sensor behavior
O: base:p1141[mw]; p1171[mw]; p1163[mw]; p1399[mw]; p1170[mw]; p1140[mw]; p1162[mw]; p1400[mw] || alt(neutral-owner-repo-io):p1183[mw@neutral-owner-repo-io] || alt(neutral-owner-repo-ignore):p1182[cw@neutral-owner-repo-ignore] || alt(retain-thin-shim):p1139[mw@retain-thin-shim] || alt(remove-extracted-module):p1139[dw@remove-extracted-module] || alt(if-prior-pack-module-doc-lands-first):p1067[ow@if-prior-pack-module-doc-lands-first]
R: serialize>137[explicit];rebaseOn>137[explicit]

## 164 `164-drift-ai-suites-leave-dozens-temporary.md`
T: Six drift-AI suites leak 58 temporary roots per full run
M: temporary test lifecycle · harness · medium · S
F: temporary repository roots created by six drift-AI test suites | raw temporary-directory allocation is not paired with registered recursive cleanup | ordinary suite temporary roots are tracked through the shared collection-time cleanup helper without changing scenarios or isolation
O: base:p1092[mw]; p1148[mw]; p1140[mw]; p1070[mw]; p1177[mw]; p1198[mw]; p1409[xd]
R: -

## 165 `165-eight-drift-command-fakes-bypass-strict-git.md`
T: Fifteen drift command fakes accept unexpected Git invocations
M: strict dependency fakes · harness · medium · S
F: Git dependency fakes in drift-AI command tests | callbacks match only args[0] and return permissive defaults for unrecognized commands | command tests accept only explicitly registered complete Git invocations and throw on every unexpected call
O: base:p1198[mw]; p1177[mw]; p1070[mw]; p1078[mw]; p1104[mw]; p1106[mw]; p1120[mw]; p1138[xd]
R: -

## 166 `166-triage-report-tests-bury-scenarios-repeated.md`
T: Valid triage-report scenarios repeat transport envelopes instead of their deltas
M: typed fixture builders · harness · medium · S
F: valid drift-triage transport fixtures | tests repeatedly inline complete valid envelopes, obscuring scenario deltas and multiplying synchronized contract edits | typed local builders centralize irrelevant valid defaults while semantically relevant fields remain explicit and parser coverage is retained
O: base:p1214[mw]; p1210[xt]
R: -

## 167 `167-codemod-fixtures-duplicate-identical-before.md`
T: No-op codemod fixtures copy 324 lines into redundant after trees
M: codemod fixture convention · harness · medium · S
F: no-op codemod fixture expected-output trees | most runners require byte-identical after copies even though another runner already treats a missing after directory as a full comparison against before | missing after means full unchanged-tree assertion, while every changed output retains an explicit after tree
O: base:p1043[mw]; p1032[mw]; p1024[mw]; p1050[mw]; p1052[mw] || alt(35-byte-identical-trees-only):p1036[dw@35-byte-identical-trees-only]
R: -

## 168 `168-harness-generator-scenarios-repeat-53-inline.md`
T: Generator tests bury scenario deltas beneath 105 repeated control envelopes
M: scenario fixture deltas · harness · medium · S
F: hook-wiring and verify-step generator test fixtures | valid scenarios repeatedly reconstruct complete control envelopes instead of expressing wiring or slot-vector deltas | typed file-local valid baselines expose scenario-specific overrides while invalid raw shapes and production generator behavior remain unchanged
O: base:p1255[mw]; p1259[mw]; p1268[xt]
R: serialize>066[explicit]

## 169 `169-suppression-allowlists-cannot-ratchet.md`
T: Suppression allowlists preserve stale grants instead of ratcheting downward
M: downward suppression ratchet · harness · medium · S
F: ESLint broad-disable and TypeScript nocheck permission allowlists | registers validate live directives against permissions only in the forward direction, leaving retired rows as dormant grants | every full-scan permission matches at least one live directive, while changed-file scans never infer repository-wide absence
O: base:p1221[mw]; p1407[mw]; p1054[xt]; p1055[mw]; p1415[mw]; p1429[mw]
R: -

## 170 `170-semgrep-rule-source-types-runtime-schema.md`
T: Semgrep manifest key lists are not compiler-bound to the rule-source types
M: manifest key exhaustiveness · harness · low · S
F: Semgrep local and registry manifest allowed-key inventories | untyped key arrays duplicate source-type fields and can silently retain removed keys or omit newly added fields | the compiler rejects both unknown listed keys and source-type keys missing from each manifest inventory while preserving parser diagnostics and exit behavior
O: base:p1190[mw]; p1190[ow]; p1191[xt]
R: -

## 171 `171-logs-audit-business-event-taxonomy.md`
T: The log auditor silently assigns unknown event families the mutation policy
M: business-event policy · harness · low · S
F: logs-audit business-event family policy | duplicated script-event exclusions and an implicit catch-all silently assign unknown event families mutation requirements | one script-local taxonomy must explicitly define business-event eligibility, family matching, outcomes, actor/reason requirements, and the mutation default
O: base:p1365[cw]; p1364[mw]; p1371[mw]; p1374[mw] || alt(182-first):p34[xd@182-first] || alt(171-first):p1366[ct@171-first]
R: prefersBefore>182[explicit]

## 172 `172-two-drift-advisories-duplicate-complete.md`
T: Three advisory suites hand-copy the complete bounded-history fixture
M: Shared advisory fixtures · harness · low · S
F: drift-ai complete bounded-history test fixtures | three advisory suites hand-copy the entire history contract, obscuring scenarios and multiplying maintenance | one test-only factory must own complete-history defaults while suites specify only scenario records and overrides
O: base:p1073[cw]; p1176[mw]; p1196[mw]; p1069[mw]; p1071[xt]
R: -

## 173 `173-near-duplicate-sensor-gate-tests-buried.md`
T: The near-duplicate sensor gate contract is buried inside the detector suite
M: Sensor test ownership · harness · low · S
F: near-duplicate sensor CLI and baseline-gate test ownership | gate tests and dedicated fixtures are buried at the end of a detector-oriented suite, making coverage ownership undiscoverable | sensor-gate contracts must live in a subject-named owner-directory suite while detector contracts remain in the detector suite
O: base:p1175[mw]; p1404[cw]; p1128[vw]; p1403[cw]; p73[xt]
R: -

## 174 `174-overview-suite-forks-code-intels-canonical.md`
T: The overview suite bypasses code-intel's canonical fixture harness
M: Canonical test fixtures · harness · low · S
F: code-intel overview test workspace harness | a private fixture fork has drifted to a narrower package-export model than sibling suites use | overview tests must consume the canonical workspace fixture and resolver while keeping only subject-specific router data local
O: base:p1014[mw]; p1022[xt]
R: -

## 175 `175-metric-strategy-registry-does-not-make.md`
T: The metric strategy registry overpromises a closed-world extension seam
M: closed-world metric contract · harness · low · S
F: lint-ratchet metric strategy extension contract | documentation and a production-exported test-only enumerator present a closed-world registry as a self-contained extension seam | the module must expose only necessary production lookup surface and accurately document every required add-a-metric edit site without introducing another catalog
O: base:p1509[mw]; p1496[xt]; p1492[xt]; p1486[xt]; p1515[xt]; p1512[xt] || alt(remove-enumerator):p1511[dw@remove-enumerator]; p1508[dw@remove-enumerator] || alt(test-scoped-enumerator):p1511[vw@test-scoped-enumerator] || alt(cycle-free-registry-derivation):p1510[mw@cycle-free-registry-derivation]
R: -

## 176 `176-lint-message-evaluation-records-source.md`
T: Lint-message evaluation reports a commit it does not verify
M: trace provenance integrity · harness · low · S
F: lint-message trace source provenance | evaluation trusts copied source while reporting an independently supplied SHA as the evaluated commit | commit, path, range, and source bytes must either be verified as one Git blob identity or clearly reported as unverified metadata
O: alt(verified-provenance):p1330[mw@verified-provenance]; p1298[xd@verified-provenance]; p1329[mw@verified-provenance]; p1224[xt@verified-provenance] || alt(unverified-metadata):p1331[mw@unverified-metadata]; p1329[mw@unverified-metadata]
R: -

## 177 `177-logs-audit-leaves-ingestion-core-flat.md`
T: Logs-audit’s ingestion kernel still lives in its CLI facade
M: CLI kernel boundary · harness · low · S
F: logs-audit ingestion and runner kernel ownership | parsing and orchestration remain embedded in the CLI facade despite the rest of the audit family having owner modules | the import-safe facade must only invoke a reusable directory-owned ingestion and runner kernel while preserving all CLI behavior
O: base:p1359[mw]; p1368[cw]; p1372[cw]; p1374[mw]; p1362[xd]; p1373[xt]
R: -

## 178 `178-local-lint-rules-lack-one-canonical.md`
T: Local lint-rule registration is hand-synchronized across plugin, activation, and manifest surfaces
M: generated lint registration · cross-cutting · medium · L
F: local ESLint rule registration across plugin, activation, and harness controls | rule identity, import, activation, source path, and command are hand-synchronized across surfaces, allowing live manifest drift | deterministic generation from rule modules and activation configuration must exclusively own plugin registration and lint-rule controls, with freshness and activation validation
O: base:p165[xt]; p1257[cw]; p152[cw]; p153[dw]; p197[mw]; p1273[xt]; p1347[xt]; p1256[cw]; p228[mw]; p1272[cw]; p1269[mw]; p1279[mw]; p149[mw]; p258[mw]; p110[ow]; p224[mw]
R: serialize>124[explicit];serialize>158[ambiguous]

## 179 `179-turn-disabled-stat-block-placeholder-party.md`
T: Persist per-participant stat-block reveal state and render a spectator-safe monster drawer
M: party stat-block reveal · cross-cutting · medium · L
F: per-participant monster stat-block reveal and spectator access | the reveal action is disabled and monster discovery is DM-only, while the existing renderer mixes reusable rules text with confidential live state and DM interactions | a persisted DM-controlled reveal must grant only a spectator-safe static monster view and must never expose or activate live participant state or DM actions
O: base:p601[mw]; p600[cw]; p902[mw]; p720[mw]; p638[xd]; p532[mw]; p479[mw]; p461[mw]; p478[mw]; p345[mw]; p274[mw]; p459[mw]; p460[mw]; p458[mw]; p457[mw]; p946[mw]
R: -

## 180 `180-homebrew-entry-type-vocabulary-enumerated.md`
T: Derive homebrew entry selectors and labels from one exhaustive client metadata registry
M: exhaustive homebrew metadata · cross-cutting · medium · M
F: homebrew entry-type presentation metadata | independent non-exhaustive option arrays can omit a valid type from creation or collection filtering despite exhaustive schemas and editors | one compiler-exhaustive ordered client registry must derive every entry-type selector, card label, and filter while keeping the `all` sentinel separate
O: base:p399[cw]; p912[xt]; p396[mw]; p394[mw]; p553[mw]; p395[mw]; p398[cw]; p392[ow]
R: -

## 181 `181-harness-diagnostics-live-application.md`
T: Move the harness diagnostics contract out of the application shared package
M: portable harness contract · cross-cutting · medium · M
F: structured harness diagnostics protocol and adoption boundary | the portable schema is owned by the application shared package and tooling bypasses its package boundary through source-relative imports | the schema, tests, package registration, imports, generated path data, and adoption documentation identify one standalone tooling package without changing protocol behavior
O: base:p1465[cw]; p1468[cw]; p1469[cw]; p1467[vt]; p1466[vw]; p905[dw]; p904[dw]; p258[mw]; p1322[mw]; p1246[mw]; p1244[mw]; p1363[mw]; p1357[mw]; p1354[mw]; p1345[mw]; p1343[mw]; p1265[mw]; p1110[mw]; p1263[mw]; p44[mw]; p1534[mw]; p149[mw]; p1388[mw]; p84[ow]; p107[ow]; p100[ow]
R: serialize>067[explicit];serialize>068[explicit];serialize>152[explicit]

## 182 `182-logging-producers-their-auditor-separately.md`
T: Logging producers and their auditor separately own policy data that must change together
M: logging policy ownership · cross-cutting · medium · M
F: structured logging policy shared by server producers and the JSONL auditor | producer and auditor independently duplicate outcome and redaction vocabularies while mutation events and reasons accept arbitrary strings | canonical policy data has one neutral authority, auditor-only defensive additions remain explicit and local, and server mutation vocabulary is closed without coupling certification logic to production
O: base:p833[ct]; p825[mw]; p612[mw]; p813[mw]; p703[mw]; p788[cw]; p812[mw]; p1370[mw]; p1364[mw]; p1360[xt]; p1361[xt]; p1374[xt]; p787[xt]
R: coLand>171[explicit];prefersBefore>171[explicit]

## 183 `183-database-url-selection-duplicated-across.md`
T: Centralize test and E2E database URL precedence across TypeScript and devcontainer provisioning
M: database URL precedence · cross-cutting · medium · M
F: test and E2E database URL selection and provisioning | five TypeScript and shell implementations duplicate precedence while differing deliberately in errors, empty-value behavior, provenance, and URL-versus-name output | one environment-parameterized resolver owns precedence and provenance while callers preserve their distinct throwing, defaulting, and deduplicated-name contracts
O: base:p616[ct]; p615[cw]; p781[mw]; p780[mw]; p1452[mw]; p132[mw]; p947[mw]; p1056[mw]; p605[cw]; p20[mw]; p1424[mw]; p257[mw]; p774[ow]
R: -

## 184 `184-human-readable-server-messages-act-client.md`
T: Client control flow is coupled to human-readable server error prose
M: typed error contracts · cross-cutting · medium · M
F: client recovery decisions and socket error categorization | control flow compares human-readable server error messages by exact equality or substring, so copy edits silently change behavior | machine-readable typed discriminators determine behavior while messages remain presentation-only, masked authorization remains indistinguishable, and formatter output exposes no arbitrary cause data
O: base:p903[ct]; p250[cw]; p784[mw]; p783[mw]; p539[mw]; p588[mw]; p728[mw]; p619[mw]; p626[mw]; p535[mw]; p534[mw]; p326[mw]; p555[mw]; p545[mw]; p560[mw]; p618[mw]; p771[mw]; p770[mw]
R: rebaseOn>187[ambiguous]

## 185 `185-repository-path-containment-has-several.md`
T: Repository path containment is implemented through incompatible local contracts
M: repository path safety · cross-cutting · medium · M
F: repository-relative path containment in tooling | local resolve/relative checks disagree on root acceptance, bare-parent and separator handling, and symlink or physical containment, allowing false rejection or unsafe writer simplification | read-only tools use one separator-correct lexical contract and every writer or deleter uses one hardened normalized, symlink-safe, physically contained target resolver
O: base:p1309[ct]; p1308[cw]; p1284[mw]; p1282[mw]; p1281[mw] || alt(slice A):p1025[mw@slice A]; p1051[mw@slice A]; p1033[mw@slice A]; p1034[mw@slice A]; p1045[mw@slice A] || alt(slice B):p1016[mw@slice B]; p1298[mw@slice B]; p987[mw@slice B]; p1401[mw@slice B]; p1451[mw@slice B]; p1249[mw@slice B]; p1278[mw@slice B]; p1301[mw@slice B]; p1379[mw@slice B]; p1192[mw@slice B]; p1150[mw@slice B]
R: rebaseOn>152[explicit];rebaseOn>109[explicit]

## 186 `186-authentication-redirects-depend-undocumented.md`
T: Authentication redirects rely on undocumented precedence between two valid navigation props
M: typed redirect contracts · cross-cutting · medium · M
F: login and registration return-path navigation | guards pass conflicting valid navigation props and depend on undocumented framework precedence while guest routes accept untyped security-sensitive search data | guest routes validate one typed return-path contract, typed login navigation and a single href-only resume boundary never supply conflicting destinations, and unsafe redirects remain rejected
O: base:p572[mw]; p574[mw]; p372[mw]; p377[mw]; p371[cw]; p536[mt]; p586[mw]; p137[mw]; p142[xt]; p952[mw]
R: -

## 187 `187-add-campaign-ownership-transfer-account.md`
T: Account deletion promises campaign ownership transfer, but no transfer operation exists
M: campaign ownership lifecycle · cross-cutting · medium · M
F: campaign ownership lifecycle before account deletion | account deletion requires transfer but no transfer operation exists, and DM-role authorization cannot prove ownership or prevent stale concurrent transfers | only the current persisted owner can atomically transfer ownership to an existing member, the campaign survives former-owner deletion, and client ownership state and caches converge
O: base:p880[mt]; p789[mw]; p813[mw]; p787[mw]; p620[mw]; p340[mw]; p338[cw]; p618[mw]; p48[mw]; p339[mw]; p62[xd]; p99[xd]; p88[xd]; p337[xd]
R: rebaseOn>184[explicit]

## 188 `188-let-lint-ratchet-proposal-previews-evaluate.md`
T: Let lint-ratchet proposal previews evaluate third-party rules safely
M: Third-party ratchet previews · cross-cutting · medium · M
F: lint-ratchet third-party rule proposal previews | proposal mode accepts only core and local rules, forcing contributors to modify allowlists and registries before measuring an external rule | third-party rules can be evaluated read-only with validated plugin identity and parser profile, synthesized preview-only policy stays isolated, and output fully discloses the governance changes needed for promotion
O: base:p1486[mw]; p1499[xd]; p1496[xt]; p1491[xd]; p1522[xd]; p1501[xt]; p1356[mw]; p1337[mw]; p1339[mw]; p1341[mw]; p1350[mw]; p1338[mw]; p109[ow]; p108[ow]; p211[mw]
R: serialize>124[explicit]

## 189 `189-downstream-packages-keep-semantic-copies.md`
T: Consolidate downstream semantic copies around shared rule authorities
M: Shared rule authorities · cross-cutting · medium · M
F: shared game-rule constants, enums, limits, and SRD tables used downstream | server and client packages retain equivalent or conflicting private declarations, positional arrays, and validator sets despite existing shared authorities, while CR proficiency is homed in client form code | each covered rule has one discoverable shared authority, downstream iteration and validation derive from it with typed local presentation maps, and schema-valid values remain accepted consistently
O: alt(Part A):p931[ct@Part A]; p878[mw@Part A]; p933[mw@Part A]; p851[mw@Part A]; p862[mw@Part A]; p844[mw@Part A]; p442[mw@Part A]; p818[mw@Part A]; p830[xt@Part A]; p579[mw@Part A]; p835[xt@Part A]; p332[mw@Part A]; p926[xt@Part A]; p341[mw@Part A]; p340[mw@Part A]; p387[mw@Part A]; p874[mt@Part A]; p873[mw@Part A]; p404[mw@Part A] || alt(Part A ability-vocabulary):p848[mt@Part A ability-vocabulary]; p426[mw@Part A ability-vocabulary]; p331[mw@Part A ability-vocabulary]; p938[xt@Part A ability-vocabulary] || alt(Part A spell-school):p412[mw@Part A spell-school]; p936[xt@Part A spell-school] || alt(Part B):p223[cw@Part B]; p47[mw@Part B]
R: prefersBefore>061[explicit];serialize>032[inferred]

## 190 `190-make-combat-history-navigable-through-one.md`
T: Make combat history navigable through one paginated read model
M: Paginated combat history · cross-cutting · medium · M
F: encounter combat-history read model and navigation | encounter detail loads an unused unbounded log relation while the visible client fetches only the oldest first page from a separate cursor endpoint | one bounded paginated endpoint owns combat-history reads, live views receive newest records first, users can load earlier pages, and the panel still renders accumulated history chronologically
O: base:p902[mt]; p800[mw]; p718[mt]; p898[xt]; p295[mw]; p271[mw]; p587[mw]; p145[mw]; p54[mw]
R: rebaseOn>024[explicit];rebaseOn>026[explicit]

## 191 `191-let-players-choose-strength-dexterity.md`
T: Let players choose Strength or Dexterity for Finesse attacks
M: Finesse ability choice · cross-cutting · medium · M
F: Finesse weapon attack ability selection | The shared rules automatically choose the higher Strength or Dexterity modifier, while the schema, server, and VTT expose no explicit legal choice. | A player may select Strength or Dexterity for a Finesse attack, the server validates that choice, and the same selected ability drives both attack and damage while omission preserves current automatic behavior.
O: base:p843[mw]; p878[mw]; p713[mw]; p530[mw]; p468[mw]; p842[mw]; p841[mw]; p877[mw]; p712[mw]; p529[mw]; p668[xt]
R: rebaseOn>031[explicit];rebaseOn>193[explicit];rebaseOn>035[explicit]

## 192 `192-expose-level-up-history-already-returned.md`
T: Present the level-up history already delivered with every character sheet
M: Level history presentation · cross-cutting · medium · M
F: Character level-up choice history | Character detail returns durable level-choice rows that production client code never renders, leaving known and legacy records unused and forcing users to reconstruct past choices. | Both responsive character-sheet layouts display a deterministic, read-only, defensively rendered history in which every stored row remains independent and unresolved identifiers remain visible.
O: base:p440[cw]; p438[cw]; p439[cw]; p437[cw]; p445[mw]; p443[mw]; p431[mw]; p441[mw]; p549[mw]; p581[mw]; p546[mw]; p423[ow]; p886[xt]; p519[xd]; p864[xd]
R: -

## 193 `193-shared-weapon-rules-helpers-demand-full.md`
T: Weapon rules helpers over-demand wire entities and force narrow server projections through double assertions
M: Narrow rules boundaries · cross-cutting · medium · S
F: Shared weapon-rule helper input contracts | Helpers require richer wire entities and closed proficiency types than their computations use, forcing narrow server database projections through double assertions. | Weapon helpers accept only the structural fields they read, retain runtime validation for unknown properties, and allow focused server rows without assertions, widened projections, or entity-reconstruction mappers.
O: base:p843[mw]; p713[mw]; p842[mw]
R: serialize>031[explicit];serialize>035[explicit];serialize>191[explicit]

## 194 `194-generic-mutation-acknowledgment-hidden-under.md`
T: Three mutation acknowledgments remain weaker than the canonical literal-true response contract
M: Response contract convergence · cross-cutting · medium · S
F: Generic mutation acknowledgment output schemas | Three domain schemas use `success: boolean` instead of the canonical literal-true acknowledgment, permitting false and weakening output-validation protection against handler fall-through. | Each domain acknowledgment resolves to the canonical `successResponseSchema`, preserves its domain-facing name where useful, and rejects every response except literal success.
O: base:p907[mw]; p917[mw]; p879[xt]; p906[cw]; p916[mw]
R: -

## 195 `195-validate-concrete-homebrew-entry-data-before.md`
T: Validate per-type homebrew payloads in the editor before sending mutations
M: Pre-submit homebrew validation · cross-cutting · medium · S
F: Homebrew editor concrete entry payloads | The client validates only the opaque mutation envelope, so invalid type-specific data reaches the server and is reduced to a generic save error despite existing shared validators and field-error plumbing. | The editor parses concrete data locally with the shared type registry, displays field-level failures without calling a mutation, forwards the parser’s stripped value on success, and retains server-side authoritative validation.
O: base:p396[mw]; p395[mw]; p913[xt]; p533[xd]; p909[xd]
R: -

## 196 `196-make-notifications-navigate-campaign-context.md`
T: Notification clicks should open the campaign context their payload already identifies
M: notification deep links · cross-cutting · medium · S
F: Campaign notification click behavior | Notification clicks only toggle unread state and the uncontrolled popover cannot navigate, even though supported payloads already identify campaign context. | Mapped notification types with a campaign ID navigate to their designated validated campaign tab and close the popover, unread state is updated independently, and unmapped or malformed notifications retain mark-read-only behavior.
O: base:p419[mw]; p421[mw]; p418[mw]; p420[mw]; p929[xt]; p531[xt]; p563[xd]
R: -

## 197 `197-shared-runtime-neutrality-enforcement.md`
T: Shared must reject Node dependencies as rigorously as browser dependencies
M: runtime boundary enforcement · cross-cutting · medium · S
F: Production runtime neutrality of `packages/shared` | Node ambient types are available and the shared lint boundary lacks Node import/global restrictions, so server-only dependencies can enter browser-consumed production code undetected. | Production shared files reject Node builtins and Node-only globals at lint time, shared tests retain intentional Node access, and all existing browser, framework, cross-package, and schema-barrel restrictions remain composed.
O: base:p157[mw]; p162[mw]; p190[mw]; p67[ow]; p940[xd]; p939[xd]
R: -

## 198 `198-worktree-provisioning-hard-wired.md`
T: Worktree database provisioning must resolve credentials for both supported topologies
M: provisioning credential sources · cross-cutting · medium · S
F: Worktree PostgreSQL admin credential resolution | Provisioning unconditionally reads the primary checkout’s devcontainer env, blocking the supported root-Compose topology and encouraging use of a topology-incompatible database hostname. | Provisioning deterministically selects override, primary devcontainer, or primary root credentials in that order; validates a non-empty PostgreSQL database URL before writes; and emits actionable credential-safe diagnostics.
O: base:p1447[mw]; p1434[mw]; p112[ow]; p32[xd]; p33[xd]
R: rebaseOn>096[explicit]

## 199 `199-magic-item-seed-validation-shadows-shared.md`
T: Magic-item seed validation should reuse the shared category and rarity schemas
M: seed contract reuse · cross-cutting · low · S
F: Magic-item seed category and rarity validation | The server seed privately duplicates shared enum schemas with no import or type relationship, allowing checked-in seed validation and API validation to drift. | The seed consumes the exported shared category and rarity schemas as its single vocabulary source while retaining its distinct persistence-input outer shape.
O: base:p678[mw]; p921[xt]
R: -

## 200 `200-generic-srd-getall-contract-client-specific.md`
T: Rename the partial SRD aggregate to expose its lookup-bundle contract
M: Explicit read-model naming · cross-cutting · low · S
F: Fixed SRD lookup-bundle read model | Generic `getAll` names across router, schema, client, tests, and mocks conceal that the endpoint is a bounded five-collection projection for lookup consumers. | Every layer uses one explicit lookup-bundle name and direct shared contract while preserving response keys, selections, ordering, mappings, caching, and single-round-trip behavior.
O: base:p657[mw]; p938[mw]; p519[mw]; p589[mw]; p656[mw]; p937[mw]; p518[mw]; p481[ow]
R: -

## 201 `201-let-users-rename-homebrew-collection-while.md`
T: Let an imported homebrew collection receive a distinct visible name
M: Import-time collection identity · cross-cutting · low · S
F: imported homebrew collection visible identity | the client resubmits the exported collection name unchanged, producing indistinguishable same-name cards | each import may receive a schema-valid visible name without altering envelope metadata, entries, references, or server-enforced privacy
O: base:p390[mw]; p389[mw]; p908[xt]; p724[xd]; p140[mw]; p135[mw] || alt(if openCollectionCopy has no other caller):p141[dw@if openCollectionCopy has no other caller]
R: -

## 202 `202-enable-existing-comment-density-sensor.md`
T: Let the comment-density sensor inspect the harness code it is meant to protect
M: Harness self-analysis coverage · cross-cutting · low · S
F: comment-density analysis coverage for harness and ESLint-rule source | root-level exclusion prefixes hide source files even though built-in filtering and conservative thresholds already remove known noise | the advisory sensor analyzes configured source roots except for narrowly justified generated, vendored, or deliberately prose-heavy paths
O: base:p119[mw]; p173[xt]; p1096[xt]; p1100[xt]
R: -

## 203 `203-add-previewapply-safety-boundary-srd.md`
T: Require an explicit apply flag before SRD backfills connect and write
M: Destructive script safety · cross-cutting · low · S
F: database-writing SRD backfill commands | commands enter the mutation path without explicit operator acknowledgement, while self-derived membership checks provide false allowlist assurance | backfills are no-op by default, expose only a credential-free target, require exact explicit apply approval, and rely on the validated corpus plus effective query restrictions for write scope
O: alt(if the backfill commands are retained):p663[mw@if the backfill commands are retained]; p667[mw@if the backfill commands are retained]; p665[mw@if the backfill commands are retained]; p662[mw@if the backfill commands are retained]; p666[mw@if the backfill commands are retained]; p664[mw@if the backfill commands are retained]
R: alternativeTo>097[explicit];prefersBefore>097[ambiguous]

## 204 `204-decompose-the-stateful-bash-verification-engine.md`
T: Decompose the stateful Bash verification engine behind its existing facade
M: The verification engine remains a framework-sized stateful Bash module · harness · medium · L
F: stateful Bash verification engine | policy, evidence-transaction, process-lifecycle, and reporting concerns share one large module and ambient mutable state | cohesive subsystems live in internal libraries while the existing facade, contracts, ordering, messages, failure behavior, and public entry points remain stable
O: base:p1314[mw]; p1294[cw]; p1292[cw]; p1293[cw]; p1433[mw]; p227[mw]
R: serialize>117[explicit]

## 205 `205-rename-auth-service-to-reflect-its.md`
T: Rename auth-service to reflect its authentication-primitive boundary
M: auth-service is named and catalogued as orchestration although it contains only authentication primitives · server · low · S
F: server authentication primitive boundary and Bearer-header interpretation | a misleading service name implies workflow orchestration while two HTTP boundaries independently duplicate transport token parsing | cryptographic primitives are named as primitives, request workflows remain router-owned, and all authenticated HTTP boundaries share one explicit Bearer extraction policy
O: base:p690[vw]; p688[cw]; p689[vw]; p687[cw]; p685[ow]; p608[mw]; p610[cw]; p782[mw]; p659[mw]; p215[mw]
R: serialize>004[ambiguous]

## 206 `206-single-source-memory-wait-timeout-validation-across.md`
T: Single-source memory-wait timeout validation across verification entry paths
M: Memory-wait timeout validation diverges across verify entry paths · harness · medium · S
F: MUSI_VERIFY_MEMORY_WAIT_TIMEOUT across verification entry paths | parallel and direct admission accept unbounded digit strings while pre-commit independently normalizes and bounds the same setting | every entry path uses one arithmetic-safe decimal parser, with entry-specific policy caps applied only after shared validation
O: base:p1441[cw]; p1443[mw]; p1442[mw]; p28[mw]; p213[mw]; p209[mw]; p212[mw]
R: -

## 207 `207-encode-ghost-files-mode-dependencies-as.md`
T: Encode ghost-files mode dependencies as discriminated scope contracts
M: Ghost-files mode requirements are not encoded in its scope types · harness · medium · M
F: drift-ai detector scopes and ghost-files execution dependencies | scope mode, file variants, inventory, and directory-listing dependencies are independently optional or unconstrained, permitting invalid combinations and silent record loss | TypeScript callers can construct only mode-consistent scopes with all required ghost-files dependencies, while untyped callers retain defensive runtime validation
O: base:p1189[mw]; p1136[mw]; p1134[mw]; p1132[mw]; p1188[mw]; p1131[mw]; p1532[mw]
R: -

## 208 `208-converge-drift-ai-command-result-and-sentinel-error.md`
T: Converge drift-ai command result and sentinel-error lifecycles
M: Drift-ai command families redeclare one result and error lifecycle · harness · medium · M
F: drift-ai command result and sentinel-error lifecycle | five command families redeclare the same base result and independently map identical help and usage-error sentinels | all command families share one neutral result contract and one sentinel mapper while unexpected errors propagate and command-specific lifecycle ownership remains local
O: base:p1066[cw]; p1178[mw]; p1079[mw]; p1187[mw]; p1098[mw]; p1149[mw]; p1093[mw]; p117[mw]
R: serialize>161[explicit]

## 209 `209-remove-the-unused-repository-root-parameter-from.md`
T: Remove the unused repository-root parameter from blame parsing
M: Blame parsing threads an unused repository-root dependency · harness · low · S
F: drift-ai coldspot blame boundary and file aggregation state | the blame parser advertises an unread repository-root dependency and coldspot aggregation writes fields that no downstream calculation reads | repository anchoring is owned only by Git-runner construction and coldspot aggregates retain only state consumed by age, birth, churn, qualification, and output semantics
O: base:p1087[mw]; p1091[mw]; p1085[mw]; p1089[xt]; p1086[mw]; p1090[mw]; p1088[mw]
R: -

## 210 `210-load-metamagics-only-after-level-up-applicability-is.md`
T: Load metamagics only after level-up applicability is established
M: Level-up queries metamagics before deciding whether metamagic applies · server · low · S
F: Sorcerer metamagic validation during level-up | the orchestrator queries existing metamagics before the Sorcerer validator determines that metamagic selection applies | existing metamagics are loaded exactly once only for applicable Sorcerer levels, with current validation results, errors, transaction boundaries, and write ordering preserved
O: base:p739[mw]; p737[mw]; p738[mw]
R: -

## 211 `211-reuse-validated-class-references-during.md`
T: Reuse validated class references during spell-bearing character creation
M: Spell-bearing character creation reloads class and subclass references already being validated · server · low · S
F: selected class and subclass references during spell-bearing character creation | general reference validation and spell validation independently load overlapping projections of the same references | one validated reference bundle supplies consistent caster metadata to both general creation and spell validation
O: base:p697[mw]; p694[mw]; p693[mw]; p696[mw]; p950[xd]
R: requires>004[explicit]

## 212 `212-give-initiative-orchestration-layers.md`
T: Give initiative orchestration layers depth-signalling names
M: Initiative rolling obscures three orchestration depths behind two names and an alias · server · low · S
F: initiative-roll service layers | three orchestration depths reuse two names and conceal a wrapper/core collision behind an import alias | authorization, caller-owned orchestration, and persistence layers have distinct depth-signalling symbol names
O: base:p719[mw]; p710[mw]; p711[mw]; p708[ow]; p715[ow]; p709[mw]; p631[mw]; p686[xt]
R: -

## 213 `213-make-character-wizard-step-validation-exhaustive.md`
T: Make character-wizard step validation exhaustive over its closed registry
M: Keep character-wizard step keys closed through validation · client · medium · S
F: character-wizard step registry and validator table | closed registry keys are widened to string, allowing a newly registered step to omit its validator and block navigation at runtime | every registered wizard step is type-checked as applicable and has an exhaustive validator entry
O: base:p368[mw]; p369[xt]; p370[mw]; p367[mw]
R: serialize>042[ambiguous]

## 214 `214-derive-the-character-sheet-srd-lookup-bundle-once-at.md`
T: Derive the character-sheet SRD lookup bundle once at composition
M: Character-sheet composition derives the SRD lookup bundle twice · client · low · S
F: character-sheet SRD lookup bundle | the parent and nested sheet-state hook observe the same query and independently derive the same maps and lookup closures | the composition boundary resolves and derives one lookup bundle that every mounted sheet consumer reuses
O: base:p549[mw]; p552[mw]; p551[mw]; p520[xd]
R: requires>200[explicit];serialize>039[ambiguous]

## 215 `215-converge-destructive-client-actions-on-one.md`
T: Converge destructive client actions on one accessible confirmation dialog
M: Map and token destructive actions bypass the dialog confirmation system · client · low · M
F: destructive client-action confirmation | five actions use browser prompts or a second bespoke dialog, duplicating interaction policy, copy, accessibility structure, and pending-state behavior | all bounded destructive actions use one accessible common confirmation shell while preserving action-specific semantics
O: base:p374[mw]; p373[mw]; p380[dw]; p379[dw]; p554[mw]; p348[mw]; p345[mw]; p320[mw]; p319[mw]; p347[mw]; p344[mw]; p322[mw]
R: -

## 216 `216-route-npc-mutations-through-the-campaign.md`
T: Route NPC mutations through the campaign invalidation facade
M: NPC mutations bypass the campaign invalidation facade · client · low · S
F: campaign NPC-list query invalidation | NPC mutations construct a private query key inside the feature panel instead of using the campaign invalidation facade | campaign-scoped NPC invalidation is a named, reusable facade operation shared by every NPC mutation
O: base:p538[mw]; p336[mw]; p537[mw]; p335[mw]; p1530[xt]
R: -

## 217 `217-correct-the-architecture-plan-s-unsupported-level-up.md`
T: Correct the architecture plan's unsupported level-up rollback claim
M: Architecture plan overstates level-choice audit rows as rollback support · docs · medium · S
F: CharacterLevelChoice history semantics in the architecture plan | partial choice audit rows are described as sufficient for reversing a level-up despite omitting complete before-state and inverse operations | architecture documentation distinguishes recorded choice history from unsupported rollback or downgrade capability
O: base:p87[ow]; p886[xt]; p729[xt]; p626[xt]
R: -

## 218 `218-replace-the-coverage-cadence-guide-s-deleted.md`
T: Replace the coverage-cadence guide's deleted baseline pointer
M: Coverage cadence points operators at deleted baseline records · docs · low · S
F: coverage-cadence baseline provenance | the guide points to a current log that lacks the named baseline records and omits the recoverable historical path and commit | operators can retrieve the exact baseline from an explicit tracked or historical location without reconstructing provenance
O: alt(historical-pointer):p103[ow@historical-pointer]; p31[xt@historical-pointer] || alt(restored-current-summary):p103[ow@restored-current-summary]; p57[cw@restored-current-summary]; p31[xt@restored-current-summary]
R: -

## 219 `219-give-repository-workflows-cwd-stable-scope-bearing.md`
T: Give repository workflows cwd-stable, scope-bearing command names
M: Repository workflow commands remain dependent on the caller's current directory · cross-cutting · medium · M
F: repository-wide and package-local dev/build workflow commands | identical script names resolve against the nearest package manifest, so command meaning changes with the caller's cwd | repository workflows have cwd-stable, scope-bearing names while conventional package-local commands retain their existing meaning
O: base:p258[mw]; p1058[xd]; p37[ow]; p958[ow]; p256[ow]; p826[xt]; p221[cw]
R: serialize>085[ambiguous]

## 220 `220-add-a-joined-explain-view-for-harness-control.md`
T: Add a joined explain view for harness control provenance
M: Harness provenance remains split across non-joinable views · harness · medium · M
F: harness control provenance for paths, package scripts, smoke tests, verify slots, hooks, and generated surfaces | validation and inventory surfaces expose isolated projections that cannot answer an end-to-end provenance query | one read-only query can deterministically explain every matched governing chain from existing authoritative harness data without a parallel join table
O: base:p1277[mw]; p226[cw]; p228[xt]; p1385[xt]; p259[xd]; p945[xt]; p218[cw]; p255[cw]
R: rebaseOn>085[ambiguous];prefersBefore>094[inferred];rebaseOn>152[explicit]

## 221 `221-rename-metamagic-slot-terminology-to-options-known.md`
T: Rename metamagic slot terminology to options-known terminology
M: Metamagic option counts are named as slots in the shared API and caller · shared · low · S
F: Metamagic options-known helper and sorcerer level-up cap | The API and caller use spell-slot terminology for a count of learned metamagic options | Public and local names identify options known while existing counts, milestones, validation, and messages remain unchanged
O: base:p864[mw]; p739[mw]; p863[mw]
R: serialize>029[explicit]

## 222 `222-make-multiclass-prerequisite-table-states-exclusive.md`
T: Make multiclass prerequisite table states exclusive and non-empty
M: Multiclass prerequisite types admit rule states the table never defines · shared · medium · S
F: Multiclass prerequisite table value representation | Optional and empty containers admit absent, mixed, and vacuous rule states that acquire unintended evaluation behavior | Every table row has exactly one non-empty `all` or `anyOf` form, with every alternative group non-empty and existing valid-row behavior preserved
O: base:p859[mw]; p858[mw]; p101[xt]
R: -

## 223 `223-remove-the-fulfilled-shared-rules-mutation-gap-from.md`
T: Remove the fulfilled shared-rules mutation gap from harness documentation
M: Harness current-gaps list still calls scheduled rules mutation missing · docs · low · S
F: AI harness Current Gaps account of weekly slow-drift mutation and timing signals | A stale gaps bullet labels already implemented scheduled capabilities as missing | Implemented mutation and timing signals are described as current capabilities, and Current Gaps lists only capabilities the workflow does not provide
O: base:p86[ow]; p26[xt]; p1405[xt] || alt(narrow-to-concrete-missing-signal):p85[ow@narrow-to-concrete-missing-signal] || alt(remove-fulfilled-portion):p85[ow@remove-fulfilled-portion]
R: rebaseOn>101[explicit]

## 224 `224-remove-lint-ratchet-compatibility-apis-used-only-by.md`
T: Remove lint-ratchet compatibility APIs used only by tests
M: Superseded lint-ratchet convenience APIs survive only in tests · harness · low · S
F: Lint-ratchet governance and registry public API shapes | Test-only compatibility wrappers and overload normalization remain after production and example consumers migrated to richer canonical APIs | Batch append, result-returning audit, and options-object validation are the sole supported shapes without observable behavior changes
O: base:p1482[dw]; p1480[mw]; p1490[dw]; p1488[mw]; p1516[dw]; p1517[dw]; p1518[mw]; p1495[mw]
R: serialize>160[ambiguous];rebaseOn>175[explicit]

## 225 `225-use-the-canonical-markdown-table-escaper-in.md`
T: Use the canonical Markdown table escaper in zero-baseline reports
M: Zero-baseline reporting bypasses canonical Markdown table escaping · harness · medium · S
F: Zero-baseline Markdown report table cells | A private partial escaper leaves Markdown, HTML, carriage-return, and backslash syntax active and diverges from the package policy | Every zero-baseline table cell uses the canonical Markdown table escaper while report structure and non-table formatting remain unchanged
O: base:p1489[mw]; p1505[xt]; p1488[mw]
R: -

## 226 `226-signal-overflow-when-token-conditions-exceed-the.md`
T: Signal overflow when token conditions exceed the visible badge limit
M: Token condition badges silently hide every condition after the fourth · client · medium · S
F: Map-token condition badge row | The renderer silently truncates after four conditions without indicating that gameplay state is hidden | The row remains capped at four positions but represents every condition through three ordered badges plus an accessible overflow count when necessary
O: base:p343[mw]; p342[mw]
R: -

## 227 `227-make-legal-license-and-srd-attribution-available.md`
T: Make legal, license, and SRD attribution available before authentication
M: The legal and license page is unavailable before authentication · client · low · S
F: Guest access to legal, license, and SRD attribution | The static legal route is auth-guarded and all existing navigation to it is hidden from unauthenticated visitors | `/legal` renders without authentication and is discoverable from login and registration while every other route keeps its current access policy
O: base:p571[mw]; p557[mw]; p559[mw]; p1533[cw]; p556[mw]; p558[mw]
R: rebaseOn>051[explicit]

## 228 `228-align-shared-export-guidance-with-adr-0005-s.md`
T: Align shared export guidance with ADR-0005's scoped-only decision
M: Resolve the promised shared aggregate-export review against ADR-0005's barrel prohibition · docs · medium · S
F: Contributor guidance for shared-package exports | The schema module guide presents aggregate barrels as deferred work while the accepted ADR and implemented export map prohibit them | Live guidance consistently treats scoped defining-file imports as policy and directs new areas toward specific subpath exports unless the ADR is formally superseded
O: base:p876[ow]; p66[xt]
R: -

## 229 `229-standardize-datetimefield-backed-mapper-result.md`
T: Standardize dateTimeField-backed mapper result boundaries
M: dateTimeField-backed mapper boundaries duplicate Date-to-ISO normalization · cross-cutting · medium · L
F: `dateTimeField`-backed server mapper result boundaries | Output-typed mappers duplicate `Date.toISOString()` normalization already owned by shared schemas, producing inconsistent pre-parse contracts and scattered serialization policy | Eligible mappers produce schema-input values with raw Dates, every public or socket path reaches the concrete schema parse, and emitted wire timestamps remain unchanged ISO strings
O: base:p806[mw]; p792[mw]; p800[mw]; p793[mw]; p642[mw]; p620[mw]; p648[mw]; p652[mw]; p649[mw]; p726[mw]; p766[mw]; p43[mw]; p831[xt]; p929[xt]; p886[xt]; p902[xt]; p887[xt]; p882[xt]; p926[xt]; p930[xt]; p927[xt]; p917[xt]; p805[xd]; p804[xd]
R: requires>014[explicit];serialize>006[explicit]

## 230 `230-single-source-edited-path-decoding-and-eslint-file.md`
T: Single-source edited-path decoding and ESLint file classification across AI hooks
M: Share edited-path extraction and ESLint file classification across AI hooks · harness · medium · S
F: AI-hook edited-path decoding and ESLint file classification | Tidy duplicates adapter payload parsing and three hooks duplicate the full extension predicate, allowing payload and file-support behavior to drift | Shared hook helpers define edited-path extraction and full ESLint eligibility for tidy, lint coverage, and Stop policy while worktree semantics and the narrower ratchet predicate remain unchanged
O: base:p969[mw]; p969[xd]; p981[mw]; p971[mw]; p977[mw]; p214[mw]; p976[xt]
R: -

## 231 `231-fail-closed-when-similarity-ts-output-no-longer.md`
T: Fail closed when similarity-ts output no longer matches the supported protocol
M: Fail closed when similarity-ts stdout no longer matches the supported protocol · harness · medium · S
F: similarity-ts near-duplicate analyzer adapter | An unpinned human-readable stdout grammar silently skips unrecognized successful output and converts protocol drift into an empty clean scan. | The adapter has a declared compatible protocol, validates that protocol, and keeps valid zero-pair output observably distinct from unsupported output.
O: base:p1068[ow]; p1174[mw]; p1175[mw] || alt(supported release exposes stable structured output):p1456[xt@supported release exposes stable structured output] || alt(supported release has no stable structured output):p1457[xt@supported release has no stable structured output]
R: -

## 232 `232-derive-drift-triage-reviewer-instructions-from-the.md`
T: Derive drift-triage reviewer instructions from the enforced verdict contract
M: Single-source drift-triage's advertised and enforced verdict contract · harness · medium · S
F: drift-triage reviewer verdict contract and packet grouping metadata | Packet instructions duplicate the collector's canonical vocabulary and fields, while an always-false oversized property is propagated through serialized contracts. | Advertised and enforced verdict metadata comes from one typed truth source, and serialized packet state contains only inhabitable fields.
O: base:p1218[mt]; p1209[mw]; p1217[mw]; p1208[mw]; p1206[mw]; p1201[mw]
R: -

## 233 `233-bulk-load-encounter-participants-before-character.md`
T: Bulk-load encounter participants before character deletion reconciliation
M: Character deletion serially reloads every active encounter's participant list · server · low · S
F: character deletion turn-index reconciliation | Serially reloading each affected encounter's participants creates a 1+N read pattern and lengthens the pre-transaction snapshot window. | All affected encounter participants are loaded in one ordered bulk read while reconciliation ordering, CAS writes, and transaction boundaries remain unchanged.
O: base:p701[mw]; p700[mw]; p62[xt]
R: serialize>007[explicit]

## 234 `234-route-manual-combat-logs-through-encounter-combat-s.md`
T: Route manual combat logs through encounter-combat's fan-out coordinator
M: Manual combat logs bypass the encounter-combat fan-out coordinator · server · low · S
F: manual encounter combat-log post-persistence fan-out | The manual-log path directly reconstructs encounter and combat-chat delivery instead of consuming the module coordinator, allowing fan-out policy and payload behavior to drift. | Every request-facing encounter-combat mutation, including manual logs, uses the shared fan-out coordinator with the established persistence and delivery semantics.
O: base:p718[mw]; p716[xd]; p715[xt]; p717[mw]
R: rebaseOn>001[explicit];serialize>001[explicit]

## 235 `235-preserve-gridtype-through-client-map-component.md`
T: Preserve GridType through client map component boundaries
M: Preserve GridType through client map component boundaries · client · low · S
F: client map grid-type component boundaries | Three adjacent props widen the shared closed grid vocabulary to string, admitting invalid values and removing exhaustiveness pressure. | GridType remains schema-derived and closed through every map component boundary, with explicit behavior for square, hex, and none.
O: base:p924[xt]; p308[mw]; p323[mw]; p321[mw]; p302[mw]
R: -

## 236 `236-use-tanstack-router-for-the-campaign-homebrew.md`
T: Use TanStack Router for the campaign homebrew empty-state link
M: Route the campaign homebrew empty-state link through TanStack Router · client · low · S
F: campaign homebrew empty-state internal navigation | A raw anchor to an application route triggers full-document navigation and bypasses router-managed state and behavior. | The located internal homebrew link navigates through TanStack Router without changing its visible contract.
O: base:p35[xd]; p301[mw]; p300[mw]
R: -

## 237 `237-associate-homebrew-validation-errors-with-their.md`
T: Associate homebrew validation errors with their controls
M: Associate homebrew validation errors with their controls · client · medium · M
F: homebrew form validation-error accessibility | Rendered error text is only visually adjacent to controls and often lacks alert semantics, invalid state, stable IDs, and control-to-message descriptions. | Every rendered homebrew validation error is announced and durably associated with the exact control or semantic group it describes, without stale or colliding references.
O: base:p381[mw]; p219[mw] || alt(standard labeled text input):p39[xd@standard labeled text input] || alt(fitting textarea):p409[xd@fitting textarea] || alt(control does not fit an existing field primitive):p1453[mw@control does not fit an existing field primitive]
R: serialize>195[ambiguous]

## 238 `238-share-class-and-subclass-caster-selection-fields.md`
T: Share class and subclass caster-selection fields
M: Share class and subclass caster-selection fields · client · low · S
F: class and subclass caster-selection form controls | Two entity forms duplicate selector presentation and empty-ability normalization despite sharing the underlying options and parsers. | Caster selector presentation and normalization have one shared implementation while entity-specific form updates and class-only controls remain local.
O: base:p406[cw]; p408[xd]; p386[mw]; p417[mw]; p407[ow]; p1455[cw]; p50[mw]
R: -

## 239 `239-extract-the-duplicated-homebrew-confirm-by-name.md`
T: Extract the duplicated homebrew confirm-by-name dialog shell
M: Extract the duplicated homebrew confirm-by-name deletion dialog · client · low · S
F: homebrew collection and entry confirm-by-name deletion dialogs | Parallel dialog stacks duplicate typed-name interaction policy, allowing matching, accessibility, pending, error, and reset behavior to drift. | Confirm-by-name interaction policy has one dedicated shared shell while mutations, cache effects, copy, and entity-specific behavior remain in thin wrappers.
O: base:p374[xd]; p405[cw]; p388[mw]; p393[mw]; p407[ow]; p55[cw]; p52[mw]
R: requires>215[explicit]

## 240 `240-classify-the-below-low-trivial-encounter-tier-as.md`
T: Classify the below-Low Trivial encounter tier as Musi policy
M: Classify the below-Low "trivial" encounter tier as Musi policy · shared · low · S
F: below-Low trivial encounter-difficulty tier provenance | An application-defined result is placed beside DMG-sourced vocabulary without qualification, and its protecting test names omit the policy source. | The below-Low trivial classification is explicitly labeled as Musi policy in the rule documentation and policy-protecting test names.
O: base:p856[ow]; p855[mw]; p101[xt]
R: -

## 241 `241-share-the-repeated-initiative-tracker-participant.md`
T: Share the repeated initiative-tracker participant test scenario
M: Initiative-tracker tests clone the same participant scenario four times · client · low · S
F: initiative-tracker Goblin participant test scenario | four suites clone the same fully populated participant scenario, allowing contract or default changes to diverge across the component family | one initiative-tracker-local typed scenario authority supplies identical defaults while every test retains explicit override precedence
O: base:p277[cw]; p281[mw]; p279[mw]; p278[mw]; p275[mw]; p583[xd]
R: -

## 242 `242-derive-slow-test-project-roots-with-fileurltopath.md`
T: Derive slow-test project roots with fileURLToPath
M: Slow-test Vitest config derives filesystem roots from a URL pathname · tests · low · S
F: slow-test Vitest repository-root derivation | using a file URL pathname as a filesystem path preserves percent encoding and produces URL-shaped Windows drive paths | the shared, server, and client slow-test roots are derived from a canonical filesystem path produced by `fileURLToPath`
O: base:p1535[mw]; p253[xd]
R: -

## 243 `243-correct-prototype-headers-that-deny-registered.md`
T: Correct prototype headers that deny registered advisory commands
M: Prototype source headers deny their now-registered advisory commands · harness · low · S
F: drift-ai prototype module ownership headers | source-local milestone comments still deny advisory commands that are registered and live | each header accurately distinguishes finding-free inventory or configuration libraries from the command modules that parse, execute, and render advisories
O: base:p1103[ow]; p1084[ow]; p1080[ow]; p1180[xt]; p1105[xt]; p1107[xt]
R: -

## 244 `244-let-multiclass-characters-choose-their-unarmored.md`
T: Let multiclass characters choose their Unarmored Defense calculation
M: Let multiclass characters choose among valid Unarmored Defense calculations · cross-cutting · medium · L
F: multiclass Unarmored Defense formula selection and authoritative character Armor Class | the first recognized class row silently selects a formula while the sheet computes independently from the persisted numeric AC used by encounters | an eligible stable formula discriminator determines a server-owned persisted AC projection, with deterministic legacy fallback and identical sheet and encounter values
O: base:p840[mw]; p839[mw]; p828[mw]; p602[mw]; p599[cw]; p609[mw]; p544[mw]; p800[xd]; p829[mw]; p101[xt]
R: -

## 245 `245-reject-empty-fixture-and-smoke-discovery-sets.md`
T: Reject empty fixture and smoke discovery sets
M: Require fixture and smoke discovery to reject empty subject sets · harness · medium · S
F: codemod fixture and smoke-test discovery populations | empty discovery registers no parameterized cases or generated smoke definitions and is therefore reported as success | each owner-local scanner fails before validation or rendering when its exact subject population is absent, with diagnostics distinguishing the ownership failure
O: base:p1043[mw]; p1038[cw]; p1395[mw]; p1394[mw]
R: serialize>167[explicit]

## 246 `246-replace-the-eslint-cache-shell-to-typescript-text.md`
T: Replace the ESLint cache shell-to-TypeScript text protocol with one typed cache plan
M: Give ESLint cache policy a typed reusable boundary · harness · low · M
F: ESLint main-cache plan shared by the shell runner and TypeScript lint agent | cache identity and arguments cross temporary files, shell globals, heredocs, generated Bash, and newline-delimited stdout without a compiler-checked result shape | one TypeScript owner returns a typed cache plan to both consumers, the shell boundary preserves complete argument boundaries, and any preparation failure still produces an uncached lint run
O: base:p1290[cw]; p1291[cw]; p1323[mw]; p1222[mw]; p1297[mw]; p1418[mw]; p1417[mw]
R: -

## 247 `247-unify-character-create-choice-chips-around.md`
T: Unify character-create choice chips around pressed-state semantics
M: Unify character-create choice chips and restore pressed-state semantics · client · medium · S
F: character-create selectable skill, language, and spell chips | three copied button implementations have diverged, leaving skill and language selection exposed only visually while spells use pressed-state semantics | one local primitive consistently exposes independent pressed and disabled state and shared interaction semantics while callers retain all domain selection rules and labels
O: base:p350[cw]; p361[mw]; p364[mw]; p360[mw]; p363[mw]
R: -

## 248 `248-preserve-registered-route-typing-through.md`
T: Preserve registered-route typing through links and page parameters
M: Preserve TanStack route types through MobileNavLink · client · low · S
F: TanStack registered-route typing at mobile-link and page-parameter boundaries | `to: string` and `strict: false` widen known destinations and required parameters, replacing compile-time guarantees with empty inputs, guards, and impossible missing-parameter branches | registered destination and required-parameter types remain exact through the wrapper and each page so invented paths or mismatched parameters fail compilation
O: base:p267[mw]; p545[mw]; p542[mw]; p553[mw]; p555[mw]; p266[mw]; p264[cw]; p576[xt]
R: -

## 249 `249-keep-charactertab-on-the-inferred-campaign-output.md`
T: Keep CharacterTab on the inferred campaign output contract
M: Keep CharacterTab on the inferred campaign output contract · client · low · S
F: CharacterTab's inferred `campaign.get` query output | a local assertion replaces the generated output type with a handwritten member projection that can drift without producing a client type error | member filtering and `{id, name}` projection compile directly against the published inferred query output with no asserted or duplicated campaign interface
O: base:p291[mw]; p290[mw]; p621[xt]
R: -

## 250 `250-correct-two-shared-assertion-boundary-categories.md`
T: Correct two shared assertion-boundary categories
M: Correct two shared type-assertion boundary categories · shared · low · S
F: type-assertion boundary metadata for shared rules runtime invariants | two local Object.entries or runtime-predicate narrowings are mislabeled as framework seams, corrupting assertion inventories and contributor examples | local built-in widening and validated narrowing assertions use the `interop` category while their reasons and runtime code remain unchanged
O: base:p859[mw]; p111[xt] || alt(250 lands before 025):p868[mw@250 lands before 025] || alt(250 co-lands atomically with 025):p1463[mw@250 co-lands atomically with 025]
R: prefersBefore>025[explicit];serialize>222[ambiguous]

## 251 `251-remove-closed-implementation-residue-from-the-active.md`
T: Remove closed implementation residue from the active lint coverage map
M: Remove completed leaf implementation instructions from the active lint coverage map · docs · medium · M
F: active lint coverage policy map | completed implementation history and obsolete follow-ups remain presented as live future work | hand-maintained prose reflects only current policy, ownership, and repository-supported unfinished actions
O: base:p93[mw]; p148[xt]; p1475[xt]; p200[xt]; p258[xt]
R: prefersBefore>111[explicit]

## 252 `252-expose-persisted-notification-history-beyond-the.md`
T: Expose persisted notification history beyond the newest twenty rows
M: The notification UI ignores the server cursor, making every persisted notification after the newest 20 inaccessible · client · medium · M
F: persisted notification history in the client UI | the client ignores the server cursor and stores only one flat twenty-row page | all cursor pages remain reachable and notification cache operations behave consistently across every loaded page
O: base:p517[mw]; p421[mw]; p222[mw]; p928[xt]; p651[xt]
R: requires>064[explicit];serialize>196[inferred]

## 253 `253-converge-typecheck-fan-out-on-the-shared-parallel.md`
T: Converge typecheck fan-out on the shared parallel runner
M: Converge typecheck fan-out on the shared live-output parallel runner · harness · medium · M
F: typecheck shell fan-out lifecycle | typecheck duplicates the shared runner's FIFO, signal, cleanup, wait, and exit-aggregation machinery | one shared runner owns process lifecycle and optional logging while typecheck owns only TypeScript-specific diagnostics
O: base:p1304[md]; p1438[mw]; p1423[mw]; p1431[mw]
R: serialize>204[ambiguous]

## 254 `254-route-current-inventory-through-the-canonical-ignore.md`
T: Route current inventory through the canonical ignore predicate
M: Route current inventory through the canonical ignore predicate · harness · medium · S
F: drift-ai current-inventory path filtering | current inventory duplicates the canonical ignore predicate and can silently diverge as ignore policy changes | every selected current-inventory path is evaluated by the canonical predicate after the correct root-relative path is chosen
O: base:p1109[mw]; p1108[mw]; p1100[xt]
R: -

## 255 `255-make-exact-token-collection-explicit-at-the.md`
T: Make exact-token collection explicit at the near-duplicate runner boundary
M: Make near-duplicate exact-token collection an explicit runner choice · harness · low · S
F: near-duplicate exact-token collection policy | the runner and extractor assign opposite meanings to an omitted `includeExactTokens` option | every runner caller declares exact-token collection explicitly while the extractor's standalone omission behavior remains documented and tested
O: base:p1174[mw]; p1083[mw]; p1173[ow]; p1172[xt]; p1175[mw]
R: serialize>173[inferred]

## 256 `256-constrain-class-caster-metadata-with-database-enums.md`
T: Constrain class caster metadata with database enums
M: Class caster metadata bypasses existing database enum boundaries · server · medium · M
F: class and subclass caster metadata persistence | unconstrained string columns admit caster types and spellcasting abilities outside the closed rules vocabulary | the database rejects out-of-domain caster metadata and trusted Prisma boundaries expose enum-constrained types
O: base:p601[mw]; p598[cw]; p938[xt]; p217[mw] || alt(Prisma-enum-typed callers only):p790[mw@Prisma-enum-typed callers only] || alt(wide structural callers remain; retain validation):p790[xd@wide structural callers remain; retain validation]
R: -

## 257 `257-delete-the-no-op-auth-configuration-validator.md`
T: Delete the no-op auth configuration validator
M: Delete the no-op validateAuthConfig startup boundary · server · low · S
F: server authentication configuration validation boundary | a named startup validator merely rereads an already parsed secret and falsely appears to own validation policy | eager environment-schema parsing is the sole validation boundary and `getJwtSecret` remains only a validated-value access boundary
O: base:p613[mw]; p612[mw]; p614[xt]; p690[xd]; p45[mw]
R: -

## 258 `258-share-inventory-draft-fields-between-add-and-edit.md`
T: Share inventory draft fields between add and edit dialogs
M: Share the inventory-item draft fields between add and edit dialogs · client · low · S
F: add and edit inventory-item draft fields | two dialogs duplicate controls, constraints, options, accessibility wiring, and numeric normalization | shared field presentation and conversion rules have one owner while dialog-specific orchestration remains separate
O: base:p56[cd]; p424[mw]; p432[mw]; p216[mw]
R: -

## 259 `259-extract-a-one-pass-homebrew-item-read-adapter.md`
T: Extract a one-pass homebrew-item read adapter
M: Give homebrew-item conversion a pure adapter boundary · client · low · S
F: homebrew-item persisted-data read conversion | React rendering code owns compatibility parsing and conversion and parses each entry separately for display and creation | one pure adapter derives display and inventory input from the same single normalized parse
O: base:p51[cd]; p435[mw]; p220[cw]; p434[mw]
R: -

## 260 `260-rename-create-only-map-layer-actions-as-enable.md`
T: Rename create-only map-layer actions as enable operations
M: Rename create-only map-layer actions as enable operations · client · low · S
F: fog and drawing map-layer action contracts | toggle-named callbacks advertise reversible behavior although they only create absent layers | callback names consistently describe enable-only semantics and remain distinct from reset, clear, or delete actions
O: base:p317[mw]; p511[mw]; p320[mw]; p319[mw]; p323[mw]; p318[mw]; p239[mw]
R: serialize>215[explicit]

## 261 `261-debounce-notes-search-before-changing-its.md`
T: Debounce notes search before changing its infinite-query key
M: Debounce notes search before changing the infinite-query key · client · medium · S
F: notes infinite-query search parameters and active-filter state | each controlled-input keystroke immediately changes the parameterized query key, issuing requests and creating cache entries for intermediate prefixes | only the trimmed, settled search value changes the query and search-active state; empty or whitespace-only input settles to the unfiltered key
O: base:p328[mw]; p327[mw]; p510[xd]
R: -

## 262 `262-single-source-the-class-and-subclass-feature-codec.md`
T: Single-source the class and subclass feature codec
M: Single-source the class-feature persistence codec · client · low · S
F: class and subclass nested feature persistence codec | independent copies of identical parsing, field mapping, and positive-integer level coercion can drift when the feature record changes | class and subclass features use one class-owned codec while whole-entity form ownership and entity-specific fields remain separate
O: base:p385[mw]; p416[mw]; p384[mw]; p415[mw]; p383[ow]; p414[ow]
R: -

## 263 `263-preserve-closed-vocabularies-through-homebrew-form.md`
T: Preserve closed vocabularies through homebrew form state
M: Preserve shared literal vocabularies in homebrew select form state · client · medium · M
F: closed magic-item and spell vocabularies in homebrew form state | schema-derived literal unions are widened to string, allowing unknown persisted or callback values into form state and hiding vocabulary drift until submission validation | closed-vocabulary form fields remain schema-narrowed from parsing and select callbacks through payload construction, with invalid inputs mapped to established defaults or sentinels
O: base:p401[mw]; p403[mw]; p400[mw]; p402[mw]; p412[mw]; p410[mw]; p411[mw]; p413[mw]; p921[xt]; p936[xt]; p938[xt]
R: requires>189[explicit]

## 264 `264-make-ad-hoc-fastify-test-teardown-failure-safe.md`
T: Make ad-hoc Fastify test teardown failure-safe
M: Ad-hoc Fastify tests skip teardown when assertions fail · tests · medium · S
F: ad-hoc Fastify instances in server application tests | tail-position close calls are bypassed when readiness, injection, plugin registration, or assertions fail, retaining application and Socket.IO resources | every custom test instance has cleanup registered before its first fallible operation and closes on both successful and failed test paths
O: base:p611[mw]; p612[xt]; p775[xt]
R: -

## 265 `265-document-cross-worktree-status-modes.md`
T: Document cross-worktree status modes
M: Document the two cross-worktree status modes in the per-worktree guide · docs · low · S
F: cross-worktree status command documentation | the per-worktree guide exposes only current-worktree status, leaving all-worktree provisioning and parallel-lane Git overview modes undiscoverable | contributors can select and correctly invoke current, all-worktree, or lane status modes from the guide without duplicating the script's implementation details
O: base:p112[ow]; p1447[xt]
R: serialize>096[inferred]

## 266 `266-add-a-human-focused-contribution-entrypoint.md`
T: Add a human-focused contribution entrypoint
M: Add a human-focused contribution entrypoint · docs · low · S
F: human contributor onboarding and repository workflow | the public README delegates contribution standards to the agent-only control file, so branch, commit, verification, push, and pull-request expectations lack a human entrypoint | human contributors can discover the normal enforced workflow through README.md and CONTRIBUTING.md, with hooks, guides, and manifests remaining authoritative
O: base:p38[cw]; p41[ow]; p37[xt]; p95[xt]; p258[xt]; p27[xt]; p24[xt]; p64[xt]
R: serialize>103[inferred];rebaseOn>085[inferred]

## 267 `267-require-current-rationale-on-durable-harness.md`
T: Require current rationale on durable harness exceptions
M: Require rationale metadata on ghost-file allow-pair exceptions · harness · low · S
F: durable ghost-file and max-lines harness exceptions | ghost-file pairs lack justification metadata while existing max-lines reasons accumulate historical ticket and line-delta prose, obscuring current validity and retirement conditions | each durable exception carries a current structural rationale and removal condition, metadata survives configuration processing, and rationale never affects matching behavior
O: base:p119[mw]; p1103[mw]; p1102[mw]; p1133[mw]; p1097[mw]; p1101[mw]; p1099[mw]; p1135[mw]; p1068[ow]; p156[mw]; p155[xt]; p1375[xd]
R: -

## 268 `268-type-dice-roll-metadata-as-rollresult-at-the.md`
T: Type dice.roll metadata as RollResult at the response boundary
M: Give dice.roll a RollResult-typed response instead of four client metadata assertions · cross-cutting · medium · S
F: dice.roll response metadata contract | a generic unknown-metadata response erases the RollResult type constructed by the producer, forcing unchecked client assertions and hiding producer/schema drift | dice.roll metadata is validated and exposed as RollResult at one response boundary, with no repeated unchecked narrowing in client consumers
O: base:p887[xt] || alt(dedicated-response-schema):p894[mw@dedicated-response-schema]; p893[mw@dedicated-response-schema]; p630[mw@dedicated-response-schema]; p509[mw@dedicated-response-schema]; p521[mw@dedicated-response-schema] || alt(generic-envelope-adapter):p894[xt@generic-envelope-adapter]; p49[cw@generic-envelope-adapter]; p509[mw@generic-envelope-adapter]; p521[mw@generic-envelope-adapter]
R: requires>001[explicit]

## 269 `269-reset-createmapdialog-state-on-every-close-path.md`
T: Reset CreateMapDialog state on every close path
M: CreateMapDialog retains its submitted draft because successful close bypasses reset · client · medium · S
F: CreateMapDialog draft and selected-file preview lifecycle | parent-driven successful close bypasses the local reset handler while the state owner remains mounted, retaining submitted values and an object URL | every transition to closed destroys the draft/preview owner so reopening starts from defaults and any selected-file object URL is revoked
O: base:p305[mw]; p304[mw]; p324[xd]; p325[xt]
R: -

## 270 `270-add-a-local-orientation-contract-for-the-client.md`
T: Add a local orientation contract for the client character-creation wizard
M: Give the client character-creation wizard a local orientation contract · client · medium · S
F: client character-creation wizard orientation contract | the multi-file wizard lacks a local module map, forcing contributors to reconstruct non-obvious state, navigation, validation, reset, and submission ownership from imports | the feature directory has an indexed local contract that identifies authoritative owners and cross-step invariants without duplicating rules or changing behavior
O: base:p351[cw]; p40[ow]; p115[xt]; p97[xt]; p366[xt]; p368[xt]; p370[xt]; p354[xt]; p543[xt]
R: rebaseOn>042[explicit];rebaseOn>045[explicit]
