# Author packet — R5C-006 → `152-path-policy-query-core-closed-over-musis.md`

- Ruling: **augment-existing-leaf** (edit `152-path-policy-query-core-closed-over-musis.md` in place)
- Leaf number: **152**
- Title (settled): Single-source smoke-subject parsing and matching
- Theme: Smoke-subject parsing and matching still have two correctness owners
- Area: harness · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make parsing and matching shared vocabulary while keeping generation, fixture diagnostics, and query orchestration separate consumers.

## Augment instruction (what to add or change in the target leaf)

Add one neutral smoke-subject line parser and one normalized exact-or-trailing-slash-directory matcher, consumed by header generation, fixture validation, and the extracted query engine; retain the generator's stronger diagnostics and update leaf 152's fixture-work exclusion narrowly.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `152-path-policy-query-core-closed-over-musis.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

The generated registry and fixture-copy precondition interpret the same # smoke-subjects syntax through separate parsers, while changed-mode selection and fixture coverage separately encode the same exact-or-directory match. A syntax or normalization change can make the validator approve metadata that selects differently, undermining the check meant to prove copied inputs select their smoke.

## Verified evidence (existence-checked at step 1b)

- `scripts/path-policy/smoke-subject-headers.ts:33-49` — The generator owns subject tokenization plus repo-relative POSIX validation.
- `scripts/path-policy/fixture-shell-dependencies.ts:40-42` — The fixture precondition declares its own smoke-subject header regex rather than consuming the generator parser.
- `scripts/path-policy/fixture-shell-dependencies.ts:106-111` — The fixture precondition independently tokenizes smoke subjects with weaker validation.
- `scripts/path-policy/path-policy-query-core.ts:125-129` — Changed-mode selection defines exact-path and trailing-slash-prefix matching privately.
- `scripts/path-policy/fixture-shell-dependencies.ts:151-161` — Fixture coverage hand-copies the selector semantics and claims exact parity in a comment without sharing implementation.

## Author constraints (all must be honored)

- Land after H15 and within leaf 152's engine extraction so the matcher is not moved twice.
- Preserve query results, ordering, path normalization, generated-surface registration, and the distinct smoke-test filename semantics landed by CQ25-119.
- Do not absorb leaf 151's broader fixture copy-model rewrite.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-119` — H14 centralized smoke-file predicates and path normalization, not smoke-subject header parsing or subject matching.

## Provenance (origins — carry into the leaf Source line context)

- R5C · lane-09-lint-machinery · `wave-1/lane-09.json::coverage.cut[10]` — original title: "smoke-subject syntax and matching have multiple owners — concrete but lower-ranked than the duplicate copy models"; rejected because: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Opened `151-fixture-copies-parsed-grouped-two.md:109-133` and `152-path-policy-query-core-closed-over-musis.md:65-132`; 151 is distinct, while 152 moves the current matcher. CQ25-119's narrower landed scope is at `../code-quality-2026-07-25/HARNESS-CLUSTER-PLAN.md:133` and `31-harness-shared-helpers.md:205-222`.
- promoter hint (non-authoritative): looks close to leaf 151 — hint only
- promoter hint (non-authoritative): looks close to leaf 152 — hint only

