# Author packet — R5B-L05-001 → `057-character-sheettesttsx-no-longer-tests.md`

- Ruling: **augment-existing-leaf** (edit `057-character-sheettesttsx-no-longer-tests.md` in place)
- Leaf number: **057**
- Title (settled): Delete the orphaned legacy DeathSaves component and stale coverage
- Theme: Remove the legacy DeathSaves production orphan and its stale test
- Area: client · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Remove the unimported legacy component and its obsolete tests while preserving the interactive death-save implementation and live primitive coverage.

## Augment instruction (what to add or change in the target leaf)

Revise the evidence, direction, and caveat that preserve the legacy `DeathSaves` describe: delete `death-saves.tsx`, remove its import/describe from the aggregate test before renaming the remainder, and retain coverage only for `DeathSavesInteractive` and any live shared dot primitive.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `057-character-sheettesttsx-no-longer-tests.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

A superseded production component survives only through a stale test, duplicating presentation and making coverage imply that production exercises behavior it never renders.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/sheet/death-saves.tsx:37-60` — The legacy read-only DeathSaves component remains exported.
- `packages/client/src/components/sheet/desktop-sheet-layout.tsx:81-89` — The live desktop sheet renders DeathSavesInteractive.
- `packages/client/src/components/sheet/mobile-sheet-tabs.tsx:87-95` — The live mobile sheet also renders DeathSavesInteractive.
- `packages/client/src` — measurement: 0 production imports (`rg -l 'death-saves\.js' packages/client/src --glob '!**/*.test.*' --glob '!**/*.spec.*' | wc -l`) — The legacy module has no production consumer; its external consumer is a test.

## Author constraints (all must be honored)

- Do not delete or weaken `DeathSavesInteractive` coverage.
- Update leaf 057's six-component counts and every statement that currently says the legacy describe must survive.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-05-client · `wave-1/lane-05.json::coverage.cut[4]` — original title: "Legacy DeathSaves is unreferenced."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `061-rollmodetoggle-complete-production-orphan.md:43-69` and `057-character-sheettesttsx-no-longer-tests.md:45-51,63-103`; 061 is distinct, while 057 directly preserves the stale test this candidate proves obsolete.
- promoter hint (non-authoritative): looks close to leaf 061 — hint only

