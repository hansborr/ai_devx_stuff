# Author packet — R5B-L02-002 → `267-require-current-rationale-on-durable-harness.md`

- Ruling: **new-leaf**
- Leaf number: **267**
- Title (settled): Require current rationale on durable harness exceptions
- Theme: Require rationale metadata on ghost-file allow-pair exceptions
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Represent each ghost-file allowed pair as files plus a required non-empty, present-tense rationale and preserve it in effective-config inspection. Also absorb R5C-005's rule that durable reasons state the current structural justification rather than change history.

## Problem statement from the promotion candidate

Durable suppressions cannot record why they are legitimate, so reviewers cannot distinguish deliberate module pairs from temporary or stale permissions.

## Verified evidence (existence-checked at step 1b)

- `drift-ai.config.json:15-29` — Eight durable currentAllowedPairs entries contain paths only.
- `scripts/drift-ai/config.ts:112-114` — GhostFileAllowedPair has only a files tuple.
- `scripts/drift-ai/config-readers.ts:149-167` — The parser requires a positional two-path array and cannot retain rationale.
- `scripts/drift-ai/README.md:344-350` — The guide describes these as explicit exceptions for residual heuristic cases.

## Author constraints (all must be honored)

- Do not broaden into stale-entry ratcheting or ghost-file matching changes owned or excluded by leaves 169 and 207.
- Do not reopen CQ25-10's analyzer-contract/Zod work; migrate only the allowed-pair boundary and the merged rationale examples.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-10` — The live slices do not add rationale metadata to `currentAllowedPairs`.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[14]` — original title: "Ghost-file allowlist pairs carry no rationale metadata"; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `169-suppression-allowlists-cannot-ratchet.md:40-65` and `207-encode-ghost-files-mode-dependencies-as.md:44-83`; neither reaches allowed-pair rationale. CQ25-10's remaining slices are limited at `../code-quality-2026-07-25/34-PLAN.md:148-155`.
- promoter hint (non-authoritative): looks close to leaf 169 — hint only
- promoter hint (non-authoritative): looks close to leaf 207 — hint only

## Folded-in promotions (merge-with-promotion rulings)

### R5C-005 — Keep durable exception reasons as current rationale, not change history

Contribution: State that rationale fields describe the current structural exception and retirement condition, rewrite the two max-lines reasons accordingly, and leave change history to Git.

The reason field is review metadata explaining why an oversized file remains exceptional, but two entries mix that current justification with historical line deltas and ticket-specific implementation prose. Later reviewers inherit archaeology instead of a concise statement of the live boundary and split condition.

- `eslint-config/max-lines-exceptions.baseline.json:147-159` — Two live reasons append implementation deltas (+1/+4) and a named ux-audit change to the durable exception justification.
- `eslint-config/max-lines-exceptions-codec.js:101-116` — The shared parser checks only that reason is nonblank, so changelog-shaped prose is retained like a durable rationale.
- `scripts/max-lines-exceptions-core.ts:73-81` — Baseline updates copy each reason through unchanged, allowing per-change clauses to accumulate across cap updates.

