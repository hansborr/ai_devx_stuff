# Author packet — R5B-L05-021 → `268-type-dice-roll-metadata-as-rollresult-at-the.md`

- Ruling: **new-leaf**
- Leaf number: **268**
- Title (settled): Type dice.roll metadata as RollResult at the response boundary
- Theme: Give dice.roll a RollResult-typed response instead of four client metadata assertions
- Area: cross-cutting · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Give `dice.roll` a response contract whose metadata is validated by the existing `rollResultSchema`, then remove the four client assertions. If a generic envelope must remain, centralize `safeParse` with explicit invalid-data handling.

## Problem statement from the promotion candidate

The procedure always returns roll metadata but exposes it as unknown, forcing four unchecked assertions. Producer/schema drift remains invisible to TypeScript, and malformed metadata can crash toast formatting or the weapon crit chain.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/routers/dice.ts:29-61` — dice.roll validates generic chat output while always producing metadata from a concrete RollResult.
- `packages/shared/src/schemas/chat-inputs.ts:52-65` — The generic chat schema leaves metadata unknown.
- `packages/shared/src/schemas/dice-inputs.ts:26-40` — A runtime rollResultSchema already exists.
- `packages/client/src/hooks/use-ability-roll.ts:23-27` — The ability hook asserts metadata and falsely describes it as guaranteed by the mutation contract.
- `packages/client/src/hooks/use-weapon-roll.ts:39-80` — Three more assertions feed toast rendering and critical-damage control flow.

## Author constraints (all must be honored)

- Preserve the persisted `RollResult`/`DiceGroupResult` shapes, including flat rolls and optional notation, per CQ25-179.
- Sequence after or with leaf 001's `dice.roll` migration and keep metadata construction caller-owned.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-179` — The refusal preserves persisted roll-result shapes; it does not cover typing this procedure's response metadata.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-05-client · `wave-1/lane-05.json::coverage.cut[21]` — original title: "Dice roll hooks repeat the same transport adapter and JSON narrowing."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `001-chat-persistence-delivery-policy.md:98-105,189-193`, which excludes metadata shapes. CQ25-179's model-shape refusal is at `../code-quality-2026-07-25/SHARED-CLUSTER-PLAN.md:130-138,718` and `25-dice-model.md:119-139`.

