# Author packet — R5B-L03-001 → `203-add-previewapply-safety-boundary-srd.md`

- Ruling: **augment-existing-leaf** (edit `203-add-previewapply-safety-boundary-srd.md` in place)
- Leaf number: **203**
- Title (settled): Remove tautological scope guards from SRD backfills
- Theme: SRD backfills advertise scope guards built from the rows they guard
- Area: server · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Delete the self-derived membership checks rather than replacing them with a second parallel allowlist. Preserve the independent monster `sourceType` restriction and leaf 203's preview/apply safety rail.

## Augment instruction (what to add or change in the target leaf)

Add a scoped cleanup for both callbacks: delete membership sets and predicates derived from the same supplied rows, preserve the monster `sourceType` constraint, and describe the supplied validated corpus plus preview/apply gate as the real boundary.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `203-add-previewapply-safety-boundary-srd.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Checks that read as allowlist enforcement cannot filter any supplied row, creating false assurance about database-write scope and dead branches reviewers must reason through.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/seed/backfill-srd-spell-combat.ts:31-44` — The allow-looking set and query predicate are derived from the same supplied rows.
- `packages/server/src/seed/backfill-srd-monster-actions.ts:42-65` — The monster backfill repeats the self-derived membership check; sourceType is the separate real constraint.

## Author constraints (all must be honored)

- If leaf 097 takes the removal branch, mark this residual moot; otherwise land it with or after leaf 203.
- Do not invent an independently maintained SRD-ID list merely to preserve the appearance of a guard.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-03-server · `wave-1/lane-03.json::coverage.cut[20]` — original title: "Both SRD backfills contain tautological allowlist-looking guards."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `203-add-previewapply-safety-boundary-srd.md:41-79` and `097-two-database-writing-srd-backfill-commands.md:52-91`; 203 omits the tautologies and 097 either keeps/document them or removes them.
- promoter hint (non-authoritative): looks close to leaf 203 — hint only
- promoter hint (non-authoritative): looks close to leaf 097 — hint only

