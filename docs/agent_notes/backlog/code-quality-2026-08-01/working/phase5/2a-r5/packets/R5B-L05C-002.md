# Author packet — R5B-L05C-002 → `269-reset-createmapdialog-state-on-every-close-path.md`

- Ruling: **new-leaf**
- Leaf number: **269**
- Title (settled): Reset CreateMapDialog state on every close path
- Theme: CreateMapDialog retains its submitted draft because successful close bypasses reset
- Area: client · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Put the create-map draft and preview lifecycle behind keyed dialog content or a single close/reset boundary used by cancel and success. Add a create-success-close-reopen regression and ensure object URLs are revoked.

## Problem statement from the promotion candidate

Successful creation bypasses the dialog's manual reset, so reopening can expose the submitted name, dimensions, image mode/file, and preview. The selected-file object URL also remains live while the always-mounted owner survives.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/campaign/maps/create-map-dialog.tsx:203-216` — The persistently mounted component owns form, image-mode, upload, file, and preview state.
- `packages/client/src/components/campaign/maps/create-map-dialog.tsx:239-253` — State resets only through handleClose.
- `packages/client/src/components/campaign/maps/maps-panel.tsx:157-168` — Success closes from the parent with setCreateOpen(false), bypassing handleClose.
- `packages/client/src/components/campaign/maps/use-map-image-preview.ts:48-53` — Object URLs are revoked on hook-owner unmount, which a controlled close does not cause.

## Author constraints (all must be honored)

- Do not broaden into a general dialog framework or reopen sheet-owner capability work.
- Preserve successful map creation and image-upload behavior while unifying all close paths.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-216` — The declined remount/reset work was bounded to the sheet-owner capability branch; CreateMapDialog was not inspected.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-05-client · `wave-1/lane-05.json::coverage.cut[28]` — original title: "CreateMapDialog manually reconstructs keyed-remount lifecycle."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `055-token-placement-mirrors-store-state-dialog.md:8-24,38-70`; it has no file or lifecycle overlap. CQ25-216's bounded refusal is at `../code-quality-2026-07-25/66-sheet-owner-capability-gate.md:3-17,110-137` and `72-sheet-capability-callback-source.md:61-89`.
- promoter hint (non-authoritative): looks close to leaf 055 — hint only

