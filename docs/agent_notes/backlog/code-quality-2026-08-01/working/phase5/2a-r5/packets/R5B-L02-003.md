# Author packet — R5B-L02-003 → `232-derive-drift-triage-reviewer-instructions-from-the.md`

- Ruling: **augment-existing-leaf** (edit `232-derive-drift-triage-reviewer-instructions-from-the.md` in place)
- Leaf number: **232**
- Title (settled): Remove the uninhabitable oversized state from drift-triage packets
- Theme: Remove the always-false oversized field from triage packet serialization
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Delete `oversized` throughout the drift-triage packet pipeline because grouping bounds every emitted packet. Keep `splitPathComponent` as the truthful exceptional-condition disclosure.

## Augment instruction (what to add or change in the target leaf)

Broaden the leaf from verdict single-sourcing to truthful packet contracts: remove `oversized` from grouping results, packet/manifest types, serialization, and fixtures, retain `splitPathComponent`, and explicitly amend the no-contract-evolution caveat for this sole field removal.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `232-derive-drift-triage-reviewer-instructions-from-the.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

The packet protocols advertise an oversized state that grouping structurally prevents, inviting dead consumer handling and synchronized maintenance across types and serializers.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-triage/triage-packet-group.ts:74-83` — Ordinary groups flush before exceeding packetSize.
- `scripts/drift-triage/triage-packet-group.ts:88-117` — Large components are split into bounded chunks before oversized is calculated.
- `scripts/drift-triage/triage-packet-types.ts:30-37` — The unreachable state is exposed in every packet.
- `scripts/drift-triage/triage-packets.ts:62-95` — The value is copied into manifest and packet serialization.

## Author constraints (all must be honored)

- Do not revive CQ25-10's refused Zod port or alter verdict parsing.
- Decide and document whether the internal packet schema version requires a bump; do not silently contradict leaf 232's current preservation caveat.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-10` — The verdict-family exclusion does not cover deleting an uninhabitable serialized field.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[17]` — original title: "Packet oversized is an always-false serialized field"; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `232-derive-drift-triage-reviewer-instructions-from-the.md:20-70`; it does not cover `oversized` but edits the same contract surfaces. Prior exclusion is at `../code-quality-2026-07-25/34-PLAN.md:77-82,138-146,224`.
- promoter hint (non-authoritative): looks close to leaf 232 — hint only

