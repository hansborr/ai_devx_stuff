# Author packet — R5B-L01-001 → `163-ratchet-driven-file-boundaries-strand.md`

- Ruling: **augment-existing-leaf** (edit `163-ratchet-driven-file-boundaries-strand.md` in place)
- Leaf number: **163**
- Title (settled): Make reusable drift-ai subprocess diagnostics caller-neutral
- Theme: Make Knip runner diagnostics caller-neutral
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make Knip heartbeat diagnostics caller-neutral at the runner boundary and let each production caller supply its command identity. Preserve the existing run/cache behavior and warning cadence.

## Augment instruction (what to add or change in the target leaf)

Broaden the problem and direction to include `knip-runner.ts`: replace its hard-coded `drift:ai` heartbeat identity with caller-supplied context, remove the standalone sensor's anchored text rewrite, refresh the stale runner header, and add focused caller-label coverage.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `163-ratchet-driven-file-boundaries-strand.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

A reusable subprocess boundary owns one caller's diagnostic identity, forcing another production caller to parse and rewrite human-facing text. Message changes can leak the wrong command identity or silently break the replacement.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/knip-runner.ts:176-203` — The exported runner accepts a generic warning callback but hard-codes drift:ai into both heartbeat messages.
- `scripts/sensor-knip-unused-exports-core.ts:234-268` — The standalone sensor wraps the callback with an anchored replacement from drift:ai to sensor:knip-unused-exports.
- `scripts/drift-ai/knip-runner.ts:1-3` — The header still describes the runner as owned solely by the orphan-files adapter.

## Author constraints (all must be honored)

- Keep `scripts/drift-ai/` flat under CQ25-146; do not turn this into directory restructuring.
- Coordinate with CQ25-41's sensor-family move and leaf 137's edits to `knip-runner.ts`; retain the public sensor facade.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-41` — The sensor-family move leaves the imported runner's hard-coded label and rewrite untouched.
- `CQ25-146` — Caller-neutral formatting survives without reopening the refused drift-ai directory restructure.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-01-harness-core · `wave-1/lane-01.json::coverage.cut[17]` — original title: "Reusable sensor engines live beneath drift-ai and rewrite its diagnostic identity."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `163-ratchet-driven-file-boundaries-strand.md:10-21,33-51,58-76`; it establishes the same wrong-caller diagnostic pattern but scopes only the ignore adapter. CQ25-41/CQ25-146 boundaries are at `../code-quality-2026-07-25/28-PLAN.md:118-148,211`.
- promoter hint (non-authoritative): looks close to leaf 163 — hint only

