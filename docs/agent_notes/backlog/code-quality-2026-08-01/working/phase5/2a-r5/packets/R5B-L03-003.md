# Author packet — R5B-L03-003 → `009-all-four-seed-generators-can-overwrite.md`

- Ruling: **augment-existing-leaf** (edit `009-all-four-seed-generators-can-overwrite.md` in place)
- Leaf number: **009**
- Title (settled): Make database seed entrypoint shutdown reliable on failure
- Theme: Database seed entrypoints duplicate a fragile forced-exit lifecycle
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Let both seed commands report failure through `process.exitCode` and complete Prisma cleanup. Keep their command-specific seeding callbacks explicit; extraction is optional, reliable cleanup is not.

## Augment instruction (what to add or change in the target leaf)

Add a separate Prisma-seed lifecycle sub-scope: replace `process.exit(1)` in both entrypoints with nonzero `process.exitCode` semantics so async `finally` disconnects, and share a runner only if it reduces duplication without obscuring command-specific work.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `009-all-four-seed-generators-can-overwrite.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Two entrypoints maintain the same database bootstrap, failure, and shutdown protocol by hand, and both force process exit before their chained finally purports to disconnect.

## Verified evidence (existence-checked at step 1b)

- `packages/server/prisma/seed.ts:8-32` — The ordinary seed configures logging and Prisma, then exits in catch before chained cleanup.
- `packages/server/prisma/seed-template.ts:10-33` — The template seed duplicates the same configuration and lifecycle.

## Author constraints (all must be honored)

- Do not conflate the Prisma seed entrypoints with leaf 009's four artifact generators or alter their data behavior.
- Coordinate the `src/seed` documentation separately with leaf 087; this augmentation does not move files.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-03-server · `wave-1/lane-03.json::coverage.cut[25]` — original title: "Seed command configuration and entrypoint lifecycle are duplicated."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `009-all-four-seed-generators-can-overwrite.md:8-17,63-102` and `087-server-contributor-maps-contradict-omit.md:105-140`; neither covers `packages/server/prisma/seed*.ts`, whose forced-exit/finally duplication is at `seed.ts:26-32` and `seed-template.ts:27-33`.
- promoter hint (non-authoritative): looks close to leaf 087 — hint only
- promoter hint (non-authoritative): looks close to leaf 009 — hint only

