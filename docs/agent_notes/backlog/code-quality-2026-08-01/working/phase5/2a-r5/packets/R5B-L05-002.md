# Author packet — R5B-L05-002 → `047-toolhandler-erases-coordinate-domains.md`

- Ruling: **augment-existing-leaf** (edit `047-toolhandler-erases-coordinate-domains.md` in place)
- Leaf number: **047**
- Title (settled): Stop non-freehand drawing gestures from accumulating freehand history
- Theme: Non-freehand drawing drags copy an unused growing point history
- Area: client · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Make point-history accumulation tool-specific: freehand keeps its history, while geometric tools retain only their start and current points.

## Augment instruction (what to add or change in the target leaf)

Add a bounded capability distinction so only `draw-freehand` appends `freehandPoints`; line, rectangle, and circle moves update only `currentPoint`. Amend the store-action exclusion only as needed for this behavior-preserving split and update the no-other-leaf claim.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `047-toolhandler-erases-coordinate-domains.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Line, rectangle, and circle drags grow and recopy a coordinate array their builders never read, creating quadratic cumulative allocation on a pointer-frequency path.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/hooks/canvas-input/tool-handlers.ts:207-225` — One handler sends every drawing tool's pointer moves through updateDrawing.
- `packages/client/src/stores/map-canvas-store.ts:443-452` — Every update copies and appends to freehandPoints.
- `packages/client/src/hooks/canvas-input/tool-handlers.ts:50-87` — Only the freehand builder consumes the history.
- `packages/client/src/components/campaign/maps/use-map-canvas-handlers.ts:60-72` — MapCanvas subscribes to the complete drawing object, including each replaced history array.

## Author constraints (all must be honored)

- Land with or after leaf 047, not concurrently in `tool-handlers.ts`.
- Preserve leaf 047's coordinate-domain typing and dispatch/finalize/cancel ordering; do not reopen CQ25-188's store split or `fireAfterCommit` reshape.

## Prior-pack residual (what is left uncovered by the named record)

- `CQ25-188` — The refusal covers store decomposition and `fireAfterCommit`, not per-tool point accumulation.

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-05-client · `wave-1/lane-05.json::coverage.cut[6]` — original title: "Non-freehand drawing gestures accumulate unused freehand history."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `047-toolhandler-erases-coordinate-domains.md:70-143`; it edits the same handler but excludes store actions. CQ25-188's narrow refusal is at `../code-quality-2026-07-25/CLIENT-CLUSTER-PLAN.md:121-139,230-236`.
- promoter hint (non-authoritative): looks close to leaf 047 — hint only

