# Author packet — R5B-L05D-001 → `248-preserve-registered-route-typing-through.md`

- Ruling: **augment-existing-leaf** (edit `248-preserve-registered-route-typing-through.md` in place)
- Leaf number: **248**
- Title (settled): Preserve registered-route typing through links and page parameters
- Theme: Preserve required route-parameter typing in parameterized pages
- Area: client · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Retain TanStack Router's registered types at both outbound wrapper and inbound page boundaries. Use exact route-path bindings for required params and delete fallback states the router cannot produce.

## Augment instruction (what to add or change in the target leaf)

Broaden the title/problem/direction beyond `MobileNavLink`: bind the four page `useParams` calls to their exact registered paths, remove empty-ID fallbacks and unreachable missing-param branches, and add compile-focused coverage where useful.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `248-preserve-registered-route-typing-through.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Four pages discard the router's compile-time guarantee that required parameters exist, spreading empty-ID fallbacks and unreachable states while allowing route/page mismatches to compile.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/routes/character-sheet-route.ts:18-21` — The registered route requires characterId.
- `packages/client/src/pages/character-sheet-page.tsx:23-30` — strict:false widens characterId, forcing an empty query input and missing-ID branch.
- `packages/client/src/pages/campaign-detail-page.tsx:231-251` — campaignId is read loosely while adjacent search/navigation calls retain exact route identity.
- `packages/client/src/pages/collection-detail-page.tsx:149-164` — Required collectionId is widened and normalized to an empty string.
- `packages/client/src/pages/join-page.tsx:52-70` — Required invite code becomes optional, producing fallback and enabled guards.

## Author constraints (all must be honored)

- Do not import route objects from lazily imported pages and create route↔page cycles; use the registered full-path form.
- Preserve leaf 051's URLs and protected-route set; no route-tree regeneration or repository-wide migration.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-05-client · `wave-1/lane-05.json::coverage.cut[46]` — original title: "Required route parameters are widened through strict:false."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `248-preserve-registered-route-typing-through.md:8-18,35-63` and `051-flat-route-tree-makes-authentication-eleven.md:48-77`; 248 shares the invariant but excludes pages, while 051 preserves the URLs needed for exact bindings.
- promoter hint (non-authoritative): looks close to leaf 248 — hint only
- promoter hint (non-authoritative): looks close to leaf 051 — hint only

