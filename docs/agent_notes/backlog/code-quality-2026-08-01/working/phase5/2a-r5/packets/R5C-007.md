# Author packet — R5C-007 → `149-generic-codemod-infrastructure-hidden-behind.md`

- Ruling: **augment-existing-leaf** (edit `149-generic-codemod-infrastructure-hidden-behind.md` in place)
- Leaf number: **149**
- Title (settled): Make the shared codemod writer the sole import-normalization owner
- Theme: Codemod write plans normalize imports redundantly through three passes
- Area: harness · Severity: **low** · Size: **S** · Confidence: medium
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Normalize each write plan once in the shared writer; keep the subsequent ESLint pass only as an explicit real-write policy whose relationship to preview output is tested.

## Augment instruction (what to add or change in the target leaf)

After extracting the neutral modules, remove producer-side `sortImportBlocks` calls from structured-logging and expand-barrel so `writeOrPreviewFiles` owns custom normalization once. Characterize and document the remaining ESLint real-write pass, with preview/write parity coverage, and narrow leaf 149's no-behavior-change exclusion accordingly.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `149-generic-codemod-infrastructure-hidden-behind.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Structured-logging and expand-barrel parse and rewrite imports at transformation time and immediately repeat the identical custom pass in writeOrPreviewFiles; successful real writes then apply another policy through ESLint. This wastes full-file parsing, obscures normalization ownership, and lets dry-run or fallback output differ from successful written output without an explicit contract.

## Verified evidence (existence-checked at step 1b)

- `scripts/codemods/structured-logging-fix-transforms.ts:324-328` — The structured-logging transformer sorts import blocks while constructing its rewrite result.
- `scripts/codemods/expand-barrel/run.ts:26-42` — Expand-barrel likewise sorts each plan before passing it to the shared writer.
- `scripts/codemods/lib/trpc-shared-schema-writes.ts:135-161` — The shared writer sorts every plan again and, for real writes, then launches ESLint's simple-import-sort rules.
- `scripts/codemods/lib/trpc-shared-schema-imports.ts:125-167` — sortImportBlocks reparses the full source with a fresh ts-morph Project and rewrites import ranges, so duplicate calls are substantive repeated work.

## Author constraints (all must be honored)

- Sequence inside or after leaf 149; do not edit the pre-extraction tRPC-named locations twice.
- Preserve emitted output unless a separately characterized ESLint difference is deliberately accepted; do not broaden into a codemod framework rewrite.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5C · lane-09-lint-machinery · `wave-1/lane-09.json::coverage.cut[13]` — original title: "codemods contain multiple import-normalization paths — lower-ranked after extracting the broader codemod-core finding"; rejected because: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Opened `149-generic-codemod-infrastructure-hidden-behind.md:56-103`; it moves the exact helpers and callers but explicitly excludes preview/write behavior, leaving this redundant-pass residual uncovered and collocated.
- promoter hint (non-authoritative): looks close to leaf 149 — hint only

