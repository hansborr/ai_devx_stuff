# Author packet — R5B-L02-004 → `209-remove-the-unused-repository-root-parameter-from.md`

- Ruling: **augment-existing-leaf** (edit `209-remove-the-unused-repository-root-parameter-from.md` in place)
- Leaf number: **209**
- Title (settled): Prune unread state from the coldspot pipeline
- Theme: Stop accumulating coldspot state that no coldspot calculation reads
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Remove the two unread aggregate fields and their writes; leave the live newest-touch age and birth selection semantics unchanged.

## Augment instruction (what to add or change in the target leaf)

Broaden the leaf to remove `FileAggregate.added` and `oldestTouchMs` from types, initialization, per-row updates, and fixtures while retaining `newestTouchMs` for age and `birthMs` for earliest-commit selection.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `209-remove-the-unused-repository-root-parameter-from.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

Coldspot aggregation performs per-file writes for an added-line total and oldest-touch bound that the lens never reads, obscuring its actual age and birth requirements.

## Verified evidence (existence-checked at step 1b)

- `scripts/drift-ai/coldspots-aggregate.ts:12-20` — FileAggregate includes added and oldestTouchMs.
- `scripts/drift-ai/coldspots-aggregate.ts:33-42` — Every row increments added and mutates both touch bounds.
- `scripts/drift-ai/coldspots-coldspot.ts:81-84` — Age calculation reads newestTouchMs only.
- `scripts/drift-ai/coldspots-coldspot.ts:162-174` — Surfaced rows do not read added or oldestTouchMs.

## Author constraints (all must be honored)

- Preserve leaf 209's injected Git-runner seam, blame arguments, stale-marker cost gate, and blobless-clone behavior.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[19]` — original title: "Coldspot aggregation retains unused accumulator state"; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `209-remove-the-unused-repository-root-parameter-from.md:8-60`; it does not presently reach aggregation, while `scripts/drift-ai/coldspots-aggregate.ts:10-42` and `coldspots-coldspot.ts:81-84,159-174` show the adjacent dead state.
- promoter hint (non-authoritative): looks close to leaf 209 — hint only

