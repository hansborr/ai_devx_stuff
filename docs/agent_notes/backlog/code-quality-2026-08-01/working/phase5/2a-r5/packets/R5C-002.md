# Author packet — R5C-002 → `270-add-a-local-orientation-contract-for-the-client.md`

- Ruling: **new-leaf**
- Leaf number: **270**
- Title (settled): Add a local orientation contract for the client character-creation wizard
- Theme: Give the client character-creation wizard a local orientation contract
- Area: client · Severity: **medium** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Add `packages/client/src/components/character-create/MODULE.md` covering state/provider ownership, step composition, conditional navigation, reset invariants, validation, submission projection, and focused test seams; regenerate the module index.

## Problem statement from the promotion candidate

A contributor entering this 21-file wizard has no local map of state ownership, conditional-step behavior, cross-step reset invariants, validation, or the client-to-server projection boundary. Those contracts are spread across reducer comments and implementation files despite meeting the repository's explicit MODULE threshold.

## Verified evidence (existence-checked at step 1b)

- `packages/client/src/components/character-create` — measurement: 21 production files and no MODULE.md (`find /workspace/packages/client/src/components/character-create -type f ! -name '*.test.ts' ! -name '*.test.tsx' | wc -l`) — This is a large multi-step feature directory without the repository's local orientation document.
- `docs/module-docs.md:24-30` — The charter requires MODULE docs for large feature directories and surfaces hiding non-obvious state or cross-file flow.
- `packages/client/src/components/character-create/wizard-state.ts:229-275` — The reducer owns cross-step invariants: class changes rewind progress and clear spells, while background changes clear equipment.
- `packages/client/src/components/character-create/wizard-validation.ts:46-74` — Step advancement is a keyed validation graph, including exact class-dependent spell counts.
- `packages/client/src/components/character-create/create-character-input.ts:58-91` — A separate projection converts wizard state into the shared mutation contract and intentionally leaves universal proficiencies to the server.
- `packages/client/src/pages/character-create-page.tsx:8-32` — The page composes nine step components plus state, navigation, and submission seams.

## Author constraints (all must be honored)

- Prefer landing after leaves 042 and 045 if scheduled so the document describes their settled architecture; otherwise document the pin accurately and require those leaves to refresh it.
- Do not mix in server leaf 004's service-module reorganization or restate rule details owned by code.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5C · lane-07-docs-dx · `wave-1/lane-07.json::coverage.cut[14]` — original title: "Character creation is a 21-file multi-step feature without a local MODULE document."; rejected because: none — the cut carries only its title

## Existing-leaf relations

- Dedup basis from 1c: Opened `042-flat-character-creation-contract-forces.md:90-168` and `045-character-creation-ability-rules-have-four.md:73-142`; both leave MODULE ownership uncovered, and 042 explicitly says none exists at `:158-159`.
- promoter hint (non-authoritative): looks close to leaf 042 — hint only
- promoter hint (non-authoritative): looks close to leaf 045 — hint only

