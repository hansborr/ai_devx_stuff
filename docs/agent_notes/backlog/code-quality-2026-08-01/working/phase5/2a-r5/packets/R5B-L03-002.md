# Author packet — R5B-L03-002 → `205-rename-auth-service-to-reflect-its.md`

- Ruling: **augment-existing-leaf** (edit `205-rename-auth-service-to-reflect-its.md` in place)
- Leaf number: **205**
- Title (settled): Single-source Bearer-header interpretation at the authentication boundary
- Theme: Bearer-header interpretation has two private production implementations
- Area: server · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Centralize authorization-header extraction and use the same token interpretation in both HTTP boundaries before calling the existing verifier.

## Augment instruction (what to add or change in the target leaf)

After settling the auth module names, add one neutral Bearer-token extractor with focused malformed/empty/header cases and consume it from tRPC context and upload authentication; keep REST reply mapping local.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `205-rename-auth-service-to-reflect-its.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

The two authenticated HTTP boundaries can silently diverge on scheme, whitespace, or empty-token policy despite delegating token semantics to the same verifier.

## Verified evidence (existence-checked at step 1b)

- `packages/server/src/trpc/context.ts:14-20` — The tRPC boundary privately defines and parses the Bearer prefix.
- `packages/server/src/routes/upload-routes.ts:49-68` — The upload boundary repeats prefix checking, slicing, and verification.

## Author constraints (all must be honored)

- Avoid a runtime route/context cycle; place transport parsing in a neutral auth-header helper if adding it to `auth-primitives.ts` would make that module incohesive.
- Amend leaf 205's API-preservation caveat only for this named helper; preserve token format and verification behavior.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-03-server · `wave-1/lane-03.json::coverage.cut[24]` — original title: "Bearer-token parsing is duplicated between tRPC and upload boundaries."; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `205-rename-auth-service-to-reflect-its.md:38-58`; it currently promises a rename only, while `packages/server/src/trpc/context.ts:14-20` and `packages/server/src/routes/upload-routes.ts:49-68` duplicate the parser.
- promoter hint (non-authoritative): looks close to leaf 205 — hint only

