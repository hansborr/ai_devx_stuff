# Author packet — R5B-L02-001 → `133-code-intel-daemon-guards-promise-wire-types.md`

- Ruling: **augment-existing-leaf** (edit `133-code-intel-daemon-guards-promise-wire-types.md` in place)
- Leaf number: **133**
- Title (settled): Keep the code-intel server CLI vocabulary reachable
- Theme: Remove the unreachable start command from the code-intel server CLI vocabulary
- Area: harness · Severity: **low** · Size: **S** · Confidence: high
- AUDIT_TARGET_SHA: `ebf096580b31f604861fadb3d4cbd4079da4f017`
- priorPackReviewSha: `948169235fef7798de67bd44b90ac4894e9cc55f`

## Settled direction (implement this; do not re-open it)

Remove the unreachable `start` variant and keep the parser, dispatcher, and usage text aligned on the three reachable lifecycle commands.

## Augment instruction (what to add or change in the target leaf)

Add a bounded server-CLI cleanup: remove `start` from `ServerCliCommand`, retain `status|stop|restart` as the parser and usage authority, and update focused CLI type/test coverage without changing daemon wire commands.

**The Area/Severity/Size above grade the material you are adding, not the host leaf.** Leave `133-code-intel-daemon-guards-promise-wire-types.md`'s own `Theme · Area · Severity · Size` header exactly as it stands — a cross-cutting host does not get re-graded because a smaller addition lands inside it. (Round 1's acceptance gate proposed exactly that rewrite on all three augmentations and the orchestrator refused it; this line exists so the question does not come up again.)

## Problem statement from the promotion candidate

The private command union claims a state that the parser cannot produce and the dispatcher does not distinguish, weakening exhaustiveness and misleading lifecycle maintenance.

## Verified evidence (existence-checked at step 1b)

- `scripts/code-intel/server-cli.ts:31` — ServerCliCommand advertises start.
- `scripts/code-intel/server-cli.ts:97-105` — The sole parser accepts only status, stop, and restart.
- `scripts/code-intel/server-cli.ts:89-94` — The dispatcher has no distinct start behavior.

## Author constraints (all must be honored)

- Do not conflate the private server CLI vocabulary with leaf 133's routable daemon command set or make `overview` routable.
- Preserve leaf 133's protocol-version and request-validation constraints.

## Prior-pack residual (what is left uncovered by the named record)

- (none)

## Provenance (origins — carry into the leaf Source line context)

- R5B · lane-02-analyzers · `wave-1/lane-02.json::coverage.cut[13]` — original title: "ServerCliCommand contains an unreachable start arm"; rejected because: coverage.cut; no rationale recorded

## Existing-leaf relations

- Dedup basis from 1c: Opened `133-code-intel-daemon-guards-promise-wire-types.md:98-198`; it covers wire guards and ping/pong, while `scripts/code-intel/server-cli.ts:31,82-105` proves the separate unreachable CLI arm.
- promoter hint (non-authoritative): looks close to leaf 133 — hint only

