{
 "batch": "wave1-lanes-01-03",
 "panels": [
  {
   "candidateId": "C-002",
   "chosenDirection": "STRUCTURAL base with COPYABILITY grafts: behavior-preserving decomposition of the shared policy bodies, no TS enforcement core. Rescope the finding from \"hook entrypoints\" (already thin generated projections) to the shared bodies policy.sh/common.sh. Slice A (M): extract the hard-deny rule set from the ai_policy_violation_reason if-chain (policy.sh:1059-1213) into a commandPolicy facet in harness.controls.json — fields {id, order, class, patterns[]|predicate, message, scope} — rendered by a scripts/harness generator into scripts/ai-hooks/policy-rules.generated.sh (exact classified-bun-scripts.generated.sh precedent, already sourced at policy.sh:39); the dispatcher collapses to a generic ordered loop where ~12 pure-regex rules become data rows and the ~7 procedural predicates (protected-file writes, push/commit-on-main, reset/stash/worktree-loss, gh mutations) stay as named shell functions referenced by row; render a policy-reference table into the generated harness docs from the same records. Slice B (M-L): split policy.sh/common.sh by responsibility — command-normalize.sh (awk heredoc stripper 270-557 moved verbatim with its spec comments and a \"copy verbatim; spec = header comments + corpus\" banner), command-paths.sh (tokenizer cluster 733-965), git-classify.sh (git semantics from common.sh:112-473 and policy.sh predicates), policy-eval.sh (rule loop + decision emission) — keeping policy.sh as a compatibility façade sourcing them so every caller and the ~8k-line test corpus are untouched and serve as the parity gate with no dual-run machinery; shrink common.sh to its documented \"harness-neutral primitives\"; split test.sh along the same seams via the smoke-subjects generator. Slice C (S): formalize one decision-record boundary function returning (verdict: block|advise|allow, ruleId, message) consumed by the hooks — the interface any future typed core must match. Update scripts/ai-hooks/README.md and docs/ai-harness.md in the same commit as each seam; run harness:check per slice. Typed TS core: demoted out of this candidate to a conditional follow-up — at most a non-enforcing `bun run harness:policy:explain` simulator dual-run against the shell corpus as CI parity; enforcement stays bash+awk+jq. Effective size drops from XL to three sequential M/M-L/S slices; severity high stands.",
   "rationale": "All three proposals independently converge on the same two conclusions, verified against the pin: (1) the highest-leverage fix for \"changing one rule requires understanding several parsers\" is declarative rule extraction — the dispatcher is already a latent rule table (ordered pattern→message rows with constants at policy.sh:11-31) and the repo's manifest→generator→projection pattern is already wired into this exact file (classified-bun-scripts.generated.sh at line 39); (2) the original direction's typed TS core is wrong for this candidate because it puts bun in the fail-closed deny path of the hook firing on every Bash call (broken bun would block the commands needed to fix bun), forces a risky port of deliberately conservative parsers whose ~8k-line corpus and header comments are the spec, and destroys bash+awk+jq copy-anywhere universality that a public reference harness trades on. STRUCTURAL wins as base: it alone goes straight to the repo-idiomatic manifest facet (MINIMAL-CHURN's in-shell rule table is a throwaway intermediate its own Leaf C would rework), grounds the priority in the empirical churn axis (rule edits dominate policy.sh history), and adds the decision-record boundary that makes a future typed core optional and cleanly sequenced instead of entangled. Grafted from COPYABILITY: the generated policy-reference docs table, copy-verbatim layer banners, and the non-enforcing explain/simulator as the only sanctioned typed step. Grafted from MINIMAL-CHURN: the façade insight (unchanged `. policy.sh` path makes the whole corpus a free parity gate — no dual-run machinery needed since nothing is reimplemented) and the vestigial soft-guidance fact (ai_policy_is_soft_guidance returns 1 unconditionally, so hard/soft class is a manifest field, not a migration burden). The synthesis rejects the lane's core \"typed command-analysis core\" deliverable and the solo review's Stages 2-3 sequencing, so constraint rulings are recorded.",
   "constraintRulings": [
    "Do not port the AI-hook policy analyzers to a Bun/TS enforcement core in this pack; instead extract the deny-rule set into a harness.controls.json commandPolicy facet with a generated .sh projection and a generic ordered rule loop, keeping enforcement in bash+awk+jq.",
    "Do not put bun or any runtime beyond bash/awk/jq/git in the fail-closed deny path of bash-pre/post-tool-use hooks; if a typed model is wanted, ship it only as a non-enforcing explain/simulator tool dual-run against the shell corpus as a CI parity check.",
    "Do not reimplement or tune the awk heredoc stripper or tokenizer semantics; move them verbatim with their spec comments into bounded modules behind an unchanged policy.sh sourcing facade so the existing ~8k-line test corpus remains the parity gate with no dual-run machinery.",
    "Do not land C-002 as one XL change; sequence it as corpus-green slices — rule-data extraction to the manifest (M), module decomposition behind the facade (M-L), explicit decision-record boundary (S) — with README.md/docs/ai-harness.md updated in the same commit as each seam.",
    "Do not spend effort thinning hook entrypoints; they are already generated thin projections — the fusion to fix is in the shared bodies (policy.sh/common.sh), so rescope work there."
   ]
  },
  {
   "candidateId": "C-004",
   "chosenDirection": "Fund a narrowed consolidation one layer below the schema seam (base: STRUCTURAL proposal, scoped by MINIMAL-CHURN, riders from COPYABILITY). (1) Canonical raw record: add a CONTROL_FIELD_NAMES as-const tuple plus one mapped `RawControlRecord` type with all-`unknown` optional fields beside control-field-validation.ts; harness-check-validation.ts adopts it as its RawControl (re-export keeps registration-manifest-checks.ts imports stable), and the generator narrows to `RawControlRecord & { readonly id: string }` via spread-construction after its existing throwing id guards — deleting the `type-assertion-boundary: json` double-cast at generate-harness-controls.ts:127 while keeping the sanctioned hand-rolled throwing envelope parse. (2) One shared, non-throwing, live-tree-free NON-LINT field walker (`collectNonLintFieldIssues(raw, { repoRoot, principleFromRegistry, includeSource, bareCoordinateCheck })`) emitting `{ field, message }` issues with byte-identical existing message strings; the field tag is load-bearing — each caller reorders/filters to reproduce its exact smoke-pinned diagnostics (checker keeps source-separate-up-front and the bare-coordinate check; generator keeps source-late). Intentional divergences become explicit commented options instead of silent path-local behavior. Do NOT unify the lint-rule paths (resolveLintRuleControl vs validateLintRuleEntry are different checks, not duplicates). (3) Callers stay thin adapters over their existing sinks: generator keeps short-circuit decisions, slots/hookWiring resolution, the categorizedControlFieldsSchema re-parse, and ResolvedControl projection; checker keeps pushFailure/Map sink and layers its checker-only live-tree checks (repairCommand/invocation script existence, ratchet-registry ids, parity sweeps) unchanged. (4) Untouched: harness-manifest-schema.ts, harness-manifest-loader.ts, harness-manifest.ts leaf, MANIFEST_DIRECT_READERS tripwire, fixture copy-closures. (5) Same-change riders: add one division-of-labor row to docs/guides/harness-manifest-parser.md naming the shared field-inventory/walker owner; drop the stray `export { isNonEmptyString }` at harness-check-validation.ts:20. Acceptance proof: before/after diagnostics A/B over test-generate-harness-controls.sh, test-harness-check.sh, and both unit suites showing byte-identical failure output. Revised severity: low-to-medium (original medium/lane-high rested on the stale schema-not-consumed claim). Revised size: M, single leaf (lane's needs-split L is obsolete). priorPackOverlap: null stands — the typed-parser migration predates the CQ25 corpus and no record covers this residual seam.",
   "rationale": "The lane finding's headline claim is factually stale against the pin: harness-check strict-parses the whole manifest (registration-manifest-checks.ts:160) and the generator re-parses each control through categorizedControlFieldsSchema at its success point (generate-harness-controls-validation.ts:147), so the original parse-once direction re-litigates a landed, test-pinned 2026-07-19 design ruling (schema header, MANIFEST_DIRECT_READERS rationales incl. the A/B-proven check-registry copy-closure regression, guide's \"when not to migrate\" test) and would destroy the generator's one-pass granular report. The live residue is real but one layer down, and I verified concrete drift that argues against closing as already-decided: the bare-backlog-coordinate principle check exists only in the checker (harness-check-validation.ts:174), and source placement differs between the two orchestrations — the dual raw model is an active drift engine, not just cosmetic. STRUCTURAL is the winning base because its field-tagged issue design uniquely dissolves the pinned-diagnostics problem (MINIMAL-CHURN's flat string[] forces order-only repins of smoke output — its own stated main cost) and its all-unknown mapped record deletes a non-test cast for free; MINIMAL-CHURN contributes the correct scoping ruling that lint-rule paths are not duplicates (meta.docs projection vs plugin-registration check — STRUCTURAL's shared lint-rule walker would abstract over genuinely different inputs and messages); COPYABILITY's Leaf B converges with this shape and its flat-function/no-framework constraint and small riders are kept, but Leaf A (loose envelope schema export) is rejected: it modifies the fenced schema module, would perturb the generator's throwing envelope messages, and the cast deletion it partly motivates is achieved more cheaply by the record type. Net: single M leaf, severity low-to-medium, mildly advancing the public-harness-reference goal (one field inventory + shared walker + thin adapters is the copyable pattern; two divergent raw models teach the wrong lesson).",
   "constraintRulings": [
    "C-004: Do not re-route harness generation and checking through one strict whole-manifest parse (L01-004's original direction) — that migration already landed behind a 2026-07-19 test-pinned ruling (harness-manifest-schema.ts + loader + shrink-only MANIFEST_DIRECT_READERS tripwire); do instead consolidate one layer down: one shared all-unknown RawControlRecord field inventory plus one shared non-throwing non-lint field walker beside control-field-validation.ts, with both consumers as thin adapters.",
    "C-004: Do not touch harness-manifest-schema.ts, harness-manifest-loader.ts, the harness-manifest.ts leaf, the MANIFEST_DIRECT_READERS tripwire, or fixture copy-closures — and do not add a loose envelope schema export for the generator; do instead keep the generator's sanctioned hand-rolled throwing envelope parse and delete its json double-cast (generate-harness-controls.ts:127) by typing the shared raw record's fields as unknown and spread-constructing after the existing id guards.",
    "C-004: Do not unify the lint-rule paths (resolveLintRuleControl vs validateLintRuleEntry) — they are different checks (meta.docs projection vs plugin-registration), not duplicates; do instead scope the shared walker to the non-lint per-kind field checklist only.",
    "C-004: Do not let the shared walker emit untagged flat strings, own short-circuiting, or perform live-tree lookups; do instead emit {field, message} issues with byte-identical message strings, keep short-circuit/report decisions and all live-tree checks (script existence, ratchet-registry ids, parity sweeps) caller-side, encode intentional divergences (bare-backlog-coordinate, source placement) as explicit commented walker options, and prove byte-identical fixture diagnostics with a before/after A/B over test-generate-harness-controls.sh and test-harness-check.sh.",
    "C-004: Do not carry this as severity medium-high / size L / needs-split per the lane banking — the schema-not-consumed premise is stale; do instead file it as one M leaf at low-to-medium severity, updating the division-of-labor table in docs/guides/harness-manifest-parser.md in the same change."
   ]
  },
  {
   "candidateId": "C-005",
   "chosenDirection": "Split into three independent leaves with different verbs; base = copyability proposal with grafts. Leaf 1 (pre-push scope pin, S/M): generate the ENTIRE near-duplicates trigger ERE — filename alternation (sensor-near-duplicates.baseline|drift-ai.config).json plus the buildSourceExtensions extension set — into a .generated.sh fragment sourced by .husky/pre-push (replacing the hardcoded pattern at line 267), registered as a generatedSurface facet so the standard freshness gate subsumes the check; delete scripts/harness/pre-push-scope-pin.ts and its test outright (net-negative LOC). Pre-push fails closed with a regen instruction if the fragment is missing (mirrors the existing stale-baseline failure), and drift-ai.config.json is declared as a generator input in the config-surface manifest. Leaf 2 (CI gate parity, S/M): do NOT generate ci.yml or a composite action; move the contract from the `# harness-ci-gate:` comment into step-level structured data (e.g. env: HARNESS_CI_GATE: verify-wrapper/verify), parse the workflow with a real YAML parser (Bun.YAML; keep extraction thin so unit tests cover only the pure comparison on parsed objects), compare {env key, run} pairs against ciGateControlIds + manifest invocations, and retire findMarkedStepInvocation/extractCiGateBindings; record that whole-file/composite generation is revisited only if ciGateControlIds grows (~3+). Leaf 3 (registration preflight, M): shrink-and-label, no generation. Single-source the data-shaped parts: timeout default via a generated constant fragment the hook sources (data-to-data, hook-timeouts pattern); derive REQUIRED_REGISTRATION_FRAGMENTS from the check/verify-steps-generator facet outputPaths instead of a duplicated const; replace the collector-source occurrence grep with assertions against the generator's declared outputs. Deliberately KEEP the behavioral ordering fingerprints (snapshot function, unstaged-to-gate ordering, admission-before-marker-before-bridge) but narrow multi-line formatting-sensitive fragments to function-name/assignment anchors, and reframe the file with a header naming it a source-fingerprint tamper tripwire (the check reads real source precisely so the edit that unwires the hook cannot regenerate its own alibi; CQ25-130 refuses the engine rewrite). Add a short harness-doc decision-rule section: generate what is data; fingerprint only behavioral/tamper invariants, and label them. Sequencing: leaf 1 first (cheapest, uncontroversial); leaves 2 and 3 independent; leaf 3's doc describes the end state of all three. Severity settles at medium; the split resolves the lane's needs-split leafability.",
   "rationale": "Verified facts drive the seam-2 decision: ciGateControlIds has exactly one entry and ci.yml exactly one marker, so the structural proposal's composite-action generator (new generator + freshness surface + manifest schema widening for n=1, producing the least portable artifact in the plan) is over-machinery, while minimal-churn's no-change leaves a comment-as-data-channel plus a 65-line private line reader as a showcased idiom — contradicting both the finding's core problem and the repo's explicit public-harness-copyability bar. Copyability's contract-as-data (env key + real YAML parse) is the only seam-2 option that is simultaneously cheaper than the status quo, kills the bespoke parser, and stays copyable; it wins seam 2 and supplies the best seam-3 doc framing. Structural contributes the load-bearing seam-1 detail (generate the full alternation including filenames, or half the pattern stays hand-written and the seam survives) and the sharpest seam-3 data/tripwire partition; minimal-churn contributes the fail-closed fragment-missing rule, the config-surface input declaration, and the growth threshold for revisiting seam-2 generation. All three proposals independently converge on seam 1 (generate, delete parser — repo has exact precedents in generate-hook-timeout-constants.ts/generate-hook-wiring.ts) and on seam 3 keep-the-tripwire (generated or self-described contracts let the unwiring edit regenerate its own alibi, and full generation is the engine rewrite CQ25-130 refused), so those are settled; the synthesis is the copyability skeleton with those grafts. Net effect: two of three private parsers deleted, the third shrunk and labeled, no new generated GitHub-owned surfaces, net-negative parser LOC.",
   "constraintRulings": [
    "Do not generate ci.yml, a marker-bounded region in it, or a composite-action fragment for CI gate parity while ciGateControlIds has fewer than ~3 entries; do instead move the harness-ci-gate binding from a YAML comment into step-level structured data (env key) and compare it to the manifest via a real YAML parser.",
    "Do not replace the registration-preflight behavioral ordering fingerprints (snapshot function, unstaged-to-gate ordering, admission-before-marker-before-bridge) with generated or hook-self-described contracts, and do not generate pre-commit hook internals (CQ25-130 stays closed); do instead shrink fingerprints to function-name/assignment anchors, label the file as a deliberate tamper tripwire, and record the residual in the harness docs.",
    "Do not treat C-005 as one L-sized blanket generation effort; do instead ship three independent leaves — pre-push trigger generation (S/M), CI contract-as-data (S/M), preflight shrink-and-label plus decision-rule docs (M) — landing the pre-push leaf first.",
    "When generating the pre-push near-duplicates trigger, do not emit only the extension set; do instead generate the entire ERE including the baseline/config filename alternation, make pre-push fail closed if the generated fragment is missing, and declare drift-ai.config.json as a generator input in the config-surface manifest."
   ]
  },
  {
   "candidateId": "C-006",
   "chosenDirection": "Single-source the verify slot programs in harness.controls.json as one ordered two-axis catalog, then compose the four gates as thin profiles. (1) Catalog: each slot declares its full-tree command form once plus an explicit changed-mode disposition — \"inherit\" (mode-invariant), a changed/staged form with a required reason string, or \"omit\" with reason (local-rule-starter). No silent default: a new slot without a declared disposition fails parse. Catalog order is execution order for every gate (verified to reproduce all four current arrays positionally). (2) Profiles: verify and verify-parallel are {mode:\"full\"} — their 16-slot byte-identity becomes structural instead of unguarded; verify-changed is {mode:\"changed\"} plus its scripts condition/dynamic; pre-commit is {mode:\"changed\"} plus reasoned overrides for test (dynamic precommit-test-timings, dropped json reporter args, fastCommitSkip) and scripts (staged condition, fastCommitSkip). fastCommitSkip stays pre-commit-only. (3) Resolve profiles to concrete VerifyStepSlot[] inside the manifest loader/schema parse so every downstream consumer (generate-verify-steps.ts, harness:check field validation, doc generator, drift-ai parity) keeps seeing materialized arrays; landing proof is scripts/verify/steps.generated.sh byte-identical before/after regeneration. (4) Derive the marker-bridge divergence set from the catalog's changed-form reasons plus pre-commit override reasons; retire scripts/harness/verify-step-bridge-divergences.ts as a hand file, but keep assertMarkerBridgeSupersets running against the resolved arrays (fed the derived set) as defense-in-depth over the resolver. (5) Pre-work: reconcile or declare-as-reasoned-override the condition-string wording drift between verify-changed's scripts slot (\"changed\", harness.controls.json:782) and pre-commit's (\"staged\", line 2152) so migration does not silently canonicalize one. (6) Carry in scope: contract comments in harness-manifest-schema.ts / verify-step-schema.ts, the verify-consumers porting-knob note (generate-verify-steps.ts:35), generator test and fixture copies (scripts/fixtures/generate-verify-steps/expected.generated.sh — known fixture copy-set hazard), and doc generator output unchanged (continues rendering materialized per-gate programs). Stage within one leaf: (a) schema+loader+resolution with byte-identical parity proof, then (b) allowlist derivation and hand-file retirement. Severity medium (partial guards already exist); size M; single-leaf; no prior-pack overlap.",
   "rationale": "All three proposals' factual claims were verified against the pin (verify==verify-parallel byte-identical and unguarded; sole membership delta local-rule-starter; pre-commit vs verify-changed differs only on test/scripts; assertSlotSuperset guards only pre-commit-as-subset via the 10-entry hand allowlist and cannot see a slot omitted from a subset gate). STRUCTURAL wins because its vocabulary matches the measured variation exactly — two axes (mode, per-consumer override) plus one mode-riding omission — where the lanes' original include/exclude/dependency scheme is over-general (\"dependency\" has no consumer; deferral is runtime logic in steps-lib.sh) and MINIMAL-CHURN, while defensible and cheapest (S), leaves the four-way synchronized-edit cost intact, needs a second hand structure to close the membership-omission blind spot for both subset gates, and cuts against the repo's documented single-sourcing trajectory (generatedSurface facets, skill gate-wiring, smoke subjects) and its public harness-reference copyability bar. Grafted from COPYABILITY: reasons carried in the manifest data with the allowlist derived from them, assertions retained against resolved output as defense-in-depth, the condition-wording (\"changed\" vs \"staged\") reconciliation pre-work item, and doc generator continuing to emit materialized per-gate programs. Grafted from MINIMAL-CHURN: byte-identical steps.generated.sh regeneration as the landing proof and staged execution within one leaf. COPYABILITY's claim that no assertion scheme could ever close the membership hole is overstated (an expected-omission constant could), so the decision rests on edit-cost removal plus trajectory/copyability fit, not on that claim. The early-resolution seam keeps every downstream consumer unchanged, which is what holds the blast radius to M on the repo's most load-bearing manifest.",
   "constraintRulings": [
    "C-006: do not introduce generic include/exclude composition vocabulary for gate profiles; do model exactly the two measured axes — per-slot full vs changed command forms in one ordered catalog, plus per-gate {mode, reasoned overrides}.",
    "C-006: do not add a slot 'dependency' field; ordering stays positional in the catalog and the dist/typecheck deferral stays runtime logic in scripts/verify/steps-lib.sh.",
    "C-006: do not layer profiles on top of the hand-maintained MARKER_BRIDGE_DIVERGENCE_ALLOWLIST (double declaration); do derive the divergence set from catalog changed-form reasons plus pre-commit override reasons and retire scripts/harness/verify-step-bridge-divergences.ts, keeping assertMarkerBridgeSupersets as a tripwire over the resolved arrays.",
    "C-006: do not let downstream consumers see profiles; do resolve profiles to materialized slot arrays in the manifest loader so harness:check, the doc generator, and drift-ai parity are untouched, with byte-identical scripts/verify/steps.generated.sh regeneration as the mandatory landing proof.",
    "C-006: do not allow a silent changed-mode default for catalog slots; every slot must declare inherit, a changed form with reason, or omit with reason, so a forgotten mode declaration is a parse error rather than silent membership drift.",
    "C-006: do not silently canonicalize the condition-string wording drift between verify-changed's scripts slot ('changed', harness.controls.json:782) and pre-commit's ('staged', line 2152); reconcile it or declare it as a reasoned override before migrating."
   ]
  },
  {
   "candidateId": "C-007",
   "chosenDirection": "Derive the walkable portion of the generatedSurface facets at projection time and shrink the hand-declared lists to semantic residue, layered additively over the existing declare+validate contract rather than replacing it. Concretely: (1) build ONE shared derivation module in scripts/harness/ that reuses the existing closure walk (closureWalkEntries + validateSeedImportClosure from fixture-closure-check.ts — no second walker) to compute effectiveFixtures = closure(source) ∪ outputPaths ∪ SHARED_FIXTURE_INFRA/FIXTURE_SYNTHESIZED handling ∪ residue and effectiveTriggers = closure(source) ∪ {source} ∪ residue, consumed by both generate-verify-steps.ts and harness:check so the graph is computed exactly once; (2) slice 1 = fixtures first: rename the shrunk facet to a residue-semantics name (e.g. fixtureExtras) where each entry carries a one-line reason, derive the copy-manifest projection (harness-check-fixture-manifest.generated.txt) from the union, and flip the closure validator from exact-equality to residue hygiene (a residue entry the walker can derive fails, so mechanical entries cannot re-accrete; emptied residue stays fail-closed, carrying MUSI_HARNESS_CHECK_ALLOW_NO_FIXTURE_PATHS semantics to the generated file); (3) slice 2 = same treatment for triggerPaths (extraTriggerPaths residue: runtime data inputs, directory prefixes, non-walkable files), separately judged and explicitly deferrable — slice 1 alone is an acceptable stopping point; (4) derivation runs only in the real tree at projection time — the walker-incapable smoke-fixture tree keeps consuming checked-in projections, and all projections (steps.generated.sh, generated-surface-freshness.generated.sh, the fixture manifest) stay checked in and freshness-checked so every graph change remains a reviewable diff; (5) docs/guides/harness-manifest-parser.md (migration step 4 and the closure-check contract) and the generated-surfaces.ts module header are rewritten in the same slice, including an explicit statement that derivation is an optimization layer over declare+validate and a walker-less copier may declare full lists by hand (the additive union makes that a valid degenerate mode); (6) prove convergence by regenerating and re-running harness:check after each slice. Severity medium (drift is already machine-prevented; payoff is legibility and tedium). Recorded fallback if the panel rejects derivation outright: the S/M closure-sync autofix writer (harness:check --fix) that writes back the validator's already-computed exact fix sets — structurally inferior (keeps derived data in the source-of-truth file, adds a second writer of a hand-authored file) but preserves every current review property.",
   "rationale": "All three proposals' load-bearing claims verified at the pin: the generator is walker-free today (the walk lives only in fixture-closure-check.ts), diffTriggerPathClosure is one-directional by design while diffFixtureClosure enforces exact coverage of the walkable portion (so fixtures are the exactly-derivable facet), the smoke tree consumes the checked-in generated manifest under the opt-out env and never runs the walker, and the validator already computes exact per-entry fix sets. MINIMAL-CHURN is cheapest but solves only the tedium half of the problem — the manifest still cannot distinguish semantic residue from closure echo, the explicit second half of the lane's problem statement — and adds a second writer of a hand-authored truth file, inverting the repo's hand-truth/generated-projection discipline; it survives as the fallback plus two grafted disciplines (reuse the existing walker, prove convergence). STRUCTURAL contributes the two best architectural ideas — a single derivation seam shared by generator and validator, and schema-visible residue naming — but frames derivation as replacing declare+validate (hurting format copyability to walker-less repos, the repo's stated public-reference bar per the Musi harness-reference goal) and orders triggers-first, deferring the admitted-higher-blast fixture slice to a second leaf that may never land, which would capture the small win (12 tautologies) and skip the large one (85-entry echo). COPYABILITY wins as base: its additive-union framing neutralizes the strongest objection to derivation while keeping the manifest format copyable; its fixtures-first ordering is better grounded (densest duplication, exactly-derivable, and the walker-incapable consumer already reads a checked-in projection so derivation risk surfaces as reviewable diffs and smoke failures rather than silent breaks); reason-per-residue-entry plus the residue-hygiene check makes the surviving lists the teaching surface and prevents re-accretion; and naming slice-1-alone an acceptable stopping point is the right risk posture for an L-sized refactor of a recently-landed, working, fail-closed system whose payoff is legibility rather than correctness. The synthesis inverts the solo review's triggers-first split, confirms its rejection of a new standalone edge-graph model, and confirms candidate-level severity medium over the lane's high. CQ25-67 overlap re-confirmed distinct (smoke-subject import-closure declarations, not manifest facets).",
   "constraintRulings": [
    "C-007: Do not build a new standalone producer/source/output/fixture/freshness edge-graph model; do instead derive from each record's existing `source` via the closure walk already in scripts/harness/fixture-closure-check.ts (closureWalkEntries + validateSeedImportClosure), factored into one shared derivation module consumed by both generate-verify-steps.ts and harness:check so the graph is computed exactly once.",
    "C-007: Do not replace the declare+validate contract; do instead layer derivation as an additive union over residue declarations (closure ∪ extras), keeping fully hand-declared lists a valid degenerate mode so the manifest format stays copyable to walker-less repos, and state this explicitly in docs/guides/harness-manifest-parser.md.",
    "C-007: Do not keep derivable entries in the same-named facets; do instead rename the shrunk facets to residue-semantics names (e.g. fixtureExtras / extraTriggerPaths) with a one-line reason per surviving entry, and flip the closure validator to residue hygiene (a residue entry the walker can derive fails) so mechanical entries cannot re-accrete.",
    "C-007: Do not split triggers-first as the solo review proposed; do instead derive fixturePaths first (85-entry echo, exactly-derivable walkable portion, smoke tree already consumes the checked-in generated manifest) and treat the trigger slice as separately judged and deferrable — slice 1 alone is an acceptable stopping point.",
    "C-007: Do not run derivation in the walker-incapable smoke-fixture tree or drop fail-closed semantics; do instead derive only at projection time in the real tree, carry MUSI_HARNESS_CHECK_ALLOW_NO_FIXTURE_PATHS semantics over as a fail-closed emptied-residue check, and keep all projections checked in and freshness-checked so graph changes remain reviewable diffs.",
    "C-007: Do not treat the guide and module header as out of scope; do instead rewrite docs/guides/harness-manifest-parser.md (migration step 4, closure-check contract) and the generated-surfaces.ts module header in the same slice that changes the contract, per the documented-decision rule.",
    "C-007: Do not file at the lane's severityHint high or as one monolithic L leaf; do instead file at severity medium (drift is already machine-prevented; the payoff is legibility/tedium) split into two leaves (fixtures, then triggers).",
    "C-007: Do not adopt a manifest auto-fix writer (harness:check --fix) as the primary direction — it keeps derived data in the source-of-truth file and adds a second writer of a hand-authored file; record it only as the S/M fallback if the panel rejects derivation."
   ]
  },
  {
   "candidateId": "C-008",
   "chosenDirection": "Assert-canonical validate mode (minimal-churn proposal, grafted): keep hook identity in the existing hookWiring facet of harness.controls.json and add an optional neutral `surface: \"bash\" | \"edit\"` field to the ~10 eligible non-aggregator bash/edit controls. In scripts/harness/hook-wiring-schema.ts, colocated with HARNESS_SUPPORTED_EVENTS / HOOK_OUTPUT_SUPPORT and using the same fail-closed `as const satisfies` idiom, add two projection tables: CANONICAL_MATCHER[(harness, surface)] (claude bash→\"Bash\", claude edit→\"Edit|Write\", codex edit→\"apply_patch\", copilot bash→\"bash|powershell\", copilot edit→\"create|edit\") and canonicalCommand(harness, bodyBasename) (claude → `bash $CLAUDE_PROJECT_DIR/.claude/hooks/<name>`; codex/copilot → the toplevel-quoted form into HOOK_SHIM_DIRS[harness]). When `surface` is declared, schema validation asserts the authored matcher and command byte-equal the canonical projection, with error messages that print the expected string (the validator is the self-teaching provenance oracle). Add the opt-out tripwire: error/warn when a non-aggregator PreToolUse/PostToolUse control whose matcher matches a known canonical value omits `surface`, forcing either declaration or an explanatory note. Grafts: (a) from STRUCTURAL — the lifecycle matcher `startup|resume|compact` is policy (which session sources fire), not adapter syntax: it stays authored and out of the tables; frame this change explicitly (in the facet schema comment) as stage 1 of a possible later emit-canonical derivation, so flipping assert→emit is a mechanical follow-up on already-tested tables; (b) from COPYABILITY — mark the projection tables with the repo's existing `porting-knob:` comment convention (precedent hook-shims.ts:139), and state the provenance rule in three existing teaching surfaces: the facet schema JSDoc/error messages, the generated harness-controls doc, and a comment atop the hookWiring section. Rule text: manifest = policy authority (event, order, body, timeout, statusMessage, outputs, notes); projection tables in hook-wiring-schema.ts = adapter-syntax authority (manifest matcher/command strings are canonical-checked copies, like committed generated output); .claude/.codex/.copilot/.github hook files = generated, never authoritative. Explicitly untouched: generate-hook-wiring.ts, hook-shims.ts (regexes stay), check-wiring.sh, all generated files, all generatedSurface trigger/fixture registrations — generated bytes identical by construction. Estimated churn ~50-70 lines in hook-wiring-schema.ts + ~10 manifest lines + schema unit tests + one doc regen. Final disposition: severity medium, size S (down from M), leafability leaf (resolves the lane's needs-split), priorPackOverlap unchanged (CQ25-122 landed; novelty = provenance enforcement, not generation). Full derivation (drop authored strings, delete backward-parse regexes, shimName aggregator override, materialized-binding doc rendering) is recorded as an optional deferred follow-up, decided later with the tables in hand — not scheduled in this pack.",
   "rationale": "Ground truth verified against the pin: all 32 bindings' matchers and commands are pure functions of (harness, surface) and (harness, body-basename) — matcher enumeration over harness.controls.json yields exactly 7 apply_patch / 7 create|edit / 7 Edit|Write / 6 Bash / 2 bash|powershell / 1 lifecycle — and hook-shims.ts (CLAUDE_PROJECT_DIR_COMMAND / TOPLEVEL_COMMAND regexes, deriveCopilotDispatch matcher-sniffing) re-derives what the manifest hand-authors, which is the provenance ambiguity L01-008 names. All three proposals agree on that diagnosis and on the aggregator/claude-only carve-outs; they differ only on whether to validate the authored strings or delete them. Minimal-churn wins because (1) enforcement, not deletion, is what removes the ambiguity: after it lands, the code tables are normative and any divergence fails closed with the canonical answer printed — the harmful half of the finding (silent drift, unclear authority) is fully fixed; (2) it is a strict prefix of full derivation (literally STRUCTURAL's own S1), so nothing is thrown away if the team later flips assert→emit; (3) it respects the live do-it-at-all contest recorded in the candidate — CQ25-122 already landed shim generation, 11/18 controls are single-harness, the surface is quadruply guarded, and explicit per-harness records have copyability/inspection value in a public harness-reference repo; (4) it converts a needs-split M against a guarded generation path into a clean S leaf touching one code file, eliminating the byte-identity and lockstep-grammar risks both other proposals concede push toward L. STRUCTURAL and COPYABILITY are not discarded: their compatible, cheap ideas (lifecycle-matcher-is-policy, stage-1 framing, porting-knob markers, provenance rule in the three existing teaching surfaces) are grafted in; their expensive core (emit-mode derivation, regex deletion) is deferred as an explicit optional follow-up rather than rejected outright, which sequences the decision to a point where the tables already exist and the residual value can be judged cheaply.",
   "constraintRulings": [
    "C-008: do not introduce a separate provider-neutral hook registry or identity layer; do instead keep hook identity and policy in the existing hookWiring facet on each control in harness.controls.json, extended with an optional neutral `surface` field.",
    "C-008: do not delete or generator-derive the manifest's per-harness matcher/command strings in this pack; do instead add fail-closed (harness,surface)->matcher and (harness,body-basename)->command projection tables in scripts/harness/hook-wiring-schema.ts and assert the authored strings byte-equal them (validate mode), leaving emit-mode derivation as an explicitly optional later follow-up.",
    "C-008: do not touch generate-hook-wiring.ts, the hook-shims.ts command-parse regexes, scripts/ai-hooks/check-wiring.sh, any generated hook file, or any generatedSurface trigger/fixture registration; the change is confined to hook-wiring-schema.ts, the hookWiring facets, tests, and doc regen, with generated bytes identical by construction.",
    "C-008: do not neutralize the aggregator controls (hook/ai-codex-*, hook/ai-copilot-*) or claude-only lifecycle/policy hooks; they omit `surface` and record their asymmetry in notes — but do add a tripwire that errors/warns when a non-aggregator PreToolUse/PostToolUse control matching a known canonical matcher omits `surface` without a note.",
    "C-008: do not project timeouts, codex statusMessage, or the lifecycle matcher (startup|resume|compact) through the tables — they are per-harness policy, not adapter syntax, and stay authored in the manifest.",
    "C-008: do not schedule this as needs-split size M per the lane; do instead land it as a single S leaf (severity medium confirmed), and mark the projection tables with the repo's porting-knob comment convention plus the provenance rule (manifest = policy authority; projection tables = adapter-syntax authority; harness hook files = generated, never authoritative) in the facet schema, the generated harness-controls doc, and the schema error messages."
   ]
  },
  {
   "candidateId": "C-009",
   "chosenDirection": "Emit the public JSON Schema FROM the existing Zod contract (base: STRUCTURAL proposal, with grafts from minimal-churn and copyability). Concretely: (1) export harnessManifestSchema from scripts/harness/harness-manifest-schema.ts and add one doc-generator script (scripts/harness/generate-manifest-json-schema.ts) that writes z.toJSONSchema output to schemas/harness.controls.schema.json with deterministic stable-key serialization; the emitter imports ONLY the Zod schema module — never harness.controls.json and never the dependency-free leaf harness-manifest.ts — so the fixture-copy-closure boundary and MANIFEST_DIRECT_READERS seam are untouched. (2) Register it as one new kind:\"doc-generator\" control with a generatedSurface facet (triggerPaths: harness-manifest-schema.ts + control-field-validation.ts; outputPaths: the emitted schema) plus verify:steps regen, inheriting freshness/harness:check enforcement for free. (3) Add a root \"$schema\": \"./schemas/harness.controls.schema.json\" key to harness.controls.json and accept it in the Zod root as $schema: z.literal(\"./schemas/harness.controls.schema.json\").optional() — pinned so a stale pointer fails parse, optional so fixture manifest copies still parse; land both edits in one commit; sweep harness-check.ts, harness-gate-parity.ts, and registration-manifest-checks.ts for hardcoded root-key inventories. (4) NO formatVersion and no migration/bump policy: the freshness-gated schema file's git diff is the compatibility signal for pin-based copiers; a versioned $id inside the generated schema is the additive later path if wanted. (5) Make the schema self-documenting: port load-bearing header comments into .meta({description}) on union arms, facet carriers, and slotsCarrier; the root description states the strictness boundary (root keys, per-kind strict field inventories, primitive types, slots minItems:1 are contractual; generatedSurface/hookWiring/skillWiring interiors and slot vocabulary are intentionally loose per the 2026-07-19 division-of-labor ruling; duplicate-id, repairCommand-iff-codemod, and other semantic checks are parser-owned). (6) Restate nonBlankString in a representable form (z.string().regex(/\\S/), behavior-identical) so it survives emission. (7) Durability: one round-trip test validating live harness.controls.json against the emitted schema via a generic validator (ajv devDep), plus extending the existing every-kind-parses parity pin so a new kind arm regenerates the schema or fails. (8) One short \"External schema\" section appended to docs/guides/harness-manifest-parser.md; no new guide. Size M holds; single-leaf holds.",
   "rationale": "All three proposals independently converge on the solo review's core reshape (Zod stays source of truth; JSON Schema is a generated projection registered through the existing generatedSurface machinery), which is strong evidence the architecture is right and the lanes' original schema-first direction is wrong. STRUCTURAL wins as the base because its implementation discipline is what makes the leaf safe: pinned-literal $schema fails closed on a stale pointer; the emitter-imports-schema-module-only rule provably preserves the fixture-copy-closure split; deterministic serialization is a hard prerequisite for freshness gating; and the root-key-consumer sweep catches the real ripple of widening the strict root. Two grafts materially improve it. From MINIMAL-CHURN: drop formatVersion entirely — under the copy-at-a-git-pin consumption model a hand-bumped integer with mechanically unenforceable bump semantics rots, while the checked-in freshness-enforced schema file already yields a reviewable diff per contract change; structural's required z.literal(1) would also force edits to every fixture manifest copy for no added fail-closed value (strict root/kind inventories already reject unknown shapes). Also from minimal: the ajv round-trip test is the one non-negotiable durability piece — without it a z.toJSONSchema fidelity gap ships silently and external consumers hit it first. From COPYABILITY: the .meta() description pass is where the finding's actual severity claim (external consumability) gets addressed — a bare shape dump unpointed-to in schemas/ delivers almost nothing, whereas $schema-in-instance editor validation plus hover-visible design rulings makes the schema the format's entry-point doc and a model instance of the repo's public-harness-reference goal. Verified premises grounding the rulings: the 2026-07-19 division-of-labor header ruling exists verbatim in harness-manifest-schema.ts; the duplicate-id superRefine and nonBlankString .refine are unrepresentable in emitted JSON Schema; the root object currently has four keys and no version/schema field.",
   "constraintRulings": [
    "C-009: Do not invert derivation to schema-first or make the in-repo parser consume a schema-derived model; keep the Zod contract in scripts/harness/harness-manifest-schema.ts as the single source of truth and emit schemas/harness.controls.schema.json from it via native z.toJSONSchema.",
    "C-009: Do not publish full-depth strictness or unify the three non-Zod facet validators (generated-surfaces.ts, hook-wiring-schema.ts, skill-inventory-schema.ts) into the published schema; publish strictness only at the levels the Zod contract owns and state that boundary in the schema's root description (2026-07-19 division-of-labor ruling stands).",
    "C-009: Do not add a formatVersion field or any migration/bump policy to harness.controls.json; pin-based copiers get their compatibility signal from the freshness-gated generated schema file's git diff — if an explicit version is ever wanted, add a versioned $id inside the generated schema (additive, zero data-file churn).",
    "C-009: Do not let the schema emitter read harness.controls.json or import the dependency-free leaf harness-manifest.ts; it imports only the Zod schema module, so the fixture-copy-closure boundary and the loader seam are untouched.",
    "C-009: Do not ship the emitter without deterministic stable-key serialization AND a round-trip test validating the live manifest against the emitted schema with a generic JSON Schema validator; without these the freshness check flaps and toJSONSchema fidelity bugs ship silently.",
    "C-009: Do not leave refinements silently lossy in the published schema; restate nonBlankString in an emission-representable form (e.g. regex /\\S/) and list parser-only semantic checks (control-id uniqueness, repairCommand-iff-codemod, slot vocabulary) in the schema description as repo-side.",
    "C-009: Do not add the $schema root key as a free string; pin it as an optional z.literal(\"./schemas/harness.controls.schema.json\") in the Zod root (stale pointer fails parse), land the Zod change and the harness.controls.json edit in the same commit, and sweep harness-check.ts, harness-gate-parity.ts, and registration-manifest-checks.ts for hardcoded root-key inventories."
   ]
  },
  {
   "candidateId": "C-010",
   "chosenDirection": "STRUCTURAL (artifact-edge extension of the existing manifest pipeline), with grafts from MINIMAL-CHURN and COPYABILITY. Core scope (M-to-L): (1) add named-artifact slot fields to scripts/harness/verify-step-schema.ts and harness.controls.json — typecheck declares produces:\"dist-outputs\"; lint and ratchet declare requiresArtifact:\"dist-outputs\" with defer-only-when-missing semantics; artifact ids bind to hand-written probe functions via a generated id-to-function map mirroring the existing dynamic-resolver binding precedent (steps-lib.sh:177-193), not declarative probePaths data. (2) generate-verify-steps.ts emits MUSI_VERIFY_SLOT_PRODUCES / MUSI_VERIFY_SLOT_REQUIRES_ARTIFACT / MUSI_VERIFY_ARTIFACT_PROBE_FUNC alongside the existing arrays (MUSI_FAST_COMMIT_SKIP_SLOTS precedent, steps.generated.sh:17); steps-lib.sh consumes them, deleting the hard-coded lint|ratchet case (musi_defer_dist_slot, steps-lib.sh:205-211) and the literal typecheck matches (:386, :399-440), after first capturing the three-way failure taxonomy (no-producer / producer-not-launched-with-NOT_RUN-passthrough / producer-failed, with per-log messaging) in behavior-pinning tests. (3) closure validation in verify:steps:check/harness:check fails generation when a slot names an unknown artifact, an artifact lacks a probe binding, a consumer slot list contains a requiresArtifact slot with no producing slot, or more than one producer is declared per artifact — this is what closes the \"looks registered but needs undocumented scheduler edits\" gap. (4) adopter degradation: empty declarations generate empty maps (bash-3.2 ${arr[@]+} guard convention) so the dependency branch vanishes for copiers without a dist step. (5) documentation in scope: shrink the pre-commit control's principle prose to policy and add the pattern to the harness guide surface. Sequential runner consumes nothing at runtime and MUSI_VERIFY_STEPS is not reordered — lint's dist preflight already makes sequential order-correct. Split leaves, not bundled: (a) refactor 13-positional musi_drain_memory_pending_slots (:234-243) via a name-prefix nameref record; (b) optional follow-on fold of expectedPeakMb (memory-budget.sh:53) into a top-level per-slot-name manifest table (never per-consumer slot entries — typecheck appears in 4 consumers) carrying the measurement provenance.",
   "rationale": "All three proposals' code claims verify at pin (actual file is scripts/verify/steps-lib.sh, not the lane's cited scripts/verify/lib/verify-steps-lib.sh; command/env/mode/fastCommitSkip are already generated, so only defer/producer/peak surfaces are genuinely hand-coded). STRUCTURAL wins on repo-convention fidelity: its probe-binding design reuses the existing dynamic-resolver id-to-function precedent instead of inventing new declarative probe data (COPYABILITY's probePaths) or baking \"dist\" into schema booleans (MINIMAL-CHURN); it alone satisfies the repo's TDD bar by requiring behavior-pinning tests before rewriting the :396-440 failure taxonomy; it alone catches the per-consumer vs per-slot-name keying hazard that would make a naive memory-peak fold-in declare each peak 4x; and it correctly drops the solo reshape's sequential topological reordering, which would change fail-fast/log ordering for no correctness gain. Grafted from MINIMAL-CHURN: the single-producer-per-artifact invariant enforced at generation time, and its correct triage that the memory-peak surface fails soft (256 MB floor) and is not part of the trap this finding names, so it stays a separate optional leaf. Grafted from COPYABILITY: adopter-empty-declaration degradation as a hard requirement and documentation (principle-prose shrink plus guide section) as in-scope deliverables, both load-bearing for the public harness-reference goal. Named artifacts over booleans is justified by that same copyability goal at ~20 generator lines of cost. Severity high stands (silent mis-scheduling trap becomes a generation-time failure); size is M-to-L for the core with the two splits carried as independent leaves, consistent with the solo review's sizing note.",
   "constraintRulings": [
    "C-010: Do not build a standalone typed slot graph artifact or a general DAG scheduler; do extend the existing harness.controls.json -> generate-verify-steps.ts -> steps.generated.sh pipeline with produces/requiresArtifact slot fields covering the single artifact-edge class.",
    "C-010: Do not encode dependencies as slot-to-slot edges (e.g. dependsOn: [\"typecheck\"]); do model them as named artifacts with defer-only-when-missing semantics, with probe functions bound via a generated artifact-id-to-function map following the dynamic-resolver precedent (no declarative probePaths format).",
    "C-010: Do not make the sequential runner consume dependency data at runtime and do not topologically reorder MUSI_VERIFY_STEPS at generation time; do leave sequential ordering and lint's dist preflight untouched, documented by the same manifest declaration.",
    "C-010: Do not rewrite steps-lib.sh's dist-deferral failure taxonomy (:396-440) without first pinning it in tests; do preserve the three-way no-producer / NOT_RUN-passthrough / producer-failed exit codes and per-slot log messaging verbatim.",
    "C-010: Do not bundle the 13-positional musi_drain_memory_pending_slots refactor into this leaf; do split it as a separate Bash-hygiene leaf using a name-prefix nameref record (~4 params).",
    "C-010: Do not fold expectedPeakMb (memory-budget.sh:53) into the core scope; if pursued as its own follow-on leaf, do declare peaks in a top-level per-slot-name manifest table (never per-consumer slot entries) and migrate the measurement-provenance notes with them.",
    "C-010: Do not ship the new slot fields without closure validation; generation must fail on unknown artifact ids, missing probe bindings, a consumer list containing a requiresArtifact slot with no producer, or multiple producers for one artifact — and empty adopter declarations must generate empty maps that disable the dependency branch entirely."
   ]
  },
  {
   "candidateId": "C-012",
   "chosenDirection": "Promote the misnamed shared library instead of rebuilding it. Move the five worktree-seed-* production modules plus two tests into a new top-level scripts/import-closure/ directory (peer of path-policy/ and drift-triage/, the repo's MODULE.md-bearing multi-module precedents; NOT scripts/lib/, a flat no-MODULE.md utility zone, and NOT scripts/worktree-db/), renaming to drop the worktree-seed- prefix (e.g. closure-walk.ts, runtime-imports.ts, loader-validation.ts, loader-identifiers.ts, loader-exports.ts). Ship a MODULE.md per docs/guides/add-module-doc.md whose contract paragraph is a substantive deliverable, not decoration: source-level mapping that deliberately diverges from package-exports resolution (@musi/* -> packages/*/src, never dist/), rootable at arbitrary --root in synthetic sandboxes with no populated node_modules (node_modules = lone typescript symlink per test-harness-check.sh), byte-deterministic for fingerprint enumeration, fail-closed on dynamic loader forms; include an explicit boundary paragraph against scripts/code-intel (real semantics, exports->dist, needs node_modules: use for symbol/dependents queries; use this walker for sandbox/fingerprint closures) and a stated threat model for the ~604-line runtime-loader soundness layer (createRequire factories, loader aliases, fail-closed non-static specifiers = deliberate evasion-proofing, motivated by the three 2026-07-19 under-closure incidents documented at path-policy/fixture-import-closure.ts:1-9). Add a parity test pinning the hand-kept @musi alias table (worktree-seed-import-closure.ts:94-108) to the workspace package list so a new package fails at test time, not mid-worktree-provisioning — this bounds the entire genuine resolver-drift exposure to ~70 lines. Keep all policy at call sites unchanged: seed allow/terminal manifest stays as SEED_BLANKET_HASHED_ROOTS/SEED_SERVER_RUNTIME_INPUTS in worktree-db.sh (updating only the MUSI_SEED_IMPORT_CLOSURE_CHECKER default path), fixture policy stays in path-policy. Execution mechanics per minimal-churn: hand-edit the 4 non-generated reference sites (worktree-db.sh:438, path-policy/fixture-import-closure.ts, harness/fixture-closure-check.ts, lint-ratchet/output.test.ts), regenerate the 2 generated surfaces via bun run test:scripts:subjects and harness:check, and amend prior-pack HARNESS-CLUSTER-PLAN.md:131 so this slice supersedes CQ25-63/H12 (side benefit: dissolves H12's dependency on unlanded 28-PLAN slice 28.1, since the neutral destination does not require scripts/worktree-db/ to exist). Acceptance criteria: the MODULE.md code-intel boundary paragraph and the alias parity test must land with the move; a rename-only execution fails the slice. Effective size M (down from XL); severity downgraded from high — the residual defect is misnaming/mishoming/undocumented-contract, not a redundant private compiler.",
   "rationale": "The lane's XL/high framing conflates ~70 lines of genuine resolution approximation (extension probing + @musi alias table, worktree-seed-import-closure.ts:68-108) with ~604 lines of intentional fail-closed runtime-loader soundness policy, and both branches of its proposedDirection fail against verified code: (a) the 'explicit seed runtime manifest' already exists (SEED_BLANKET_HASHED_ROOTS/SEED_SERVER_RUNTIME_INPUTS in worktree-db.sh:434-455) and the walker is that manifest's drift validator — three documented 2026-07-19 under-closure incidents are why hand manifests alone are distrusted; (b) a 'repository-wide resolver service' rebase on scripts/code-intel is semantically wrong because real resolution honors package exports (dist/) and requires populated node_modules, while the walker must map @musi/* to src/ and run in synthetic sandbox roots. The real defect is identity: a four-domain shared library (worktree-db, path-policy, harness, lint-ratchet consumers, all verified) carries one consumer's name loose at scripts/ top level, which invites exactly this 'private resolver' misreading and a fifth copy. The structural proposal scored highest (correct three-layer decomposition, destination argument verified against the tree: scripts/lib is flat/undocumented, path-policy//drift-triage/ are the MODULE.md precedents); grafted from minimal-churn: the H12 supersession-by-amendment mechanics with its blocker-dissolving side effect and the exact hand-edit/regen surface list; grafted from copyability: acceptance-criteria enforcement so the slice cannot degrade into a cosmetic rename, and MODULE.md framing of the loader layer's threat model. Rejected from minimal-churn: dropping the parity test — its 'already fails loudly' claim is technically true (throw at worktree-seed-import-closure.ts:104-106) but moves the failure to worktree-provisioning/gate time, and the parity guard is the only mitigation for the single genuinely approximated layer, at trivial cost. Contested axis for the record: a reviewer preferring blanket-hashing broader roots over precise closures would delete most of the family; that trades fingerprint precision (spurious reseeds) and reintroduces the documented incident class, and it contradicts the repo's public harness-reference goal, where manifest-plus-independent-closure-validator is the exportable pattern.",
   "constraintRulings": [
    "Do not reduce the seed import-closure check to a hand-maintained runtime manifest — the manifest already exists (SEED_BLANKET_HASHED_ROOTS/SEED_SERVER_RUNTIME_INPUTS, scripts/worktree-db.sh:434-455) and the walker is its drift validator (three 2026-07-19 under-closure incidents, path-policy/fixture-import-closure.ts:1-9); do instead keep the manifest-at-call-site + independent-closure-validator pair intact.",
    "Do not rebase the closure walker on scripts/code-intel or ts.resolveModuleName — real resolution honors package exports (@musi/shared -> dist/) and requires populated node_modules, both wrong for source-level fingerprinting in synthetic sandbox roots; do instead document the code-intel-vs-walker boundary in the new MODULE.md.",
    "Do not treat the ~604-line runtime-loader soundness layer (runtime-loaders/loader-validation/identifiers/exports) as accidental compiler growth to shrink or delete — it is deliberate fail-closed evasion-proofing (createRequire factories, loader aliases, non-static specifiers); do instead keep it and state its threat model in the MODULE.md.",
    "Do not let prior-pack slice CQ25-63/H12 land the worktree-seed family into scripts/worktree-db/ — that re-encodes seed-private naming for a library with three non-seed consumers (path-policy, harness, lint-ratchet); do instead supersede H12 via an amendment to code-quality-2026-07-25/HARNESS-CLUSTER-PLAN.md:131 and move the family to scripts/import-closure/ (which also dissolves H12's blocker on unlanded 28-PLAN slice 28.1).",
    "Do not home the family in scripts/lib/ — it is a flat single-file utility zone with no MODULE.md precedent; do instead create top-level scripts/import-closure/ as a peer of path-policy/ and drift-triage/, the repo's documented multi-module directory convention.",
    "Do not schedule C-012 at the lane's XL/high 'replace a private resolver' scope — the genuine approximation is ~70 lines (worktree-seed-import-closure.ts:68-108); do instead scope it as M: rename/re-home + MODULE.md + @musi-alias-vs-workspace-package parity test + prior-pack plan amendment.",
    "Do not accept a rename-only execution of this slice — the MODULE.md contract/boundary paragraphs and the alias parity test are the substantive deliverables and must be acceptance criteria; a move without them leaves the finding's legitimate discoverability complaint unfixed.",
    "Do not hand-edit the generated reference surfaces during the move (path-policy-smoke-subjects-data.ts, generated-surface-freshness.generated.sh) — do instead regenerate them via bun run test:scripts:subjects and bun run harness:check after updating the four hand-maintained reference sites."
   ]
  },
  {
   "candidateId": "C-013",
   "chosenDirection": "Adopt the structural proposal: a TS manifest with a Zod schema (precedent scripts/lint-ratchet/lint-ratchet-config.ts) becomes the sole coverage-map policy source, with stable per-entry ids, section enum, explicit repo-rooted minimatch globs, asserted policy fields (status enum, proposedRuleTool, blockerFollowUp, prose notes) and checkable facts (normalLint, ratchet ids validated against the lintRatchets registry). Organize the checker around a derived/asserted split: everything computable (Files counts, ESLint reach, drift-ai table) is rendered/verified at generation time; the manifest asserts only genuine policy. All membership goes through the canonical matcher (tools/lint-ratchet/src/kernel/ratchet-globs.ts, minimatch dot:true, ESLint-flat-config-identical); the entire private dialect in scripts/lint-coverage-map-check-patterns.ts (base-dir grammar, expandBraces, globVariantToRegExp, .ext.ext normalization) is deleted, not ported; trackedFileIsInScope survives as real scope policy. scripts/lint-coverage-map-gen.ts converts from splice-one-block to rendering the complete docs/generated/lint-coverage-map.md via the existing doc-generator/lint-coverage-map facet in harness.controls.json (regen verify:steps), making every doc-generator facet uniform and removing the \"hybrid ownership\" header. docs:lint-coverage-map:suggest emits ready-to-paste manifest entries keyed by entry id instead of Markdown rows/\"append to line N\". The manifest imports shared glob constants from eslint-config/shared-policy.js where scopes coincide, so coverage-map scope co-moves with ESLint scope. Split (needs-split confirmed, 3 leaves): (1) manifest extraction + dual-run parity harness — mechanize extraction by running the CURRENT extractPathPatterns to produce the frozen resolved-pattern set (grafted from minimal-churn; scripted, never hand-transcribed), translate to rooted minimatch globs, prove per-file membership equivalence over git ls-files, and keep the parity check in the gate for the interim window; (2) checker rewired onto the manifest; (3) full-document renderer + parser-family/private-matcher deletion + suggest retarget + facet/verify-steps regen. Sequencing: land strictly before, or folded into, CQ25-44 slice 28.11's atomic relocation of this family.",
   "rationale": "All three proposals' factual claims verify against the tree (private dialect at scripts/lint-coverage-map-check-patterns.ts:53-157, fixed column constants at lines 3-6, splice-one-block at scripts/lint-coverage-map-gen.ts:29, \"hybrid ownership\" doc header, ~227 rows, CQ25-44 28.11 \"last, most entangled\" corpus entry). Structural wins: it fully realizes the lane direction and adds three substantive elements the others lack — shared-policy glob-constant imports closing a drift axis Markdown structurally cannot, stable entry ids replacing line-number anchors, and the derived/asserted split as the checker's organizing principle — plus the only mentions of the interim-window gate requirement and the silent-membership-correction hazard. Copyability corroborates the same direction and supplies the best severity framing (the doc is the sole counterexample to the repo's flagship single-sourcing pattern; splice-one-block is the anomalous facet shape) but adds no distinct technical content. Minimal-churn is rejected as an end state: keeping Markdown-as-policy-source and hybrid splice ownership preserves exactly the defect the finding names, contradicts the repo's public-harness copyability bar and every other single-sourced surface, and its retained parseRows/splice layer (~60 lines) is churn-then-delete; however its extraction mechanics (scripted rewrite driven by the current parser's own resolved output) are the best concrete slice-1 mechanization and are grafted in. The synthesis confirms rather than reshapes the lanes' original direction; the constraint rulings are binding execution constraints and one explicit rejection of the halfway alternative.",
   "constraintRulings": [
    "Do not stop at an explicit-rooted-globs-in-Markdown halfway state as the end state for the coverage map; do instead carry the change through to the typed manifest with full-document generation (Markdown-as-policy-source is the defect, not just the glob dialect).",
    "Do not delete or replace the private glob engine in scripts/lint-coverage-map-check-patterns.ts without a dual-run parity proof; do instead freeze the current extractPathPatterns-resolved pattern set via script (never hand-transcription), translate to rooted minimatch globs, and prove per-file membership equivalence over git ls-files before deletion — parity is the deletion license.",
    "Do not land membership changes discovered during glob translation silently inside the migration; do instead land each intentional membership correction as an explicit, separately-reviewed manifest edit.",
    "Do not leave the interim window between manifest extraction and checker cutover ungated; do instead run the dual-run parity check in the gate for as long as both representations exist.",
    "Do not use a raw JSON manifest; do instead use a TS module with a Zod schema so entries can import shared glob constants from eslint-config/shared-policy.js and carry typed rationale prose.",
    "Do not interleave this source-of-truth change with CQ25-44 slice 28.11's relocation of the lint-coverage-map family; do instead land it strictly before that slice or fold it into that slice's atomic move.",
    "Do not port the base-directory grammar, implicit **/ prefixing, or duplicate-extension normalization into the manifest world; do instead require explicit repo-rooted minimatch globs matched only through tools/lint-ratchet/src/kernel/ratchet-globs.ts (trackedFileIsInScope survives as scope policy).",
    "Do not leave docs:lint-coverage-map:suggest emitting Markdown rows or line-number targets; do instead have it emit ready-to-paste manifest entries keyed by stable entry ids."
   ]
  },
  {
   "candidateId": "C-014",
   "chosenDirection": "Bash-only, move-only decomposition of scripts/lib/verify-metadata.sh (1,406 lines, 72 side-effect-free function definitions; lane path scripts/verify/lib/ is wrong, line ranges match) into six flat single-concern engine libs in scripts/lib/, following the verified internal dependency DAG: verify-state-paths.sh (~270 ln: git-readonly + fingerprint/identity primitives + musi_standard_* state/lock/marker paths + the three MUSI_GATE_* budget constants), verify-commit-queue.sh (~70 ln: waiter tickets), verify-fast-commit.sh (~135 ln: provenance log + pending marker + tripwire), verify-path-policy.sh (~300 ln: path-policy query bridge + staged classification + staged/precommit fingerprints), verify-markers.sh (~270 ln: success markers + verify-marker bridge), verify-run-meta.sh (~350 ln: shims over verify-metadata-core.ts). scripts/lib/verify-metadata.sh remains PERMANENTLY as the source-all aggregator in dependency order — it is the public API, not a transition aid: all ~40 consumers (.husky/pre-commit, pre-push, post-commit, verify.sh, land.sh, lint-shell.sh, ai-hooks/cache.sh, tests) stay byte-identical, and no consumer is ever repointed at a leaf lib. Every function keeps its name and body (function names are the ABI); each leaf lib gets a __MUSI_*_SOURCED re-source guard and a header contract comment stating what it owns, what must be sourced first (leaves never source each other; the aggregator owns order), and the standing invariant \"function definitions only, no source-time side effects\". Any hand-rolled parsing discovered during the move extends the existing verify-metadata-core.ts, never a new module. Pacing: THREE layer-grouped slices with full-gate lands, ordered by blast radius — (1) verify-run-meta.sh + verify-commit-queue.sh (fully self-contained; proves the aggregator pattern), (2) verify-fast-commit.sh + verify-path-policy.sh, (3) verify-state-paths.sh + verify-markers.sh, leaving the aggregator as constants + six source lines. Three slices rather than CQ25-126's one-seam pacing is justified because that precedent does not transfer: worktree-db.sh is a 2,526-line CLI with command dispatch, top-level effects, and live-infra blast radius, whereas this file is a mechanically verified acyclic function library whose moves are consumer-invisible behind the unchanged aggregator; if slice 1 surfaces a hidden cross-section dependency, drop to one-seam-per-slice for the remainder. Per slice: bash scripts/tests/test-verify-metadata.sh (pinned smoke), scripts/tests/test-pre-push.sh + test-verify.sh, shellcheck via lint-shell.sh, bun run harness:check, plus a registration checklist item — confirm whether each new lib needs smoke-subjects regeneration, fixture-closure annotations, or harness.controls.json generated-surface rows before landing. Final slice: shrink the verify-metadata.sh top comment to the aggregator contract with an honest concern map, and cross-link docs/guides/verify-gate-lifecycle.md sections to the per-concern files (this repo's public harness-reference goal is part of the justification: fast-commit provenance, the verify-marker bridge, and commit-queue waiters become one self-describing copyable file each instead of hand-extraction from a 1,406-line file). Out of scope: any TS migration or typed facade (Substrate Ruling + CQ25-124 do-not-reopen, measured spawn-cost record), consumer repointing (CQ25-130 fence), file rename (40 sourcers for zero isolation value), and verify-engine.sh (same smell, separate finding). Size M, three slices.",
   "rationale": "The structural proposal wins because it grounded its design in verified facts rather than analogy: I independently confirmed the file is pure function definitions plus three constants (no source-time side effects), that the run-meta section is fully self-contained shims over verify-metadata-core.ts, that ~40 consumers source the aggregator, and that scripts/lib/ already holds 13 single-concern bash libs as the convention — so a move-only split behind a permanent aggregator has a regression surface of \"did the aggregator source everything in order\", which the pinned 1,323-line smoke and lint-shell.sh (which itself sources the aggregator) both catch immediately. Its key judgment calls are correct: the aggregator as permanent public API avoids re-entering the CQ25-130-fenced .husky consumer surface; three layer-grouped slices correctly refuses to over-transfer CQ25-126, whose worktree-db one-seam calibration was driven by CLI dispatch, top-level effects, and live-infra risk that this function library does not have. Minimal-churn's one-seam answer under-delivers on the finding's core (the conflation itself — ~1,050 lines would remain) while its best ideas (registration checklist, no-rename discipline, demand-driven fallback) are grafted in. Copyability's six-slice pacing overprices a medium organization finding, but its shaping is grafted: verify- prefixed names, header contract comments with sourcing assumptions, re-source guards, verify-gate-lifecycle.md cross-links, and the public-harness-reference rationale that tips the full split over a partial one given this repo's stated bar (judge harness work on copyability). The lanes' original typed-modules/facade direction is rejected outright: it re-litigates the Substrate Ruling (gate glue stays bash, sharing extracted engine libs — the exact clause this synthesis implements) and CQ25-124's do-not-reopen with its measured 18ms→130ms spawn-cost record.",
   "constraintRulings": [
    "Do not create new typed modules, a TS facade, or any new codec for verify-metadata concerns (rejects lane L01-014's original direction; Substrate Ruling + CQ25-124 do-not-reopen with measured spawn-cost record); do instead a bash-only move-only split into single-concern engine libs in scripts/lib/, extending the existing verify-metadata-core.ts for any hand-rolled parsing found during the move.",
    "Do not repoint any of the ~40 consumers (.husky hooks, verify.sh, land.sh, lint-shell.sh, ai-hooks, tests) at the new leaf libs or rename verify-metadata.sh (CQ25-130 fences the hook consumer surface); do instead keep scripts/lib/verify-metadata.sh permanently as the source-all aggregator and sole public entry point, with leaf libs as internal layering guarded by re-source variables.",
    "Do not rename any function or change behavior during the decomposition (function names are the ABI for ~40 sourcers); do instead move-only, dependency-ordered extractions verified per slice by the pinned test-verify-metadata.sh smoke, test-pre-push.sh, test-verify.sh, shellcheck, harness:check, and a full gate on land.",
    "Do not treat CQ25-126's one-seam worktree-db.sh ruling as binding on verify-metadata.sh (worktree-db is a CLI with dispatch, top-level effects, and live-infra blast radius; this is a verified side-effect-free acyclic function library behind an unchanged aggregator); do instead land three layer-grouped slices ordered by blast radius, dropping to one-seam-per-slice only if a slice surfaces a hidden cross-section dependency.",
    "Do not fold verify-engine.sh (991 lines, same smell) into this leaf; do instead keep it a separate finding with its own scheduling decision."
   ]
  },
  {
   "candidateId": "C-015",
   "chosenDirection": "Adopt the MINIMAL-CHURN proposal as the base, with two grafts. Base: (1) add one adapter, run_inline_check <title> <hint> <control> <fn>, that owns the section header and sets/clears CURRENT_CONTROL/CURRENT_HINT on every return path; strip that boilerplate from the eight inline check_* bodies (doctor.sh:461-799) and register them through the wrapper, so every check — inline or subprocess — becomes one uniform registration line whose first token names its protocol adapter (tally-prose=run_subcommand, report-rc=run_report_subcommand, drift-warn=run_drift_report_subcommand, native-json=run_migration_safety, inline-shell=run_inline_check, plus the documented run_harness_check special). (2) Add a HOW-TO-ADD-A-CHECK comment above the registration list: pick the adapter from a 5-row protocol decision table, add the harness.controls.json doctor-check entry (two-way parity in harness-check already fails otherwise), append one registration line. Grafts: from STRUCTURAL, use its named-adapter vocabulary in the decision table and in comments on each run_* function so the protocol names are greppable; from COPYABILITY, document in the file header the resilience asymmetry — prose mode is the works-when-broken bootstrap path (pure sourced bash), --json mode already requires bun+jq (harness-emit-envelope.ts) — as an explicit contract for readers copying the harness. No registry framework, no generator, no TS port, no protocol unification. One S-size leaf slice; verification = byte-identical prose and --json output before/after, scripts/tests/test-doctor-json.sh, bun run harness:check, exit policy unchanged (1 iff FAIL). Bypass-folding (migration-safety/harness-check as ordinary rows) and any parity extension remain optional follow-ups deliberately out of scope.",
   "rationale": "MINIMAL-CHURN scores highest against both the problem and the repo bar. The problem's real demonstrated hazard is ambient CURRENT_CONTROL/CURRENT_HINT management with per-early-return clear sites (verified: check_port_binding has three separate clear points at doctor.sh:548/567/584; eight inline checks each hand-manage the pair) — run_inline_check structurally closes it. The which-convention pain is irreducible (five adapters encode genuinely different upstream output contracts; DX1.3 forbids reimplementation and doctor.sh:172-177 forbids messageId merging), so a decision table is the correct fix, not unification. STRUCTURAL and COPYABILITY both hinge on generator machinery whose payoff evaporates on verification: harness-check parity is ALREADY two-way and gate-enforced (registration-manifest-checks.ts:188-189 — missing manifest entry fails, orphan manifest entry fails), so manifest-as-single-source removes only one of two already-gated touch points while adding a generator surface, a generatedSurface facet, and (STRUCTURAL) a body/registration split or (COPYABILITY) marker-block generation over a sequence that is actually interleaved — run_subcommand calls at doctor.sh:428/433 precede the inline checks, contradicting the contiguous-block premise. Both runners-up correctly reject the TS rewrite and protocol unification; their durable contributions (adapter naming vocabulary, resilience-asymmetry documentation) are grafted. The repo's copyability goal is served better by a self-contained, fully hand-readable diagnostic with an explicit add-a-check recipe than by generation machinery on a working, smoke-tested, deliberately-documented 886-line file. Size drops from L/needs-split to S/leaf with a byte-identical output gate, keeping regression risk near zero; severity medium stands.",
   "constraintRulings": [
    "C-015: do not build a typed-descriptor check framework or port doctor.sh's dispatcher/checks to TS/bun; do instead keep doctor a bash aggregator whose bootstrap checks and shared freshness libs (dependency-freshness.sh, prisma-client-freshness.sh — also consumed by vitest.sh, write-install-digest.sh, test-dist-preflight.sh) stay plain shell so the prose path still runs when bun/node_modules are the broken thing.",
    "C-015: do not unify doctor's runner variants into one check protocol or merge the messageId schemes (doctor.sh:172-177, DX1.3 stream-as-is contract); do instead treat the five variants as named protocol adapters (tally-prose, report-rc, drift-warn, native-json, inline-shell) selected per registration line, documented in a decision table.",
    "C-015: do not create a second check inventory — no string-array registry, no manifest doctor facet + generator, no generated registration file/block; do instead keep the existing argv-safe registration-call list as the table, anchored by the already-gate-enforced two-way doctor-check id parity in harness-check (registration-manifest-checks.ts:188-189).",
    "C-015: do not leave inline check_* functions hand-managing CURRENT_CONTROL/CURRENT_HINT with per-early-return clear sites; do instead route all eight through a single run_inline_check adapter that sets and clears section state on every return path.",
    "C-015: do not treat this as size L / needs-split; do instead land one S-size leaf slice with byte-identical prose and --json output verified against scripts/tests/test-doctor-json.sh — folding the migration-safety/harness-check bypasses or extending parity are optional follow-ups, not in scope.",
    "C-015: do not present doctor as uniformly resilient; do instead document in its header that only prose mode is the works-when-broken path — --json mode requires bun+jq (harness-emit-envelope.ts, doctor.sh:876)."
   ]
  },
  {
   "candidateId": "C-016",
   "chosenDirection": "Full whole-tool port per the CQ25-124 precedent, STRUCTURAL shape: new scripts/lib/migration-safety-core.ts layered as (a) domain types (MigrationRule 4-member union, RuleHit, AllowlistEntry, ScanReport), (b) pure core — scanSqlText as a whole-file state machine preserving cross-line block-comment state (port bug-for-bug, including the current non-handling of dollar-quoted $$ strings), parseAllowlist, classifyHits, findStaleEntries with an injected migration-existence predicate, (c) TS effects layer for the read-only fs walk (no process glue exists to justify bash), (d) two pure renderers over one ScanReport — renderHumanReport preserving the exact WARN:/INFO:/PASS: grammar and toHarnessFindings typed against harnessFindingSchema, (e) a runMigrationSafetyCli(argv, io) function in the VerifyMetadataCliResult style so grammar/contract tests run in vitest without spawning. scripts/migration-safety-scan.sh survives at its exact path as a ~15-line exec-forwarder (doctor.sh:354/:386 call it by path, test-doctor-json.sh installs filename-keyed fakes, and it is a registered path-policy smoke subject in scripts/path-policy/path-policy-smoke-subjects-data.ts). JSON mode: extract the envelope-construction body of scripts/harness-emit-envelope.ts (harnessFindingSchema validation + summarize + emit) into a shared function called in-process by the core, while the harness-emit-envelope.ts CLI stays as the adapter for the four other bash producers (generate-module-index.sh, verify-logs.sh, lint-agent-changed.sh, doctor.sh); tool id migration-safety-scan stays in MUSI_HARNESS_TOOLS. Deletes the awk lexer, the tab-framed TSV intermediate and its tab-in-path FAIL guard, the jq dependency and require_jq_for_json paths, and the duplicated ack-classification double-walk. Slicing (needs-split confirmed, 3 slices): S1 land core + envelope-builder extraction + exec-forwarder, with the existing 697-line shell smoke kept green as the parity oracle plus a one-off dual-run parity check (old bash vs new core over the real migrations dir and smoke fixtures; byte-compare human output, canonical-compare JSON); S2 migrate grammar/contract assertions into migration-safety-core.test.ts (precedent verify-metadata-core.test.ts) and slim the shell smoke to facade-level concerns (exit-0 warn-only, envelope validity, -- handling, any-cwd invocation); S3 remove dead guards, update pinned smoke lines, add one sentence to docs/ai-harness.md naming this as the second worked bash-facade/TS-core precedent, rerun harness:check and path-policy smoke-subject regeneration. Non-goals: no rule expansion, no exit-code change (stays warn-only 0), no second JSON shape, no entrypoint rename. Accepted cost: human mode gains a bun dependency — fine because scripts/ is repo-local and the bash-portability exemption belongs only to .claude/skills/**. Size stays L overall but each slice is S/M.",
   "rationale": "MINIMAL-CHURN has the best risk sequencing (smoke-as-parity-oracle, bug-for-bug discipline) but its terminal state leaves allowlist parsing, ack/stale policy, both output walks, and the TSV framing fragility in bash — directly against the substrate ruling (docs/ai-harness.md: parsing, policy evaluation, and reporting live in TS), so it only defers a re-flag. COPYABILITY reaches the same destination as STRUCTURAL and contributes the doc-precedent sentence, unit-tests-as-spec framing, and explicit non-goals, but its S1 (JSON through the TS core while human mode still runs the bash/awk analysis) transiently duplicates the analysis across the substrate boundary — the exact 'duplicates are defects' hazard — whereas STRUCTURAL swaps the whole tool at once behind a parity oracle. STRUCTURAL is also the only proposal that resolves the JSON seam correctly, and I verified its premise: harness-emit-envelope.ts has four other bash consumers, so extracting the builder for in-process TS use while keeping the CLI as the bash-producer adapter is the single-owner shape the ruling mandates (TS-to-TS tunneled through an NDJSON CLI is backwards; a hand-built envelope is a duplicate serializer). Synthesis = STRUCTURAL's shape and slicing + MINIMAL-CHURN's parity discipline in S1 + COPYABILITY's documentation/test-philosophy/exemption grafts in S2-S3. This materially reshapes the lane's original direction (which contained a path typo and prescribed 'independent JSON and human renderers' — a duplicate-serializer trap), hence the constraint rulings.",
   "constraintRulings": [
    "C-016: do not build an independent JSON renderer for migration safety; do instead flow --json through a single shared envelope builder extracted from scripts/harness-emit-envelope.ts, called in-process by the TS core, with the harness-emit-envelope.ts CLI retained unchanged as the adapter for its four other bash producers (generate-module-index.sh, verify-logs.sh, lint-agent-changed.sh, doctor.sh).",
    "C-016: do not cite scripts/migration-safety.sh; do instead use the real path scripts/migration-safety-scan.sh (653 lines verified at the pin).",
    "C-016: do not rename, drop, or bypass the scripts/migration-safety-scan.sh entrypoint; do instead keep it at its exact path as a thin exec-forwarder, because doctor.sh:354/:386 call it by path, test-doctor-json.sh installs filename-keyed fakes, and it is a registered path-policy smoke subject — rerun harness:check and path-policy smoke-subject regeneration after the port.",
    "C-016: do not stop at a kernel-only port that leaves allowlist parsing, ack/stale policy, and the renderers in bash as the end state; do instead port the whole tool to a layered TS core (the substrate ruling classifies parsing, policy evaluation, and reporting as TS-side), sequencing the lexer/rules port first if slicing demands it.",
    "C-016: do not slice so that --json runs the TS analysis while human mode still runs the bash/awk analysis; do instead swap the entire tool in one slice behind a parity oracle, so the analysis never has two live implementations across the substrate boundary.",
    "C-016: do not fix or extend rule semantics during the port (e.g. dollar-quoted $$ string handling) and do not change the warn-only exit-0 contract or the WARN:/INFO:/PASS: line grammar; do instead port bug-for-bug and prove it with a one-off dual-run parity check plus the existing 697-line shell smoke kept green through S1.",
    "C-016: do not rescope or shrink scripts/tests/test-migration-safety-scan.sh in the same slice as the port; do instead keep it as the S1 regression oracle and migrate grammar assertions to vitest (migration-safety-core.test.ts, per verify-metadata-core.test.ts) only in a later slice, slimming the smoke to process-contract concerns last.",
    "C-016: do not use the verify-metadata-style per-operation subcommand codec (bash loop shelling to TS per file); do instead use a single-invocation whole-tool port — this scanner has zero process glue, and per-file spawns plus delimiter-framed intermediates are the fragility being removed.",
    "C-016: do not treat the new bun dependency in human mode as a blocker; do instead accept it explicitly — the bash-portability exemption applies only to .claude/skills/**, and doctor already spawns bun in --json mode.",
    "C-016: do carry a one-sentence docs/ai-harness.md update naming migration-safety-core.ts as the second worked bash-facade/TS-core precedent alongside verify-metadata-core.ts (CQ25-124), so the ruling points at a repeatable pattern rather than a single migration."
   ]
  },
  {
   "candidateId": "C-017",
   "chosenDirection": "Reject both the lanes' registry/generation direction and the solo review's manifest+generator reshape; adopt the cross-surface parity-gate shape (base: MINIMAL-CHURN, grafting COPYABILITY's framing and STRUCTURAL's guardrails). Concretely: (1) add one scripts-Vitest file, e.g. scripts/baseline-family-parity.test.ts (auto-discovered, no registration surfaces), that treats MUSI_BASELINE_DRIVERS in scripts/git/baseline-drivers.sh as the authoritative key set and asserts per key: the descriptor case arm in scripts/git/baseline-merge-driver.sh yields driver_label/semantic_driver/install_hint; the metadata case in scripts/git/baseline-post-merge-truth-up.sh yields _tu_label/_tu_baseline_file plus a dispatched handler shim; BASELINE_MERGE_CLI_TABLE (direct import) agrees on displayLabel/cliPath with the driver arm; BASELINE_PATH_BY_DRIVER_KEY in scripts/generate-baseline-conflict-recipes.ts (needs a one-keyword export) agrees with _tu_baseline_file; the conventional per-family files exist (<key>-merge-driver-lib.sh, install/check shims, <key>-post-merge-baseline-truth-up.sh), the lib's info-attributes merge= line names the same baseline file, the shims' MERGE_DRIVER_METRIC_LABEL/MERGE_DRIVER_INSTALL_COMMAND match the driver arm, and every referenced bun-run script name exists in the relevant package.json. (2) Per-field authority election is explicit and documented in the test header (keys from baseline-drivers.sh; label/CLI path from the driver arm; baseline path from truth-up) — structural's answer to the schema-in-checker concern, without a new JSON manifest. (3) Failure messages name the missing/disagreeing surface per key so the test IS the add-a-family checklist. (4) Extend the baseline-drivers.sh header comment with the enumerated surface checklist pointing at the parity test as the completeness oracle. Reuse the case-arm extraction style already landed in generate-baseline-conflict-recipes.ts; fail-loud on parse misses. Out of scope: conflict-recipe prose (already single-sourced + doc-projected), truth-up per-key handlers and driver fallback behavior (genuinely divergent), any generation into or runtime-querying from the bash surfaces. Clean upgrade path: if the family ever grows fast, the test's extractors become a generator's --check verbatim. Restate: severity low-medium (4 stable entries), size S (down from M), leafability single-leaf holds. Evidence correction for the pack record: lane paths baseline-merge-cli-table.sh / conflict-recipes.sh / post-merge-truth-up.sh do not exist; real surfaces are scripts/baseline-merge-cli-table.ts, the recipe heredocs inside scripts/git/baseline-merge-driver.sh, and scripts/git/baseline-post-merge-truth-up.sh.",
   "rationale": "The finding's actual complaint is folklore-dependent completeness review over a 4-entry, rarely-growing family — not edit volume — so a verify-time parity gate captures essentially all the value at ~10-20% of the manifest+generator churn while honoring two recorded prior decisions (CQ25-83's standing \"do not parameterize the baseline merge-driver family further\" and baseline-merge-cli-table.ts's explicit rejection of broader unification). The generator reshape is additionally disqualified by a structural fact all verified at the pin: baseline-merge-driver.sh is already the parse source for generate-baseline-conflict-recipes.ts, so splicing generated arms into it creates an authored/spliced/parsed triple-role hook-critical file; and two consumers (installed standalone driver copy, minimal-PATH in-process truth-up) contractually forbid runtime query, while the CLI table is deliberately import-free for its sandbox smoke. Structural's whole-file shim generation targets exactly the ~15-line installers CQ25-83 rules are fine hand-written, for near-zero payoff on stable boilerplate. Minimal-churn wins on concreteness and executability; copyability independently reaches the same shape and adds the decisive repo-fit argument (the repo already showcases declare-once+generate five times over — a parity gate over intentionally hand-written surfaces is the pattern it does not yet teach, and matches the public harness-reference goal). Grafts from runners-up: structural's explicit per-field authority election and never-splice-the-driver guardrail; copyability's failure-messages-as-checklist design and severity/size restatement.",
   "constraintRulings": [
    "C-017: Do not build a baseline-family metadata manifest/generator or generate any projection into the scripts/git baseline bash surfaces (upholds CQ25-83 and the recorded rejection in scripts/baseline-merge-cli-table.ts); do instead enforce coherence with one scripts-Vitest cross-surface parity test keyed off MUSI_BASELINE_DRIVERS whose failures name the missing or disagreeing surface per key.",
    "C-017: Do not make scripts/git/baseline-merge-driver.sh a splice/generation target — it is already the parse source for generate-baseline-conflict-recipes.ts; keep it hand-authored and single-role.",
    "C-017: Do not runtime-query any shared registry from the installed driver copy, the truth-up bodies, or the merge-CLI table (standalone pure-bash, minimal-PATH, and import-free-at-runtime contracts); parity is enforced at verify time only.",
    "C-017: Do not move the conflict-recipe prose or the per-key truth-up handlers into any registry or parity scope beyond existence/dispatch checks — recipes are already single-sourced in baseline-merge-driver.sh with doc projection, and the handlers are genuinely divergent behavior."
   ]
  },
  {
   "candidateId": "C-018",
   "chosenDirection": "Make dedicated exit codes at the tools' existing return sites the complete, documented, test-pinned verdict contract; do not add envelope emission. Concretely: (1) lint-ratchet CLI (scripts/lint-ratchet.ts error mapping): give WorseBaselineError a dedicated exit code 3; exit 1 remains unclassified failure. (2) knip sensor (scripts/sensor-knip-unused-exports-core.ts return sites at 111/126/147 plus sensor-knip-unused-exports-baseline.ts): assign dedicated codes >=3 for stale-entries, summary-drift-only, and corrupt-baseline; leave 1/2 as unclassified/transient failure so an uncaught bun exception (exit 1) can never masquerade as a verdict — this structurally replaces the one real defense the greps provide today. (3) near-duplicates --check-baseline: move the stale FAIL verdict to a dedicated code >=3 for the same crash-safety reason. (4) In scripts/git/baseline-post-merge-truth-up.sh delete all three grep branches (lines ~170, ~205-215, ~252) and their SIGPIPE-herestring defense commentary; each handler becomes a case switch on the exit code, keeping captured output only for the tail -n 20 display path; advisory wording stays in bash. (5) Also in scope: .husky/pre-push:310 repeats the same ^FAIL: herestring grep for near-duplicates and there it decides push blocking — convert it to the same exit-code switch (a fourth capture-grep-decide copy no proposal or the solo review caught). (6) Document each tool's verdict-code map in its usage/contract header and in the truth-up comments, and pin every code with exit-code assertions in the existing smokes/unit tests (scripts/tests/test-lint-ratchet.sh, sensor tests, hook-shape smokes). (7) No change to examples/lint-ratchet-demo — verified at the pin it already classifies by exit code only (lines 126-136; the solo review's 'demo repeats the grep' premise is false); the fix converges the main repo to the demo's shape. Refresh guide/truth-up commentary to name the layering: exit code is the verdict contract, stdout is presentation, consumers never parse prose. (8) Deferral guard: if a payload consumer (counts, repair commands) ever materializes, extend the existing HarnessDiagnostics emission — ideally with a verdict-as-data note per the COPYABILITY proposal — never a bespoke per-tool status JSON. Size shrinks from M to S.",
   "rationale": "MINIMAL-CHURN wins on verified facts and proportionality. The decisive fact flip: the demo mirror is already exit-code-only (examples/lint-ratchet-demo/.../baseline-post-merge-truth-up.sh:126-136 — line 123's 'lint:ratchet:' is a comment, not a grep), so the solo review's and both envelope proposals' 'demo teaches the anti-pattern' copyability argument is false at the pin; the copy showcase already teaches the minimal contract and the main repo is the outlier. Verified that every sub-verdict the greps disambiguate is a distinct return site (scripts/lint-ratchet.ts:155-157 sole WorseBaselineError catch; sensor-knip-unused-exports-core.ts:111/126/147; near-duplicates' documented exit-1-exclusively-FAIL contract quoted at baseline-post-merge-truth-up.sh:244-246), so the ambiguity is purely in the exit-code mapping. The envelope route (STRUCTURAL/COPYABILITY, per the solo reshape) requires real new emission in all three checkers (check-baseline throws without emitting, modes.ts:195; sensors emit nothing), temp-file/env plumbing and a bun classifier subprocess in the hooks, and — because exit codes stay as the bun-less fallback — two classification paths tested forever, all to fix what is (at the truth-up sites) advisory-wording routing. One graft is load-bearing: the greps currently do defend against uncaught bun exceptions exiting 1 (knip's exit-1 fallback misroutes a crash to 'stale' even today; pre-push's grep prevents a crash from blocking a push), so verdict codes must sit at >=3 with 1/2 reserved for unclassified failure — sharpening MINIMAL-CHURN's own 1=stale pairing. Second addition: .husky/pre-push:310 is a fourth copy of the pattern where misclassification changes push blocking, strengthening the finding's severity story and belonging in scope. STRUCTURAL contributes the documented-and-test-pinned code-map discipline; COPYABILITY contributes the deferral shape (future payload goes through HarnessDiagnostics with a verdict note, never bespoke JSON). Ripple is bounded: verify gate slots consume only zero/nonzero, remaining consumers are the truth-up/pre-push handlers and the smokes being updated deliberately.",
   "constraintRulings": [
    "C-018: do not introduce any machine-readable status envelope for baseline truth-up classification (neither a new format nor HarnessDiagnostics emission); do instead make dedicated per-tool exit codes at the existing return sites the complete verdict contract, documented in each tool's usage header and pinned by exit-code test assertions.",
    "C-018: do not keep any stdout grep or SIGPIPE-herestring defense as a fallback classification path; do instead delete all four grep branches (scripts/git/baseline-post-merge-truth-up.sh lines ~170/~205-215/~252 and .husky/pre-push:310) and branch on exit codes only, keeping captured output solely for tail display.",
    "C-018: do not assign verdict meaning to exit codes 1 or 2; do instead place verdict codes at 3+ and route unclassified nonzero to the could-not-run/generic advisory, because an uncaught bun exception exits 1 and must never read as a stale-baseline verdict (or block a push).",
    "C-018: do not modify examples/lint-ratchet-demo's truth-up hook for this item — at the audit pin it already classifies by exit code only; do instead treat the demo as the reference shape, converge the main repo to it, and correct any guide or review text claiming the demo greps prose.",
    "C-018: do not add a bun classifier helper or HARNESS_DIAGNOSTICS_OUTPUT temp-file plumbing to the git hooks; if a payload consumer (counts, repair commands) ever appears, do extend the existing HarnessDiagnostics emission (verdict-as-data note convention) rather than minting a bespoke per-tool status JSON."
   ]
  },
  {
   "candidateId": "C-020",
   "chosenDirection": "Relocate only the two executables (agent-run.sh, agent-wait.sh) via git mv from .claude/skills/agent-cli/scripts/ to a neutral repo-owned scripts/agent-cli/ (sibling of the scripts/ai-hooks/ adapter-boundary precedent), keeping them plain bash with no build step so the pre-bun-install constraint holds. Skill trees become documentation-only projections under the existing single-source model: .claude/skills/agent-cli stays canonical for docs, the .codex mirror stays generated. No new projection machinery. Verified touch list: (1) hand-edit one path string in canonical SKILL.md (10 refs), references/portability.md, references/trailer-contract.md; (2) scripts/tests/test-skill-dispatch-wrappers.sh — WRAPPER/WAITER defaults at lines 38-39, the two smoke-subjects headers at lines 12-13, and remaining literal refs; (3) scripts/path-policy/path-policy.test.ts line 227; (4) harness.controls.json skill/agent-cli control — delete the {\"path\":\"scripts\",\"kind\":\"canonical-only\"} overlay (~line 2758); gitignoreOptIns stay unchanged (skill dirs still hold docs; scripts/ is tracked normally so the new home needs no opt-in); (5) drop the now-moot {prefix: \".claude/skills/\", extension: \".sh\"} shellSurfaces clause in scripts/path-policy/path-policy.ts line 214 — after the move no skill ships .sh, and the new home is auto-covered by the existing scripts/ prefix clause (no ShellCheck glob edit exists or is needed); (6) regens: bun run harness:skills:refresh (rewrites all 12 .codex-side refs), bun run test:scripts:subjects (rewrites path-policy-smoke-subjects-data.ts), then bun run harness:check. Documented-decision carries land in the same change: amend the docs/ai-harness.md recorded rejection (~lines 313-319) to retire only the \"skill stays self-contained\" placement clause while preserving the no-TS-rewrite decision and recording the new invariant (executables in scripts/agent-cli/; skill dirs are doc projections); add one sentence to the Adapter Boundary section stating agent-cli dispatch executables follow the same neutral-home rule as hooks; rewrite portability.md's copy story (line ~16 wholesale-copy claim, line ~75 shellcheck-coverage claim) to the two-piece copy set (scripts/agent-cli/ + your provider's thin skill dir, plus the pre-existing .cursor/cli.json note). Invocations stay repo-root-anchored ($(git rev-parse --show-toplevel)/scripts/agent-cli/...); no symlinks or stub entrypoints in provider trees. Historical backlog docs referencing the old path are frozen records and stay untouched. Size: S-to-M single leaf (MINIMAL-CHURN's corrected accounting holds: ~6 hand-edited files with one string substitution, 1 manifest overlay, 1 path-policy clause, 2 regens, 3 substantive doc paragraphs); risk low because the dispatch-wrapper smoke suite re-verifies both entrypoints at the new path and no invocation relies on skill-relative resolution.",
   "rationale": "All three proposals and the solo review converge on the same reshaped core, so the decision is about accuracy of the touch list and the justification frame. MINIMAL-CHURN wins on scope discipline and churn accounting but misstates the shell-lint mechanism (lint-shell.sh selects via path-policy shellSurfaces, not a repo-wide find — verified at scripts/path-policy/path-policy.ts:208-216); its conclusion that lint follows the move automatically survives via the scripts/ prefix clause, and its gitignoreOptIns-unchanged call is correct where STRUCTURAL erred. STRUCTURAL contributes the structurally honest deletion of the now-moot .claude/skills/ shellSurface clause and the explicit rejection of the mirror-the-scripts-into-.codex alternative (two committed executable copies violate the repo's duplicates-are-defects rule). COPYABILITY supplies the decisive justification — docs/ai-harness.md's Adapter Boundary section states the exact rule this layout violates, and the bundling defense is hollow because every invocation is already repo-root-anchored (verified: 10 refs in canonical SKILL.md, 12 in the .codex mirror, none skill-relative) — plus the one-sentence Adapter Boundary extension that converts two patterns into one stated rule. The candidate is upgraded from contested cleanup to restoring the repo's own flagship invariant on its most-copied artifact, consistent with the public-harness-reference goal. The lanes' original direction materially overstated the work (a projection system already exists via harness:skills:refresh) and missed that the move contradicts two recorded texts (the 2026-07-07 rejection clause and portability.md's wholesale-copy contract), so those amendments are mandatory in-scope carries per the documented-decision rule. The doc-only fallback is rejected: it cures the broken-copy trap by instruction while leaving the reference repo demonstrating the anti-pattern.",
   "constraintRulings": [
    "C-020: Do not build any new projection, generation, or linking system for provider skill entrypoints (the original direction's 'generate or link each provider skill's thin documentation and entrypoint projection'); do instead edit the canonical .claude sources, delete the scripts canonical-only overlay from the skill/agent-cli control in harness.controls.json, and rerun harness:skills:refresh — the .codex mirror machinery already exists.",
    "C-020: Do not symlink or stub agent-run.sh/agent-wait.sh entrypoints into provider trees; do instead keep every documented invocation repo-root-anchored via $(git rev-parse --show-toplevel)/scripts/agent-cli/....",
    "C-020: Do not resolve the broken-copy problem by mirroring the executables into .codex (two committed copies held together by the generator); the duplicates-are-defects adapter-boundary rule makes that worse than both the move and the status quo.",
    "C-020: Do not land the relocation without the same-change documented-decision amendments: amend the docs/ai-harness.md recorded rejection (~line 313) to retire only the 'skill stays self-contained' clause while preserving the no-TS-rewrite decision, and rewrite portability.md's 'copy .claude/skills/agent-cli/ wholesale' story (and its line-75 shellcheck-coverage claim) to the two-piece copy set.",
    "C-020: Do not rewrite the dispatch scripts in TypeScript or add any build step as part of this move; the pre-bun-install plain-bash constraint from the 2026-07-07 decision remains binding.",
    "C-020: Do not edit frozen historical backlog documents that reference the old .claude/skills/agent-cli/scripts/ path (e.g. agent-pain-points-2026-07-21, pain-points-2026-07-29); they are records, not live surfaces.",
    "C-020: Do not hand-edit generated surfaces (path-policy-smoke-subjects-data.ts, the .codex skill mirror, generated smoke-subject blocks); do instead edit the smoke-subjects headers and canonical sources, then regenerate via test:scripts:subjects and harness:skills:refresh and validate with harness:check."
   ]
  },
  {
   "candidateId": "C-021",
   "chosenDirection": "Split into three independent per-family leaves using each family's existing idiom; no cross-family adapter/kernel. (1) drift-ai layer-direction (M, land first — only leaf with a live external-adopter defect): replace makeEmptyCheckConfig in layer-direction-check-config.ts with a real typed LayerDirectionConfig parsed through the existing CheckConfigMetadata/config-readers idiom (ghost-files-check-config.ts is the template): rules array {id, sourceLayer, targetLayer, sourcePrefix, targetPrefix, hint} with layer labels as free strings (drop the ServerLayer union), plus allowedEdges pairs. Built-in default = ZERO rules, per the family's own README contract (scripts/drift-ai/README.md:682-684: foreign targets get built-in defaults) and the comments.ts:112 precedent; Musi's two rules and two allowed-edge exceptions (layer-direction.ts:33-63) move into the committed root drift-ai.config.json, with an example added to drift-ai.config.example.json and the README starter-config section. Emit an explicit notice both when the check runs with no rules configured and when configured rules match zero inventory files, so an empty result is never silently authoritative. buildLayerDirectionFindings takes the config as a parameter. No new config file, so no config-surface-manifest change expected (note: drift-ai.config.json is not currently in the manifest; verify against docs/guides if touching the example file counts as a new surface). (2) code-intel overview (S): extract the repo conventions in overview-call-targets.ts:93-181 — \"/services/\" fragment, \"/socket/\" and \"/utils/character-campaign\" fragments, broadcast/emit/*Broadcast name prefixes/suffixes, and the io/to/emit identifier spellings — into one exported plain-data OverviewConventions object in a new overview-conventions.ts, with predicates closing over it and collectImportedCallTargets/collectBroadcasts taking it as a defaulted last parameter (call sites unchanged). The .to(...).emit(...) chain AST shape-matching stays in place as mechanism; only names/fragments become data. File-level doc comment must state both \"Musi conventions — an adopter swaps this module wholesale\" and \"these are heuristics; absence of matches is not evidence of absence\". No config-file mechanism. (3) drift-triage (XS): extract only the ordered pathArea prefix taxonomy (triage-packet-group.ts:145-153; order-sensitive — scripts/drift-ai/ before scripts/) into a named readonly {prefix, area}[] policy constant with the existing first-segment fallback, and update scripts/drift-triage/MODULE.md in the same leaf. Leave sourceFamily (lines 124-134) alone: semgrep/dolos/drift is the pipeline's closed input taxonomy bound to CoreLane[\"source\"], not repo policy. Each leaf pins its shipped policy data in a focused test. Post-split severity: high stands for the drift-ai leaf only; code-intel and drift-triage are copyability polish.",
   "rationale": "Copyability wins as the base with grafts from both runners-up. The decisive dispute was whether Musi's layer rules ship as drift-ai built-in defaults (solo review, minimal-churn, structural) or move to Musi's committed drift-ai.config.json with a zero-rules built-in default (copyability). Tree evidence settles it for copyability: the family's README (lines 682-684) already promises foreign targets get built-in defaults distinct from Musi's committed config, and comments.ts:112 documents the exact precedent of relocating Musi-specific policy into the config file so non-Musi repos are unaffected; shipping Musi topology as code defaults would violate the family's own published contract and bake a repo-specific exception allowlist into a nominally portable analyzer. The zero-rules default is safe because layer-direction is already opt-in (runByDefault: false, verified). Grafted from minimal-churn: the zero-match notice (extended to also fire when configured rules match no files), and both of its trims — drift-triage sourceFamily stays (closed CoreLane[\"source\"] input taxonomy, verified) and the drift-triage leaf shrinks to XS (pathArea already degrades gracefully via its generic fallback). Grafted from structural: the mechanism-vs-policy line in code-intel (parameterize identifier names and path fragments as data; keep the Socket.io chain-shape AST logic in the kernel) and defaulted-parameter injection so overview-query.ts call sites do not change. All three proposals and the solo review agree on rejecting the lanes' original cross-family adapter framing, which couples three independently-copyable script families and contradicts drift-ai's existing per-check config architecture. One factual correction to copyability: drift-ai.config.json is not currently registered in eslint-config/config-surface-manifest.json (grep count 0), so its manifest claim was wrong, though the practical conclusion (no manifest churn since no new config file) holds. Key files: /workspace/scripts/drift-ai/layer-direction.ts, /workspace/scripts/drift-ai/layer-direction-check-config.ts, /workspace/scripts/drift-ai/ghost-files-check-config.ts, /workspace/scripts/drift-ai/README.md, /workspace/scripts/code-intel/overview-call-targets.ts, /workspace/scripts/drift-triage/triage-packet-group.ts.",
   "constraintRulings": [
    "Do not build a cross-family repository-policy adapter, shared kernel, or injection layer across drift-ai/code-intel/drift-triage; do extract policy per family in each family's existing idiom as three independent leaves (drift-ai M, code-intel S, drift-triage XS).",
    "Do not ship Musi's layer-direction rules or allowed-edge exceptions as drift-ai built-in defaults; do default to zero rules with an explicit no-rules-configured notice and move Musi's rules into the committed drift-ai.config.json plus drift-ai.config.example.json, per the README contract (scripts/drift-ai/README.md:682-684) and the comments.ts precedent.",
    "Do not let layer-direction return a bare empty result when its rules match nothing; do print an explicit notice when zero rules are configured or configured rule prefixes match zero inventory files.",
    "Do not add a config-file mechanism to code-intel; do extract Musi conventions into one swappable plain-data module (overview-conventions.ts) passed as a defaulted parameter, with a doc header stating both swap-wholesale and heuristic-absence caveats.",
    "Do not move the Socket.io .to().emit() chain AST shape-matching out of overview-call-targets.ts; do parameterize only the identifier spellings, path fragments, and broadcast name prefixes as conventions data.",
    "Do not extract drift-triage's sourceFamily evidence-source switch (it is the pipeline's closed input taxonomy typed by CoreLane[\"source\"], not repo policy); do extract only the ordered pathArea prefix taxonomy into a named policy module and update scripts/drift-triage/MODULE.md in the same leaf.",
    "Do not create any new config file for these leaves (avoids config-surface-manifest churn; note drift-ai.config.json itself is currently unregistered in the manifest); do extend the existing drift-ai.config.json schema only."
   ]
  },
  {
   "candidateId": "C-023",
   "chosenDirection": "Synthesis on the STRUCTURAL base (\"protocol module owns the whole protocol\"), grafting COPYABILITY's precedent correction and boundary-contract comment and MINIMAL-CHURN's verified micro-fixes. Four moves, size M, severity medium. (1) First-class lifecycle arms in daemon-protocol.ts: request command union gains {kind:\"ping\"}, ok-response gains a distinct pong arm; protocol exports buildPingRequest(id) and isPongResult; daemon-server pongResponse returns the real arm (replacing the fabricated {kind:\"results\", header:\"pong\", results:[]} at daemon-server.ts:216); lifecycle-probe.ts builds its request via the protocol module (replacing hand-written JSON at :44-48) and requires the pong arm instead of accepting any ok:true (:102); bump CODE_INTEL_DAEMON_PROTOCOL_VERSION to 2 — the bump is load-bearing, not cosmetic: with a tightened probe, a same-version stale daemon's fabricated pong would misroute to \"unverified\" instead of \"stale\"→restart; the version gate (probe :99, client :102-104) makes migration one automatic restart. (2) Request side: protocol-exported decodeDaemonRequest(payload) using Zod schemas for the envelope plus the SIX routable command arms (def, defName, exports, refs, dependents, tests — overview is intentionally non-routable) and ping, each statically bound `satisfies z.ZodType<...>` against the EXISTING compiler-owned types (in-tree binding precedent: packages/shared/src/schemas/homebrew.ts:234; cli-args.ts proves only that zod is already a scripts dependency). This deletes the daemon-server.ts:199 cast (which is already marked but whose marker overclaims — id is never validated, fixing MINIMAL-CHURN's confirmed non-string-id unsoundness) and the kind-only isDaemonRoutableCommand lie (daemon-commands.ts:18-22); retype DAEMON_ROUTABLE_COMMAND_KINDS with `satisfies readonly ExecutableCliCommand[\"kind\"][]` so renames fail typecheck; unknown/non-routable kinds fail decode and map to the existing DAEMON_FALLBACK_ERROR_NAME message so client fallback semantics are unchanged. Add z.strictObject plus a mutual-assignability type test beside protocol tests to cover the one-way satisfies gap. (3) Response side: KEEP shallow validation (upholds the documented daemon-protocol.ts:48-49 decision, H18/H19, merge 57ef569e5, 2026-08-01) but make it honest — narrow isDaemonQueryResult's claimed type to the shallow wire shape it actually checks, put ONE `// type-assertion-boundary: json` cast at the single consumption point in daemon-client.ts interpretResponse (matching the existing :145 marker), and expand the :48-49 comment into a 3-4 line boundary-contract note (why shallow: protocolVersion lockstep is the defense, both ends compiler-owned in one repo, daemon exists for latency). Optionally fold the duplicated response-envelope checks (daemon-client.ts:110-129 / lifecycle-probe.ts:82-101) into one shared shallow envelope decoder with caller-specific failure taxonomies. (4) Contested-question resolution: all three independent proposals converged on keep-shallow-but-honest; the copyability goal argues FOR differentiated documented boundary discipline (contrast with the tRPC/shared-Zod untrusted boundary), not uniform maximal validation. Moves 1-2 are uncontested. If a future decision moves H18/H19, response arms get the same statically-bound-schema pattern, never z.infer derivation, and the scope must carry the protocol-comment update. Key files: scripts/code-intel/{daemon-protocol,daemon-server,daemon-commands,daemon-client,lifecycle-probe}.ts, plus their tests; precedent file packages/shared/src/schemas/homebrew.ts:234.",
   "rationale": "STRUCTURAL wins as the base: its organizing principle (every arm that crosses the socket is declared, encoded, and decoded by daemon-protocol.ts) directly resolves all four evidence items of L02-003, and its full request decode deletes both dishonest narrowings outright rather than re-marking them, at negligible runtime cost (six flat arms + ping, once per request — nothing like per-result validation). COPYABILITY grafts in the only correct precedent citation (verified: cli-args.ts has no satisfies-binding; homebrew.ts:234 does — citing the wrong one would send the implementing delegate chasing a nonexistent pattern) plus the boundary-contract comment expansion and the decisive stance that the public-harness-reference goal favors keep-shallow-but-honest, not the full codec. MINIMAL-CHURN loses as base because it under-fixes the finding's central defect — it leaves isDaemonQueryResult's false `value is CodeIntelQueryResult` promise untouched — and its no-version-bump stance is only consistent because it also leaves the probe's accept-any-ok:true looseness in place; but it contributes three verified ground-truth corrections the leaf brief must carry (the :199 cast is already marked json but the marker overclaims since id is unvalidated; non-string id flows through typed as string; DAEMON_ROUTABLE_COMMAND_KINDS is readonly string[] so renames compile silently) and its severed-scope framing. The contested response-side question resolves without a panel escalation: all three independent proposals, written from different lenses, converged on upholding the one-day-old documented H18/H19 decision (daemon-protocol.ts:48-49) while making the predicate honest — unanimity across adversarial lenses is the strongest available signal under the documented-decision-conflicts rule. The lane's original single-codec direction is rejected: it would silently overturn that landed decision, invert type ownership for CodeIntelQueryResult/IntelResult (internal types used tree-wide) from one IPC seam, and add deep per-result cost on a latency-motivated hot path. Size M holds for the synthesis; the original codec trends L.",
   "constraintRulings": [
    "Do not implement C-023 as the lane's single runtime discriminated codec with wire types derived via z.infer — do instead write Zod schemas statically bound to the EXISTING compiler-owned types (`satisfies z.ZodType<CodeIntelDaemonRequest>` / per-arm equivalents) so drift fails typecheck and type ownership stays with the compiler, never the codec.",
    "Do not deep-validate daemon response result-arm interiors — that silently overturns the documented shallow-validation decision at scripts/code-intel/daemon-protocol.ts:48-49 (H18/H19, merge 57ef569e5, landed 2026-08-01) on a latency-motivated hot path; do instead narrow isDaemonQueryResult's claimed type to the shallow wire shape it actually checks and confine the residual trust to one marked `type-assertion-boundary: json` cast in daemon-client.ts interpretResponse; any future reversal of H18/H19 must use the same statically-bound-schema pattern and carry the daemon-protocol.ts comment update in scope.",
    "Do not cite scripts/code-intel/cli-args.ts as the static schema-to-type binding precedent (it uses plain z.object with no satisfies binding; it proves only that zod is already a scripts dependency) — do instead cite packages/shared/src/schemas/homebrew.ts:234 (`satisfies Record<HomebrewEntryType, z.ZodType<...>>`) as the in-tree pattern.",
    "Do not describe daemon-server.ts:199 as an unmarked cast (it already carries `type-assertion-boundary: json`) — do instead fix what is actually wrong there: the marker overclaims (`id` is never validated; a non-string id flows through typed as string) and the envelope narrowing is kind-only; the replacement decode must validate id as a string.",
    "Do not tighten lifecycle-probe pong acceptance (requiring a real pong arm instead of any ok:true) without bumping CODE_INTEL_DAEMON_PROTOCOL_VERSION — a same-version stale daemon's fabricated pong would misroute to 'unverified' instead of 'stale'-then-restart; do instead bump the version so skewed daemons route through the existing stale-restart path.",
    "Do not model the daemon protocol as seven routable command arms — overview is intentionally non-routable; do instead decode exactly the six routable arms (def, defName, exports, refs, dependents, tests) plus ping, and map every other kind to the existing DAEMON_FALLBACK_ERROR_NAME fallback so client one-shot-fallback semantics are unchanged."
   ]
  },
  {
   "candidateId": "C-024",
   "chosenDirection": "Adopt the COPYABILITY proposal as the skeleton, grafting MINIMAL-CHURN's scoping discipline and STRUCTURAL's presence-exposure mechanics; drop the descriptor-table engine from both the lane direction and the solo-review reshape. Converge every CLI in the two stacks on the one existing canonical pattern — parseCli options array + per-tool Zod z.object schema + hand-written literal usage + hand-written compiler-checked result assembly — instead of introducing a derivation layer. Slices, reordered so the cheap pattern-proving work lands first: (A, S) additive substrate extension: expose parser-observed presence on ParsedCli (insertion-ordered readonly seen-option names from parseCli's existing seen Map); use it in drift-triage-options.ts to delete firstPacketSelectionFlag and the PACKET_SELECTION_FLAGS list (dependency becomes a post-parse check keyed by a group constant, diagnostic stays byte-identical: first-seen-in-argv-order names the error); in the same slice add ~10-line parity tests to both drift-triage-options.ts and cli-args.ts asserting parseCli option names == Zod schema keys and usage text mentions every option name (presence, not derivation — cli-args usage lines are example invocations and must stay hand-owned bytes). (B, M, may split further by subcommand family along existing per-subcommand test boundaries) retire the SubcommandSpec callback-record layer: move drift-ai subcommands off subcommand-args.ts's mutable accumulators onto per-subcommand parseCli+Zod parses, with SubcommandBaseOptions (--format/--output/opt-in --config) and the bounded-history five-field contract becoming shared Zod shape fragments composed by spreading; reproduce readUntrimmedPath/readValue/\"does not accept a value.\" error text byte-exactly via valueErrorMessage/inlineValueErrorMessage and Zod fragments (cli-args.ts's own hand-parser-to-parseCli migration is the precedent that this identity is reproducible); update the subcommand-args.ts:6-9 deferral comment in-slice per the documented-decision convention; stay flag-surface-neutral. Acceptance criterion for every slice is the existing byte-pinned help/error tests passing unchanged. Non-goals: no cli-descriptors.ts or any single-table derivation of specs/schemas/usage/results; no changes to cli.ts walk mechanics or error identity beyond the additive presence field; no unification of drift-ai main vs subcommand flag surfaces; no extension to code-intel/lint-ratchet. Severity stays medium; net size drops from L to S+M; leafability needs-split stands with this new split; priorPackOverlap novelty (CQ25-10, not covered by CQ25-45..47) holds.",
   "rationale": "COPYABILITY wins because it re-diagnoses the finding correctly and matches the repo's bar. Verified against the tree: the two parsers the finding cites as five-way registries (drift-triage-options.ts, cli-args.ts) are already on parseCli+Zod where the schema owns spelling/default/validation/typing and result assembly is compiler-checked against Zod inference — so the invisible-omission class there is only options-array/schema agreement, usage presence, and the PACKET_SELECTION_FLAGS argv rescan, all of which parity tests plus a 15-line presence extension eliminate without an engine. The genuinely anti-copyable artifact is the subcommand stack (subcommand-args.ts callback records, semgrep-candidates-args.ts:49-108 accumulators, 14 callers), which MINIMAL-CHURN leaves standing (fatal: it fixes the least-broken part and tests around the most-broken part) and which STRUCTURAL fixes only by building a new derivation layer whose const-generic typing it itself flags as cast-risk and which contradicts cli.ts's documented adopt-what-fits policy and the repo's public-harness-reference goal (judged on copyability). Converging subcommands onto the one existing pattern discharges the code's own anticipated path (subcommand-args.ts:7-9) without threatening the deferral's stated reason (main-command flag-surface stability), and cli-args.ts's prior hand-parser migration proves byte-identical error reproduction on this substrate is achievable. Grafts: MINIMAL-CHURN's rulings that result assembly and cli-args usage must stay hand-owned (the two main cast/byte-drift sites), and STRUCTURAL's insertion-ordered seenOptions design for the presence field. The prior-pack tensions (leaf 30 \"triage, not a sweep\"; CQ25-10 narrowing) are respected: scope stays inside the two named stacks, uses existing substrate, and the largest slice completes a documented in-code plan rather than sweeping.",
   "constraintRulings": [
    "C-024: do not build a descriptor-table engine or new cli-descriptors layer (no single table deriving parseCli specs, Zod schemas, usage blocks, or result assembly — this rejects both the lane direction and the solo-review reshape); do instead converge both CLI stacks on the existing parseCli + per-tool Zod-schema pattern and guard registry agreement with parity tests (parseCli option names == Zod schema keys; usage mentions every option name).",
    "C-024: do not derive or generate help/usage or error text from option metadata in any slice (cli.ts S1 spike record and pinned-byte tests forbid it); do instead keep usage as hand-written literal blocks, byte-identical under the existing pinned tests, which are every slice's acceptance criterion.",
    "C-024: do not derive typed result assembly from a table (the cast-free mapped-type trick is a hard-fail risk under the no-type-assertion policy); do instead keep hand-written per-tool result assembly, which is already compiler-checked against Zod-inferred schema output.",
    "C-024: do not keep raw argv rescans for option-dependency diagnostics (drift-triage firstPacketSelectionFlag); do instead expose parser-observed presence as an additive insertion-ordered seen-options field on ParsedCli and drive the byte-identical diagnostic from it.",
    "C-024: do not migrate drift-ai subcommands to any new pattern; do instead retire SubcommandSpec callback records and mutable accumulators by moving each subcommand onto per-subcommand parseCli+Zod parses with shared Zod shape fragments for the universal and bounded-history options, staying flag-surface-neutral and updating the subcommand-args.ts:6-9 deferral comment in the same slice.",
    "C-024: do not extend CLI-substrate work beyond the drift-ai and drift-triage stacks (prior-pack leaf 30: triage, not a sweep); code-intel and lint-ratchet stay out of scope.",
    "C-024: sizing/staging ruling — replace the solo review's three-leaf engine-first split with: slice A (S) substrate presence field + drift-triage rescan deletion + parity tests for both parseCli stacks; slice B (M, further splittable along existing per-subcommand test boundaries) subcommand convergence; net size L -> S+M, severity stays medium."
   ]
  },
  {
   "candidateId": "C-026",
   "chosenDirection": "Extract a pure syntax-classification kernel, scripts/lib/ts-module-refs.ts, with exactly one exported function and two types: extractModuleRefs(source: ts.SourceFile): readonly ModuleRef[], where ModuleRef = { specifier, kind: \"import\" | \"export-from\" | \"dynamic-import\", typeOnly } (typeOnly always false for dynamic-import; one ref per occurrence, no dedup). The kernel imports { ts } from \"ts-morph\" — load-bearing, not convenience: both stacks run on ts-morph's bundled compiler, and a direct `typescript` import would risk a second compiler instance with divergent SyntaxKind numbering; state this invariant in a comment. Header comment declares the module the single source of truth for \"what counts as a runtime import edge\" and names both consumers. Kernel owns only the currently-duplicated semantics: recursive visit of the three syntax families, StringLiteral/NoSubstitutionTemplateLiteral specifier acceptance, whole-clause type-only (phaseModifier/isTypeOnly), per-binding all-type-only rule, default/namespace/bare-side-effect/`export * from` = runtime, empty-clause edge cases. No injection points of any kind. Per-stack, semantics unchanged: drift-ai deletes extractSpecifiers and consumes ModuleRef (parsing incl. readFileSync/createSourceFile/scriptKind, isResolutionCandidate, resolveSpecifier, partiality accounting, and AND-merge stay put); code-intel's collectImportEdges calls extractModuleRefs(sourceFile.compilerNode) — with a one-line boundary comment naming the \"drop to compiler API at pure-syntax seams\" pattern — then applies its unchanged vi.mock specifier filter, kind→Via mapping (import→direct, export-from→re-export, dynamic-import→dynamic), runtime = !typeOnly, resolver/self-edge-skip/uniqueEdges/sort. Two gate layers: (1) scripts/lib/ts-module-refs.test.ts characterization table of inline snippets → expected refs covering the full edge-case matrix (whole-clause import type, per-binding type incl. mixed, default, namespace, bare side-effect import, empty named clauses, export * from, export * as ns from, export type {...} from, per-binding export type, dynamic import string and template literal, non-literal dynamic import ignored) — the table doubles as the human-readable classification spec; (2) a small cross-stack fixture parity test asserting drift-ai's and code-intel's edge builders classify identical sources identically, kept as a re-fork tripwire on the mapping layers. Residual duplication accepted deliberately: literalFirstArgument's 6-line compiler-node echo inside the kernel and code-intel's vi.mock descendant walk are policy above the kernel line. Size M, single leaf (no split).",
   "rationale": "STRUCTURAL wins: it implements the solo reshape with the tightest contract (one function, two types, zero injection points), is the only proposal that gets the compiler-instance invariant right — verified against the tree, drift-ai imports ts from ts-morph (import-cycles-graph.ts:16) while root separately pins typescript ~6.0.3, so COPYABILITY's \"sole dependency typescript\" would create a second-compiler SyntaxKind hazard — and it keeps the cross-stack parity test that COPYABILITY drops, repositioned as a re-fork detector, matching the repo's locked-by-a-gate bar. COPYABILITY supplies the best kernel-vs-parity-test-only argument (the stacks speak two API dialects for the same rule: hand-rolled phaseModifier check at import-cycles-graph.ts:281 vs ts-morph isTypeOnly() at import-graph.ts:87, so library-upgrade-driven syntax evolution silently splits them and fixture parity only catches anticipated cases) plus graftable framing: single-source-of-truth header, the commented compilerNode seam as taught pattern, fixture table as readable spec — all grafted. MINIMAL-CHURN is a coherent zero-risk floor but leaves the extraction traversal double-encoded, which is exactly where new TS syntax families land, so the finding's core problem (fixes made twice) survives for half the surface; its verified predicate-equivalence audit and enumerated edge-case matrix are grafted into the characterization table. The synthesis materially reshapes the lane's original policy-injected repo-agnostic kernel: resolution (portable per-tsconfig vs Musi workspace model), vi.mock filtering, partiality accounting, and merge/dedup semantics differ by design between the two products, and parameterizing them would rebuild a framework around intentional divergence, hurting the repo's harness-copyability goal.",
   "constraintRulings": [
    "C-026: do not build a policy-injected, repository-agnostic edge-builder kernel for drift-ai/code-intel (no injected resolution, filter, partiality, or dedup policy); do instead extract a pure syntax-classification module in scripts/lib (extractModuleRefs over raw ts.SourceFile returning {specifier, kind, typeOnly}) and leave resolution, vi.mock filtering, candidate/unresolved accounting, and edge-merge/dedup semantics per-stack and unchanged.",
    "C-026: do not import the `typescript` package directly in the shared kernel; do import { ts } from \"ts-morph\" so kernel and both consumers share one compiler instance (SyntaxKind parity with nodes callers pass in), and state that invariant in a comment.",
    "C-026: do not pull file reading or parsing (readFileSync, ts.createSourceFile, scriptKind selection) into the kernel; do keep parsing per-stack and pass an already-parsed ts.SourceFile (code-intel crosses via sourceFile.compilerNode with a one-line boundary comment).",
    "C-026: do not rely on kernel unit tests alone to lock cross-stack agreement; do add a small cross-stack fixture parity test (drift-ai edge builder vs code-intel edge builder on identical sources) as a re-fork tripwire in addition to the kernel's characterization-table test.",
    "C-026: do not treat this as an L-sized needs-split item; do land it as a single M leaf (kernel + two thin call-site rewrites + both test layers)."
   ]
  },
  {
   "candidateId": "C-031",
   "chosenDirection": "STRUCTURAL base with COPYABILITY's didactic-comment graft and MINIMAL-CHURN's lint-spelling caution. Single file, scripts/drift-ai/semgrep-rule-manifest.ts, zero runtime diff, zero test churn, size S: (1) anchor both key arrays to their arm types — `const LOCAL_SOURCE_KEYS = [...] as const satisfies readonly (keyof SemgrepLocalRuleSource)[]` and the REGISTRY_SOURCE_KEYS/SemgrepRegistryRuleSource counterpart — matching the existing drift-ai house pattern (commit-intent.ts:53, prototype-subcommand-definitions.ts:50); arrays stay assignable to assertManifestKeys's `readonly string[]` param, no call-site changes. (2) Add a named per-arm completeness assertion (e.g. `type AssertAllKeys<T extends never> = T; type _LocalKeysComplete = AssertAllKeys<Exclude<keyof SemgrepLocalRuleSource, (typeof LOCAL_SOURCE_KEYS)[number]>>;`, mirrored for registry) so an added field also breaks the build; pick the spelling that passes the repo's unused-var/unused-expression lint. (3) Extend the existing module-header comment (which already teaches the exit-2/exit-0 taxonomy) with two sentences: the key arrays are compiler-proven against the arm types so any field add/remove/rename fails the build at this file, and the parser deliberately stays bespoke because the manifest-path + 'sources[i].key' error wording and DriftAiError->exit-2 mapping are a tested contract, with a schema-library port scoped as a separate leaf. Together with the builders' existing return/contextual typing this statically closes every drift surface L02-011 named; the arm types in semgrep-rule-sources.ts remain the single authoritative contract. Acceptance: typecheck + existing semgrep-rule-manifest/semgrep-rule-sources/semgrep-candidates-args tests pass byte-identically; a deliberate scratch mutation (drop 'sha256' from LOCAL_SOURCE_KEYS, or add a field to SemgrepLocalRuleSource) must fail compile, not tests. Severity low, size S stand; leafability single-leaf confirmed.",
   "rationale": "All three proposals independently converged on the solo review's reshaped static-proof fix, which is strong confirmation the reshape is right: of the finding's claimed four-way sync hazard, only the two string[] key arrays are genuinely unsynchronized — the arm builders (manifest lines 74-129) and the CLI collector are already compiler-proven against the union via return/contextual typing (verified: a field-type change like `sha256: string|null` -> `boolean` fails the builder's return-typed literal, so even value-type drift is caught there). STRUCTURAL wins as base for the most accurate closure/residual-gap accounting, verified house-precedent grounding for the `as const satisfies` idiom, the fullest rejected-alternatives ledger, and a concrete falsification recipe. COPYABILITY's header-comment graft is adopted because the repo is judged on copyability as a public harness reference (per project goal): documenting both the proof and the deliberate bespoke-vs-zod boundary is half the deliverable, and it converts drift-ai's dual validation idioms (zod in cli-args.ts/duplicate-schemas.ts, bespoke here) from unexplained inconsistency into a documented decision. MINIMAL-CHURN's lint caution on the assertion spelling is kept as an implementation note. The lane's headline zod-authoritative arm is rejected per repo precedent (code-quality-2026-07-25 34-PLAN step-7: behavior-preserving zod ports of bespoke drift-ai parsers are separately scoped leaves with explicit message/exit-code parity criteria) — it would relocate rather than delete the bespoke layer (issue-formatter must reproduce manifest-path prefix, 'sources[i].key' wording, trimSentencePeriod re-wrapping, known-pack license-conflict cross-check, exit-2 mapping) and exceeds size S; the typed arrays do not preclude a later port. The lane's 'centralize arm builders' arm is rejected as proof-neutral coupling of two distinct error contracts. Lane's severityHint medium / sizeHint M drop to low / S with the four-way claim halved and the fix reduced to ~15 type-level lines.",
   "constraintRulings": [
    "Do not port the semgrep rule-source manifest parser (scripts/drift-ai/semgrep-rule-manifest.ts) to a zod/runtime-authoritative schema in this leaf; do instead keep the bespoke parser and its tested error contract (manifest-path + 'sources[i].key' wording, DriftAiError -> exit 2, known-pack license-conflict rejection, shared readLocalRuleConfig/readRegistryPack reuse), and if a zod port is wanted file it as its own separately scoped leaf per the code-quality-2026-07-25 34-PLAN step-7 shape (message parity incl. keyPath/index positions, strict-key wording, cross-field checks, exit-code mapping).",
    "Do not centralize the manifest arm builders with the CLI collector in semgrep-rule-sources.ts; do instead leave both in place — each is already compiler-proven against the union via return/contextual typing and they serve different error contracts (manifest strict-key keyPath wording vs CLI flag defaults).",
    "Do not extract a generic key-exhaustiveness assertion utility into scripts/lib as part of this leaf (even though sibling findings could reuse one); do instead keep the named 4-line inline idiom in semgrep-rule-manifest.ts, and only extract in a later leaf that brings multiple real call sites.",
    "Do not co-locate the allowed-key arrays with the arm types in semgrep-rule-sources.ts; do instead keep them in semgrep-rule-manifest.ts with keyof anchoring — the compiler enforces the cross-file link, and strict-key rejection is a manifest-parser-only concern."
   ]
  },
  {
   "candidateId": "C-033",
   "chosenDirection": "Producer-side identity fencing, one branded type, dedicated liftable module. (1) New scripts/drift-triage/triage-item-id.ts (~50-100 lines) as the sole ID owner: branded TriageItemId (brand minted only by one private function inside this module — no assertion markers leak elsewhere) plus three constructors taking structured identity records — driftItemId({inputPath, check, index, file}), pairItemId(structured locations: path + optional range, so pair identity needs no re-decision if L02-014 lands), semgrepItemId({path, ranges, identity}). Relocate pairKey here (re-export from triage-report-support.ts for its other importers). Grammars stay verbatim with readable drift:/pair:/semgrep: prefixes, except the semgrep JSON.stringify(row.ranges) segment becomes a deterministic sorted per-range startLine.startCol-endLine.endCol encoding joined with a fixed separator — kills property-order dependence and hand-copy-hostile length while preserving column granularity (no new merge collisions) and staying visually cross-checkable against the item's locations. No digest segment. (2) Thread the brand producer-side only: TriageItem.id, MutableItem.id, BuildState.items: Map<TriageItemId, ...>, addReviewItem key param (triage-report-types.ts, triage-report-support.ts, triage-report.ts). Merge key = published ID stays ONE type by explicit decision — the single mint site is the mechanism forcing future identity decisions; since TriageItemId extends string, packet building (triage-packets.ts) and all collect-side equality compile unchanged. (3) Do NOT brand or grammar-parse verdict/manifest itemIds at collect: they are re-parsed per-run JSON already exhaustively validated against the same-run manifest (unknown item, wrong packet, duplicate, self-referential canonicalItemId all throw); branding there is machinery with no consumer. No codec version: identity evolution is fenced by manifest schemaVersion + git-head/dirty/input-hash provenance. (4) Tripwire tests in triage-report.test.ts: a golden ID block per source family explicitly labeled as the published-ID contract, plus a case proving the semgrep ID is invariant under range-object property reordering. (5) MODULE.md Identity section: the three grammars, single mint site, the merge-key=published-ID decision, that IDs are opaque tokens downstream (never parsed), why no codec version (manifest provenance is the versioning surface), and that per-run-only stability is the contract — the drift positional index is load-bearing for duplicate same-check/same-file findings, so cross-run stability is out of scope by design. Land atomically with test updates; note in the commit body that emitted semgrep IDs change (safe under the per-run contract, invalidates any in-flight swarm run). Sequencing: independent of CQ25-47 (it narrows report inputs, not IDs; verdict parsing stays unbranded here). Single leaf, size S/M, module-internal per MODULE.md's external-import-is-a-bug contract.",
   "rationale": "Verified against the tree: the only prefix-sniffing consumer (triage-report-support.ts:147) ranks evidence.source, not item IDs, and triage-verdict-collect.ts does pure Map-equality against manifest itemIds — so the leak is entirely producer-side, which decides the central inter-proposal dispute for MINIMAL-CHURN. It wins on cost-fit (about half the solo reshape's diff for the same durability at the generation seam), grounded scoping, and the best regression defense (golden published-ID tests + property-order-invariance case). Grafted from STRUCTURAL: the explicit one-type ruling (merge key = published ID; a second internal type and a publish seam have no consumer — same over-engineering smell as the dropped codec version) and structured identity-record constructor inputs that keep pair identity stable across L02-014. Grafted from COPYABILITY: the dedicated liftable triage-item-id.ts module (avoids deepening the known five-concern support junction; single-file identity owner is the copyable artifact), the no-digest readable-ID ruling (reviewers hand-copy IDs and cross-check against locations), and recording the why-no-codec-version rationale in MODULE.md. Rejected: STRUCTURAL's parseTriageItemId and verdict/manifest brand threading (collect re-parses JSON, so branding there needs its own mint machinery duplicating strictly weaker validation than existing manifest membership checks) and its optional TriagePacketId (scope creep past M); COPYABILITY's collect-side lookup-mint fails for the same re-parsed-JSON reason. Severity medium and size S/M confirmed; novelty vs CQ25-45..47 holds (none touch ID identity).",
   "constraintRulings": [
    "Do not introduce a separate internal merge-key type distinct from the published item ID; do keep one branded TriageItemId whose single mint site in triage-item-id.ts plus a MODULE.md identity note is the mechanism forcing explicit identity decisions.",
    "Do not version the ID codec; do let the run manifest's schemaVersion + git-head/input-hash provenance remain the identity-evolution surface, and record that rationale in scripts/drift-triage/MODULE.md.",
    "Do not brand or grammar-parse inbound verdict/manifest itemIds at collect time; do keep them plain strings fenced by the existing exhaustive same-run manifest validation, and brand producer-side only.",
    "Do not replace the readable drift:/pair:/semgrep: prefixes or introduce a content-digest ID segment; do replace only the JSON.stringify(row.ranges) segment with a deterministic sorted range encoding that preserves column granularity.",
    "Do not put ID generation in triage-report-support.ts; do create a dedicated liftable scripts/drift-triage/triage-item-id.ts and move pairKey into it.",
    "Do not promise or pursue cross-run ID stability; do document per-run-only stability as the contract in MODULE.md — the drift positional index is load-bearing identity for duplicate same-check/same-file findings.",
    "Do not brand TriagePacketId in this leaf; treat it as out of scope to keep the leaf at S/M."
   ]
  },
  {
   "candidateId": "C-037",
   "chosenDirection": "Eliminate the TS-side duplicate of the state-path protocol at the process boundary; keep bash (scripts/lib/verify-metadata.sh) as the protocol's single owner. (1) Repoint package.json's \"logs:audit\" script from `bun scripts/logs-audit.ts` to a thin bash shim that sources scripts/lib/verify-metadata.sh, exports MUSI_STANDARD_VERIFY_LOG_DIR=$(musi_standard_verify_log_dir) and MUSI_STANDARD_BUN_LOG_DIR=$(musi_standard_bun_log_dir), and execs `bun scripts/logs-audit.ts \"$@\"`; the shim may skip derivation when --latest is absent so --file-only runs pay nothing. One bash spawn on a cold interactive path; the ai-hooks per-tool-call path (cache.sh) is untouched. (2) Delete logs-audit-latest.ts:30-74 and collapse defaultLatestLogRoots (lines 92-108) to the pure env chain; when --latest is requested and neither override nor standard env var is present, fail closed with a hint to run `bun run logs:audit --latest` (same register as the existing no-logs hint at scripts/logs-audit.ts:32). The keep-in-sync comment disappears because nothing is left to sync. (3) Add a shell-smoke assertion (test-verify-metadata.sh or a sibling) that the shim's exported values equal the accessor outputs — this is NEW coverage: test-verify-metadata.sh currently has zero musi_standard_state_path/musi_worktree_key derivation tests. (4) Register the new shim in the changed-test mapping (scripts/test-changed.sh:231 pattern list) and confirm the ai-hooks wrapped-bun policy still treats `bun run logs:audit` correctly (scripts/ai-hooks/policy.sh:96, test.sh:2051). (5) Give the shim a short comment framing the pattern for copiers: the env contract the TS side already documents partially (logs-audit-latest.ts:68-69) is being made total, matching the verify.sh/verify-async.sh norm. logs-audit.test.ts's latestLogRoots injection seam keeps TS tests intact but shrinking. Severity medium; size M; single leaf (shim + TS deletion + smoke + doc line land together). Key files: /workspace/scripts/logs-audit/logs-audit-latest.ts, /workspace/scripts/logs-audit.ts, /workspace/scripts/lib/verify-metadata.sh, /workspace/package.json, /workspace/scripts/tests/test-verify-metadata.sh, /workspace/scripts/test-changed.sh, /workspace/scripts/ai-hooks/policy.sh.",
   "rationale": "The contested question was shape only. Verified ground truth: logs-audit-latest.ts is the sole TS consumer of the state-path protocol and needs it only to compute two directory strings at startup when no wrapper exported them; the bash owner sits on the ai-hooks per-tool-call hot path, so the lanes' original \"one owner consumed by the shell facade\" would put a bun spawn on a latency-budgeted path (contradicting the CQ25-124 cost split). Among the three proposals, STRUCTURAL is the only one that removes the duplication instead of pinning it: the pin shapes (MINIMAL-CHURN fixture, COPYABILITY/solo-reshape module+corpus) all keep two implementations forever and add fixture/module surface to guard a duplicate that can simply be deleted, and both misdescribe the existing corpus as dual-asserted (its header says tests never drive bash). STRUCTURAL also follows the repo's established seam direction (bash wrapper exports env then runs bun — verify.sh, verify-async.sh) and its one behavior change is safe in-tree: all documented usage goes through `bun run logs:audit --latest`, and the only scripted consumer uses --file. Grafts from runners-up: fail-closed message quality and --file-skip (MINIMAL-CHURN's cost discipline), explicit copier-facing framing of the total-env-contract pattern (COPYABILITY), plus a correction — the shim-parity smoke is new coverage, not an extension of existing accessor smokes, because test-verify-metadata.sh has none today.",
   "constraintRulings": [
    "C-037: do not make verify-metadata.sh accessors or ai-hooks consume a TypeScript state-path owner at runtime — musi_worktree_key/musi_standard_bun_log_dir run per tool call in scripts/ai-hooks/cache.sh and a bun spawn there regresses hook latency budgets; keep the protocol's runtime owner in bash (CQ25-124 cost split).",
    "C-037: do not extract a TS state-path module or add a bash-TS parity corpus to pin the duplicate — delete the TS reimplementation instead: repoint the logs:audit package script to a thin bash shim that sources verify-metadata.sh, exports MUSI_STANDARD_{VERIFY,BUN}_LOG_DIR, and execs bun scripts/logs-audit.ts.",
    "C-037: do not retain the original direction's 'retain direct-CLI fallback behavior' for raw `bun scripts/logs-audit.ts --latest` — when --latest is requested with no override or standard env var, fail closed with a hint pointing at `bun run logs:audit --latest`; silent local path reconstruction is the defect being removed.",
    "C-037: do not describe scripts/lib/fixtures/verify-metadata-core-corpus.json as a dual-asserted bash/TS pattern — its header states tests never drive the bash implementation; and do not claim test-verify-metadata.sh already smokes the musi_standard_* accessors — it has zero such coverage, so the shim-equals-accessors smoke is new coverage that must be added, not extended."
   ]
  },
  {
   "candidateId": "C-042",
   "chosenDirection": "Base: STRUCTURAL proposal, with COPYABILITY's slice ordering and doc discipline grafted in, and MINIMAL-CHURN's descope fallback recorded. Build packages/server/src/services/chat-message-service.ts (flat-service tier, with chat-message-MODULE.md) in three layers: (1) mechanism halves — persistChatMessage(db: DbClient | TxClient, data) absorbing create + MESSAGE_AUTHOR_INCLUDE + mapChatMessage, and deliverChatMessage(server, message, opts) absorbing getSocketIO + campaignRoom + broadcastChatMessage (no-op when io is null; dmUserId stays caller-resolved per chat-broadcast.ts's documented boundary); (2) exactly two named policy entry points — sendChatMessageInRequest (persist then deliver, throwing, returns mapped message; for chat.send and dice.roll) and postChatMessageBestEffort (try persist+deliver, warn-and-swallow; for cast-spell, combat-chat, and eventually feature.ts) — rest-service intentionally gets NO composed wrapper and uses the raw halves (persist-in-tx via TxClient, void post-commit broadcast), documented in the MODULE.md as the supported third pattern; (3) a shape-1 request-facing sendChatMessage(ctx, input) service extracting chat.ts:24-94 plus the send body (whisper validation, DM resolution, presence check, offline notification, preview truncation), built on the halves, keeping presence/notification machinery out of the generic surface. Migrate utils/combat-chat.ts into the service preserving its persist/emit split as the two named halves (consciously acting on the CQ25-75 deferred taxonomy question; the corpus records it as a future leaf and the halves structure institutionalizes the documented auditability rationale). feature.ts migrates two-step: mechanically onto the throwing path first (behavior-preserving), then a separate explicitly-flagged behavioral commit switching it to postChatMessageBestEffort — its current un-caught await after a committed CAS update is the policy inconsistency this surface exists to prevent. The MODULE.md must state the decision rule as a required deliverable: \"source mutation already committed → best-effort log-and-swallow; user-visible in-request action → throw\", plus the invariant that persistChatMessage stays side-effect-free beyond the single create so Serializable-retry callers (rest-service P2034 retry) remain safe. Landing order (finding is needs-split): slice B FIRST = chat.send extraction + routers-MODULE.md update (the only outright thin-router violation; highest standalone value; lands alone if the rest is descoped); slice A = service halves + policy entry points + mechanical migration of the 5 non-chat writers + services/README.md utils-example swap + rest-MODULE.md update folded into the same commit; slice A2 = the flagged feature.ts policy flip. No trailing doc-only slice — every landed commit leaves docs consistent. Descope fallback if slice A is rejected in review: land persistChatMessage alone (the create+include+map triple is pure duplication with zero policy content). Callers keep content construction throughout.",
   "rationale": "STRUCTURAL scores highest: it alone surfaced the decisive evidence (verified at feature.ts:58-99) that a sixth writer re-derived failure policy and got it wrong — chat persist/broadcast awaited un-caught after the feature-use CAS update already committed — which converts the contested \"indirection over intentionally-varied policies\" objection into a demonstrated defect class, and its layering (composable halves, two named policies, rest-service exempted onto raw halves, TxClient retry invariant) matches every load-bearing constraint the solo review verified. COPYABILITY contributes the correct priority inversion (chat.send extraction is the only outright AGENTS.md/routers-MODULE.md violation and the highest-value standalone slice for a public reference repo), the kill of the trailing doc slice, and the MODULE.md decision rule as a required deliverable. MINIMAL-CHURN is right that the LOC win is marginal and supplies the persistChatMessage-only descope fallback, but loses as a base because leaving policy implicit at call sites preserves exactly the re-derivation failure mode feature.ts already exhibits, and its keep-combat-chat-in-utils stance leaves two homes for one pattern. On the two contested axes: combat-chat migration proceeds (2 of 3 proposals, weigh-don't-auto-reject norm, docs carried in scope, and the halves structure satisfies the in-code auditability rationale); the \"only slice B is worth it\" objection is answered by ordering slice B first and making A independently droppable.",
   "constraintRulings": [
    "C-042: do not centralize all six chat writers behind one monolithic send call owning persistence+broadcast+failure policy; do instead expose composable halves — persistChatMessage(db: DbClient | TxClient) and deliverChatMessage — so rest-service's inside-Serializable-transaction persist with post-commit broadcast stays legal.",
    "C-042: do not collapse failure handling into a single centralized policy; do instead ship exactly two named entry points (in-request throwing; post-commit best-effort log-and-swallow) and leave rest-service on the raw halves as a documented third pattern with no composed wrapper.",
    "C-042: do not put whisper recipient resolution, presence checks, or offline-notification into the generic chat-message surface; do instead extract them into a chat-send-specific shape-1 service, since only ordinary chat needs them.",
    "C-042: do not change feature.ts's failure behavior inside the mechanical migration; do instead migrate it behavior-preserving (throwing) first and flip it to best-effort in a separate explicitly-flagged behavioral commit.",
    "C-042: do not land a trailing doc-update slice; do instead fold services/README.md and rest-MODULE.md updates into the writer-migration slice and routers-MODULE.md into the chat.send-extraction slice so no landed commit leaves docs stale.",
    "C-042: do not treat the CQ25-75 combat-chat deferral or combat-chat.ts's in-code documented decision as an auto-reject; do instead migrate combat-chat into the service preserving its persist/emit split as the two named halves, with the doc updates carried in scope.",
    "C-042: do not land the 6-writer unification first; do instead land the chat.send router extraction as the priority slice — it fixes the only outright thin-router violation and must stand alone if the unification slice is descoped (minimal fallback: persistChatMessage only).",
    "C-042: persistChatMessage must stay side-effect-free beyond the single create (Serializable P2034 retry safety for rest-service); state this invariant in chat-message-MODULE.md."
   ]
  },
  {
   "candidateId": "C-046",
   "chosenDirection": "Promote character creation to packages/server/src/services/character-create/ and amend services/README.md in the same leaf — the taxonomy amendment is inseparable from the move, not a flagged conflict. Physical shape (per existing deep-module conventions): logic-bearing facade character-create/character-create.ts (no barrel index.ts) keeping createCharacter plus the live test seams validateCreateInput/buildCreateData exported; types.ts owning ValidatedSrdRefs (+ CharacterCreateContext), which alone breaks the helpers->entry type-only cycle at character-create-helpers.ts:10; internals with the redundant prefix dropped — nested-creates.ts (was character-create-helpers.ts; its one export is buildNestedCreates), spells.ts (was character-create-spells.ts), and starting-equipment.ts (was starting-equipment-service.ts, folded in: sole production importer is character-create.ts:13 and it runs inside the creation $transaction). Colocated test files move into the folder with their subjects. MODULE.md is the deliberately light tier per docs/module-docs.md sections: pipeline stages (parallel validate -> build create-input -> single $transaction with level-1 features + starting inventory -> refetch/map), transaction ownership (service owns the $transaction; router character.ts create at routers/character.ts:45 is a thin shape-1 pass-through), contract notes (no Socket.io broadcast; auth is just protectedProcedure), and the exported test seams; if docs/module-docs.md's charter presumes invariants/broadcasts, add a one-line note admitting the light tier inside the leaf. README amendment: recast the promotion rubric as two named paths that separate what earns a folder from what earns the heavy charter — (i) invariant-driven: the existing all-three rubric with full MODULE.md charter, unchanged; (ii) structure-driven: a single mutation whose implementation already spans >=3 substantive implementation files (tests excluded) earns a folder on structural grounds alone, carrying a light MODULE.md instead of the concurrency charter. Keep the \"one mutation does not need a module\" line, qualified to one mutation in one file. Fix the inventory in the same edit: remove character-create and starting-equipment-service from both flat lists (the Use-when examples at lines 56-58 and the inventory at lines 198-202, curing the existing drift that omits character-create-spells.ts), add character-create/ to the deep-module inventory and examples as the named exemplar of the structure-driven path. Mechanical updates are 6 external import sites — routers/character.ts:23, routers/character-updated-broadcast.test.ts:6, routers/seed-read-normalization.test.ts:8, routers/character-create-transaction.test.ts:5, services/level-up/level-up-test-helper.ts:5, utils/character-mapping.test.ts:7 — plus the vi.mock path string at routers/character-create-transaction.test.ts:11. Verification is typecheck + existing tests; no behavior change. Severity medium, size M (confirmed by the corrected blast radius). Fallback if a panel overrides toward stay-flat: the minimal-churn option (types file + flat-companion MODULE.md + inventory fix) is a strict content subset of this direction, but it must still amend the README and would leave the flat tier describing a nameless multi-file cluster — the README doc-touch cost is not avoided by staying flat.",
   "rationale": "All three proposals agree on the verified facts (640 lines/4 production files, type-only cycle at character-create-helpers.ts:10, starting-equipment single-consumer at character-create.ts:13, sole production caller routers/character.ts). They split on whether the code or the rubric moves. Minimal-churn preserves the rubric's bright line at lowest cost, but the flat tier's own definition is \"single file\" and the inventory already leaks ((+ character-create-helpers.ts) parenthetical, omitted -spells.ts, starting-equipment listed as an independent peer of the transaction it runs inside) — staying flat forces the showcase README to name a \"multi-file flat cluster\" category it does not have, which distorts the taxonomy more than promotion does and cuts against the public harness-reference/copyability goal. Structural supplies the decisive conceptual move: the current rubric conflates folder-earning (structural cohesion) with charter-earning (concurrency invariants), so splitting them resolves the contradiction while keeping the invariant bar intact; it also corrected the mechanical blast radius from 3 to 6 external sites (independently re-verified here). Copyability supplies the best README expression of that split — two named promotion paths with an objective greppable >=3-substantive-impl-files threshold and a named in-tree exemplar per path — which is the mitigation for the real rubric-erosion risk both promotion proposals acknowledge. Grafts from the runner-up: minimal-churn's vi.mock path-string hazard (routers/character-create-transaction.test.ts:11), its observation that both flat lists (lines 56-58 and 198-202) need the edit, and its strict-subset framing preserved as the documented panel fallback. This matches the working rule that documented-decision conflicts are weighed, not auto-rejected: here the code reality has outgrown the flat tier, so the doc moves, and the doc update travels in the same leaf.",
   "constraintRulings": [
    "C-046: do not land the character-create folder promotion while merely flagging a conflict with services/README.md; amend the README in the same leaf — recast the promotion rubric as two named paths (invariant-driven: existing all-three rubric + full charter, unchanged; structure-driven: a single mutation spanning >=3 substantive implementation files, tests excluded, earns a folder with a light MODULE.md) and keep the one-mutation-needs-no-module line, qualified to one mutation in one file.",
    "C-046: do not resolve this by staying flat with a flat-companion character-create-MODULE.md; the flat tier's own single-file definition and its already-drifted inventory cannot describe a four-production-file cluster — promote instead, and keep the stay-flat variant only as a panel-override fallback that still requires the README amendment.",
    "C-046: do not add a re-export-only index.ts barrel; use the logic-bearing facade character-create/character-create.ts per the README facade convention, with prefix-dropped internals types.ts, nested-creates.ts, spells.ts, starting-equipment.ts.",
    "C-046: do not leave starting-equipment-service.ts as a standalone flat service; fold it into character-create/ as an internal transaction stage (sole production importer is character-create.ts:13) and remove it from both flat lists in the README.",
    "C-046: do not leave ValidatedSrdRefs exported from the entry file; move it (with CharacterCreateContext) to character-create/types.ts so the helpers->entry type-only cycle is broken structurally, not documented around.",
    "C-046: do not write the full concurrency-charter MODULE.md for this module; write the light tier (pipeline stages, transaction ownership with the thin router pass-through at routers/character.ts:45, no-Socket.io contract, exported test seams) and, if docs/module-docs.md's charter presumes invariants, add a one-line light-tier note there inside the same leaf.",
    "C-046: do not size the mechanical rewiring at 3 import sites; it is 6 external importers plus the vi.mock path string at routers/character-create-transaction.test.ts:11, and the colocated character-create*/starting-equipment* test files move into the folder with their subjects.",
    "C-046: do not add the folder without fixing the README inventory in the same edit; remove character-create and starting-equipment-service from the flat examples (lines 56-58) and flat inventory (lines 198-202, curing the omitted character-create-spells.ts drift) and list character-create/ in the deep-module inventory as the named structure-driven exemplar."
   ]
  },
  {
   "candidateId": "C-050",
   "chosenDirection": "Deduplicate the MapLayer mapper by promoting the existing one, not by creating a new module. (1) In packages/server/src/utils/map-helpers.ts, export the existing mapLayer (line 106) in place in the \"Mapping helpers\" section, and switch its data field from unchecked fromJson to fromJsonValidated(l.data, mapLayerSchema.shape.data, {}) — reusing the shared schema's own field (packages/shared/src/schemas/map.ts:110) instead of re-declaring z.record(z.string(), z.unknown()) server-side, so a future per-layer-type tightening of the shared schema tightens the DB read boundary and wire contract in one edit. This also fixes the real behavioral divergence: today one malformed layer row fails the whole map-detail response at the router's .output() boundary, whereas the CRUD path degrades that layer's data to {}. (2) In packages/server/src/routers/map-layer.ts, delete the local layerDataSchema and mapLayer (lines 20-32), import the shared mapLayer, and drop the now-unused imports (z, fromJsonValidated, PrismaMapLayer, MapLayer type); no casts needed — the shared mapper returns MapLayerResult (z.input of mapLayerSchema), exactly what resolvers behind .output(mapLayerSchema) accept, and Prisma rows are already structurally assignable to MapLayerRow. (3) Leave timestamps as Date in the mapper; dropping the CRUD path's hand-rolled .toISOString() calls falls out of the deletion — shared dateTimeField (packages/shared/src/constants.ts:109) is the documented single Date->ISO normalization point at every .output() boundary, so the wire contract is unchanged. (4) Extend the EXISTING packages/server/src/utils/map-helpers.test.ts (no new test file) with contract-named cases: one malformed layer among good ones degrades to {} in mapMapDetail instead of failing the whole response; the exported mapLayer degrades malformed data to {} and passes valid records through; and mapLayerSchema.parse(mapLayer(row)) round-trips a Date row to ISO strings, pinning the mapper-emits-input / schema-emits-output seam. Commit body must name the intended behavior change (detail path: whole-map INTERNAL_SERVER_ERROR -> per-layer {} degradation) rather than presenting it as a pure refactor, and the leaf brief should note that the mapper's Date output is deliberate — do not \"fix\" it by re-adding toISOString (dateTimeField accepts both string and Date at every validated boundary; the only current broadcast, broadcastMapLayerUpdate, carries ids only). Size XS-S, single-leaf, no schema/migration/socket impact.",
   "rationale": "All three proposals and the solo review independently converge against the lane's original direction on the same two points, and every load-bearing code claim checks out at the pin (map-helpers.ts:106-116 unchecked fromJson + unexported mapper; map-layer.ts:20-32 duplicate mapper with local schema and .toISOString(); prisma-json.ts:8-16 fromJsonValidated rule of thumb; constants.ts:104-111 dateTimeField as documented single Date->ISO point; map.ts:110 shared data field; existing map-helpers.test.ts). STRUCTURAL wins on the one substantive delta: validating with mapLayerSchema.shape.data instead of a re-declared server-side record schema removes the third statement of the layer-data shape, follows the AGENTS.md \"shared Zod schemas are the contract; derive from them\" principle, and has precedent (spell-mapping validates reads with composed shared schemas) — MINIMAL-CHURN's moved-over local const would keep a redundant declaration, and COPYABILITY independently endorses the same .shape refinement. Grafted from MINIMAL-CHURN: extend the existing map-helpers.test.ts rather than adding a file, and the verified no-cast import-cleanup detail. Grafted from COPYABILITY: the commit body must state the 500->degrade behavior change explicitly, and the leaf brief warns against re-adding toISOString (the one latent hazard: a future non-Zod-boundary caller could leak Date objects, currently theoretical since broadcastMapLayerUpdate carries ids only). The original direction's \"divergent date contracts\" severity claim was correctly discounted by the review — both paths already agree on the wire via dateTimeField — so the fix's real behavioral payload is the malformed-JSON convergence, which the tests pin deliberately.",
   "constraintRulings": [
    "C-050: Do not create a new focused mapping module for the MapLayer mapper; do instead export the existing mapLayer from the 'Mapping helpers' section of packages/server/src/utils/map-helpers.ts and delete the router-local duplicate in packages/server/src/routers/map-layer.ts.",
    "C-050: Do not normalize timestamps (toISOString) inside the mapper or anywhere before the router boundary; do instead return Date from the mapper and let the shared dateTimeField transform at .output() remain the single Date->ISO normalization point (drop the CRUD path's hand-rolled .toISOString() calls).",
    "C-050: Do not re-declare the layer-data record schema on the server side; do instead validate the JSON read boundary with the shared schema's own field, fromJsonValidated(l.data, mapLayerSchema.shape.data, {}).",
    "C-050: Do not present the map-detail path change (malformed layer JSON: whole-response INTERNAL_SERVER_ERROR -> that layer degrading to {}) as a pure refactor; do state it as an intended behavior change in the commit body and pin it with tests added to the existing map-helpers.test.ts."
   ]
  },
  {
   "candidateId": "C-056",
   "chosenDirection": "STRUCTURAL base with grafts from COPYABILITY and MINIMAL-CHURN. Finish the CQ25-118 injection pattern via plain parameter injection, no DI container, no new port types, no auth-function injection. Shape: (1) BuildServerOptions gains a flat optional `prisma?: PrismaClient` (not a nested deps bundle) resolved exactly once in buildServer (`const prismaClient = opts.prisma ?? prisma`), with a short comment stating the rule: production defaults are resolved only at the composition root; every interior signature is required. buildServer derives `const db = toDbClient(prismaClient)` (carrying the sanctioned CONCURRENCY.md comment) and feeds: registerUploadRoutes(server, db: DbClient), the tRPC context factory, cleanExpiredSessions(prismaClient), and setupSocketIO. (2) upload-routes.ts: delete the module-level `toDbClient(prisma)` (lines 40-43) and singleton import; keep verifyAccessToken as a direct import. (3) trpc/context.ts: convert to `createContextFactory({ db }: { db: DbClient })` returning the handler; promote the inline return type to an explicit exported `Context` interface with the handler annotated `Promise<Context>` so the four existing Context/SessionUser importers stay untouched; update app.test.ts lines 156/268 which import createContext directly. (4) socket path: SetupSocketIOOptions gains a REQUIRED `prisma: PrismaClient` (no default — buildServer is the sole defaulting site); socket/index.ts drops its singleton import, keeps createPresenceService(io, opts.prisma) unchanged, derives `const db = toDbClient(opts.prisma)` beside presence creation, and threads db (DbClient, not raw — campaignMember is unrestricted so findFirst typechecks; least authority) through registerConnectionHandler into registerCampaignRoomHandlers and registerHeartbeatHandler. handleDisconnect's signature does NOT change — it touches no Prisma. (5) Docs in scope: routes/MODULE.md, socket/MODULE.md data-flow lines, AND the prisma-types.ts:173-181 doc comment that enumerates production narrowing sites (must now name the two composition roots; no proposal caught this). (6) Slicing: two implementation leaves — HTTP path (upload route + context factory + BuildServerOptions.prisma + session-cleanup rewire + prisma-types comment + routes/MODULE.md + app.test.ts) and socket path (SetupSocketIOOptions + threading + socket/MODULE.md) — plus a final optional enforcement leaf: a lint restriction (per docs/guides/lint-ratchet.md and the config-surface manifest) allowing prisma/client.js value imports only from the composition roots, src/test/**, and seeds/scripts. Combined implementation size M, not L. Hard requirement preserved: main.ts, test/app-helper.ts, and test/socket-helper.ts stay byte-identical; payoff is signature honesty and harness copyability, not test unblocking. Each leaf asserts helper byte-compat via git diff.",
   "rationale": "STRUCTURAL wins on verified correctness: it alone caught that handleDisconnect (campaign-room-handler.ts:136-165) uses no Prisma — the solo reshape and MINIMAL-CHURN both thread a client into a function with no db dependency — and its DbClient-for-room-handlers choice is verified to typecheck (campaignMember is not a restricted delegate in prisma-types.ts) while preserving least authority, which is the actual point of a signature-honesty finding; its required-not-defaulted SetupSocketIOOptions.prisma keeps exactly one defaulting site. MINIMAL-CHURN's uniform raw-PrismaClient seams and in-function narrowing were rejected (wider-than-consumed signatures, duplicate sanctioned-comment sites), but two of its contributions are grafted: the verified app.test.ts:156/268 direct createContext imports that break the 'tests unchanged' claim, and the sizing argument that ~60-90 lines does not justify four review cycles — consolidated to two implementation leaves. COPYABILITY's flat prisma? field (over a one-key deps bundle), its stated root-only-default invariant, and its lint-enforcement slice are grafted — the enforcement ratchet is high-leverage for this repo's public-harness-reference identity and cheap once production importers collapse to the two composition roots; its single-narrowing-site idea was rejected because setupSocketIO needs the raw client for presence-service regardless, making two composition-root narrowing sites the cleaner shape. One gap all three missed, added in synthesis: prisma-types.ts:173-181 enumerates the production narrowing sites by filename and must be updated when they move. Severity medium stands; sizeHint reduced L to M; leafability needs-split confirmed but at two implementation leaves plus an optional enforcement leaf rather than four.",
   "constraintRulings": [
    "Do not inject verifyAccessToken or any auth-service function at these boundaries — auth-service captures no Prisma state (crypto/bcryptjs/jose/shared imports only; the lane's 'and auth service' evidence is inaccurate); keep direct imports.",
    "Do not create a 'room membership port', any new port interface, or a DI container — do plain parameter injection using the existing PrismaClient/DbClient types, matching the presence-service and cleanExpiredSessions convention.",
    "Do not change handleDisconnect's signature — campaign-room-handler.ts:136-165 touches no Prisma (presenceService and fetchSockets only); thread the injected db only into registerCampaignRoomHandlers and registerHeartbeatHandler.",
    "Do not put a singleton default anywhere below buildServer — resolve `opts.prisma ?? prisma` exactly once in buildServer; SetupSocketIOOptions.prisma and every interior parameter are required, not optional.",
    "Do not use a nested deps bundle on BuildServerOptions — use a flat optional `prisma?: PrismaClient` field; introduce a bundle only when a second injectable dependency actually exists.",
    "Do not hand the raw write-capable PrismaClient to the campaign-room handlers — narrow to DbClient at the socket composition root (campaignMember is unrestricted, so findFirst typechecks); presence-service keeps the raw client unchanged.",
    "Do not claim test surfaces are untouched — app.test.ts imports createContext directly at lines 156 and 268 and must adopt the factory; main.ts, test/app-helper.ts, and test/socket-helper.ts must stay byte-identical (assert via git diff).",
    "Do not move the toDbClient narrowing without updating the prisma-types.ts:173-181 doc comment that enumerates production narrowing sites by filename, alongside the routes/MODULE.md and socket/MODULE.md data-flow updates.",
    "Do not split into four per-boundary slices or land as one L leaf — land as two implementation leaves (HTTP path, socket path) plus an optional final lint-enforcement leaf restricting prisma/client.js value imports to composition roots, src/test/**, and seeds/scripts; combined implementation size is M."
   ]
  },
  {
   "candidateId": "C-058",
   "chosenDirection": "MINIMAL-CHURN base with COPYABILITY grafts, landed as ONE S/M leaf (not needs-split L). Scope: (1) Create packages/server/src/seed/MODULE.md per docs/guides/add-module-doc.md declaring the directory policy: two corpus classes (vendored data/reference/ JSON loads via readSeedJson; generated data/ JSON via direct read — the loader's ENOENT text is reference-restore-specific), two validation levels (schema-parse with Zod-inferred types, or assert-with-marker for pinned vendored/generated data, promoting the reasoned rationale already at seed-srd-reference-tables.ts:48 into the level definition), the invariant that narrowing happens exactly once at the load call site with downstream re-assertions banned, and that schemas/types stay local to seed/ (vendored SRD shapes, not app contracts — never packages/shared). Include COPYABILITY's per-corpus table stating each file's CURRENT level, so the mixed state becomes declared policy immediately and any later conversion is a one-row doc diff; record the drift backstop as a MODULE.md gotcha (\"new corpus => add a table row + level\"), no new lint/generator machinery. (2) Add the schema-accepting overload readSeedJson(baseDir, filename, schema) returning z.infer<S>, wrapping ZodError in the same actionable file-naming style as the existing ENOENT branch; backgrounds (:118-119) and equipment (:266-268) adopt it as one-line changes; the 2-arg unknown form remains for declared assert-level corpora. (3) Fix the sole downstream re-narrowing: type MonsterJson.spellcasting (seed-srd-monsters.ts:53) as SpellcastingJson | null so the single load-boundary assert at :201 covers it; delete the as-unknown-as double-assert at :183 AND the per-field casts in extractSpellcastingScalars (:78-84), which share the same root cause. (4) Update assert-level markers to cite the MODULE.md level name so the policy is grep-discoverable from any call site. Explicitly out of scope: converting the six assert corpora to Zod (available only as individually-deprioritizable follow-up leaves, one per corpus, never blocking), rerouting spells/monsters/magic-items through readSeedJson, generator/seeder round-trip schemas, a corpus-parses-clean test suite (the pattern already exists for spells at seed-srd-spells.test.ts:47; extend opportunistically only), and any lint/manifest enforcement of validation levels. Implementer caution: this is the directory's first MODULE.md — follow the add-module-doc registration steps.",
   "rationale": "The finding's problem statement is \"no directory-level rule\", not \"insufficient validation\", so the durable fix is the declared policy, not blanket Zod conversion. MINIMAL-CHURN scored highest on factual accuracy (verified: the three bypass consumers never use readSeedJson and read generated data/ not data/reference/; the only genuine narrow-twice violation is the monsters spellcasting shape, fixable by typing MonsterJson.spellcasting) and on effort-vs-payoff for pinned, operator-driven seed data. COPYABILITY contributed the decisive grafts: the per-corpus level table (converts drift into evaluable policy on day one and makes follow-up leaves one-row diffs), marker-cites-policy breadcrumbs, and the explicit no-new-harness-machinery stance — all directly serving the repo's public-harness-reference goal. STRUCTURAL was rejected on three grounds: its mandatory two-level end-state overrides the documented in-repo cost/benefit decision at reference-tables:48 rather than weighing it (a reasoned opt-out is itself exemplary harness content); unifying the load paths requires generalizing a deliberately reference-specific ENOENT message for zero behavior payoff; and its generator/seeder round-trip schemas brush against parked prior-pack territory (CQ25-171/172: generated-corpus rewrites are unverifiable without docs/refs/). Its corpus-parses-clean test idea is partially pre-existing (seed-srd-spells.test.ts:47) and left opportunistic. The solo review's needs-split L structure collapses to one S/M leaf because per-corpus conversions move from scheduled tail to optional follow-ups; all assertion sites already carry sanctioned markers, so nothing here is a lint fix. No priorPackOverlap recorded by the lane and none found in the dedup corpus beyond the parked-adjacent items handled above.",
   "constraintRulings": [
    "C-058: do not convert the six assert-level seed corpora to Zod as part of this leaf; do instead declare assert-with-marker as a documented validation level with a per-corpus level table in packages/server/src/seed/MODULE.md, leaving per-corpus conversions as individually-deprioritizable follow-up leaves.",
    "C-058: do not reroute the readFileSync corpora (seed-srd-spells, seed-srd-monsters, seed-srd-magic-items) through readSeedJson; do instead document two corpus classes in MODULE.md (vendored data/reference/ via readSeedJson, generated data/ via direct read) — the loader's ENOENT message is reference-restore-specific by design.",
    "C-058: do not treat this as a needs-split L candidate; do instead land one S/M leaf (schema-accepting readSeedJson overload + MODULE.md policy/table + typing MonsterJson.spellcasting as SpellcastingJson | null, deleting the seed-srd-monsters.ts:183 double-assert and the :78-84 per-field casts).",
    "C-058: do not add generator/seeder round-trip schemas, a mandatory corpus-parses-clean test suite, or any lint/manifest/generator machinery for seed validation levels (generated-corpus rewrites are parked per CQ25-171/172); do instead have assert-level markers cite the MODULE.md level name so the policy is grep-discoverable.",
    "C-058: do not move seed corpus schemas or inferred types into packages/shared despite the shared-schemas convention; do instead keep them local to packages/server/src/seed/ — they model vendored external SRD shapes, not app contracts."
   ]
  }
 ]
}
