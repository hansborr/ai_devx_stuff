# Dedup corpus — 2026-07-25 pack, distilled for the 2026-08-01 audit

Status: Reference — Phase-1 exclusion corpus, frozen before waves 1–2

The audit lanes used these records to identify prior-pack overlap, record
duplicate drops, and avoid silently reopening settled directions. The corpus
was frozen when the waves launched; it is a launch-time exclusion snapshot,
not a live status view of the prior pack.

Distillation ran at audit branch commit
`ebf096580b31f604861fadb3d4cbd4079da4f017` on 2026-08-01. The prior-pack tree
at that point is byte-identical to the later live-review pin
`priorPackReviewSha = 948169235fef7798de67bd44b90ac4894e9cc55f`, which pinned the
2026-07-25 pack for the exhaustive Phase-5 drop review. The prior pack's own
evidence pin is `883d48bf` (`PRIOR_AUDIT_SHA`) — cite corpus records, never
that SHA. See [ORCHESTRATION.md](./ORCHESTRATION.md#dedup-corpus-built-in-phase-1-frozen-for-the-waves)
and its [pin record](./ORCHESTRATION.md#pins-1).

**Record shape:** `CQ25-<n>` · kind (`open-leaf|plan-slice|ruling|do-not-reopen`) · status · one-line scope · (source anchor: file § heading, all files under `docs/agent_notes/backlog/code-quality-2026-07-25/`). Lanes cite record ids in `priorPackOverlap` and `droppedAsPriorPackDuplicate`.
**Status vocabulary:** leaves/slices use `scheduled|optional|blocked|no-work-left` (`optional` also covers recorded-but-unscheduled carve-outs and follow-ups); rulings are `standing`; do-not-reopen records carry `landed|closed-declined|dropped|parked|refused|recorded-decision`.
**How to judge duplication:** a finding that restates a record's scope is a duplicate (drop and record it); a finding in the same area is *materially new* only with new paths/counts or a different direction. Superseded leaves (most of section 1) point at their plan-slice records — dedup against the slices, not the leaf title.

## 1. Open leaves (20)

- **CQ25-1** open-leaf · optional — Leaf 03: server authz helpers take the caller three ways (`packages/server/src/utils/{character,campaign,note}-auth.ts`); superseded — no scheduled work left, only optional S17/S18 remain → CQ25-68/69. (00-index.md § Open leaves)
- **CQ25-2** open-leaf · scheduled — Leaf 07: spell-casting and level-up widen their own types then re-narrow downstream (`packages/server/src/services/{spell-casting,level-up}/`); superseded by 07-PLAN → CQ25-21..26 (two independent streams). (00-index.md § Open leaves)
- **CQ25-3** open-leaf · scheduled — Leaf 27: shell smoke suites re-declare their own test framework per file (44 `fail()` copies across `scripts/tests/`, `scripts/ai-hooks/test.sh`); superseded by 27-PLAN → CQ25-27..33. (00-index.md § Open leaves)
- **CQ25-4** open-leaf · scheduled — Leaf 28: the `scripts/` layout contract is prose-only (62 top-level files across seven flat families vs `scripts/README.md`); superseded by 28-PLAN → CQ25-34..44. (00-index.md § Open leaves)
- **CQ25-5** open-leaf · blocked — Leaf 29: `scripts/worktree-db.sh`/`scripts/ai-hooks/stop-policy.sh` state-codec copies; H1/H2 landed (`2667ee8e0`), remaining work is H11/H12 → CQ25-62/63, blocked on 28.1. (00-index.md § Open leaves)
- **CQ25-6** open-leaf · no-work-left — Leaf 30: argv-offset spellings / `parseCli` adoption tail in `scripts/`; H3-H5 landed (`ac3ce2b0f`); remaining hand-rolled walkers converge opportunistically on touch by standing policy (`scripts/lib/process-argv.ts:8`) — see CQ25-127/128. (00-index.md § Open leaves)
- **CQ25-7** open-leaf · blocked — Leaf 31: leaf-utility layer (`scripts/lib/` records/error-message adoption, shell finding shape, `scripts/path-policy/` duplication); H6-H10/H14 landed; H15 → CQ25-64 blocked on 27.3; two unscheduled follow-ups → CQ25-72/73. (00-index.md § Open leaves)
- **CQ25-8** open-leaf · no-work-left — Leaf 32: git hooks hold ~900 lines of gate orchestration inline (`.husky/`); H16/H17 landed (`c6e1be2a2`); steps 4-5 (pre-commit dispatcher split) not scheduled, conditional reopen only → CQ25-130. (00-index.md § Open leaves)
- **CQ25-9** open-leaf · no-work-left — Leaf 33: harness env-var prefixes; H17 landed the `HARNESS_`/`MUSI_`/`AI_` naming rule in `docs/ai-harness.md`; inventory/enforcement steps dropped → CQ25-131. (00-index.md § Open leaves)
- **CQ25-10** open-leaf · scheduled — Leaf 34: `scripts/drift-ai/`+`scripts/drift-triage/` carry contracts as free-form records and positional params; superseded by 34-PLAN → CQ25-45..47. (00-index.md § Open leaves)
- **CQ25-11** open-leaf · optional — Leaf 35: `scripts/code-intel/` and `scripts/logs-audit/` carry structure by convention; H18/H19 landed (`57ef569e5`); only optional H20/H21 remain → CQ25-65/66. (00-index.md § Open leaves)
- **CQ25-12** open-leaf · no-work-left — Leaf 36: lint-ratchet portable kernel vocabulary (`tools/lint-ratchet/`); H22/H23 landed (`e2dc60cb9`); step 3 dropped-as-wrong → CQ25-133, step 4 `testId`→`ratchetId` dropped to its own future leaf → CQ25-74. (00-index.md § Open leaves)
- **CQ25-13** open-leaf · scheduled — Leaf 40: test inputs are inline literals and positional tuples, not typed factories (client fixtures, shared homebrew schema tests, server encounter helpers); superseded by 40-PLAN → CQ25-48..51. (00-index.md § Open leaves)
- **CQ25-14** open-leaf · optional — Leaf 41: the client's 603-line untyped mock tRPC shadow router (`packages/client/src/test/mock-trpc.tsx`); 41.1/41.2 implemented; only optional 41.3 remains → CQ25-52. (00-index.md § Open leaves)
- **CQ25-15** open-leaf · scheduled — Leaf 42: encounter combat E2E is one 22-test serial narrative repairing shared state (`e2e/encounter-combat.spec.ts`); superseded by 42-PLAN → CQ25-53..55; the file split itself stays constrained → CQ25-77. (00-index.md § Open leaves)
- **CQ25-16** open-leaf · optional — Leaf 46: identifiers name the wrong thing (owns leaf 17's pure renames); only optional S19/S20 remain → CQ25-70/71; client `pid` sweep unscheduled-opportunistic → CQ25-169. (00-index.md § Open leaves)
- **CQ25-17** open-leaf · blocked — Leaf 49: `scripts/path-policy/` fixture analyzer (nine modules, one 803-line test); H13/H14 landed (`64a7fac64`); H15 → CQ25-64 blocked on 27.3; the test split was dropped → CQ25-134. (00-index.md § Open leaves)
- **CQ25-18** open-leaf · scheduled — Leaf 53: initiative ties need a DM-facing resolution workflow; superseded by 53-PLAN → CQ25-56..61; owner ruling stands: DM resolution, never an automatic secondary sort key. (00-index.md § Open leaves)
- **CQ25-19** open-leaf · scheduled — Leaf 71: lint and runtime nested-write walkers duplicate one wrapper/data/ambiguous payload-state machine (`eslint-rules/concurrency-guard.js:307-423` vs `packages/server/src/prisma/nested-write-guard.ts:15,128-161`); simplification only — runtime stays authoritative, 45-case lint corpus floor stays. (71-nested-write-walker-parity.md § Problem)
- **CQ25-20** open-leaf · scheduled — Leaf 72: sheet capability gating can fail open when a prop builder gains a callback (`packages/client/src/pages/character-sheet/sheet-helpers.ts:117-215` spread into `sheet-layout.tsx:244-258` then selectively overridden); compile-time hardening; empty viewer details region is opportunistic-only. (72-sheet-capability-callback-source.md § Problem)

## 2. Plan slices and recorded-but-unscheduled work (56)

### 07-PLAN (leaf 07)

- **CQ25-21** plan-slice · scheduled — 07.1 remainder (creation half landed via S11, merge `11af9d4c8`): delete the false "Caster first" sentence above `collectAffectedCharacterIds` in `services/spell-casting/spell-casting.ts`, correct `spell-casting/MODULE.md` ordering prose to UTF-16 code-unit, pin the `affectedCharacterIds` ordering contract in the facade test; do not touch `seed/spell-parser/extract-spell-combat.ts`. (07-PLAN.md § Slices)
- **CQ25-22** plan-slice · scheduled — 07.2: type-fidelity in `services/spell-casting/` — `ABILITY_FULL_NAMES` import in `resolve-character-spell.ts`, narrow `combat` on `CharacterSpellContext` (drop `!combat ||` branches, retype `resolveDamageDice`), type `getTargetParticipantIds` as `CastCombatSpellInput` in `load-participants.ts`; keep the `MAX_COMBAT_SPELL_TARGETS` throw. (07-PLAN.md § Slices)
- **CQ25-23** plan-slice · scheduled — 07.3: characterization-only — three tests in `spell-casting-non-combat.test.ts` pinning ritual/cantrip discard of `castAtLevel`+`metamagicIds`, plus an "Open rules question" section in the leaf; gates any future `CastMode` work. (07-PLAN.md § Slices)
- **CQ25-24** plan-slice · scheduled — 07.4 (race-sensitive): extend `level-up/asi.test.ts`+`level-up-concurrency.test.ts` to all six abilities, then `AsiContext.abilityDeltas` → `ReadonlyMap<keyof AbilityScores, number>` in `level-up/types.ts`, remove both `type-assertion-boundary` casts in `asi.ts`. (07-PLAN.md § Slices)
- **CQ25-25** plan-slice · scheduled — 07.5: `newClassLevel: number` on `LevelUpContext` (`core.ts`/`level-up.ts`/`apply-level-up.ts`), `hpMethod: string` → shared `HpMethod`, relocate three misfiled describes from `asi.test.ts` to `core.test.ts`; no `buildClassOperation` rename, no `core.ts` split. (07-PLAN.md § Slices)
- **CQ25-26** plan-slice · optional — Deferred, not dropped: class-transition discriminated union for `LevelUpContext`; opens as its own leaf only if 07.5 lands clean. (07-PLAN.md § Rejected alternatives — why)

### 27-PLAN (leaf 27) — plan has no State column; 00-index.md is the landing-state authority (no 27.x landed)

- **CQ25-27** plan-slice · scheduled — 27.1: write the two-tier test contract into `scripts/README.md:68-81` (TS-first boundary, `scripts/test-support/`, new-suite-over-extension rule, ai-hooks colocation paragraph); no runtime change. (27-PLAN.md § Slices)
- **CQ25-28** plan-slice · scheduled — 27.2: add `scripts/tests/lib/test-assertions.sh` (`fail()`/`ok()`/pass counter lifted verbatim from `scripts/ai-hooks/test-support.sh:1-86`), source it there, migrate exactly one small suite; API frozen at this slice. (27-PLAN.md § Slices)
- **CQ25-29** plan-slice · scheduled — 27.3 (blocks H15): extract `scripts/tests/test-pre-commit.sh` out of `scripts/tests/test-dependency-freshness.sh:271-2386`, hoist 7 hook helpers to `scripts/tests/lib/`, move `.husky/pre-commit`/`.husky/post-commit`/`scripts/prisma-client-freshness.sh`/`scripts/doc-length-policy.sh` smoke subjects. (27-PLAN.md § Slices)
- **CQ25-30** plan-slice · scheduled — 27.4-27.7 (banded, one family per session): extract ai-hooks families out of `scripts/ai-hooks/test.sh` into `test-<family>.sh` — command-policy block, backlog-note-lint, commit/worktree/queue block, failure-guidance/output-filter. (27-PLAN.md § Slices)
- **CQ25-31** plan-slice · scheduled — 27.8+ (open-ended): split `scripts/tests/test-lint-ratchet.sh` (72 subjects) one subject cluster per session — merge-driver/git-rail, governance/config, kernel/CLI. (27-PLAN.md § Slices)
- **CQ25-32** plan-slice · scheduled — 27.9 (prereq for 27.10): widen single-valued `smokeTest` in `harness.controls.json` to a list; teach `addSmokeSubjects` (`scripts/harness/skill-artifact-projection.ts:117-127`) to route; no shell relocation. (27-PLAN.md § Slices)
- **CQ25-33** plan-slice · scheduled — 27.10: split `scripts/tests/test-skill-dispatch-wrappers.sh` by routable skill/backend group; the generated block `:3-23` is `harness:skills:refresh` output, never hand-edited. (27-PLAN.md § Slices)

### 28-PLAN (leaf 28) — plan has no State column; 00-index.md is the authority (28.1 unlanded; it blocks 28.4-28.11 and H11/H12)

- **CQ25-34** plan-slice · scheduled — 28.1: the written layout position — mark `docs/agent_notes/backlog/scripts-flat-family-reorg.md` Superseded, move/exception/defer ruling for the seven flat families, add `data/`/`lint-message-eval/`/`test-support/` rows and the two `scripts/harness/` spellings to `scripts/README.md`; doc-only. (28-PLAN.md § Slices)
- **CQ25-35** plan-slice · scheduled — 28.2: add `scripts/drift-ai/MODULE.md` (name prefix families, promote the layering law from `check-metadata.ts:1-6`); no restructuring. (28-PLAN.md § Slices)
- **CQ25-36** plan-slice · scheduled — 28.3: harness-audit consolidation — move `scripts/harness-audit.ts`'s report module and `scripts/harness/harness-audit.test.ts` into `scripts/harness-audit/`; facade stays; `fixtures/` unmoved; no `eslint-config/shared-policy.js` or `tsconfig.scripts.json` edits. (28-PLAN.md § Slices)
- **CQ25-37** plan-slice · scheduled — 28.4: declarative layout inventory + checker over all 162 direct children of `scripts/`; wired into full `harness:check` only, never registration admission. (28-PLAN.md § Slices)
- **CQ25-38** plan-slice · scheduled — 28.5: move `client-test-isolation` internals/tests under an owner directory; runner/facade retained. (28-PLAN.md § Slices)
- **CQ25-39** plan-slice · scheduled — 28.6: same move for `backlog-lint`; `scripts/backlog-lint.ts` facade retained. (28-PLAN.md § Slices)
- **CQ25-40** plan-slice · scheduled — 28.7: same move for `suppression-ledger`; `scripts/suppression-ledger.ts` facade retained. (28-PLAN.md § Slices)
- **CQ25-41** plan-slice · scheduled — 28.8: same move for `sensor-knip-unused-exports`; sensor + merge-CLI facades retained; two sensor families get two directories, never one `scripts/sensor/`. (28-PLAN.md § Slices)
- **CQ25-42** plan-slice · scheduled — 28.9: same move for `sensor-near-duplicates`; pre-push-visible paths preserved. (28-PLAN.md § Slices)
- **CQ25-43** plan-slice · scheduled — 28.10: same move for `max-lines`; command + merge-CLI facades retained; no `eslint-config/` edits. (28-PLAN.md § Slices)
- **CQ25-44** plan-slice · scheduled — 28.11 (last, most entangled): `lint-coverage-map` move — `triggerPaths`, fixture paths, shell fixture copies, `docs/generated/lint-coverage-map.md` and subjects updated atomically. (28-PLAN.md § Slices)

### 34-PLAN (leaf 34)

- **CQ25-45** plan-slice · scheduled — 34.1: export `TYPE_ONLY_CYCLE_DETAIL_KEY`+`isTypeOnlyCycleFinding` from `scripts/drift-ai/types.ts`, write at `import-cycles.ts`, repoint `runner.ts` and `scripts/drift-triage/triage-report-support.ts`; annotate the three triage sets as `ReadonlySet<string>` with `DriftCheckId` literals; refresh `scripts/drift-triage/MODULE.md`. (34-PLAN.md § Slices)
- **CQ25-46** plan-slice · scheduled — 34.2: move the knip memo into `env.reportCache` (thread from `knip-pass-through-check.ts:resolveKnipRunner`), delete `clearKnipRunCache` + its `report-builder.ts` call, rewrite 15+ call sites across `scripts/drift-ai.test.ts`/`knip-runner.test.ts`/`knip-unused-exports.test.ts`. (34-PLAN.md § Slices)
- **CQ25-47** plan-slice · optional — Step-7 carve-out (scoped, not scheduled, not dropped): zod narrowing of the triage-contracts family only — 465 lines across `triage-report-input.ts`+`triage-report-drift-input.ts`; `triage-verdict-input.ts` excluded. (34-PLAN.md § Step 7 ruling: carved out, scoped, not dropped)

### 40-PLAN (leaf 40)

- **CQ25-48** plan-slice · scheduled — 40.1: `packages/client/src/test/fixtures-character.ts` intersection annotation `CharacterDetail & { stats: NonNullable<…> }`; delete 8 inline casts there plus 7 `as unknown as CharacterDetail` and 15 of 16 `.stats as NonNullable<…>` across cast-rail/tabs/`asi-step.test.tsx`. (40-PLAN.md § Slices)
- **CQ25-49** plan-slice · scheduled — 40.2: file-local `buildMonsterPayload(overrides)` in `packages/shared/src/schemas/homebrew.test.ts` (collapse 5 range-rejection cases, sweep 8 full literals; no `monsterDataSchema` export), then move 7 describes into new `homebrew-inputs.test.ts`. (40-PLAN.md § Slices)
- **CQ25-50** plan-slice · optional — 40.3 (opportunistic, no standalone session): promote `buildFeatData` into `packages/server/src/test/homebrew-test-helper.ts` and the `OriginColumns`/`CLEARED_ORIGIN`/`readOrigin` cluster into `map-test-helper.ts`. (40-PLAN.md § Slices)
- **CQ25-51** plan-slice · scheduled — 40.4: add a player+monster active-encounter builder to `packages/server/src/test/encounter-test-helper.ts` wrapping private `createAndActivateEncounter`; route `encounter-hp-attribution.test.ts:46` and `encounter-combat-concurrency.test.ts:41` through it. (40-PLAN.md § Slices)

### 41-PLAN (leaf 41)

- **CQ25-52** plan-slice · optional — 41.3 (explicitly not scheduled): domain split of the 16 in-file builders in `packages/client/src/test/mock-trpc.tsx` into per-domain `mock-trpc-*.ts`, MODULE.md refresh, lower/delete the max-lines exception; must first place `cursorIdFromPageParam` (`mock-trpc.tsx:264`). (41-PLAN.md § Slices)

### 42-PLAN (leaf 42)

- **CQ25-53** plan-slice · scheduled — 42.1: in `e2e/encounter-combat.spec.ts`, rewrite the restoration loop at `:379-386` to poll `EncounterPO.currentParticipantName()` (kill three guaranteed 5s failed waits); delete the second restoration block `:695-698`; give `:719` its own `goto` precondition. (42-PLAN.md § Slices)
- **CQ25-54** plan-slice · scheduled — 42.2 (deliberately outside leaf 42's stated surface): swap `loginViaUi` for `loginViaApi` in `e2e/helpers/campaign-setup.ts:116/:120` (`setupDmAndPlayer`), optional second commit for `setupUserWithCharacter`; do not touch `e2e/auth-refresh.spec.ts`. (42-PLAN.md § Slices)
- **CQ25-55** plan-slice · optional — Step-1 carve-out (demand-gated, built only when a second consumer appears): six API wrappers in `e2e/helpers/api.ts` (encounter create/addParticipant/setInitiative/transitionState/advanceTurn + campaign.assignCharacter) plus a layered composed active-encounter seed; no `rollAllInitiative` wrapper. (42-PLAN.md § Step 1 ruling: carved out behind a demand gate)

### 53-PLAN (leaf 53)

- **CQ25-56** plan-slice · scheduled — T1: `packages/shared/src/rules/initiative.ts` — add `findInitiativeTieGroups`+`suggestInitiativeOrder`, rename `sortByInitiative` away (no wrapper), rewrite doc comment and the three policy tests with SRD/Musi-policy/suggestion provenance. (53-PLAN.md § Slices)
- **CQ25-57** plan-slice · scheduled — T2 (only irreversible slice): Prisma `EncounterParticipant.initiativeTieResolved Boolean @default(false)` + backfill; roll-all and `buildBlindData` write `false`; expose on `encounterParticipantSchema` and `mapParticipant`. (53-PLAN.md § Slices)
- **CQ25-58** plan-slice · scheduled — T3: `orderBy: PARTICIPANT_ORDER` on both `findMany`s (`combat-actions/initiative.ts:32-34`, `activate-encounter.ts:35-37`); activation sort initiative-descending only; rewrite both "deliberately unordered" comments; no contiguous-`sortOrder` assertion. (53-PLAN.md § Slices)
- **CQ25-59** plan-slice · scheduled — T4: derive `initiativeTies` in `mapEncounterDetail` with `isDm` projection, add to `encounterDetailSchema` (`packages/shared/src/schemas/encounter.ts:179-185`), pending-group `BAD_REQUEST` in `activateEncounter` pre-tx and in-tx. (53-PLAN.md § Slices)
- **CQ25-60** plan-slice · scheduled — T5: new `resolveInitiativeTie` mutation (shared input schema + router entry + service under `services/encounter-combat/`); DM-only, setup-only, in-tx re-read, set-equality validation, within-group `sortOrder` permutation + flag; two concurrency tests. (53-PLAN.md § Slices)
- **CQ25-61** plan-slice · scheduled — T6: new `initiative-tie-panel.tsx` under `components/campaign/combat/initiative-tracker/`, mounted from `encounter-detail-view.tsx` (tracker hidden in setup); arrow reorder, per-group Confirm, Start Combat disabled + tooltip, tied badge, non-DM read-only. (53-PLAN.md § Slices)

### HARNESS-CLUSTER-PLAN remaining slices

- **CQ25-62** plan-slice · blocked (on 28.1, and H1→H11) — H11: create `scripts/worktree-db/state.sh` part sourced from the `scripts/worktree-db.sh` facade with `__MUSI_WTDB_STATE_SOURCED` guard; merge-driver installers stay put. (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-63** plan-slice · blocked (on 28.1, H11→H12) — H12: move the `worktree-seed` family (5 non-test modules + 2 tests) into `scripts/worktree-db/`; update `scripts/worktree-db.sh:433` `MUSI_SEED_IMPORT_CLOSURE_CHECKER` default. (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-64** plan-slice · blocked (on 27.3, H14→H15) — H15: `scripts/path-policy/` dead data — delete `path-policy.ts:256-264` `sourceRelevant` + type field, hoist `excludedDirectoryNames`, resolve `matchesFormatCheckCandidate` (`path-policy-query-core.ts:115-118`), memoise `SCRIPT_SMOKE_TEST_NAMES`. (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-65** plan-slice · optional — H20 (unstarted): reshape the seven-optional `CodeIntelContext` bag (`scripts/code-intel/source-project.ts:15-23`) and its `query-executor.ts` consumers; keep `graph-cache.ts:152` `{}` call working. (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-66** plan-slice · optional — H21 (unstarted): extract `ManifestGatedCache<TEntry>` from `scripts/code-intel/graph-cache.ts`/`ProjectCache`; record the routing reason at `daemon-query.ts:46-55`; payloads NOT merged. (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-67** plan-slice · optional — H15 follow-up (separate migration, not settled): a sensor requiring every `scripts/**` TS/JS smoke subject to declare its static import closure (~47 pre-existing rows across `path-policy-smoke-subjects{,-data}.ts`, `scripts/lint-ratchet/paths.ts`, `eslint-config/config-surfaces.js`). (HARNESS-CLUSTER-PLAN.md § Slices, H15 blockquote)

### SERVER-COMMENTS-PLAN remaining slices (all optional)

- **CQ25-68** plan-slice · optional — S17 (leaf 03 step 4): convert `assertCharacterOwner` (`utils/character-auth.ts:18-23`) to `(prisma, characterId, caller: AuthzCallerContext)` in ONE commit across all 21 sites (12 production routers/services + 8 test + internal `:207`), deleting the `:205-207` re-split. (SERVER-COMMENTS-PLAN.md § Slices)
- **CQ25-69** plan-slice · optional — S18 (leaf 03 step 6; hard edge S17→S18): converge `utils/campaign-auth.ts` `assertCampaignMember`/`assertCampaignDm` (42 production + 16 test sites) and `utils/note-auth.ts` `loadNoteForMutation` (8 sites) on the object caller, one file per commit; boundary comments byte-unchanged. (SERVER-COMMENTS-PLAN.md § Slices)
- **CQ25-70** plan-slice · optional — S19 (leaf 46 steps 1+3; discharges leaf 05 step 3): rename `action`→`actionMessage`/`concentration`→`concentrationMessage` in `utils/combat-chat.ts:92/:98`; collapse ten one/two-letter mapper params in `routers/srd.ts` onto `row` (the `f`-is-two-entities hazard). (SERVER-COMMENTS-PLAN.md § Slices)
- **CQ25-71** plan-slice · optional — S20 (replaces leaf 46 step 8): declare the `{x;y;w;h}` fog callback rect once and import at the five inline sites (`hooks/canvas-input/tool-handlers.ts:139`, `use-canvas-input.ts:41`, `maps/map-canvas.tsx:35`, `map-fog-actions.ts:32,:47`); keep `w`/`h`; do not touch `packages/shared/src/map/fog.ts`. (SERVER-COMMENTS-PLAN.md § Slices)

### Recorded-but-unscheduled follow-ups (no slice exists; findings here duplicate these records)

- **CQ25-72** plan-slice · optional — Leaf 31 unscheduled review follow-up (not H9/H10): seven `scripts/drift-ai/` modules repeat the same normalized changed-path `Set` construction (`duplicate-shapes.ts`, `near-duplicates.ts`, `knip-duplicates.ts`, `knip-orphan-files.ts`, `knip-unused-exports.ts`, `import-cycles.ts`, `layer-direction.ts`). (31-harness-shared-helpers.md § Proposed direction, "Unscheduled review follow-up")
- **CQ25-73** plan-slice · optional — Leaf 31 unscheduled review follow-up: `defaultProcessIdentityProbe` (`scripts/code-intel/lifecycle-probe.ts:75,118`) has no direct test — an `&&`→`||` mutation in `hasMatchingProcessIdentity` survives; add focused coverage separately. (31-harness-shared-helpers.md § Proposed direction, "Unscheduled review follow-up")
- **CQ25-74** plan-slice · optional — Leaf 36 step 4, dropped to its own future leaf: the `testId`→`ratchetId` rename (~170 occurrences) across the lint-ratchet kernel vocabulary. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-75** plan-slice · optional — Deliberately not acted on, future leaf: the `utils/`-orchestration taxonomy question — `packages/server/src/utils/character-campaign.ts:51-72` and `utils/combat-chat.ts:48-104` orchestrate vs `services/README.md:76-78`. (SERVER-COMMENTS-PLAN.md § One taxonomy observation, correctly scoped, that this plan does not act on)
- **CQ25-76** plan-slice · optional — Declined-but-explicitly-available: narrow `skillModifier`'s `skillName` param to `SkillName` (leaf 20's stated blocker was false — neither cited caller calls it). (SHARED-CLUSTER-PLAN.md § Rejected alternatives — why)

## 3. CONSTRAINTS.md rulings (38) — do not re-propose

All anchors: CONSTRAINTS.md § Constraints on future proposals (table row starting with the quoted "Do not" phrase).

- **CQ25-77** ruling · standing — Do not split `e2e/encounter-combat.spec.ts` (779-line serial describe, nine shared `let` fixtures) before reusable API seeding exists; leaf 42 owns the split.
- **CQ25-78** ruling · standing — Do not fold the four lint-ratchet guides together; the split is audience-partitioned and deliberate (2,080 lines, no overlapping text).
- **CQ25-79** ruling · standing — Do not treat `sensor-near-duplicates.baseline.json` as a work queue; it is adjudicated triage output (drift-triage-2026-07-13 verdicts).
- **CQ25-80** ruling · standing — Do not merge `trpc-shared-input-schema.js`/`-output-schema.js`; shared machinery already lives in `trpc-shared-schema-import-collector.js`.
- **CQ25-81** ruling · standing — Do not merge the codemod input/output candidate finders (inherits the rule-merge remedy above).
- **CQ25-82** ruling · standing — Do not "de-duplicate" `harness-emit-envelope.ts`'s two validators; distinct user-facing messages, recorded accept-with-reason.
- **CQ25-83** ruling · standing — Do not parameterize the baseline merge-driver family; already parameterized (~15-line installers over a 155-line shared body).
- **CQ25-84** ruling · standing — Do not remove `WEAPON_MASTERY_MAP`'s `?? null` as dead code; live under `noUncheckedIndexedAccess: true`.
- **CQ25-85** ruling · standing — Do not collapse the two lookup mechanisms in `conditions.ts`; `isValidCondition` is a type predicate, `Map.has()` does not narrow.
- **CQ25-86** ruling · standing — Do not force `homebrew.ts`'s three hand-rolled schemas onto `.extend`; different contracts (read-vs-write damage-type split documented at `monster.ts:161-176`).
- **CQ25-87** ruling · standing — Do not consolidate result/response schemas in `combat-action.ts`; already consolidated (`spellCastResultSchema` is the `.output(...)` at `cast-spell.ts:103`).
- **CQ25-88** ruling · standing — Do not build one authoritative SRD equipment catalog; drift guard exists (`srd-weapon-sync.test.ts`) and both consolidations contradict documented decisions.
- **CQ25-89** ruling · standing — Do not replace the e2e response readers wholesale; the one 35-line partial reader (`e2e/page-objects/vtt-drawer-response.ts`) is deliberate runtime validation.
- **CQ25-90** ruling · standing — Do not delete the `in-vtt-drawer` effect as redundant with render; `close` resets the whole global drawer store.
- **CQ25-91** ruling · standing — Do not give `use-weapon-roll.ts`'s `isNatural20` the `d20`-notation gate `detectCrit` has; asymmetry deliberate, premise pinned producer-side (`equipment-summary.test.tsx`).
- **CQ25-92** ruling · standing — Do not convert `PROFICIENCY_BONUS_TABLE` or the three cantrip tables to clamp-then-formula; permanently refuted — not behaviour-preserving (`proficiencyBonus(21)`: 2 vs 6) and the guide mandates table form.
- **CQ25-93** ruling · standing — Do not rename `twoHandedDice` to `versatileDice` in any writer or persisted schema; Zod strips unknown keys and erases legacy rows; leaf 19 steps 3-4 are not scheduled.
- **CQ25-94** ruling · standing — Do not derive `ACTION_ECONOMY_CONFIG`'s `field` from `ActionEconomyState`; three independent booleans encode no relation; leaf 26 step 3's action-economy half is dropped.
- **CQ25-95** ruling · standing — Do not call a rules-facing slice complete without classifying SRD / named-outside-source / Musi-policy provenance in the policy-protecting test names.
- **CQ25-96** ruling · standing — Do not call a "make the doc true" slice complete after checking only cited claims; editing a factual documentation claim makes the whole claim yours.
- **CQ25-97** ruling · standing — Do not add `characterSpell` to `RestrictedDelegates`/`GATED_DELEGATES`; leaf 51 refused it (transaction-local fix; single-writer table); reopen only with a full call-site inventory.
- **CQ25-98** ruling · standing — Do not simplify `isPrismaSerializationFailure` to the P2034 check or hand-roll retry-shape tests; commit-time `DriverAdapterError`/`TransactionWriteConflict` is the common case (measured 17:1), pinned against the real driver.
- **CQ25-99** ruling · standing — Do not restore wording that SSI protects long rest against concurrent `performLevelUp`, or `resetAllHitDice` as universal barrier; both false — `serializable-isolation.test.ts` pins the real mechanism.
- **CQ25-100** ruling · standing — Do not hand-list relation names in `local/concurrency-guard` or the runtime nested-write guard, or drop model/context tracking; the generated relation-subgraph artifact is authoritative and drift-tested.
- **CQ25-101** ruling · standing — Do not expand the nested-write runtime guard to create/delete/connect-style operators, or add bypass/env-switch/telemetry/config surfaces or a raw client factory; leaf 60 v1 boundary is deliberate.
- **CQ25-102** ruling · standing — Do not retype service/utility contexts as `{ req: { server: FastifyInstance } }`; the narrow shape is deliberate (`inventory-service.ts:52-55`); the `BroadcastHost` port landed instead.
- **CQ25-103** ruling · standing — Do not trim the `packages/server/src/utils/*-mutations.ts` headers against `docs/CONCURRENCY.md`; three altitudes of one contract; leaf 45 step 4 refused permanently.
- **CQ25-104** ruling · standing — Do not rename `w`/`h` in `packages/shared/src/map/fog.ts`; persisted `FogLayerData`; leaf 46 step 8 was replaced by S20 (extract the inline rects, keep the spelling).
- **CQ25-105** ruling · standing — Do not remove the `routers/encounter.ts:192-193` casts by making `isValidTransition` a type guard; a predicate cannot narrow `input.to`; casts stay marked `interop` (S14 fixed the header instead).
- **CQ25-106** ruling · standing — Do not write a scanning guard that can pass when its scan finds nothing, or that discovers subjects from the allowlist it compares against; discovery by pattern + non-empty assertion per subject.
- **CQ25-107** ruling · standing — Do not treat `X extends Port` over an all-optional port as pinning a seam; vacuous — use identity assertions (`Equal<BroadcastHost["io"], FastifyInstance["io"]>` in `socket/__type-tests__/broadcast-host.ts`).
- **CQ25-108** ruling · standing — Do not plan "export the type first, adopt it later" as two commits; `sensor-knip-unused-exports` fails an unconsumed export on every pre-commit; order the adopters instead.
- **CQ25-109** ruling · standing — Do not derive a destructive statement's allowlist from a module that parses the same names; each `DROP` guard owns its alphabet/bounds, sharing only identity constants — the duplication is the boundary, not debt.
- **CQ25-110** ruling · standing — Do not call a value/schema single-sourced before a whole-tree semantic-straggler sweep (by identifier AND literal/semantic role); acceptance criterion for every single-sourcing slice.
- **CQ25-111** ruling · standing — Do not add the user-targeted `character:associationChanged` event without new product evidence; leaf 63's whole-family invalidation is the recorded tradeoff — unbounded cross-client staleness accepted.
- **CQ25-112** ruling · standing — Do not give `eslint-config/` its own Vitest project merely to colocate its three config-focused suites; leaf 38 implemented and removed exactly that split.
- **CQ25-113** ruling · standing — Do not rebuild a shared manifest/fixture framework/list parser for the two lint-ratchet sandbox copy sets; leaf 68 closed it with focused closure assertions; the shell loop stays `unmodelled-copy`.
- **CQ25-114** ruling · standing — Do not add a separate failed-campaign-lookup roll-hook composition test; leaf 69's optional case is deliberately closed — the two existing layers already compose.

## 4. Do-not-reopen (109)

### Landed state (cite for `priorPackOverlap.kind: "landed"`)

- **CQ25-115** do-not-reopen · landed — Shared cluster finished: 21/22 slices across `028a21d5` (W1), `7a4b10ac` (W2,A1,D1,D2,K1-K3), `ec4d732c4` (I1-I3,R1-R3), `75bad57dc` (U1,U2,S1-S3,E1,E2) — weapon/dice/class-identity/constants/schema-layout/dead-code work across `packages/shared/src/`. (SHARED-CLUSTER-PLAN.md § Slices)
- **CQ25-116** do-not-reopen · closed-declined — U3: the four `campaignId`-only socket payload schemas (`campaign:join`/`leave`/`updated`, `presence:heartbeat`) stay independent wire contracts; derive/alias would defeat `broadcast-registry.test.ts:112`'s schema-identity mis-wiring gate for ~8 lines; all three panelists concurred. (SHARED-CLUSTER-PLAN.md § Slices, U3 row)
- **CQ25-117** do-not-reopen · landed — Client cluster finished: 15/15 slices across `6cf8c78d5` (C1-C4,V1,V2,O1) and `d539cfdbd` (N1,Q1-Q3,F1,F2,X1,O2) — sheet MODULE.md, store reset unification, viewer/role provider, NPC typing, query layer, homebrew form helpers; post-cluster Branch A landed `c5985d1da` (leaf 54); Branch B material lives on ONLY as open leaf 63. (CLIENT-CLUSTER-PLAN.md § Slices)
- **CQ25-118** do-not-reopen · landed — SERVER-COMMENTS S1-S16 landed (`672cef373` S1/S2, `08d9443ad` S3-S6, `48ff021ed` S7/S8, `11af9d4c8` S9-S11, `3b7830ce4` S12/S13, `a01edb455` S14-S16): socket typing home + `BroadcastHost` port, registry split, prepared-limit extraction, authz caller migration, seed/test-db guards, lint-ratchet `principle` rewrites, comment-truth fixes, combat renames. (SERVER-COMMENTS-PLAN.md § Slices)
- **CQ25-119** do-not-reopen · landed — Harness cluster 18/23 landed: `2667ee8e0` (H1/H2 state writers), `ac3ce2b0f` (H3-H5 CLI), `1bfbfc115` (H6/H7 records/errorMessage), `e7462ee51` (H8 `scripts/lib/harness-finding.sh`), `bdc120756` (H9/H10 drift-ai/triage tidies), `c6e1be2a2` (H16/H17 hook-wiring doc + env rule), `e2dc60cb9` (H22/H23 lint-ratchet kernel), `57ef569e5` (H18/H19 code-intel/logs-audit typing), `64a7fac64` (H13/H14 path-policy MODULE.md + vocabulary). (HARNESS-CLUSTER-PLAN.md § Slices)
- **CQ25-120** do-not-reopen · landed — 41-PLAN slices 41.1 (`withStrictMockAccess` proxy guard in `packages/client/src/test/mock-trpc-guard.ts`) and 41.2 (names-only router-binding type test) implemented on `fix/cq-41-mock-trpc-binding`; pack docs record the branch only (git shows merge `8deccfcbb`). (41-PLAN.md § Slices)
- **CQ25-121** do-not-reopen · landed — 52 leaves landed or deliberately closed (01, 02, 04-06, 08-26, 37-39, 43-45, 47, 48, 50-52, 54-70) across twenty-seven deliveries 2026-07-26..07-31; each leaf's Status header carries branch/merge/divergences. (00-index.md § Landed)
- **CQ25-122** do-not-reopen · landed — Prior art: `arch-plans-2026-07/03-harness-hook-shim-generation.md` (Done, `3e9b28df`) owns the generated `.claude`/`.codex`/`.copilot` shims; leaf 32 owned only the git hooks that convention excludes. (CONSTRAINTS.md § Prior art: landed plans these leaves re-enter)
- **CQ25-123** do-not-reopen · landed — Prior art: `arch-plans-2026-07/02-harness-cli-parse-spec.md` (Done, `62285ebb`) built spec-driven `parseCli` in `scripts/lib/cli.ts`; surviving hand-rolled argv walkers are policy ("converge opportunistically", `scripts/lib/process-argv.ts:8`), not oversights. (CONSTRAINTS.md § Prior art)
- **CQ25-124** do-not-reopen · landed — Prior art: `arch-plans-2026-07/05-verify-metadata-ts-analytical-core.md` (S0+S1 `ebe99dd0`, S3 dropped) is the measured precedent for bash-facade-over-TS-core splits; `jq` stays a hook/CLI-local exception. (CONSTRAINTS.md § Prior art)

### Harness cluster / 27-PLAN / 28-PLAN declined directions

- **CQ25-125** do-not-reopen · dropped — Leaf 29 step 5: `scripts/worktree-db/*-core.ts` TypeScript codec extraction; `worktree-db.sh` fails the 05-plan measured bar for that split. (HARNESS-CLUSTER-PLAN.md § Substrate verdict)
- **CQ25-126** do-not-reopen · dropped — Leaf 29 step 3, and the full seven-seam `worktree-db.sh` decomposition; shrunk to H11's one seam + H12's `worktree-seed` move. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-127** do-not-reopen · recorded-decision — The bulk argv-offset sweep (~22 remaining local re-declarations) and leaf 30 steps 3-remainder/4 (19 hand-rolled walkers): not scheduled; classify/converge on touch per the standing policy comment. (HARNESS-CLUSTER-PLAN.md § Substrate verdict; § Per-leaf disposition)
- **CQ25-128** do-not-reopen · recorded-decision — A lint rule banning new local argv-offset constants: named follow-up, explicitly not scheduled. (HARNESS-CLUSTER-PLAN.md § Rejected alternatives + why)
- **CQ25-129** do-not-reopen · refused — Leaf 32 step 2: rewriting `scripts/harness/generate-hook-wiring.ts`'s JSON scanner onto `jsonc-parser` — closed as won't-do. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-130** do-not-reopen · refused — Leaf 32 steps 4-5: the `.husky/pre-commit` dispatcher split — do not schedule; reopen only under all five recorded conditions (27.3 landed; second body consumer; `registration-preflight-wiring.ts` repointed first; fail-closed `wrapper_command` dispatcher; lands alone). (HARNESS-CLUSTER-PLAN.md § Operational risk: the commit gate)
- **CQ25-131** do-not-reopen · dropped — Leaf 33 steps 2-3: env-var inventory and enforcement machinery. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-132** do-not-reopen · dropped — Leaf 35 step 6b: merging `GraphCache`/`ProjectCache` payloads; needs an unmade measurement first. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-133** do-not-reopen · refused — Leaf 36 step 3: moving `allowEmpty` out of the lint-ratchet kernel — dropped and recorded as wrong (it is kernel behaviour). (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-134** do-not-reopen · dropped — Leaf 49 step 3: splitting `fixture-shell-dependencies.test.ts` (803 lines); both consults declined independently. (HARNESS-CLUSTER-PLAN.md § Per-leaf disposition)
- **CQ25-135** do-not-reopen · recorded-decision — Wider atomic-writer consolidation beyond H2 (`scripts/ai-hooks/cache.sh`, `scripts/ai-hooks/throttle-state.sh`, two in `scripts/verify-async.sh`): not scheduled. (HARNESS-CLUSTER-PLAN.md § Slices, out-of-scope prose)
- **CQ25-136** do-not-reopen · dropped — Leaf 27 step 2: a `register_cleanup` registry for shell suites; ship `make_sandbox` alone only when a consumer needs it (gate-critical shared trap component). (27-PLAN.md § Reconciled scope decision, Dropped or downgraded)
- **CQ25-137** do-not-reopen · dropped — Leaf 27 step 3: a shared `new_repo()` git-fixture layer over the 15 differing bodies in `scripts/tests/`; deferred as speculative — extract only at three proven identical seams. (27-PLAN.md § Reconciled scope decision)
- **CQ25-138** do-not-reopen · dropped — Leaf 27 step 4: migrating all ~52 shell suites onto the shared substrate as a completion criterion; replaced by the 27.1 growth-control policy — retire preambles opportunistically on touch. (27-PLAN.md § Reconciled scope decision)
- **CQ25-139** do-not-reopen · recorded-decision — Leaf 27 step 7: the Stop-policy assertion rewrite (`scripts/ai-hooks/` stop-policy tests) moved out of leaf 27, to be scheduled separately; any future rewrite must keep its `declare -F` auto-discovery. (27-PLAN.md § Reconciled scope decision; § Rejected alternatives + why)
- **CQ25-140** do-not-reopen · dropped — Leaf 27 step 9: `scripts/tests/test-test-*.sh` renames — pure churn; also rejected: rewriting shell smokes in Vitest wholesale. (27-PLAN.md § Reconciled scope decision; § Rejected alternatives + why)
- **CQ25-141** do-not-reopen · recorded-decision — No second pre-commit smoke suite under any other name (`test-precommit-hook.sh` included); 27.3's extraction is the only sanctioned split. (27-PLAN.md § Dependency edges)
- **CQ25-142** do-not-reopen · recorded-decision — Never wire a new check into pre-commit registration admission (`.husky/pre-commit:341-344`, 5s budget); layout inventory and similar guards go in full `harness:check` only. (28-PLAN.md § Operational risk; 27-PLAN.md § Operational risk)
- **CQ25-143** do-not-reopen · closed-declined — `docs/agent_notes/backlog/scripts-flat-family-reorg.md` is absorbed and closed by 28-PLAN's reconciliation verdict; it does not become the owner — the index's "reconcile before scheduling either" row is stale. (28-PLAN.md § Reconciliation verdict (definitive))
- **CQ25-144** do-not-reopen · refused — Leaf 28 step 2: a role-derived layout checker parsing `package.json` bodies/`.husky/` hooks/workflows/`doctor.sh`; rejected by both consults — the closed-world declarative inventory (28.4) replaces it. (28-PLAN.md § Reconciled scope decision)
- **CQ25-145** do-not-reopen · dropped — Leaf 28 step 5: the `harness-` de-prefix rename in `scripts/harness/`; the two spellings are documented in `scripts/README.md` instead. (28-PLAN.md § Also dropped or changed from the leaf)
- **CQ25-146** do-not-reopen · recorded-decision — `scripts/drift-ai/` stays 344 flat files: subdirectory restructuring rejected by both consults; 28.2 adds `MODULE.md` only. (28-PLAN.md § Also dropped or changed from the leaf)
- **CQ25-147** do-not-reopen · recorded-decision — The "when do companions become a family" threshold is deliberately not mechanised; reviewer judgement, not a filename-prefix count rule. (28-PLAN.md § Reconciled scope decision)
- **CQ25-148** do-not-reopen · refused — One `scripts/sensor/` directory for both sensor families; the two families get two directories. (28-PLAN.md § Rejected alternatives + why)

### 07/34/40/41/42/53-PLAN declined directions

- **CQ25-149** do-not-reopen · dropped — Leaf 07 step 3: `castWithoutSlot` extraction in `spell-casting/non-combat-cast.ts`; revisit only after 07.3's rules decision. (07-PLAN.md § Dropped)
- **CQ25-150** do-not-reopen · dropped — Leaf 07 step 4: `combat-transaction.ts` transaction-return restyle + `damageByTarget` hoist — dropped permanently (concurrency-sensitive style twin). (07-PLAN.md § Dropped)
- **CQ25-151** do-not-reopen · recorded-decision — Leaf 07 step 5: the internal `CastMode` union — deferred, replaced by 07.3's characterization tests; blocked on a rules decision, do not build it first. (07-PLAN.md § The step-5 call)
- **CQ25-152** do-not-reopen · dropped — Leaf 07 step 8: breaking the type-only cycle (`resolve-spell.ts`/`types.ts` `CasterRaw`/`TargetRaw`); the leaf's supporting claim was false. (07-PLAN.md § Dropped)
- **CQ25-153** do-not-reopen · dropped — Leaf 07 step 9 rename/re-export of the `castNonCombatSpell` facade (documented seam) and step 12 split of `level-up/core.ts` (premise false); only the stale doc block (07.1) and three misfiled describes (07.5) survive. (07-PLAN.md § Dropped)
- **CQ25-154** do-not-reopen · recorded-decision — Leaf 07 step 13 (seed parsers) reassigned to leaf 06, dissolving the `06 ↔ 07` edge; and the `MAX_COMBAT_SPELL_TARGETS` guard is kept by ruling, not deleted. (07-PLAN.md § Dropped; § Evidence corrections)
- **CQ25-155** do-not-reopen · dropped — Leaf 34 steps 4 (pass-existing-objects in `chunks.ts`), 5 (bounded-subprocess kernel across `duplicates/knip/semgrep/dolos-runner.ts`), and 6 (splitting the 2,765-line `scripts/drift-ai.test.ts`) — all dropped permanently. (34-PLAN.md § Step disposition)
- **CQ25-156** do-not-reopen · recorded-decision — `scripts/drift-ai/knip-runner.ts`'s SIGTERM-vs-SIGKILL classifier mismatch: recorded, explicitly not scheduled, not a live defect. (34-PLAN.md § Evidence honesty)
- **CQ25-157** do-not-reopen · dropped — Leaf 40 steps 7 (objectify the `CurrentPathEntry` tuple), 8 (split `tools/lint-ratchet/src/kernel/baseline.test.ts` — revisit conditions recorded), 9 (relocate `makeMockContext`), 10 (`*-inputs.test.ts` title sweep) — dropped. (40-PLAN.md § Step disposition; § Step 8 ruling)
- **CQ25-158** do-not-reopen · closed-declined — Leaf 40: `createMapForCampaign`+`errorOf` promotions dropped; `satisfies CharacterDetail` declined for the intersection annotation; `monsterDataSchema` export declined (leaf 24 territory); `packages/shared/src/schemas/encounter-inputs.test.ts` untouched. (40-PLAN.md § Rejected alternatives — why; § Operational risks)
- **CQ25-159** do-not-reopen · dropped — Leaf 41 step 4 (per-procedure mock value typing; all three consults) and step 6 (`AnyRecord` deletion in `mock-trpc-helpers.ts` as a goal — narrow opportunistically only). (41-PLAN.md § Step disposition)
- **CQ25-160** do-not-reopen · refused — Generalized mock input-schema parsing beyond `srd.listSpells` ("the correct generalization size is zero"); `enabled`-comparison changes re-declined. (41-PLAN.md § The input-parsing ruling: generalize to zero)
- **CQ25-161** do-not-reopen · dropped — Leaf 42 steps 2/3/4: extracting HP attribution, drawer authorization, or the six spell tests into their own specs — dropped (spell tests are browser-ordered; `:642-645` permanently levels the sorcerer). (42-PLAN.md § Step disposition)
- **CQ25-162** do-not-reopen · recorded-decision — The encounter-combat lifecycle stays `serial`; de-serialisation is not reopened (owned and closed by `testsuite-audit` leaf 04, Done 2026-07-19). (42-PLAN.md § Step disposition)
- **CQ25-163** do-not-reopen · closed-declined — Mid-combat initiative tie resolution (both round-boundary and immediate-remap variants); in combat, ties surface as a badge only. (53-PLAN.md § The leaf's four open design questions — answered)
- **CQ25-164** do-not-reopen · closed-declined — Player-facing tie ordering: declined as Musi policy; SRD 5.2.1 p.13's GM/player split deliberately not implemented. (53-PLAN.md § The leaf's four open design questions — answered)
- **CQ25-165** do-not-reopen · refused — 53-PLAN rejected mechanisms: new socket event, `Encounter.version`, new `EncounterState`, persisted tie-group snapshot/table, encounter-scoped flag, 3-column enum, narrowing `updateParticipantInputSchema`, conditional `sortOrder` blocking, drag-and-drop, combat-log entry, keeping `sortByInitiative` as a wrapper, restoring `originalIndex`, asserting contiguous `sortOrder` at activation. (53-PLAN.md § Rejected alternatives — why)

### Server-comments plan declined directions

- **CQ25-166** do-not-reopen · dropped — Leaf 04 step 6: extracting the room-exit tail (`socket/campaign-room-handler.ts:154-158`); also: the "half a boundary" framing was never adopted, and S5's surviving broadcaster deliberately lives in new `services/encounter-hp-attribution.ts` (not `character-live-state/`). (SERVER-COMMENTS-PLAN.md § Leaf disposition; § Second landing outcome)
- **CQ25-167** do-not-reopen · dropped — Leaf 05 steps 4b (promote rest to `services/rest/` — criterion 3 of `services/README.md:21-28` fails), 6 (split `routers/srd.ts` by content family), 7 (merge the two `assertTurnLock` branches — `encounter-state-mutations.ts:209-264` stays verbatim); step 3 rides optional S19 only. (SERVER-COMMENTS-PLAN.md § Leaf disposition; § Dependency edges)
- **CQ25-168** do-not-reopen · refused — Leaf 44 steps 6-9: no 60+ bare-coordinate qualification sweep (38 in `scripts/drift-ai/`; single carve-out `scripts/lib/eslint-json.ts:4-5` landed in S13), no header trims (`routes/upload-routes.ts`, `scripts/harness/harness-diagnostics-output.ts`), and step 9's failure-reporting restructure is not comment work. (SERVER-COMMENTS-PLAN.md § Ruling: leaf 44)
- **CQ25-169** do-not-reopen · dropped — Leaf 46 steps 2/4/5 (`services/rest-service.ts` locals; `EpochWindow` rename in `scripts/lib/verify-metadata-core.ts:78`; `utils/srd-query-helpers.ts` `item`/`fetch` at 44 sites); the client `pid` sweep (27 occurrences, 6 `packages/client/src` files) accepted into leaf 46 then explicitly left opportunistic-only — trap at `combat-map-content.tsx:121`. (SERVER-COMMENTS-PLAN.md § Ruling: leaf 46)
- **CQ25-170** do-not-reopen · closed-declined — Making `AuthzCallerContext.logger` required: declined with the cost written down (5 public context types + 18 fixtures across 13 files); the optional shape is the documented contract. (SERVER-COMMENTS-PLAN.md § Third landing outcome)
- **CQ25-171** do-not-reopen · parked — Leaf 06 steps 5-8 (prettier-through-generators, per-class `subclass-features/{a,b}.ts` split, `seed-srd-*` prefix rename, table-driving the eight reference-table seeders): parked, not deferred — they rewrite or depend on committed generator output unverifiable without `docs/refs/`; do not re-file as pickup work. (00-index.md § Next up; SERVER-COMMENTS-PLAN.md § Verdict)
- **CQ25-172** do-not-reopen · recorded-decision — S10/leaf 52 residue: the byte-identical-regeneration Done criterion was traded for one `// Generated by …` header per `class-features/*.ts`; provenance is an attestation, not a drift gate (no `:check` script); byte-identical regeneration proof is still owed by whoever provisions `docs/refs/`. (SERVER-COMMENTS-PLAN.md § Fourth landing outcome; 52-seed-generator-cwd-dependence.md § Status)

### Shared cluster declined directions

- **CQ25-173** do-not-reopen · dropped — Leaf 19 step 3 (normalize persisted read seams in `schemas/inventory.ts`/`srd.ts`/`homebrew.ts`): not scheduled — no reader exists outside the adapter; the `?? srd?.versatileDice` third term was dropped as incorrect (would attach SRD dice to custom weapons). (SHARED-CLUSTER-PLAN.md § What survives of leaf 19; § Corrections to the leaves, verified)
- **CQ25-174** do-not-reopen · dropped — Leaf 19 step 7's `unarmoredAc(hasShield)` half; `calculateArmorClass` (`rules/armor-class.ts:71-89`) reads `hasShield` twice by design — only the naming half (A1) landed. (SHARED-CLUSTER-PLAN.md § What survives of leaf 19)
- **CQ25-175** do-not-reopen · closed-declined — Leaf 23 steps 2-4: resolved by documenting, not extracting — no `spell-inputs.ts`/`auth-inputs.ts`/`srd-inputs.ts`; the 10 `listOutputSchema`/`searchOutputSchema` domain renames dropped (opportunistic only, and only with the `scripts/codemods/trpc-shared-output-candidates.ts:86,:91` fix in the same commit). (SHARED-CLUSTER-PLAN.md § Ruling: leaf 23)
- **CQ25-176** do-not-reopen · dropped — Leaf 22 steps 3 and 5's discriminated `notificationSchema`/`choiceDataSchema` variants; the `{}` mapper fallback does not protect `.output(...)` — only U2's read-path degradation landed. (SHARED-CLUSTER-PLAN.md § Corrections to the leaves, verified)
- **CQ25-177** do-not-reopen · recorded-decision — Leaf 18 reshapes: the name `ClassId` rejected for `SrdClassId`; two hand-written maps rejected for one total `Record<SpellClassId, SrdClassId>` + derived reverse (no bijection assertion); public lookup signatures keep `string` with internal `isSrdClassId` narrowing (preserving `?? 0`/`return []`/`?? null`); step 5 re-scoped preserve-per-site — six behaviour decisions deliberately not taken. (SHARED-CLUSTER-PLAN.md § Ruling: leaf 18 — class identity)
- **CQ25-178** do-not-reopen · refused — Do not normalise persisted `Spell.classes`; `server/src/utils/spell-mapping.ts` parses tags with no fallback — a partial backfill breaks spell reads. (SHARED-CLUSTER-PLAN.md § Persisted data and back-compat)
- **CQ25-179** do-not-reopen · refused — Dice model: never add a `kind` discriminator to `DiceGroupResult`/`RollResult` or drop `rolls` for flat terms (persisted into `ChatMessage.metadata`); keep the conditional spread at `dice-roller.ts:43`; `ParsedNotation.notation` stays optional; the `1d20+5` fixture was rewritten, not deleted. (SHARED-CLUSTER-PLAN.md § Ruling: leaf 25 — the dice model)
- **CQ25-180** do-not-reopen · recorded-decision — Constants keeps: `MAX_D20_ROLL` stays exported and `NATURAL_CRIT` named; `resolveConcentrationSave` is not routed through `resolveD20Roll` (persisted verbatim); the three `HALF_DIVISOR = 2` are not merged; `BYTES_PER_MB` (10^6) vs `MAP_IMAGE_BYTES_PER_MB` (2^20) stay distinct — adjudicated, pinned by `upload-service.test.ts:65-69`. (SHARED-CLUSTER-PLAN.md § Slices K2; § Operational risks)
- **CQ25-181** do-not-reopen · dropped — Leaf 26: step 1 (`GridCell`/`TemplateParams` export — interface is `ComputeTemplateCellsInput`, client owns its own `GridCell`); the five area-template shape functions stay exported; `ACTION_ECONOMY_TYPES` is not deletable; step 6 shrunk to one `schemas/MODULE.md` line (no `.toISOString()`/`dateTimeField` sweep; `schemas/note-inputs.ts:77` excluded). (SHARED-CLUSTER-PLAN.md § Corrections to the leaves, verified; § Slices E1/S1)
- **CQ25-182** do-not-reopen · recorded-decision — The cantrip tables cannot receive SRD provenance (keyed on `CasterType`; 2024 SRD differs — a citation would be fictitious; the non-SRD decision is named instead); leaf 20 step 2's CR mirror fold demoted (property test already round-trips every CR). (SHARED-CLUSTER-PLAN.md § Corrections to the leaves, verified)
- **CQ25-183** do-not-reopen · recorded-decision — K1 landed with no re-export shim in `schemas/character.ts` (ADR-0005 / no-barrel; no runtime cycle via `schemas/character.js`); R3 landed with no `eslint-disable` wrapper on `map/drawing.ts` constants. (SHARED-CLUSTER-PLAN.md § Slices K1/R3)
- **CQ25-184** do-not-reopen · recorded-decision — E1's straggler sweep deliberately left `encounter-query.ts`'s similar three-field object (privacy redaction, not a turn-start reset); E2's initiative tie-break is named Musi policy — the product decision belongs to leaf 53 only. (SHARED-CLUSTER-PLAN.md § Final delivery outcomes)
- **CQ25-185** do-not-reopen · recorded-decision — The post-landing class-semantics contradiction (a non-`SrdClassId` full caster blocks the character-create Spell step) is a latent extension-contract decision, deliberately not filed as a leaf; do not change the fallback speculatively. (SHARED-CLUSTER-PLAN.md § Post-landing class-semantics follow-on, not a slice)
- **CQ25-186** do-not-reopen · recorded-decision — U1/U2/S3 landed deliberate behaviour changes (preserve `{messageId}`-only notification rows; reject wrong-typed `campaignId`; degrade non-object `choiceData` to `{}`; tighten `character.list` output against out-of-range legacy rows) — intentional red-first changes, not regressions to "fix". (SHARED-CLUSTER-PLAN.md § Final delivery outcomes; § Persisted data and back-compat)

### Client cluster declined directions

- **CQ25-187** do-not-reopen · dropped — Leaf 08 steps 6-7: no ~49-site hand-rolled `Label`/`Input` form sweep across 22 files, no loose-root component regrouping — dropped permanently. (CLIENT-CLUSTER-PLAN.md § Leaf disposition)
- **CQ25-188** do-not-reopen · dropped — Leaf 09 steps 3-4: no `fireAfterCommit` reshape (`stores/map-canvas-store.ts:216-247`); if the store is ever split, split by *lifetime*, not tool family — recorded target, not scheduled. (CLIENT-CLUSTER-PLAN.md § Leaf disposition; § Ruling: store ownership)
- **CQ25-189** do-not-reopen · dropped — Leaf 10 steps 4-6: the no-stale-frame proof test dropped; the `enterSurface(id)` read gate dropped and *not held in reserve* (if the frame is ever proven, the answer is store instancing); deriving combat token selection from `selectedParticipantId` dropped (not total either direction); the deep-link justification is false (`maps-panel.tsx:89`, `encounters-panel.tsx:97` are component-local `useState`). (CLIENT-CLUSTER-PLAN.md § Leaf disposition; § Corrections to the leaves, verified)
- **CQ25-190** do-not-reopen · dropped — Leaf 13 dropped in full except step 1: no `<MapCanvasFrame>`, no panel primitives (`components/vtt/MODULE.md:15-18` assigns the shell to `VttSurface`); any shared unit needing a boolean flag or optional callback is out of scope — leave the duplication. (CLIENT-CLUSTER-PLAN.md § Leaf disposition)
- **CQ25-191** do-not-reopen · dropped — Leaf 14 dropped as a session (no `SheetDialogState` grouping, no `s`/`d` renames — documented dep-array identity hazard; only O2's tidies + F2's rider landed); leaf 15 steps 5-9 (drawer nested-atom union, `vtt-drawer-store.ts:42-49`) dropped — revisit only when a new drawer mode or cast variant is added, union first. (CLIENT-CLUSTER-PLAN.md § Leaf disposition)
- **CQ25-192** do-not-reopen · dropped — Leaf 17 steps 3, 5, 6 (incl. the `?? 0` default risk in `useSpellSlots`/`useSorceryPoints` and the four `realtime-invalidation.ts` hook renames); do not rename the `useFeatureUse` callback to `useFeature`. (CLIENT-CLUSTER-PLAN.md § Leaf disposition; 17-client-hook-naming.md § Scope / caveats)
- **CQ25-193** do-not-reopen · refused — Replacing `SheetSharedProps` with React context — rejected and re-affirmed 2026-07-28 (no `useEffect` may maintain such a context); leaf 12 deliberately not used to shrink the three viewer-shaped members. (CLIENT-CLUSTER-PLAN.md § Decided — design panel, 2026-07-28; § Corrections to the leaves, verified)
- **CQ25-194** do-not-reopen · refused — Per-surface Zustand store instancing (codex Move 1): rejected as a schedule, diagnosis adopted; recorded revisit triggers — a visible stale frame on Konva, two simultaneous VTT surfaces, or a fourth store in the reset set. (CLIENT-CLUSTER-PLAN.md § Ruling: store ownership)
- **CQ25-195** do-not-reopen · refused — A `features/` package-root reorganisation (both consults + author) and splitting `components/sheet/` (87 files) into subdirectories (leaf 48 is documentation-only; a split must be its own proposal). (CLIENT-CLUSTER-PLAN.md § Verdict; 48-sheet-module-doc.md § Scope / caveats)
- **CQ25-196** do-not-reopen · refused — Q1's descriptor-map/generic binder for `lib/query-invalidation.ts` (six shapes; longer than the closures) and C4's shared `useInlineEdit`/`useFieldEditor` extraction. (CLIENT-CLUSTER-PLAN.md § Rejected alternatives — why; § Slices C4)
- **CQ25-197** do-not-reopen · recorded-decision — The three homebrew `numStr` contracts stay three (`numOrStr`/`numOnly`/`numWithFallback` differ in behaviour — collapsing silently changes form defaults); `numOnly`/`numWithFallback`/`parseStringArray` stay local per the two-consumer threshold (`homebrew/shared/MODULE.md:20-21`). (CLIENT-CLUSTER-PLAN.md § Slices F1; § Rejected alternatives — why)
- **CQ25-198** do-not-reopen · closed-declined — Four client review remedies closed-declined, not pending: the remaining `isDm` half-boundaries (InitiativeTracker Next Turn; MapCanvas token dragging), renaming `useCampaignViewerScope`, regrouping `createToolAndSelectionActions`, and a `role: null` drawer pin (covered by `drawer-perms.test.ts`). (CLIENT-CLUSTER-PLAN.md § First landing outcome)
- **CQ25-199** do-not-reopen · refused — No identity branding for presence/character-socket/roll hooks/`useDmStatsCallbacks` (server backstops re-check; branding buys casts for nothing); do not merge the three stores (`stores/MODULE.md:17-19`); no server-derived or URL-addressable state in stores (`docs/architecture-plan.md:116`). (CLIENT-CLUSTER-PLAN.md § Decided — design panel, 2026-07-28; § Operational risks)
- **CQ25-200** do-not-reopen · recorded-decision — V1/V2 are not a security fix (affected population empty on producible data; the seeded unit test is the honest guard); leaf 54: the route search param was deleted as provenance, not validated — do not reintroduce it, and do not weaken the server's `NOT_FOUND`. (CLIENT-CLUSTER-PLAN.md § First landing outcome; 54-character-sheet-campaign-context.md § Scope / caveats)

### Landed-leaf divergences and residual declines

- **CQ25-201** do-not-reopen · recorded-decision — Leaf 38 residue: the `CANONICAL_FACTORY_SOURCES` central-mock extraction (step 7) was not shipped and not re-filed (`vi.mock` hoisting makes a shared plain-data module unimplementable); the thirteen parked config tests stay where they are (ten fight a documented layout). (38-eslint-rule-helpers.md § Outcome; § Scope / caveats)
- **CQ25-202** do-not-reopen · dropped — Leaf 39 steps 5 and 7 skipped as unjustified; leaf 01 step 2 skipped per the leaf's own instruction — none re-schedulable from the leaves. (39-server-test-lifecycle.md § Status; 01-prisma-boundary-type-erosion.md § Status)
- **CQ25-203** do-not-reopen · recorded-decision — Leaf 47 divergence: steps 4/5 landed in the opposite order, and five of the eight `Api*` interfaces were kept as derived aliases rather than deleted; the e2e helper is not to be replaced with the real tRPC client. (47-e2e-api-client-typing.md § Status; § Scope / caveats)
- **CQ25-204** do-not-reopen · refused — Leaf 50: the type-closure alternative (direction 3) stays refused; the six-name hand relation list was superseded by the generated artifact; nested `create`/`delete` deliberately outside the gate. (50-nested-relation-concurrency-gate.md § Status; § Scope / caveats)
- **CQ25-205** do-not-reopen · refused — Leaf 51: the conditional-write/CAS route rejected (does not fit a set-level invariant); step 4 stays with leaf 05 step 5's landed shape; the leaf's SSI framing is superseded by the landed row. (51-prepared-spell-limit-race.md § Status)
- **CQ25-206** do-not-reopen · recorded-decision — Leaf 55: Common-language identity kept byte-exact (`"common"`/`" Common"` remain distinct proficiencies; the `type:name` merge key deliberately not normalized); the review step still omits Common display when no extra language is chosen — known pre-existing omission, not fixed there; no bespoke migration test harness exists. (55-common-language-ownership.md § Status)
- **CQ25-207** do-not-reopen · recorded-decision — Leaf 56: `maxPages` deliberately not a house rule for cursor lists (evicting rendered rows is a UX change); `gcTime: Infinity` not copied to the parameterized monster/magic-item searches; notes and inventory left normally staleable. (56-infinite-list-refetch-cost.md § Status)
- **CQ25-208** do-not-reopen · recorded-decision — Leaf 57: ratchet `exitPath` validation is existence-only; semantic ownership and `file.md#section` anchor validation are out of scope (a pack-wide convention change that must not ride a one-line validator fix). (57-ratchet-exit-path-validation.md § Status)
- **CQ25-209** do-not-reopen · recorded-decision — Leaf 58: existing over-cap characters are not backfilled (same unanswered-backfill shape as leaf 55, deliberately not blocking the forward fix); do not "fix" by raising `WIZARD_PREPARED`; the wizard's 5th/6th choices stay known-but-unprepared. (58-creation-writes-over-prepared-cap.md § Scope / caveats)
- **CQ25-210** do-not-reopen · recorded-decision — Leaf 59: no global P2002 tRPC middleware; the sibling `remove` P2025 shape explicitly not covered/verified; `togglePrepared`'s P2025 window recorded but out of scope. (59-character-spell-add-unique-race.md § Scope / caveats)
- **CQ25-211** do-not-reopen · recorded-decision — Leaf 60 outcome: the nested ts-morph detector stays retired (45 former parity cases demoted to a lint-only corpus); post-review deletions (FK-ownership emission, inverse pairing, per-model wrappers, cast-defeatable client brand) stay deleted until connect-style policy has a consumer; ADR-0007 supersedes ADR-0001. (60-nested-write-runtime-guard.md § Outcome)
- **CQ25-212** do-not-reopen · recorded-decision — Leaf 61 residue: tightening `CreateCharacterInput.proficiencies[].name` to `abilityAbbreviationSchema` deliberately not done — owes a persisted-name audit plus dedup under `@@unique([characterId, type, name])` first. (61-saving-throw-proficiency-identity.md § Not done — deliberate)
- **CQ25-213** do-not-reopen · recorded-decision — Leaf 63: do not widen `character:updated` as a substitute for the declined `character:associationChanged` event (see CQ25-111); the residual cross-client mismatch has no time bound by explicit tradeoff. (63-character-assignment-cross-client-freshness.md § Declined authoritative direction)
- **CQ25-214** do-not-reopen · recorded-decision — Leaf 64: the direct and nested concurrency corpora stay deliberately separate (combining adds structure without strengthening either); no shared substrate with leaf 70's filesystem classification; the `nested-writes.ts:5` universal-parity claim was re-scoped, not made true. (64-concurrency-guard-direct-branch-parity.md § Outcome)
- **CQ25-215** do-not-reopen · recorded-decision — Leaf 65: no shared assertion helper for scanning guards (each guard keeps its reason local); historical `ratchetExcluded` fields left as baseline metadata (no format migration); the `maxLinesPolicy.ratchets` surface was retired rather than given a non-empty precondition. (65-vacuous-scanning-guards.md § Status)
- **CQ25-216** do-not-reopen · closed-declined — Leaf 66 declines: whole-sheet `readOnly` prop, a capability framework, dialog remount/reset work, changed inventory copy, and any client-side defensive authorization layer; `inventory.list`/`characterSpell.list` read gates must not widen; the first over-scoped implementation was withdrawn (discarded tip `d552c6b9d`); residuals live only as open leaf 72. (66-sheet-owner-capability-gate.md § Outcome; § Status)
- **CQ25-217** do-not-reopen · recorded-decision — Leaf 67: documentation-only by design — the rest broadcasters and `utils/prepared-spell-toggle.ts` were deliberately not moved to make `services/README.md`'s old prose true. (67-service-readme-ownership-exceptions.md § Proposed direction)
- **CQ25-218** do-not-reopen · recorded-decision — Leaf 70: renamed semantic clones (`getParent` vs `parentOf`) explicitly not detected; no new lint rule or general module-classification framework; helper homes deliberately not merged into `ast-helpers.js`. (70-eslint-helper-collision-coverage.md § Scope / caveats)
- **CQ25-219** do-not-reopen · recorded-decision — Leaf 18 caveats: `UNARMORED_DEFENSE_CLASSES` (`armor-class.ts:112`, keyed on display names) out of scope; persisted-data normalisation/migration deliberately separated from the compile-time fix. (18-shared-class-identity.md § Scope / caveats)
- **CQ25-220** do-not-reopen · recorded-decision — Leaf 06 caveats: no `:check` script in the `harness:wiring:check` idiom for seed provenance; `seedRulesGlossary` not folded into the reference-table set (eight, not nine, table-shaped functions); `worker-test-database.ts:298`'s `{1,6}` legacy variant stays; step 9 was test-db work, not seed-pipeline work. (06-seed-pipeline-and-generators.md § Scope / caveats)
- **CQ25-221** do-not-reopen · recorded-decision — Leaf 43 divergence: the step-1 feasibility stop was overturned in review and the work landed via the `.mjs` route — do not re-derive the stop. (43-stryker-config-duplication.md § Status)
- **CQ25-222** do-not-reopen · recorded-decision — Leaf 02 divergences (must not be re-scheduled): `socket/index.ts` does not re-export `AppSocketServer` (ADR-0005/`local/no-barrel` — importers point at `socket/socket-types.ts`); the port is a documented structural Fastify view, not a framework-free port; `InviteServiceContext` was migrated though unlisted. (02-fastify-io-augmentation.md § Status)
- **CQ25-223** do-not-reopen · recorded-decision — Leaf 45 sequencing residue: `encounter-state-mutations.ts:209-264`'s two `assertTurnLock` branches stay verbatim because leaf 05 step 7 was dropped — the "shape 5" comment is settled, not pending. (45-comments-compensating-for-code.md § Status)

## Distillation notes (status ambiguities and discrepancies hit while building this corpus)

1. **27-PLAN and 28-PLAN carry no per-slice landing state** (no State column; `Status: Planned`). `00-index.md` is the sole authority that no 27.x/28.x slice has landed (27.3 blocks H15; 28.1 blocks H11/H12). Records above follow the index.
2. **27-PLAN is stale on its downstream edge:** it still writes `27.3 → leaf 32 step 4` and calls 27.3 "the one that unblocks leaf 32"; HARNESS-CLUSTER-PLAN narrowed this to `27.3 → H15` because leaf 32 step 4 is not scheduled. The harness plan and index are trusted (more specific, later).
3. **The index's "Competing parked work: leaf 28 vs `scripts-flat-family-reorg.md` — reconcile before scheduling either" is stale:** 28-PLAN's "Reconciliation verdict (definitive)" already performed that reconciliation (note absorbed and closed; 28.1 merely stamps it). Recorded as CQ25-143.
4. **Index row 53 is pending-stale vs 53-PLAN:** the plan's reconciliation mandates Size L→M and Area without `socket` (unanimous "no socket work"), applied when T1 lands; the index still shows `shared+server+client+socket` / `L*`. Also `53-PLAN.md` is missing from the index's plan-inventory prose.
5. **Index still lists leaf 40 among the self-split exceptions ("40 (three leaves)")** while 40-PLAN rejects the three-leaf split outright; pending until a 40 slice lands.
6. **41-PLAN and the index record no merge SHA for 41.1/41.2** (branch name only); git shows merge `8deccfcbb`. Recorded inside CQ25-120; a lane cannot confirm the landing from the pack docs alone.
7. **SERVER-COMMENTS-PLAN has no landing-outcome prose for S12-S16** (only First-Fourth outcomes exist, covering S1-S11), and contains stale pre-landing text ("S12 … nothing gates S12"; "S3-S16 are the scheduled remainder"). The State column + index are authoritative; any S12-S16 implementation divergences are unrecorded in the pack.
8. **Two leaf-31 unscheduled follow-ups exist only in the leaf file** (`31-harness-shared-helpers.md`, Proposed-direction notes), not in HARNESS-CLUSTER-PLAN — recorded as CQ25-72/73 so lanes do not report them as novel.
9. **Index wording is weaker than the plans in two places:** leaf 36 "steps 3 and 4 were dropped" (plan: step 3 dropped *as wrong*, step 4 dropped *to its own future leaf* — CQ25-133/74); leaf 49 "the test split was dropped" (plan: both consults declined independently — CQ25-134).
10. **CLIENT-CLUSTER-PLAN's "finished" is slice-scoped:** the file's post-cluster sections (round-2 review, 2026-07-28 design panel, Branch A) are historical, and Branch B material is open leaf 63 — do not treat "client cluster finished" as covering association freshness.
11. **42-PLAN and 53-PLAN reconciliation items point at the index's constraints section, which moved to CONSTRAINTS.md:** CONSTRAINTS row 1 (CQ25-77) still says "Leaf 42 owns the split" rather than referencing the demand-gated carve-out (CQ25-55), and 53-PLAN's promised new constraint row is not present in CONSTRAINTS.md.
12. **Minor count drift:** 27-PLAN says 52 shell smoke suites; HARNESS-CLUSTER-PLAN and 28-PLAN say 53. Also HARNESS-CLUSTER-PLAN's "Corrections to the pack" supersedes several leaf-cited metrics (`worktree-db.sh` 123 top-level functions, not 126; 33 argv-offset sites/7 spellings, not 32; leaf 49 arithmetic 5,109/1,851).
13. **07-PLAN's "Index reconciliation (slice 07.1 applies these)" heading is stale:** its own header records the items as already applied, so 07.1's remainder is only the three comment/doc/test items in CQ25-21.
14. **Kind/status conventions extended beyond the spec sentence:** `optional` is also used for recorded-but-unscheduled carve-outs/follow-ups (CQ25-26/47/55/67/72-76); rulings are `standing`; do-not-reopen statuses are descriptive (`landed|closed-declined|dropped|parked|refused|recorded-decision`). Sequential ids CQ25-1..223, no gaps.
