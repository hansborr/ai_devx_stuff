# Drain ledger

Status: The table below is narrative history; § Standing grants is live policy.
Updated: 2026-08-13

The rows below are preserved as the historical review narrative. New landings
need no row or merge-sha backfill.

| Date | Unit | Branch | Review rounds | Merge | Notes (narrowing, bugs fixed) |
|---|---|---|---|---|---|
| 2026-08-11 | 169 | `fix/cq-169` | 2 | `6d8390255` | Full-scan stale-grant expiry and all-overlap match accounting implemented; no narrowing. Also fixed drain validation rejecting required pending rows beside open batch lanes. |
| 2026-08-11 | 176 | `fix/cq-169` | 2 | `6d8390255` | Commit-bound trace provenance implemented; no narrowing; no bugs fixed. |
| 2026-08-11 | 225 | `fix/cq-169` | 2 | `6d8390255` | Canonical zero-baseline table escaping implemented; no narrowing; no bugs fixed. |
| 2026-08-11 | 230 | `fix/cq-169` | 2 | `6d8390255` | Shared hook extraction and full-extension policy implemented with Edit, Write, relative payload-cwd, and sibling-worktree fixtures; narrow ratchet predicate preserved by scope; no narrowing or bugs fixed. |
| 2026-08-11 | 170 | `fix/cq-170` | 3 | `08a096223` | Semgrep manifest key inventories compiler-bound to both source arms with completeness checks; no narrowing or bugs fixed. |
| 2026-08-11 | 102 | `fix/cq-170` | 3 | `08a096223` | Generated-doc ownership table now records the lint coverage map's hybrid boundary; no narrowing or bugs fixed. |
| 2026-08-11 | 246 | `fix/cq-170` | 3 | `08a096223` | ESLint cache identity, pruning, and argv policy moved to one typed owner with validated shell transport and lint-agent fallback; no narrowing or bugs fixed. |
| 2026-08-11 | 154 | `fix/cq-154` | 1 | `8b8ec7d51` | Direct and nested concurrency analyzers extracted behind the registered rule with corpus behavior preserved; no narrowing or bugs fixed. |
| 2026-08-11 | 155 | `fix/cq-154` | 1 | `8b8ec7d51` | Transaction callback discovery and inherited function lifecycle shared between both transaction rules with explicit declaration floors; no narrowing or bugs fixed. |
| 2026-08-12 | 224 | `fix/cq-224` | 2 | `4006d00d9` | Removed the singular debt-log append, string-only zero-baseline audit, and bare-set registry validator compatibility paths; no narrowing or bugs fixed. Audit census corrected from one to three bare-set test calls because the original single-line regex missed multiline calls; all remained test-only. |
| 2026-08-12 | 175 | `fix/cq-224` | 2 | `4006d00d9` | Documented the closed-world metric edit checklist and removed the test-only strategy enumerator; no narrowing or bugs fixed. |
| 2026-08-12 | 129 | `fix/cq-224` | 2 | `4006d00d9` | Dedicated exit-code verdict contracts now drive truth-up and pre-push without parsing diagnostic prose; no narrowing. Round-1 review fixed a branch regression that had stopped pre-push from blocking unreviewed working-tree baseline growth, assigning that verdict exit 4 (re-assigned to exit 6 in design-review Lane H) and pinning both hook consumers. |
| 2026-08-12 | 081 | `fix/cq-081` | 3 | `0632d0f1d` | Rewrote Tier-1 adoption and reference guidance around the package-plus-adapter seam, exact exports, demo-supported scripts and CI, adopter-owned plugin policy, and explicitly labeled Musi preflight/reporting extensions; no narrowing or bugs fixed. |
| 2026-08-12 | 090 | `fix/cq-081` | 3 | `0632d0f1d` | Added the advisory flock preflight and regression smoke, documented the Bash 4.1/flock prerequisites and symmetric debt invariant, and repaired the zone-cap rationale; no narrowing or incidental bugs fixed. |
| 2026-08-12 | 148 | `fix/cq-148` | 2 | `36e5848b1` | Replaced the regex scanner with parse-once TypeScript AST discovery and public syntactic diagnostics, keeping data leaves opaque; all 19 live `buildRuleSourceHashesById` results remained byte-identical; no narrowing. Round-1 review found the first implementation had made reachable MJS, CJS, JSX, and extensionless JavaScript helpers opaque, dropping their transitive imports and their dynamic-load rejection; the fix parses them and pins both behaviours. |
| 2026-08-12 | 150 | `fix/cq-148` | 2 | `36e5848b1` | Added optional repository-relative local-rule and cache directory bindings with stable defaults, generated-import coverage, and override-contained stale sweeping; no narrowing or bugs fixed. Round-1 review added the missing assertion that an overridden `localRulesDirectory` reaches the generated import URL. |
| 2026-08-12 | 160 | `fix/cq-160` | 3 | `87387f089` | Added explicit `accepted-debt` authoring, legacy kind-less normalization, unknown-kind diagnostics, and exhaustive downstream dispatch; no narrowing. Round-1 review fixed the append validator accepting and serializing raw kind-less accepted-debt objects while preserving committed legacy reads; round-3 review reported no findings for this unit. |
| 2026-08-12 | 156 | `fix/cq-160` | 3 | `87387f089` | Generated the shell edit-check contract from one TypeScript row-layout table, added all four decoders and real-emitter parity, and retargeted hook readers and fakes. No behavioral narrowing; the schema remains a freshness trigger but is omitted from `fixturePaths` because it is not in the computed runtime copy closure. Round-3 review made generation fail unless the checked layout retains the hook-required fields and remains the exact regression-reader prefix, preserving wire bytes and empty-field handling. |
| 2026-08-12 | 113 | `fix/cq-113` | 2 | `abbf8572f` | Added an adapter-side hard freshness preflight that invokes the existing POSIX dependency helper, preserving digest and legacy-mtime semantics, plus identity-only and mixed baseline-refresh classifications with separate hash and debt-floor counts. Updated adopter guidance and portable smoke coverage; no narrowing or bugs fixed. Round-1 review made missing `bun.lock` fail closed while preserving the missing-marker mtime fallback, added real shell-smoke coverage for both states, and excluded `messagesFingerprint`-only edits from the debt-floor delta count. |
| 2026-08-12 | 123 | `fix/cq-123` | 2 | `2b0683c70` | Generated canonical per-delegate concurrency repair guidance and migrated both lint and codemod consumers; no narrowing. Fixed the dead Pattern C `docs/CONCURRENCY.md` anchor and pinned the corrected anchor in generator and consumer-output tests. Round-1 review corrected the concurrency guide and ADR's structural-only descriptor descriptions, then added generator-time anchor validation. Round-2 review wired that assertion into the always-on concurrency drift suite and added a simulated Pattern C heading-rename failure, closing the gap left by advisory generated-surface freshness. |
| 2026-08-12 | 251 | `fix/cq-251` | 4 | `658e963d5` | Re-audited the lint coverage map against the live tree, removed closed implementation chronology, and corrected stale scope counts while preserving its generated span; no narrowing or bugs fixed. Round-1 review removed three remaining landing-chronology claims. Round-2 review corrected the shell partition to 139 regular + 41 ai-hooks + 1 separately itemized generated fixture using `git ls-files -- 'scripts/*.sh'`, named the live `local/max-lines` production floor, and corrected three fixture rows to the targeted `scripts/drift-ai/fixtures/**` ignore. Round-3 review found no additional map defect; the shared production row was kept current with unit 197's completed type-global fence. Round-4 review corrected the portable-package boundary description to allow internal relative/self imports while rejecting outside/sibling-package reach and removed remaining builder/retirement chronology; every review preserved the generated span. |
| 2026-08-12 | 197 | `fix/cq-251` | 4 | `658e963d5` | Enforced production shared-package Node neutrality for static and dynamic built-in imports, runtime globals, and the `NodeJS` namespace with an explicit test/helper carveout; no narrowing. Round-1 review closed the literal dynamic-import gap for prefixed and bare builtins and hardened the shared real-config test hang guard after repeated parallel-gate timeouts. The static `node:fs/promises` concern did not reproduce with installed `ignore@7.0.5`, and real-ESLint regression probes pin both prefixed and bare subpaths. Round-2 review expanded the local module-reference fence to no-substitution templates, TypeScript import types, unshadowed `require`, and TypeScript import-equals, pinned every good-faith static and dynamic module-reference form, and restored the `z.any()` comment to its schema-policy block. Round-3 review added type-position selectors. Round-4 review replaced that spelling census with one scope-resolved local-rule capability covering ambient `process`, `Buffer`, `__dirname`, `__filename`, and `NodeJS` references in value, type, query, qualified-name, and heritage positions while allowing local bindings; computed specifiers and deliberate circumvention remain out of scope. |
| 2026-08-12 | 157 | `fix/cq-157` | 1 | `5bb6b8930` | Split the shared lint-policy grab bag into focused path/glob, script/test, package-boundary, and max-lines modules; retargeted all 19 consumers and removed accidental max-lines I/O from unrelated shell fixtures; no narrowing or bugs fixed. Round-1 review retargeted the remaining live diagnostics, harness principle, max-lines reference, and restricted-syntax representative from the deleted module, and added regression coverage for live diagnostic and snapshot paths. |
| 2026-08-12 | 153 | `fix/cq-157` | 1 | `5bb6b8930` | Composed all seven live restricted-import rule sites through the focused package-boundary policy, including unit 197's shared Node fence and the portable-tools site; no narrowing or bugs fixed. Round-1 batch review reported no defect in the restricted-import composer or its callers. |
| 2026-08-13 | 158 | `fix/cq-158` | 3 | `0295fad7d` | Generated colocated declarations from the six post-157 checked-JavaScript policy modules and retained only the two excluded ESLint-rule ambient bindings. The semantic diff preserved readonly arrays, literal unions, template literals, and codec discriminants without `any`; it also surfaced the previously absent `restrictedImportsRule` contract. No narrowing or bugs fixed. Round-1 review added the formatter configuration and dependency lock to freshness triggers and corrected the active max-lines type-ownership reference. Round-3 review moved those triggers from two unrelated facets to the declaration facet that consumes Prettier and the installed compiler. |
| 2026-08-13 | 188 | `fix/cq-158` | 3 | `0295fad7d` | Added read-only third-party proposal previews with allowlist inference or preview-scoped synthesis, promotable governance output, parser-profile controls, and actionable plugin diagnostics. No narrowing or bugs fixed. Round-1 review rejected malformed local namespaces, disambiguated allowlist inference on the full module/namespace pair, and made a wrong default plugin export produce the actionable named-export correction. Round-3 review made named `module.plugin` recovery use a namespace import, so it works without a package default export and cannot repeat the missing-default failure. |
| 2026-08-14 | 124 | `fix/cq-124` | 3 | `aaaa0be1c` | Added the authoritative `as const` 14-mode command catalog and derived the parser tables, mode union, usage text, ownership scoping checks, preflight tiers, and exhaustive non-fallthrough dispatch from it; the silent `runDefault` fallthrough became a compile error plus dispatch test, and behavior was differentially fuzz-verified byte-identical to main. Only the terminal command registration is cataloged; `propose-cli-options.ts` sub-grammar internals stay excluded per scope. Round-1 review (codex + Opus 5) completed the derivations validation, option assignment, and terminal-mode mapping had left hand-enumerated, removed the wrapper/dynamic-import handler ceremony and a dead derive helper, restored the narrow handler mode type, and replaced self-referential tests with table-driven option coverage. Round-2 review and the Fable 5 + Grok 4.6 pre-land gate reported no P0/P1. |
| 2026-08-14 | 122 | `fix/cq-122` | 3 | `e7bf2753d` | Made command spellings a required adapter-owned `LintRatchetWorkflowVocabulary` with no kernel defaults, threaded through kernel and governance so all 17 `bun run` literals left the portable package; write and staleness paths share one binding, byte-pinned to the committed baseline annotation. Fixed an in-scope portability bug (conflict-marker remediation hard-coded Musi's restore command) and kept the standalone merge CLI's reduced closure portable. Narrowing: no new registry-paste field — the live tree already injects it via `registryHint`. Round-1 review (codex + Opus 5) restored byte-exact legacy recovery output, gave the demo a real trend command with its own binding, deleted a 184-line test façade in favor of the real vocabulary-required APIs, removed identity accessors and dead parameters, normalized the API naming, widened the boundary guard to all portable sources, and added exact locks on injected diagnostics; the residual adapter-side `lint:ratchet`/`lint:fix` literals were adjudicated in round 2 as separate commands outside the vocabulary's fields. Round-2 review and the Fable 5 + Grok 4.6 pre-land gate reported no P0/P1. |

## Standing grants

Merge-on-green grants from the owner, with their scope. A grant is permission
for work that has **not** landed.

| Date | Scope | Granted by | Still open? |
|---|---|---|---|
| 2026-08-08 | Merge-on-green for every unit in this pack: land a lane once its review round is clean and its bookkeeping is committed, without asking. Excludes push and PR. | owner | yes |

## Deferred for owner

Accepted P2s, deferred cross-pack calls, and open questions a lane left
behind. Conductors append a row when the lane closes; the owner disposes.
Rows are never deleted, only annotated.

| Unit | Deferred | Disposed? |
|---|---|---|
| 206 | Three P2s accepted at the clean round. | |
| 271 | Trailing-newline codec-shape P2. | |
| 144 | Two cosmetic P2s. | |
| 182/171 | `requiredStableFields` honored only by the socket-family auditor; `dev.sh` arg-forwarding covered only by a stubbed-argv assertion; stale-`.tsbuildinfo` remedy missing from both preflights. | |
| 181 | Two accepted P2s; `docs/ai-harness.md:322`'s starter recipe yields `Cannot find package '@musi/harness-diagnostics'` (leaf 080's subject); the schema silently left `stryker.config.mjs`'s mutation lane. | |
| 119 | Provenance comment overstating the old awk's FS behavior; a stale test name. Two documented detect-more divergences judged correct. | |
| 173/152 | Deferred cross-pack calls. 152 is now excluded from the `policy-extraction` queue: its caveats require it to land after 2026-07-25 H15, which is `Open` behind 27-PLAN slice 27.3 (`scripts/tests/test-pre-commit.sh` does not exist), and no queue here schedules 27.x. Owner call: schedule 27-PLAN 27.3 -> H15, or waive 152's ordering caveat. Accepted P2s from the 118/173 round-1 panel: `scripts/doctor.sh:81`'s ambient-state comment still describes the hand-managed set/clear the adapter now forbids; `RENAMED_VARIABLES`/`functionsFrom` are copied into `scripts/sensor-near-duplicates.test.ts` rather than hoisted to `scripts/test-support/`; the new HOW-TO step 2 does not say that non-`doctor-check/` rows are outside `checkDoctorParity`; `run_inline_check` treats a misspelled guard/check callback as not-applicable rather than failing; `DRAIN-NOTES.md:156` still names 152 as the `policy-extraction` hub and does not list the new exclusion beside the 171 one; two parked 2026-07 plans still point at the pre-move gate-suite location. Round 2 added two more: fixture-only edits no longer reach changed-test selection through `scripts/lint-coverage-map-manifest-script-fixtures.ts`, and 118's P2s are filed under this 173/152 row rather than a 118 key. | |
| 177 | Deleted "without retaining raw record payloads" invariant. | |
| 137, 209, 204, 253, 207, 139, 255, 133, 168, 172, 174 | Accepted P2s (see each unit's review answer files if still present). | |
| 080 | Open P2s from the clean round 4, all documentation-completeness: the manifest's "does not cover" list omits the structural repo-layout assumptions `scripts/harness/harness-paths.ts` carries into the hook-control copy set (`.claude/`, `.codex/`, `.github/hooks/`, `scripts/verify/`, `scripts/ai-hooks/`), which `scripts/ai-hooks/README.md:389-402` already documents; the empty-knob branch renders marker-absence as value-absence, an inference the scan cannot make; `docs/ai-harness.md`'s S2 prose enumerates three closure directories where the derived hook-control set spans four (`scripts/lint-ratchet/` missing); `importsSpecifier` is a unit-tested test helper whose only consumer already covers it; and the pre-existing public-archive sentence does not list `scripts/lib/`, `scripts/import-closure/`, or `tools/lint-ratchet/`, which the manifest now sends adopters to. Declined across rounds 1-3 and left standing: `importedBy` corroborated as closure membership rather than as an actual import edge (proving the edge means a second partial resolver); `eslint-config/path-glob-policy.js` kept in `adapterPaths` though no recipe reaches it; `packageCopies`/`boundaryNotes` kept optional; the byte pin and its two named live cases kept; type-only `import x = require()` left throwing rather than followed. **Panel outage:** the GPT-5.6 seat (`cursor -m gpt-5.6-sol-high`) could not run round 4 — the cursor account hit its monthly usage limit on 2026-08-24 and the retry failed identically; it resets 2026-09-09. Round 4 landed on the four remaining seats (Opus 5, Fable 5, Sonnet 5, Grok 4.6), none reporting P0/P1. The owner may want to re-check the fixed panel's composition before the next pack. | |
| 077 | Accepted P2s from the clean round 4 (all seats static; every round ran on three Claude seats): dead `clearDiceNotation` in `e2e/page-objects/campaign-chat.po.ts` (page objects are unit 079's); pre-existing dead `^castSingleTargetSpell$` in `eslint-config/test-configs.js`; unread `DmPlayerCampaign.campaignName`; `DEFAULT_CHARACTER_INPUT` export now module-internal only; collab's invalid-code join now duplicates `navigation-errors.spec.ts:27`; collab scenario could use `setupCampaignOwner`; pre-`try` seeding leak on an already-failing run; three single-assert seeds kept for retry granularity (recorded in the leaf's Disposition). Two environment notes: `e2e/encounter-combat.spec.ts` "DM opens encounter and adds participants" fails locally on `main` too (30 s timeout in `EncounterPO.addMonster`; out of scope, CQ25-162), and no verify slot runs e2e, so `land.sh` covers this lane by typecheck/lint only; the default `~/.cache/ms-playwright` holds chromium 1208 with an unwritable `.links/`, so lanes need `PLAYWRIGHT_BROWSERS_PATH=/home/node/persist/ms-playwright` (1223) to run e2e. | |
| 078 | Accepted P2 from the panel (every round ran on three Claude seats; all seats had a working shell but ran no tests per the consult preamble): the stale-baseline failure message in `e2e/a11y.spec.ts` is built eagerly on every pass, matching the pre-existing unbaselined-message pattern — cosmetic, no change. Round 1's other two P2s (map doc comment overclaimed rule-ID-granular ratcheting; rationale stated three times) were fixed in `2643ce09f`. Environment notes: no verify slot runs e2e, so the lane ran `e2e/a11y.spec.ts` by hand (both ratchet directions demonstrated, `--repeat-each=3` green); a pre-existing intermittent failure of the authenticated a11y test at `getByRole("link", { name: charName }).click()` under `--repeat-each=3` reproduced once on `main`'s spec too and was not chased (outside the leaf's blast radius). Per-node (sub-rule-ID) baselines remain the leaf's declared follow-on. | |
| 079 | Accepted P2s from the clean round 2 (every round ran on three Claude seats; all seats had a working shell but ran no tests per the consult preamble): pre-existing dead public field `CharacterWizardPO.equipmentOptionA` in `e2e/page-objects/character-wizard.po.ts` (no reader; `selectEquipmentOption` re-spells the same selector) — one-line deletion left for the owner since the leaf declines privatizing fields; `e2e/character-create.spec.ts` "character card appears on dashboard after creation" repeats a full wizard walk for one `toHaveURL` that `submitCreate` already covers (suite structure is outside this leaf). Environment note: no verify slot runs e2e, so the lane ran `wizard-validation`, `character-data-integrity`, `character-create` (15 passed) and `a11y` (`--repeat-each=3`, 10 passed) by hand with `PLAYWRIGHT_BROWSERS_PATH=/home/node/persist/ms-playwright`; `land.sh` covers this lane by typecheck/lint only. | |
| 063 | Accepted P2s from the clean round 2 (every round ran on three Claude seats — Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot seats were quota- or policy-dead; all seats had a working shell but ran no tests per the consult preamble): `scripts/tests/test-worktree-locking.sh` carries a duplicate `init_lock_holder_pid=""` initialisation inherited from the monolith, one `error_file="$(mktemp)"` that bypasses the new `test-tmpdir.sh` registry (pre-existing), and three mid-suite `rm -rf` calls on already-registered dirs (`test-worktree-new.sh`, `test-worktree-drop-gc.sh`) that are now no-ops at exit; the leaf Disposition's rationale for declaring `scripts/worktree-db.sh` in `test-worktree-new.sh`'s header says the suite "never sources that file directly" where the suite in fact sources it transitively on every run (conclusion unchanged); `29-bash-to-ts-cores.md:104-109` still pins state-writer/GC-stub cases at monolith line numbers (already wrong on `main`; outside step 7's named lines). Production-comment drift for a future lane that opens `scripts/worktree-new.sh` (H11 or 198): its double-source guard rationale "Tests source both files" is now only true of the one probe in `test-worktree-new.sh`. Round 1's P1 (band-2 suite lost changed-mode selection on `worktree-db.sh` edits) was fixed in `8797b2dcf`. | |
| 064 | Accepted P2s from the clean round 2 (every round ran on three Claude seats — Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot seats were quota- or policy-dead; all seats had a working shell but ran no tests per the consult preamble, so round 2 is a static read): `packages/server/src/routers/encounter-combat-logs.test.ts:300` comment still describes the round-1 `cursor: { id: "" }` mechanism the keyset code no longer uses; `packages/server/src/utils/cursor-pagination.test.ts:17` second case is subsumed by the first's `toEqual`; `chat.test.ts` "boundary message leaves the caller's view" and "partly visible tie group" cases overlap the notification pin and each other (candidate trims); `notification.ts` no-cursor path now awaits `findMany` after `Promise.all(boundary, count)` instead of running `findMany` and `count` in parallel. Round 1 was also clean (P2 only); its fix round surfaced and fixed the real `skip: 1` OFFSET defect recorded in the leaf's Disposition. | |
| 065 | Accepted P2s from the clean round 2 (every round ran on three Claude seats — Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot seats were quota- or policy-dead; all seats had a working shell but ran no tests per the consult preamble, so both rounds are static reads over focused runs the lane itself made): `packages/server/src/services/notification-service.test.ts:21-23` keeps a suite-local `afterAll(prisma.$disconnect)` that duplicates `setup.ts`'s global teardown — same re-does-the-global-lifecycle pattern as the unit, but out of its cleanDb-only scope; the three-line phase-reset justification in `invite-service.test.ts` is repeated verbatim at `:87-89` and `:272-274` (cosmetic). Round 1's P2 on the race-loop keeper's overclaiming comment in `level-up/level-up-concurrency.test.ts` was fixed in `f3d0bcf21` (the call stays; the leaf fixes the keeper set at five). | |
| 066 | Accepted P2s from the clean round 3 (three rounds, every round on three Claude seats — Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot seats were quota- or policy-dead; the shell smoke was permission-blocked for most seats, so runner claims are static reads over the focused runs the lane itself made; round 1 found two P1s — the printed recovery command did not run, and Stryker 9.6.1's default `disableTypeChecks` makes the in-place write scope the whole tree, not the mutate globs — both fixed in `1bca0ca6a` with backup-first recovery; round 2 P2s fixed in `c934c9ce7`): `scripts/mutation-run.sh` lane-config sniffing takes the first existing `*.{mjs,cjs,js,json}` positional, so a file-valued flag placed before the config on the bare `test:mutation` form (`--incrementalFile x.json cfg.mjs`) classifies from the wrong file and runs an in-place lane with no rails (fails open; only reachable by misuse of the generic wrapper); `process_start_identity` uses `ps -o lstart=`, which is TZ/locale-dependent, so two terminals with different `TZ` can misread a live run as stale (fix: `LC_ALL=C TZ=UTC` or `/proc/<pid>/stat` field 22); the printed `--restore` command drops the invocation's pass-through args including `--mutate`, and the `docs/ai-harness.md` recipe omits that `--restore` then starts a run; preflight/stale refusals exit 1, colliding with Stryker's below-threshold exit; the printed `git restore --worktree` line word-splits paths with spaces (no lane target has one); a bare `stryker run` in another terminal is diagnosed as "interrupted"; the tracked-pass producer-failure branch (`could not list the worktree tracked files`) and bin resolution without `MUSI_MUTATION_RUN_STRYKER` have no dedicated smoke case; three subsumed test pairs in `scripts/mutation-targets.test.ts` (`--nope`/`--help` with and without `refuseStdin`) and `MUTATION_RANGE_REGEX` (unreachable from any lane) are deletion candidates. Landing history carries two commits mislabelled with lane 065's message (`c228288e5`, reverted by `4c091d51b`; `1bca0ca6a`, tree correct) because the commit guard rejected the heredoc+commit compound and the retry read a stale scratch file — amend/reset are policy-blocked; recorded in `pain_points/git-hooks-and-commit-workflow.md`. Leaf 101 (docs) now lands after this one: its step 3 paragraph already exists in `docs/ai-harness.md` and describes the backup-first runner; steps 1–2 remain. | |
| — | `.claude/skills/agent-cli/references/cursor.md` GPT rows were stale until 2026-08-24. | 2026-08-24 |
| 074 (+076, 242) | Every round ran on a three-seat claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated); most seats were shell-restricted, so Vitest-internals claims were source-reads of the installed 4.1.7 and coverage figures rest on the lane's own runs. Round 1 P1s (per-project coverage blocks are inert in projects mode, so the root exclude is the load-bearing copy; `coverageConfigDefaults.exclude` is `[]` on Vitest 4, making the spread and its smoke assertion no-ops) fixed in `106ac576b`/`9152fa995`; round 2 P1 (the `tools/lint-ratchet/test/**` carve-out rested on the same disproven premise) fixed in `d6bbc91c1`. Accepted P2s from the clean round 3: leaf 074's "against 16 before" measures the lane's own first commit, not main (where the answer is 0 — main had no root `coverage.exclude`); the Disposition does not name the five extra config files the branch touches (two functionally, `src/**/*-test-helper.ts` in both server configs, rationale only in `de4b3b4fc`'s body); the three literal `eslint-rules` scaffolding paths are not `# smoke-subjects:` of `scripts/tests/test-test-slow.sh`, so a rename fails only at land-time `test:scripts`, not at the commit gate; the four-line "Not the load-bearing copy" paragraph is copied verbatim into seven configs (one line each would do); `grep -v '/worktrees/'` in the smoke enumerator filters paths `git ls-files` can never emit; the root config comment carries round-history derivation that duplicates the leaf; section 6's `%`-check is subsumed by `existsSync` (kept for the sharper message). Sonnet's round-2 preference to delete all per-project coverage blocks was refused (leaf-prescribed; markers instead) and is left as an owner call. Pre-existing, untouched: `eslint-rules/**` and `tools/lint-ratchet/src/**` fail their floors on main already (074's Disposition has the figures; the cadence guide makes re-flooring its own follow-up). 076 leaves known label survivors outside its four-file scope (`encounter-inputs.ts:92`, `map.ts:11`, ~10 sites in `packages/shared/src/map/` tests) owned by no leaf. `5747b68e1` lacks the Co-Authored-By trailer (amend is policy-blocked). |  |
| 070 (+071, 264) | Both rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5; cursor/codex/copilot unavailable this session); all seats had a shell but ran no tests. Round 1 P2s (six tautological tests, the `SHARED_FIELDS` substring denylist, a copying spread, the stale third count in `00-index.md`) were fixed in `3a235d2f4`/`3edd04c8a`. Round 2 accepted P2s, all removal preferences in tension with leaf 070's own title or direction: drop or collapse the source-text parity describe (it only catches a deliberate un-refactor, and pins the `testTimeout` spelling); drop the `""`-vs-throw test (it pays the DB-backed project's `cleanDb()` for a one-line `??`); inline `resolveServerTestJwtSecret` and its unused `env` parameter; keep `vitest/config` out of `src/` by having `SERVER_TEST_EXCLUDE` hold only the two repo globs (it is the only `src/` import of `vitest/config` and rides into `dist/`); `packages/server/src/test/MODULE.md:41` says the module is imported by the two configs only, but the test imports it too. Optional `closeAfterTest` helper for `app.test.ts` declined per leaf 264. The first `land.sh` verify failed one slot: `scripts/tests/test-test-slow.sh` statically grepped `packages/server/vitest.config.ts` for the literal slow-tier exclude that 070 moved into the shared module; `aaf411fb7` made its section 1 resolve each package config at runtime and assert the composed `test.exclude`, and registered the module as a smoke subject. Round 3 (clean) added P2s: section 1 could reuse section 0's existing `bun --eval` instead of a second process; the smoke's header/`ok` wording says "every package project" for 3 of 8 workspace projects (`vitest.unit.config.ts` is unchecked); the parity describe could live in the DB-free `scripts` Vitest project; nothing asserts the configs still reference `SERVER_TEST_EXCLUDE`. Lesson: shell smokes that grep config *text* break on any config refactor — the same smoke still greps the client and shared configs. | |
| 082 (+083) | Both rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated); every seat's subagent fan-out was permission-blocked, so each seat worked the angles inline, and no seat executed tests (static reads over the lane's own focused runs). Round 1 P1 — the reuse checklist omitted the `.env` keys that change in lockstep (three `*_DATABASE_URL`s, `CORS_ORIGIN`) — fixed in `bb2c092b2` with an `.env.example`-exhaustiveness assertion; round-1 P2s fixed in `397ef5110`/`98325e92f`/`f159d515f`. Accepted P2s from the clean round 2 (all Opus seat): the Prerequisites claim that a missing `devcontainers.slice` stops pod creation is asserted, not derived — systemd can create the slice implicitly, making the likelier failure a silently absent memory cap; the intro's "all four fail before any of this runs" over-counts by one now that prerequisite 3 self-provisions; leaf 082's Disposition presents the contract test as pinning the Prerequisites values where it pins the four headline literals (`userns_mode`, `remoteUser`, `/commandhistory`, `shellcheck`/`yamllint`, `waitFor`, `pod_args` stay unpinned with no line number either); the regenerated queue keeps a "Resolved since the 2026-07-13 reconciliation" section that is a second register of closed work the leaf's direction said to drop; `devcontainer-contract.test.ts`'s COPY-sources assertion is mostly subsumed by the exact-list test and false-positives on a future `COPY --chmod=` flag; the sentinel assertion is a reader-side substring pin only. Sub-P2 gaps outside the checklist's stated `.env`-only exhaustiveness contract: `devcontainer.json`'s `"name": "Claude Code Sandbox"` and compose's firewall-only `cap_add: NET_ADMIN/NET_RAW` appear in neither reuse column. Declined in the round-1 fix: dropping `?? ""` (required by `noUncheckedIndexedAccess`) and unifying the test's two file-read idioms (churn). | |
| 084 | All four rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session). Accepted P2s from the clean round 4: `lifecycleFromStatus`'s known-limits JSDoc bakes dated corpus totals (282/311/116 of 709) that rot on the next status flip while `CATALOG.md` self-corrects, and two docs cite it as the dated measurement; a `buildPackShapes` comment claims a parse-once invariant the full lint run does not hold (core and packs each parse the corpus); `docs/agent_notes/README.md` sits one line under its 150-line advisory cap after the three-surface section; a broken two-word line wrap in `backlog/README.md`'s regeneration paragraph; duplicated full-row assertions across the render/classify test layers; `DEFAULT_BACKLOG_DIR` literal duplicated in `backlog-lint-core.ts` beside the facade's exported `BACKLOG_DIR`. Known accepted classifier limits are documented in the JSDoc/READMEs (completion words past the first token read actionable; named self-misdescribing notes). | |
| 085 (+086, 087) | All three rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated); every seat's subagent fan-out was permission-blocked so angles ran inline, and no seat executed tests (static reads over the lane's own focused runs). Round 1 P1 — twelve generated-surface refresh scripts rendered `effect: check` ("writes nothing") while rewriting committed files — fixed in `d427c4bdc` by deriving writer effect from `generatedSurface.refreshScript` capability; round-1 P2s fixed in `07e0f4434`/`124a923b3`/`739f7369a`/`6775b5ed0`/`ca2070376`/`8e169bf80`; round-2 P2s fixed in `639a9a22c`..`d746b5687`. Accepted P2s from the clean round 3: a manifest schema failure degrades the catalog to empty and buries the real error under ~117 `(command catalog)` coverage lines (`registration-manifest-checks.ts:290`; a one-line skip-on-schema-failure guard was suggested); the authored-override clause in `command-catalog.ts` (`derived === "generator" || "repair"` beats an authored effect) currently has no possible subject since no aggregate script is a writer; a baked "193 more strings" count in `command-catalog.ts:27` is already stale (195); latent count divergence if two generated surfaces ever share one `checkScript` (renderer counts sources, gate deduplicates — no instance in the live manifest, and nothing validates checkScript uniqueness). Seats split delete-vs-keep on two surfaces, left as owner calls: the workspace-pattern/`--cwd` fallback in `package-manifest-scripts.ts` (unexercised today — all tracked non-root manifests are workspace members) and the "unreachable while harness:check is green" `controlPurpose` fallback branch. Also accepted: the `harness.controls.json` whole-file reserialization noise the first implementer commit introduced (semantically inert — `docs/generated/harness-controls.md` moved 14 additions, 0 deletions), and leaf 086's trimmed README landing at 307 lines against the leaf's "roughly 150-250" target (authoritative tables and the superset quickstart deliberately stayed). | |
| 088 (+089) | All three rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated); no seat could fan out subagents (no Agent tool / Workflow approval-gated), so angles ran inline; every seat had a working shell and ran the doc gates (`module:index:check`, `backlog:lint`, `docs:backlog-catalog:check`, `harness:check`) but no Vitest (docs-only diff). Round 1 (no P0/P1, 8 P2s across seats; the Opus seat's answer file carried only its final message — findings recovered by resuming the session) fixed in `235db3082`..`2a5e12596`; one declined: the `removingId` remove-spinner sentence stays (owned local state is State Ownership's job). Round 2 found 1 P1 — the round-1 fix's own Test Seams sentence ("fixtures either way") had re-inverted, the pack's known doc-correction failure mode — plus 8 P2s; fixed in `d9ce06fd9`/`668ae3472`. Accepted P2s from the clean round 3: `encounters/MODULE.md:49` says the add-participant dialog has character/NPC/monster tabs but `add-participant-dialog.tsx:300-311` renders four (Characters, Monsters, Homebrew, Custom NPC); `:55-57` omits `end-encounter-dialog.tsx` as a third `calculateEncounterXp` call site and pairs `difficulty-indicator.tsx` with a function it does not call; `:102` "participant updates send `expectedVersion`" overstates — the action-economy toggle path (`encounter-detail-view.tsx:101-110`) sends none; `docs/module-docs.md:51` "internal composition landmarks belong in Data Flow" is stated absolutely while combat/vtt deliberately keep labelled internal rows under scope notes on this same branch (one-clause allowance suggested); `vtt/MODULE.md:51` preamble ("first three rows are imported from outside") reads in tension with the ruling-protected "single public entry point" row — additive scoping with a pointer to the 2026-07-25 ruling suggested; Opus removal preference on the `AddLogForm` draft-field enumeration in combat State Ownership declined (it was itself a round-2 panel fix). | |
| 091 (+092, 093, 095, 097) | Round 1 ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper); the cursor, codex, and copilot backends were unavailable this session per owner statement 2026-08-30, so the fixed five-seat panel ran without its two off-family seats. The Opus and Fable seats had no subagent fan-out available (no Agent tool / permission-blocked Workflow) and worked the angles inline; no seat reported executing tests or gates, so every claim is a static read over git/grep verification (docs-plus-deletion diff). Round 1: no P0, one P1 and four P2s, all fixed in this lane's round-1 fix commits. P1 — the manifest-parser guide's "typed whole-parse consumers" header claimed a whole-manifest typed parse for `generate-skill-artifacts.ts`, which reads unvalidated via `readHarnessManifest` and facet-parses only `kind === "skill"` controls, and its closing test told a contributor to migrate that very reader (`2bcfafa4e`). P2s — `add-e2e-test.md` step 4's "line above the call" placement is wrong for prettier-wrapped chains, since the rule reports on `node.callee` (`3085aca71`); step 11 respelled the disable directive it already cross-referenced (`4cfc736d0`); `packages/server/src/seed/MODULE.md` kept a second copy of the backfill-removal story that `docs/srd-data-sources.md` §"No backfill step" owns (`3afacf8b4`); and this row's own pre-recorded past-tense narration of rounds that had not run (fixed by this rewrite). Declined from that last finding: dropping or padding the row to six cells — the six-column header it was measured against belongs to the landing table above; this row sits in the three-column *Deferred for owner* table and renders correctly, as do siblings 084/085/088. Landing 097 as removal executes the mooting side of d-097-01/d-203-01: leaf 203's evidence lines now point at deleted files, and its own `Status:` flip is left to an owner-sanctioned pass (the drain gate already blocks 203 as recorded-moot, so no lane can be dispatched onto it meanwhile). Round 2 (fresh three-seat panel; the Opus seat was SIGTERM-killed mid-flight and rerun): no P0/P1, four P2s, all fixed — `docs/CONCURRENCY.md`'s `createPrismaClient` caller list still named the just-deleted backfills (`fbed5ce35`); the manifest guide pinned the `kind === "skill"` facet filter to `skill-inventory-schema.ts` when it lives in `skill-artifact-projection.ts` (`9d013cf6d`); step 4's placement wording still misdirected for locators inside wrapping calls like `Promise.all([`, restated as one receiver-line rule (`b204511da`); the closing migrate test restated the three group headers as a second drift-prone taxonomy, collapsed to one sentence (`68855e061`); and the 203-mooting clause above was added (`d9a540414`). Round 3 (Opus seat again SIGTERM-killed and rerun): clean — no P0/P1. Accepted P2s from the clean round 3, all Opus-seat style/deferral preferences: the homebrew parent recipe's "not its owner" clause could read as ruling `entry-dialog.tsx` out even though its hand-maintained `ENTRY_TYPE_OPTIONS` array still needs an entry for a new type (the presentation-metadata step is leaf 180's, deliberately not duplicated here); the manifest guide's hand-maintained "Five readers" numeral is unguarded drift surface (delete-the-numeral suggested); `docs/CONCURRENCY.md`'s opening "the two paths that qualify" restates the count owned by §"Serializable isolation exception"; and `seed/MODULE.md`'s "a reader might expect a separate migration script to fill" clause is a softer residual tombstone. All three round-3 seats also asked that this row narrate round 2 — done by this edit. | |
| 094 (+098, 099, 100, 217) | All three rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated 2026-08-30); every seat's subagent fan-out was permission-blocked so angles ran inline, and no seat executed tests or generators (static reads over the lane's own focused runs, though seats re-derived the guide-table and importer sets empirically via git/grep). Round 1: one P1 — the repo-root `README.md:148` still branded `ai-harness.md` the whole-harness inventory after the authority split, the sole live outlier — fixed in `d85d1d3aa` with a tree-wide class sweep that also caught `scripts/ai-hooks/README.md:4`; three P2s fixed in `c75fdf796`/`9a69b9f68` (subsumed freshness test deleted and replaced with a header-reworded-away case that uniquely kills a `GUIDES_TABLE_HEADER_RE` widening; the two guide-finding walks folded into one; two malformed rewrap lines in `00-index.md`); one P2 refuted — the guide-path anchoring of `guide-missing-from-table` follows the sensor's split-by-subject convention, asserted by a landed test. Round 2: no P0/P1, five P2s; four fixed in `362d9269d`/`9a7969c2e` (`docs/README.md` no longer hand-copies the generated eleven-kind enumeration, the cross-file `local/*` disclaimer and "It is the map;" restatement deleted, the tour line's referent corrected, a walk-transcribing comment sentence dropped); one refuted — collapsing `unreferenced-guide` into `guide-missing-from-table` was declined because the two categories carry different repair hints (delete-stale-file vs add-row), `details.category` is filterable JSON output, and leaf 094 mechanic 4 mandates the distinction. Round 3: clean — no P0/P1. Accepted P2 from the clean round 3 (Opus seat): the 099 `initiativeModifier` sweep left `docs/CONCURRENCY.md:190` and `:755` diverging without explanation — `:190` describes `BlindParticipantFields` (whose `Pick` does include the field, though no current writer sets it) while `:755` describes the `updateParticipant` whitelist (which cannot route it); a half-sentence at either site would close it. | |
| 228 (+103, 104, 105, 106) | All three rounds ran on a three-seat Claude-only panel (Opus 5, Fable 5, Sonnet 5 via the agent-cli wrapper; cursor/codex/copilot unavailable this session, owner-stated 2026-08-30); every seat's subagent fan-out was blocked (no Agent tool / permission-gated Workflow / sandboxed agent-cli) so angles ran inline, and shell access varied by seat — the Opus seats were command-filtered to read-only git, so their claims are static reads; no seat executed the new test (the lane's own focused runs cover it). Round 1: no P0/P1, four P2s, all fixed in `479fa9ebb`/`5c6b5f098`/`1c8160e7e` (the new `existsSync` justification comment claimed Compose refuses to start on a missing mount source when short syntax creates it as a directory — inverted to state the real silent-failure mode; the root SQL header's hand-transcribed `./`-vs-`../` mount inventory trimmed; the coverage-map manifest description widened to the test's new scope and the map regenerated; the thrice-repeated filter caveat in `character-keys.ts` hoisted to one note). Round 2: no P0/P1, two P2s, both fixed in `9960bdb46` (README.md:28 claimed macOS unqualified while `verify.sh`/`dev.sh`/`worktree-db.sh` use Bash-4-only `declare -A`/`mapfile` against stock macOS Bash 3.2 — now "macOS with a modern Bash (Homebrew `bash` >= 4)"; the mount regex's `:ro$` anchor turned a good-faith `,z` SELinux flag into a false failure — now flag-tolerant, verified against `:ro`, `:ro,z`, and bare forms). That fix rode as one commit under a README-flavored subject (the fixer meant two; amend and reset are policy-blocked). Round 3: clean — Fable and Sonnet zero findings. Accepted P2s from the clean round 3, both Opus-seat style preferences: the `(?::\S+)?$` flag-tolerant group plus its comment could be a bare unanchored regex (drop the `$`, identical capture, six lines shorter); README.md:28 is one ~470-char unwrapped source line, denser than its two neighbors, trimmable to the leaf's two facts (a round-2 seat judged the density intentional per leaf 103's own mechanics — left as an owner call). Residual round-3 observation, not a finding: `scripts/verify.sh` pairs a `#!/bin/bash` shebang with Bash-4-only `mapfile`, so direct execution on stock macOS fails — pre-existing, a candidate future leaf if direct invocation ever becomes a documented path. Unit 106 landed its option 1 (single-source, devcontainer mounts `../init-test-db.sql`): live evidence overturned the audit's portability caveat — 082's landed rewrite already states copying `.devcontainer/` wholesale does not work, and the compose file already reaches up via `../:/workspace:z` — with a two-file drift test replacing option 2's doctor check. | |
