#!/usr/bin/env bash
bash "$(dirname "$0")/../scripts/git/install-all-merge-drivers.sh"
if [ "${1:-0}" != 1 ]; then
  bash "$(dirname "$0")/../scripts/git/run-baseline-truth-up.sh"
fi
. "$(dirname "$0")/../scripts/worktree-drift-hook.sh"
musi_worktree_drift_hook "post-merge" "$@"
