/** Named timeout constants for E2E tests. */

/** Element visibility after an action (button appears, text updates). */
export const TIMEOUT_SHORT = 5_000;

/** Navigation, page load, initial data fetch. */
export const TIMEOUT_MEDIUM = 10_000;

/** Socket.io / real-time message delivery. */
export const TIMEOUT_LONG = 15_000;
