dist/
node_modules/
bun.lock
lint-ratchet.baseline.json
docs/
.claude/
worktrees/
.worktree-state/
scripts/codemods/fixtures/
scripts/drift-ai/fixtures/
scripts/fixtures/lint-ratchet/
packages/server/src/generated/prisma/

# Vendored, verbatim SRD reference JSON (upstream 5e-bits/5e-database src/2024).
# Must stay byte-for-byte with the pinned upstream for provenance/checksum
# integrity, so prettier must not reformat it. See
# packages/server/src/seed/data/reference/NOTICE.md.
packages/server/src/seed/data/reference/5e-SRD-*.json

# Published JSON Schema, emitted byte-for-byte by
# scripts/harness/generate-manifest-json-schema.ts with deterministic
# codepoint-sorted keys, and freshness-gated against that emitter. prettier
# collapses short JSON arrays onto one line, which the emitter does not, so
# leaving it formatted would deadlock `format` against
# `harness:manifest-schema:check`.
schemas/harness.controls.schema.json

# Markdown is intentionally not auto-formatted. prettier reflowing prose (line
# wrapping, list markers, em-dashes) was low-value edit-time noise; `docs/` was
# already ignored above. See docs/agent_notes/backlog/agent-friction-2026-06 (C1).
*.md
